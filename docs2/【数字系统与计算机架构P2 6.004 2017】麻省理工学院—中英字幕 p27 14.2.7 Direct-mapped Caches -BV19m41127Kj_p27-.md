# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p27 14.2.7 Direct-mapped Caches -BV19m41127Kj_p27-

The simplest cache hardware consists of an SRAM with a few additional pieces of logic。

The cache hardwareer designed so that each memory location in the CPU's address space maps to a particular cache line。

 hence the name direct mapped cache。

![](img/5577dbcfa2326a0ddf938f417a14f406_1.png)

There are， of course， many more memory locations than there are cache lines。

 so many addresses are mapped to the same cache line。

 and the cache will only be able to hold the data for one of those addresses at a time。

The operation of a direct map cache is straightforward。

We'll use part of the incoming address as an index to select a single cache line to be searched。

The search consists of comparing the rest of the incoming address with the address tag of the selected Ca line。

If the tag matches the address， there's a cache hit and we can immediately use the data in the cache line to satisfy the request。

In this design we've included an additional valid bit。

 which is one when the tag and data fields hold valid information。

The valid for each cache line is initialized to 0 when the cache is powered on。

 indicating that all cash lines are empty。As data is brought into the cache。

 the valid bit is set to one when the cache lines tag and data fields are filled。

The CPU can request it the Val be cleared for a particular cache line。

 this is called flushushing the cache。If， for example， the CPU initiates a read from disk。

 the disk hardware will read its data into a block of main memory。

 so any cached values for that block will be out of date。

So the CPU will flush those locations from the cache by marking any matching cache lines is invalid。

Let's see how this works using a small direct map cache with eight lines where each cache line contains a single word。

4 bytes of data。Here's a CP request for the location aide address HeX E8。

Since there are four bytes of data in each cache line。

 the bottom two address bits indicate the appropriate byte offset into the cached word。

Since the cache deals only with word accesses， the bite off set bits aren't used。

Next we'll need to use three address bits to select which of the eight cache lines to search。

We choose these cash index bits from the low order bits of the address。Why。Well。

 iss because of locality。The principle of locality tells us that it's likely that the CPU will be requesting nearby addresses and for the cash to perform well。

 we'd like to arrange for nearby locations to be able to be held in the cash at the same time。

This means that nearby locations will have to be mapped to different cache lines。

The addresses of nearby locations differ in their lower order address bits。

 so we'll use those bits as the cash index bits。That way。

 nearby locations will map to different cache lines。The data。

 tag and valids selected by the cache line are read from the Sram。To complete the search。

 we check the remaining address against the tag field of the cache。

If they're equal and the valid is 1， we have a cache hit。

And the data field can be used to satisfy the request。

How come the tag field isn't 32 bits since we have a 32 bit address。We could have done that。

 but since all value stored in cache line 2 will have the same index bits，010。

We've saved a few bits of SRAM and chose not to save those bits in the tag。In other words。

 there's no point in using SRAM to save bits we can generate from the incoming address。

So the cache hardware in this example is an8 location by 60 bit SRAM。

 plus a 27 bit comparator and a single hand gate。The cache access time is the access time of the SRAM。

 plus the propagation delays of the comparator and end gate。

About as simple and fast as we could hope for。The downside of the simplicity is that for each CP request。

 we're only looking in a single cache location to see if the cache holds the desired data。

Not much of a search， is it？But the mapping of addresses to Cashline helps us out here。

Using the low order address bits as the cash index。

 we've arranged for nearby locations to be mapped to different cache lines。So， for example。

 if the CPU were executing at8 instruction loop， all eight instructions can be held in the cache at the same time。

A more complicated search mechanism couldn't improve on that。The bottom line。

 this extremely simple search is sufficient to get good cash hit ratios for the cases we care about。



![](img/5577dbcfa2326a0ddf938f417a14f406_3.png)

Let's try a few more examples in this case， using a direct map cache with 64 lines。

Suppose the cache gets a read request for location 4000 C。To see how the request is processed。

 we first write the address in binary so we can easily divide it into the offset index and tag fields。

For this address， the offset bits have the value 0。The cash line index bits have the value 3。

 and the tag bits have the value 40。So the tag field of Ca line 3 is compared with the tag field of the address。

 since there is a match， we have a cache hit and the value in the data field of the cache line can be used to satisfy the request。

Would an access to location 4008 be a cache hit？This address is similar to that in our first example。

 except the cash line index is now two instead of three。Looking in cache line 2。

 we see that its tag field 58 doesn't match the tag field in the address 40。

 so this access would be a cache miss。Or do the addresses of the words held by cash line 0，1 and 2。

 all of which have the same tag field。Well， we can run the address matching process backwards。

For an address to match these three cache lines， it would have to look like the binary shown here。

 where we've used the information in the cache tag field to fill in the higher order address bits。

 and the lower order address bits will come from the index value。If we fill in the indices 01 and 2。

 then convert the resulting binary to hex， we get 5800。

 5804 and 5808 as the addresses for the data held in cache lines 01 and2。



![](img/5577dbcfa2326a0ddf938f417a14f406_5.png)

Note that the complete address of the cache location is formed by combining the tag field of the cache line with the index of the cache line。

We， of course， need to be able to recover the complete address from the information held in the cache so it can be correctly compared against address requests from the CPU。



![](img/5577dbcfa2326a0ddf938f417a14f406_7.png)