# 杜克大学《Java编程和软件工程基础-1｜Java Programming and Software Engineering Fundamentals》中英字幕 - P6：06_01_04_元数据与分区元素.zh_en - GPT中英字幕课程资源 - BV1gM4m117nk

Let's look more closely at some different types of HTML tags or elements。 Right now。

 we will look at two types of elements。 One group is metadata elements。

 These contain information about the page。 Examples include the HTML tag， head and title tags。



![](img/1b4bce5b49b53d78ae301ffa777f6a04_1.png)

Sectioning elements which define regions of the page include tags like body， header tags and div。

In the context of a web page， metadata is a set of data that describes and gives information about the web page。

 The H tag is a metadata element。 All other elements must be defined between the H start and ending tags。

 H T M L tells a browser that it should use the Hm standard in displaying the web page。

 The head tag is also a metadata element。 It contains general information about the page。

 This includes the title， information about scripts and information about displaying the page using CS。

The title tag is a metadata element， too。 It specifies the text that is the title for the page and must be nested between the start and ending head tags。

For an example here。Is the beginning of the Hm L from Dukelear toprogram com， the Htm tag。

The head tags between which other metadata goes， such as the title tags。Next。

 we see examples of sectioning elements。The body tag is a sectioning element。

 The start and ending body tags define the body of the whole web page。

 All the text and other items seen on the page will be inside the body tags。

 Other sectioning elements defined sectioning within the body of the web page。 For example。

 the H1 tag defines a region with a more important and usually larger header。

The H in H1 means section head。 The H2 tag is also a section header。

 but typically a little smaller than an H1 tag。 The smallest header tag is H 6。

The divff tag is also a sectioning tag。 This tag defines a section or division of a web page。

 It is useful for grouping elements together to use C SS styling。

Sectioning elements can also be found in the H for Duke learn topro do com。

 The body starts here after the metadata and the closing body tag is not shown because there is a lot of HTML on this page。

 Here is an H1 header element。 Here is the first div on the page。 It uses the CS class title bar。

 Hopefully， the layout of a Web page's Hm L makes more sense Now。

