# 斯坦福大学《算法（分治／排序／搜索／随机算法、图搜索／最短路径／数据结构、贪心算法／最小生成树／动态规划、最短路径／NP）｜Algorithms》中英字幕 - P151：23_02_03_顶点覆盖的智能搜索二.zh_en - GPT中英字幕课程资源 - BV1Rx4y1U7sZ

Let's see how this substructure lemma naturally leads to a recursive search algorithm。



![](img/fa3054fab322c5c126c4118b279f9222_1.png)

The algorithm is recursive， I'm not going to bother to specify the base cases。

 which is when you're given a graph with at most one vertex or whether you're given a value of k equal to 0 or1。

 so let's assume that the graph has at least two vertices and that K is at least equal to2。

I wouldn't be surprised if this algorithm reminds you of some of the recursive implementations we discussed for various dynamic programming algorithms。

And in the same way， those algorithms exhibited exponential running time without memorization。

 we're going to see an exponential running time here。For all of our dynamic programming algorithms。

 we are able to cleverly organize sub problems from smallest to largest。

 thereby eliminating redundantly solving the same subproblems over and over again。

 yielding polynomial runtime bounds Here， we're going to be stuck with the exponential running time bound。

 obviously not surprising， given that it's an NP complete problem， but still。

 if you really want to understand this deeply， take this recursive search algorithm。

 try to come up with a polynomial time version。 try to come up with a small set of subpros ordered from smallest to largest that you can solve systematically。

 You will be inevitably stymied in those attempts， but you'll better appreciate what's non-trivial about the recursive solution here。

In step one of the algorithm， we pick some arbitrary edge U comma V。

 Why is it useful to focus on an edge， Well， by the definition of a vertex cover。

 it has to include either U or V。 So that's our initial clue。We're going to proceed optimistically。

 We're looking for a vertex cover of size K。 So Let's assume one exists。

 What does the substructure lemma tells us， It says that if such a solution exists。

 then necessarily either G U or G V or both themselves have small vertex covers of size only K -1。

 Moreover， it's a simple matter to extend such vertex covers to a small one for G you just augment them by the missing vertex。

So let's first adopt is a working hypothesis that G sub U has a small vertex cover of size only K -1。

 Let's recursively try to find it。If our recursive search comes back with a solution。

 a vertex cover of size K -1 for G U， we're good to go。 We just augment that by the vertex U。

 And that's our vertex cover of size K for the original graph G。

If that recursive call fails to find a small vertex cover， we say， oh， well。

 then it must be that V is the key to getting a small vertex cover。 So we do exactly the same thing。

 We recursively search G sub V for a vertex cover of size K -1。

If the second recursive call also fails to find a small vertex cover， one of size only K -1。

 then by the substructure claim the other direction。

 we know that the original graph G cannot have a vertex cover of size K。 If it did。

 the substructure Lamma tells us one of the two recursive calls would have succeeded since they both failed。

 we conclude correctly that the original graph did not have a small vertex cover。

The correctness analysis is straightforward forward formally， you would proceed by induction。

 so the inductive hypothesis would then guarantee the correctness of the two recursive calls。

 So on the smaller subproblems G and G V， the recursive calls correctly detect whether or not there are vertex covers of size K -1。

The substructure lemma guarantees that we correctly compile the results of the two recursive calls into the output of our algorithm。

 So if one of the two recursive calls succeeds， we're good to go。

 we just output the vertex cover of size K as desired。 And this is the crucial part。

 If both of the recursive calls fail， then by the substructure lemma。

 we know we correctly report fail。 We know there is not a vertex cover of size K or less in the original graph G。

So let's analyze the running time using an ad hoc analysis。

 let's think about the number of recursive calls that there can be over the entire trajectory of the algorithm or equivalently the number of nodes in the recursion tree generated by the algorithm。

 and then we'll come up with a bound on how much work is done in a given recursive call。

 not counting the work done by its recursive subcas。And now， the cool part。

 and this is really where the rule of a small value of k comes into play。

 I that each time we recurse， we subtract one from the target size of a vertex cover that we're looking for。

 Remember， if we are given as input， the parameter K。

 each of our recursive calls involves the parameter value， K -1。😊。

So what this does is limit the recursion depth or the depth of the recursion tree generated by this algorithm to be at most K。

 Initially， you start with K。 each time you recurse， you decrement it by one。

 you're going to hit base cases by the time k is around 1 or 0。

Putting those two observations together， branching factor bounded by two recursion depth bounded by K。

 the number of recursive calls in total over the entire lifetime of this algorithm is going to be bounded by2 to the K。



![](img/fa3054fab322c5c126c4118b279f9222_3.png)

Moreover， if you inspect the pseudocode， you see that not a whole lot of actions going on other than the recursive subcas。

 So even with a very sloppy implementation where you have to construct a G sub and G sub V from the input graph G。

 even if you do that crudely， you're gonna get a linear time bound。

 Big O of M work per recursive call， not counting the work that gets done later by the recursive subcas。

So that means an upper bound on the total amount of work done by this algorithm is the number of recursive calls 2 to the K times the work per call M 2 to the K times M。

Now this running time you'll of course notice is still exponential in K， but of course。

 it has to be exponential unless we intend on proving p equals NPp。

 Don't forget the vertex cover problem is NPp complete。So what have we accomplished。

 We had a trivial exponential time algorithm by brutefor search earlier。

 Why did we go through this righamma roll to come up with a second algorithm with this exponential run time bound。

 Well， this is a qualitatively better running time bound than we had before with naive brute force search。

 Here's two ways to see why this running time bound is qualitatively superior。😊，So first。

 let's just inspect this running time from a mathematical perspective and let's ask how large can we take K while still having a polynomial time algorithm。

 recall that our naive brute force search algorithm。

 where you just try each subset of K vertices random in time theta of n to the K。

 and so that's polynomial time， if K is constant and it's super polynomial time if K is bigger than a constant。

For this running time bound 2 to the K times M， we can let K grow as large as log of the graph size and still have a polynomial time algorithm。

So now let's segue from these mathematical bounds to thinking through actually implementing these algorithms and running them on actual data。

 So for the naive bruteport search， you've got this running time of end to the K。

 and even for a relatively small graph， you know， let's say n is maybe 50 or 100。

 You're not going to be able to run this bruteport naive brutefor search algorithm。

 except for extremely small values of K 3，4， maybe 5， if you're lucky。

 So naive bruteport search has very limited range of applicability。😊。

So by contrast our smarter search algorithm can accommodate a noticeably larger range of ks。

 so remember the running time here is 2 to the K times the graph size and so even for values of k up to 20 for many networks of interest。

 you're going to be able to implement and run this algorithm in a reasonable amount of time。

