# hhp3《xv6 操作系统内核｜The xv6 Kernel 2022》中英字幕 p22 -22-xv6 Kernel-22_ Anatomy of a System Call.zh_en -BV11CkSBsEtN_p22-

![](img/d776f5ca8a095cd2f620acc20b57c0f7_0.png)

What exactly happens during a system called？This video is titledAntomy of a system Call， and in it。

 I'll walk through all the actions that occur whenever system call is invoked。

I'll look at the S break system call as an example。SB is found in UniX and Linux systemss。

 and we're talking about the XV6 operating system kernel。

 and that is a simplified version of UniX and Linux。

Here are the functions and files that we'll be discussing in this video。

Every user mode program that wants to make a system call will need to include this user do H file。

 and we have a number of assembly code functions in Uis do S that are run in user mode。

The definitions in Ciscal dot H are used both by the user mode code and by the kernel code。

 So this file is shared between both user mode and kernel code。Once the trap occurs。

 we will be thrown into the code in the trampoline page。 This is assembly language code。

And the user vet code will then invoke the user trap function。

 Use trap function will determine that we've got a system call and invoke the function cis call。

Cis call will determine which system call we want， and it will， in our case。

 invoke SP to do the system call that we're going to be looking at。SP will then invoke GrowProc。

 which will actually increase the size of the address space， and then when everything returns。

 Cis call will then end by going back to user trap RET。

 and user traprap ReT will then jump to user REt， the assembly language code。

 which will execute the Sret instruction and bring us back into executing user mode code。

So let's begin with user。h。Any user mode program that wants to invoke a system call needs to include this file。

 and it contains a number of function prototypes， so it contains one prototype for each of the 21 system calls。

 and in particular we see the prototype for S break here。SsB is past a single argument。

And that argument is the amount to grow the heap by。

 It can be positive or it could be negative if you want to shrink the heap。It can also be0。

 It returns the previous size of the heap。 So if you want to know how big the heap is。

 you can just invoke it with zero。In the risk five system。Arguments are passed in registers a 0。

 a 1 and a2 and so on。 So this function has three arguments。 ours has one。 So only a0 would be used。

 if there is a return value and all of the system calls have a return value。

 that would be placed in a 0。This function also includes sorry this file user。

h also includes a number of other definitions for helper functions like print app and string length and so on。

 but we won't talk about those here。So next， let's ask which function gets invoked when a user mode program calls something like SB。

Or fork or read or write or any other system calls。 Well。

 the answer is a short assembly language program that runs in user mode is invoked。

 Those functions are often called Stub routines， and they're all collected。

 and all found in this file， Uis dot S。This is an assembly language file that contains a short stub routine for each of the 21 system calls。

 This file is actually automatically generated by a pearl script， U do P L。But I've shown here what。

Goes into that file。 It begins with an include， which is ciscal dot H。

 And let me just stop here because ciscal ciscal dot H is pretty straightforward。

 And I can dispense with it quickly。Thisis called that age。

Associateates a number with each of the system calls。

This number is how the user mode code communicates with the kernel as to which system call is desired。

 In our case， S break is 12 and so we include those definitions here。

And then for each one of the 21 system calls， we have these five lines in the file。

 I've only shown the code， the stub routineine for S break。

 but it's repeated once for each of the other 20 system calls。

And what does this dub function look like， Well， it's got a label and return。

 So it's like any function。 It's also got a global which exports that label。

 So it's visible in the linking phase。But primarily it has these three instructions， load immediate。

E call and read。Load immediate will move into register A 7 some value。 In this case。

 we remember that。The aspirate constant。It was 12， so this just moves 12 into a 7。

And then we execute the environment call or E call instruction， which performs the system call。

At the point which any of these global these stepub functions is called。

 we can expect the arguments to be in registers a0 through a5。

 we can accommodate up to six arguments to any system call。And。😊。

Those registers will be unchanged by this load immediate， and so they'll still be valid。

 and the kernelel will fetch the values that it saved for those registers。

 and that's how it accesses the arguments。The kernel code will also place or overwrite the a 0 register with whatever the return value should be。

 So after the kernel comes back， that is after we resume executing in user mode。At this point。

 a 0 will contain our result， and we don't touch it。 We just do the return instruction。

So in risk five， whenever a function is called， the return address is placed in Reg RA return address。

 and then the return instruction just simply copies the value and RA to the program counter。你。

Colonel will preserve all registers， except for a 0。 And so we can be sure that at this point。

 the value that was in our a here will still be there。 And so this can return properly。Okay。

 so we're executing in user mode and at some point an E call instruction is executed。

 and that causes a trap。 After that， we execute in kernel mode and then finally we execute the SRAD instruction in kernel mode and that returns us to user mode and we resume execution directly after the E call instruction。

 which is the return instruction in the subub routine。

So let's look at that in a little bit more detail， here is the process from the trap all the way down to the SRAD instruction。

A trap will change the mode into kernel mode， Disable interrupts。

Save the program counter and jump to the userve function。 user usererve is located in the。

Trimpoline page and it will save the user registers。

 load the kernel registers and change to the kernel's virtual address space。

 and finally we' jump to user trap， we'll look at user trap in more detail。

 but it will figure out that it's a system call and so on but eventually usert will call user trap RE and that will then invoke user RE。

 which is the simply code and that will finish the process and execute the Sre instruction and particular we'll go back to the user's address space。

 restore the user' registers， and then the Sra instruction itself。

 will change the mode to user mode and restore the program counter to whatever was saved and reenable interrupts。

Here's a diagram I used in an earlier video showing the same thing in a little bit more detail。

From the trap to the SRt instruction。Again， you see。the。Assembly code in the trampoline page。

At Uzervek being invoked to save the state of the process。It jumps to the user trap function。

Which then does some stuff and eventually called userTrap RE。

And that restores the state of the user process along with user RET and ultimately executes the SRt instruction。

So what happens in user trap？Well， we have a big if statement that determines the cause of the trap。

 Is it an exception or is it device requiring attention or is it a timer interrupt or is it a system call。

 If it's a system call， we enable interrupts and then invoke the Cis call function。

 and after that returns， it invokes the user trap rep to begin the process of going back to the user mode code。

We also check the killed flag。There's a boolean value that is set if somebody wants this process to die。

 and if it was set， then we invoke exit。I've also hinted at here that there could be some process switching going on。

The moment we re enableable interrupts， it's possible that a timer interrupt could occur or a device could require attention。

 so the scheduler could get involved。 And I tried to indicate that over here to say that any time between the interrupts being enabled here and the interrupts being disabled at the beginning of user trap Re。

 It's possible that we could switch to another process。

 But eventually we'll come back and finish executing whatever code is involved with the system call。

So now let's take a look at the。User trap function in more detail。

So I'm not going to go over everything here， but。We。Basically。

 have an if statement that looks at the。Status and control register called S cause to determine whether it's a system call or something else。

 And so we have some other stuff down here in the S statement。 But with risk 5。

 the S cause will have a value of 8 if it's a system call。 So that's what happens here。

 We basically execute the system call function。First。

 we check the killed flag to see whether someone has set it。 and if so， we invoke the exit function。

Exit will never return。 So if that happens， this process will stop right here。

 but assuming it continues going， we then look at the EP field in the trap frame。

When the trap occurred， the code and userbe will save all the registers and the program counter will have gotten saved in the EPC field。

The program counter will point to the E call instruction itself。

 and we need to advance it past the E call instruction， which is 4 bys to the next instruction。

 That's presumably the return instruction in the stub routine。The E instruction is four bytes。

 so we advance it to point to the next instruction， so when we return。

 we'll execute that return instruction。Here， we turn interrupts on， and then we invoke S call。

 After Cis call returns down here， we check the killed flag again and perhaps exit right there。

The which device would be set if there's a device interrupt。

 but we initialize it up here to zero and it's not changed。

 so we will not execute this yield function here。And then continuing with this function after the yield。

 we call user trap Re and then that which is shown right here。

 basically restores the state and ends up going back to the。

User RE function down here and returning to the user mode code。

After the function user trap figures out that the cause of the trap was a system call。

 it will invoke the Cis call function， which is located in the Cis call do C file and here's what happens。

Remember at the time of the trap， the code will save all of the registers of the user mode process。

 and the stub function has preloaded into register A7， a number in the case of S break。

 it would be 12。A number which indicates which system call is required。

 So the first thing we do is we get a pointer to the proc structure and then we chase it to the track frame and look at the a 7。

 the register a 7 field and retrieve that。 Okay， then what we're going to do is we're going to use that as an index into an array。

 So let me take a look at what occurs earlier in this file。

 This is a collection of function prototypes。 And here we see S break all lowercase。

 This is the function we're going to be calling to get the work done。And then we have an array。

 This array is called cis calls， and it's a bunch of pointers to functions。And so for number 12。

 where is it here， it's right here， we have the value of a pointer to the function。Cs S break。 Okay。

 and even though these aren't in quite the same order。

 all 21 system calls are represented with functions to execute the system call。And。

The array has all 12 entries for each one of those。 So that's what's going on here。 in Cis call。

 We look up the and get the pointer to the function and then we invoke that function。

 it's passed nothing and it's going to return a value and that value will be stored in the trap frame in a0 overr whatever was in a0。

 so that's where we actually call the SP function。 before we do the call。

 we just check to make sure that the user mode code is providing a legal number So we check to make sure it's greater than0 and that it's an entry in the cis call array and that that pointer is has been initialized to something and is' not null。

 So if it's all okay， we invoke it。 But if the user has provided a bad system call number。

 then we print。Message，Normally the kernel wouldn't just directly print a message for this sort of thing。

 but it would just return an error code。 So we do see the error code being returned。

 Were writing a -1 into a0。 but we're also printing some information here so we can debug both the kernel and the user mode program。

 The process I， the process name and the number that was actually provided。Okay。

 so in this file we have a couple of other functions， so I want to look at this first one。

These functions are helper functions that will be used in。

Functions like cysts brake that implement the actual system called。

 So we need a way to fetch the arguments。 So our raw。Will be past a number。

 That's the number of the argument you want 0 through 5，0。 So we' switching on 0 through 5 here。

 And all it does is it goes into the trap frame and returns the value of the users register a0， a 1。

 a2，3，4， or 5。And then it returns that value。That is used by this next function。 Arc int。

 and Arc int is just going to call Arc raw， but it's also going to store that value。

 It's past a pointer to an integer。 and so it stores it in that integer variable。

We have a couple of others， there's a a address， it's exactly the same code。

 So the only difference is that the variable that is being stored into has a type U int 64 as opposed to in inger here There's also one4 Arg。

String， okay， so fetch the inth word size system call argument。 Okay。

 so that's the number between 0 and 5。As a null terminated string。

Copyied into buffer and at most max characters。 So in addition to the number of the argument we want。

 we're past a point or to some buffer in its size， and it returns a string length of O。

 and -1 up there's a problem。 So here we are。Thattching the address here and putting in a local variable address。

 And then we have this function fetch string to do the work。 And fetch string is right here。

Fect the null terminated string at address from the current process。

And it returns the length of the string。And minus one for an error。

 so here we see that it's calling copy in string， we discussed that in a previous video when I discussed the virtual memory functions。

 helper functions， so basically it's going to use the current procedures page table。

And here's the address and kernel memory， and here's the virtual address and the maximum number of characters to copy。

 and then it's going to copy that return to string length。

There's also another helper function called a Fech address。

And the idea is the argument to the system call will be an address。

 and we want to go into the virtual address space and fetch a 64 bit value from the virtual address space。

 So this is past the virtual address， and the value that's fetched will be stored and it will be stored at the location pointed to by this pointer here。

So to do this， we see that the copy in function is used。

 we'll pass a page table data indicate which virtual ladder space it is， and here is IP。

 That's the place where we're supposed to store the8 bytes right This thing will be 64 bits or 8 bytes and here's the virtual address。

 and here is the number of bytes that we need to copy。 and well IP is a pointer to 64 bit values。

 So the size of that is 8 bytes。 So this is8 here。We see it expressed slightly differently here。

 so this is also eight bytes。First， I want to say that if there's a problem， we just return-1。

 Otherwise we return 0 here we're checking to make sure that the address that the user mode program has provided is correct。

 And we see that we're checking to make sure that the address。 Well。

 it better be somewhere between0 and the size of the address space right the top of the heap。

 the break point。 So we're checking to make sure that it is less than the break address。 Okay。

 and then we are adding well 8 bys to that。 And again。

 we are looking to see whether it is not too large。 okay。And if there's a problem with it。

 we return -1。Now， you might say， why in the world would we perform this check， Because clearly。

 if this is true and then you add8 to address， this is going to definitely be true。

 But here is the thing。 What about wraparound， we're using unsigned integers。

 What if address is way up at the very top of the 64 bit values， and when you add 8 to it。

 it wraps around。 So this one could be okay。 while this one is not。

 But if this one is below the size of the virtual addresser space。

 which is limited to substantially limited， then we won't have a wraparound here。

 So this is the kind of thing where you have security flaws in kernels。 So you need to be really。

 really careful with all possible values that the user code could provide。

 And so this is an example why this second check is actually necessary。Now。

 we're ready to look at the SP function that actually does the work。 It's located in the file。

 Cisproc do C， along with a bunch of other functions for other system calls。 Some are pretty simple。

 Like here's the one for the get P I D。 and all it does is simply go to the proc structure and fetch the process I D and return that。

😊，Each of these functions is past， no arguments and returns a return value that will go into a0。

Other functions or are more complicated here we see the fork system call。

 and it's just calling this function fork， which is located elsewhere to do all the work。

Let's go let's go down here to where we see。

![](img/d776f5ca8a095cd2f620acc20b57c0f7_2.png)

试。So let's go down to where we see cis break and we see that it will call the function growproc to do the work。

 But first it has a single argument which is number 0 in register a0。 so local variable n is here。

 the call to a in is just going to fetch the value from register a 0， and place it in the。

Variable in。 and we are then going to capture the previous size of this process。

 So we are just getting the size field and saving it here。 And that's what we will be returning。

 And then we talk call growproc to do the work。 Finally， let's look at the growproc function。

 which comes from the file proc dot C。So this will be past a number of bytes and it will grow or shrink the user memory。

 returning minus1 if there's a problem。We check the number of bytes to see if it's positive。

 in which case we call UVVM ac， or if it's negative， we call UVVM Dalc， or if it's0， we do nothing。

And U VM Alc is past the old size and the new size。

 So here we are capturing the old size from the proc structure， P。And adding a positive number to it。

 And if there is any problem， for example， if the pages cannot be allocated， we return -1。Otherwise。

 we will be returning zero。If we are shrinking the address based then in is a negative number and here the old size is larger than the new size。

 and UVMDalEC will always succeed so we don't need to check， but in any case。

 both of these return the size of the address space afterwards。

 so we save that in the size field of the proc structure。Okay。

 that's it for the anatomy of a system call。 I will see you in the next video。

