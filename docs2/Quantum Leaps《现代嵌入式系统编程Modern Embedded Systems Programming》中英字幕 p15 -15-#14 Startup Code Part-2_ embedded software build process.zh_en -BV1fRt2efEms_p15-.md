# Quantum Leaps《现代嵌入式系统编程Modern Embedded Systems Programming》中英字幕 p15 -15-#14 Startup Code Part-2_ embedded software build process.zh_en -BV1fRt2efEms_p15-

Welcome to the Modern Emded Systems Program course。

🎼My name is Mirosak and in this lesson I'll continue the subject of the startup code。

🎼Today you will learn about the embedded self Re process so that you can replace the generic vector table from the IR library with your own。

As usual， let's get started with making a copy of the previous lessons 13 project and renaming it to lesson 14。

 If you are just joining the course， you can download the previous projects from statemachine do com s Quickstar。



![](img/37a2cc0e5d7baed5e62431502256c5ec_1.png)

Get inside the new lesson 14 directory and double click on the workspace file to open the IR tool set。

 If you don't have the IR tool set， go back to lesson 0 to quickly summarize what happened so far at the end of the last lesson you encountered an important data structure at address 0 in RAM。



![](img/37a2cc0e5d7baed5e62431502256c5ec_3.png)

This turned out to be the vector table of your arm cortex M processor。The vector table you saw。

 however， turned out to be incomplete compared to the vector table described in the data sheet for your specific TM4 microcontroller。

This was because you are using the default start code from the I R library。 In this lesson。

 you will replace this generic vector table with your own。

But to understand how the replacing would work， you need to step back and take a deeper look at the embedded software built process。



![](img/37a2cc0e5d7baed5e62431502256c5ec_5.png)

![](img/37a2cc0e5d7baed5e62431502256c5ec_6.png)

So here is a diagram that shows the main steps of building your embedded project。 First。

 I'd like to make sure， however， that you understand that all these steps are performed by tools such as I R workbench on a desktop computer called the host machine。

Even though the produced program is for a completely different computer。

 such as your launchpad board， called the targetrget machine。This aspect， called cross development。

 is very characteristic for embedded systems。 It just doesn't make sense to run the compiler and linker on a small embedded target machine。

 such as the launchpad board。This is also in stark contrast with software development for the desktop computers where you typically both develop and run the software on the same machine。

 So the host is also the target。This type of software development is often called native development。

But going back to the embedded build process， the source files such as main dot C and Det C are fed to the C language compiler。

 which turns them into the so called object files， main dot O and delete at O。Next。

 all object flies from the project， together with any standard and other libraries。

 as well as the linker script are fed to the linker， which combines them into the final program。

At this point， most textbooks would simply tell you that an object file contains relocatable machine code that is not directly executable because it is not yet committed to any specific address in memory。

It is the job of the linker to combine all the objects。

 resolve the cross module references and fix the addresses。I could also leave it at that。

 but this course is about looking under the hood。 So I think it is worth while to see how object files are organized and what a relocable code really means。

In your project， object files are located in the subdirecty debug s OPJ。Here， among others。

 you can find delay dot O and main dot O。If you open one of them in a text editor。

 you will see mostly garbage because it is a binary file。However， even viewed as text。

 you should recognize that the file appears to contain distinct sections。Also。

 the first few ASI characters of the file spell out ELF。This is the indicator of the L file format。

 which stands for executable and linkable format， also known as extensible linking format。

Elf is not the only format for object files， but it is one of the most popular formats used by modern development tools。

 So I think it is a good idea for you to get somewhat familiar with the Elf files and the tools that allow you to inspect them。

For example， the IAR toolchain comes with a command line tool called ILdumparm。X。

 which you can use to dump the contents of the binaryL file in a human readable text。

Another popular tool for this is the object dump utility from the G compiler collection。

 which you can also use on the L files generated by IAR， because EF is a standard format。So here。

 for example， I invoke the IRIL dump arm utility in the Windows command prompt。

In most of such programs， to see a quick help， just launch the program without any parameters。

From the short help， you can see that the option minus minus all allows you to dump all the sections of the E file。

Because the listing is quite long， I capture the output into a text file， main do TXT。



![](img/37a2cc0e5d7baed5e62431502256c5ec_8.png)

Let's open the file in the IAR IDE。As you can see， the E file indeed contains several sections。

 many of which you have encountered in the last lesson 13， such as dot data。

 dot BSS and dot text for initialized data uninitialized data and code respectively。

The remaining sections hold the symbolic information for the linker。

 and a lot of sections contain debug information for the debugger。At this point。

 it should become clear that you should never use the size of the object file to assess the code size generated from a given dot C source code。

 because the actual machine code is only a small part among many other parts in an object file。

The only reliable source of information about the code size of various modules is the linker map file that you encountered in the previous lesson 13。

Now， let's take a look at the final image seeed out produced by the linker。 Again。

 if you quickly open it as text， you can see the Elf signature at the top。

 So this is also an Elf file。This means that you can also dump the content of the Cdot out file using the IAR IL D arm utility like so。

So now let's open the human readable dump of the final image and compare it with the dump of the main dot O object file by putting them side by side。



![](img/37a2cc0e5d7baed5e62431502256c5ec_10.png)

Let me scroll the final image to the dot text 16 section。

 which contains the code for the main function。By the way。

 the Elf dump utility is one of the quickest ways to see the disassembly of the generated code without necessarily loading the program into the target and inspecting the disassembly view。

You can use either the damp of the specific object file or the final image。

When you compare the elf dumps， you can see that most instructions are exactly the same in the main dot O as they are in the final image ct out。

However， interestingly， some instructions have a different encoding。For example。

 the 32 bit BL instruction by which main calls the delay function is encoded as hex F7 F F F FFE in the object file and hex F 0。

0，0， hex F 820 in the final image。 What's going on here。Well， B L is a PC relative instruction。

 meaning that in order to branch to the delay function。

 the program counter PC will be incremented by the signed immediate offset encode coded in the instruction itself。

 The problem is that the object file does not know where the delay function will end up in memory。

 So the BL up code in the object file contains a generic offset Hex 7 F， F， F， F F E。

This offset is then fixed by the linker after the linker decides where the delay function will be in memory with respect to main。

But this does not end here。 The linker needs to fix not just addresses of functions。

 but of variables as well。For example， the actual constant addresses of the variables P1。

 WTP2 and W2 are not known at compile time either， so if you look in the constant pool in the section question mark question mark main underscore 0 that follows immediately after the main function code you can see that all these addresses are0 in the object file。

So here again， when compared with the final Elf image。

 the linker had to fix this constant pool after figuring out where to put the variables P1， W T。

 and so on in the data section。From all these examples。

 I hope you start to appreciate the job of the linker and see for yourself what relocable code really means。

You can also understand now that the linker must be specific to the target processor because it must know the instructions and how to fix them at the binary upcode level。

This means， for example， that the link designed for the X86 processor of your PC cannot be used to link programs for the arm processor。

 even though all these tools might be using the L file format。

You need both a compiler and the linker for the same processor。Okay。

 so now that you understand what kind of code is generated into the object files by the compiler and perhaps more importantly。

 in which ways the object files are still incomplete。

 let's talk a bit about how the linker resolves the cross module references to functions and global variables。



![](img/37a2cc0e5d7baed5e62431502256c5ec_12.png)

First， you need to realize that every object provides symbols that it exports。

 meaning that they are defined in the object and can be used by other objects。



![](img/37a2cc0e5d7baed5e62431502256c5ec_14.png)

For example， main dot O exports the symbols P1， W，T， P2， W1， W2， and main。

An object might also have imported symbols that is symbols that it needs， but does not define。

 For example， may not O imports the function delay because it calls it， yet does not define it。Now。

 resolving interdependencies means that the linker must match all imported references to the exported references。



![](img/37a2cc0e5d7baed5e62431502256c5ec_16.png)

![](img/37a2cc0e5d7baed5e62431502256c5ec_17.png)

As the linker works on one object file at a time， it internally uses tool lists。

A list of exported symbols and a list of undefined symbols in all the objects encountered so far。

For example， at the very beginning of the linking process of your project。

 the exported list is empty， and the undefined list contains only one symbol underscore underscore vector table。

This latter symbol is specific to the I R tool set and might be different for other toolets。



![](img/37a2cc0e5d7baed5e62431502256c5ec_19.png)

The general rule is that all object files directly included in the project such as main O and Ded O are always linked into the final image。

Because of this， the order of linking does not matter for these files。

 but let's assume that the first will be made at O。



![](img/37a2cc0e5d7baed5e62431502256c5ec_21.png)

![](img/37a2cc0e5d7baed5e62431502256c5ec_22.png)

As the linker processes this object file， it adds all the exported symbols to the exported list and also takes every symbol imported by the object file。

 such as delay and tries to find it in the exported list。 If the symbol is not found as in this case。

 the linker adds it to the undefined list。So after processing the main O object。

 the undefined list contains vector table and delay。



![](img/37a2cc0e5d7baed5e62431502256c5ec_24.png)

Next is the delayed O object file。 This file exports delay。

 which is added to the exported list and at the same time。

 removed from the undefined list because it is now known and resolved。

 The delayed at O file does not import anything else。

 So the undefined list contains only one underscore underscore vector table symbol。At this point。

 there are no more object files in your project， yet the undefined list is still not empty。

 so the linker proceeds to look through the standard libraries。



![](img/37a2cc0e5d7baed5e62431502256c5ec_26.png)

Libraries are simply bundled collections of object files。 However。

 the linking rules are different for libraries than for objects included directly。

The critical difference is that objects from a library are added to the final image only if they contain symbols in the undefined list。

 otherwise， they are not added at all。It turns out that the underscore underscoreco vector table symbol is found to be exported by the object file。

 vector underscore table underscore M。0 in the IAR Library，T7m underscore Tl。A。How do I notice， Well。

 you can find it inside the Lier map file by searching for underscore underscore vector table。

 You find that symbol in the entry list section where you can see that it comes from the vector underscore table underscore M O object。

Next， you search the map file for this object name。

 and you find it again in the module summary section under the module R T7 m underscore Tl。 A。

 which you recognize as a library by the extension dot a archive。



![](img/37a2cc0e5d7baed5e62431502256c5ec_28.png)

However， it turns out that the object file， vector table M。 O。

 has also its own imported symbols such as underscore underscore IER program start。

 Pass fault handler， and so on。So at this point， the linker applies another rule for linking libraries。

 which is to keep searching all object files in the current library for the undefined symbols。

So again， as you can find out from the map file， underscoreco underscore IER program Start is found in the C Startup underscore M。

0 object in the same library。This object has some more imported symbols of its own。

 such as I R data in it 3， I R 0 in it，3， low level in it， and some others。Interestingly。

 IeR program start imports also the main function， which is resolved immediately because it is in the exported list。

 and， in fact， it is defined in your own object file， main at O。



![](img/37a2cc0e5d7baed5e62431502256c5ec_30.png)

The other undefined symbols are resolved by the library linking rule because they are exported by the other objects in the standard library。

An interesting observation from linking libraries is that the object files in libraries typically contain only one function or one variable。

 as opposed to containing a whole bunch of them。This fine granularity of objects ensures that only stuff actually needed is taken from the library。

If， on the other hand， the object would contain multiple functions and variables。

 all this would be linked in。 And so you would blow the final image unnecessarily。

So if you ever develop your own libraries， remember to make objects small and nimble。 Ideally。

 you should define only one function or one global variable per module。

But going back to your project， the linker eventually resolves all the references from the standard libraries。

 The undefined list becomes empty， and the linking process ends。Of course。

 another possible outcome is that the linker runs out of all objects and libraries。

 yet the undefined list still contains some symbols。In this case。

 youll get the linker error and a dump of all the unresolved references still present in the undefined list。

Typically to fix such errors， you need to add an object or a library。However。

 sometimes you might need to change the order of libraries。Occasionally。

 you might even have to specify some libraries more than once in the linking order to resolve circular interdependencies among them。

I don't have the time here to present you an example of circular dependencies among libraries。

 but in the class notes to this video， I provide a web link to a good article about linking programs with libraries。



![](img/37a2cc0e5d7baed5e62431502256c5ec_32.png)

Finally， in the last few minutes of this lesson， let's try to apply the knowledge of the build process to the task at hand。

 which is to replace the generic vector table from the IR library with your own。Well。

 how about you define the simple underscore underscore vector table in your own object module linked directly into your project。

This module then would be linked in before any standard IR libraries。

 so the library version of the vector table won't be used。This is exactly what you need。

So the plan is to add another C file to your project， which I will name Start underscore Tm4c。

c because it will be specific to your Tm4c microcontroller。But wait a minute。

 how a C module can accomplish anything at the start up time where the machine is not ready yet for executing C。

I mean， the stack pointer is not set up yet。 The initialized data is not copied from Ram to Ram。

 and the dot P S S section is not cleared yet either。So， yes。

 you are absolutely right to write such objections。 And， in fact。

 the start code for most other processors can't be written in C and requires you to use assembly language。

However， the arm cortex M has been specifically designed to reduce the need for low level assembly programming。

But even for cortex M， the startup code will require some nonstand language extensions。

 and of course， you will have to be careful not to assume an initialization of the dot data section or clearing of the dot BSS。

With these caveats， let's start coding the start T4 C file。

The main objective is to define the global array named underscore underscore table with the layout you saw at the very beginning of this lesson in the disassembly view。

So the first attempt to provide your own vector table is to define it as an end array and initialize it explicitly at the point of definition。

 as you've learned in a previous lesson。For starters。

 let's skim over the initialization by just typing a couple of zeros to make the compiler happy。

 and instead let's focus first on the biggest challenge here。

 which is the proper placement of the vector table in memory。

To see exactly where the linker has put the original vector table from the library。

 open the Lier map file。As you can see， the default vector table was in the dot inf section at address0 in RAM。

To understand how this section is defined， you need to open the Lier script file project aticf。

I've mentioned the linker script already in lesson 10。

 where you saw how to adjust the size of the stack。Today。

 you also already saw the linker script in the diagram of the embedded built process。

 The purpose of the Lier script is to tell the linker where to place the various merge program sections in the address space。

So let's open the link script file， Project doticF。The part of the script。

 delimited by the special comments， is controlled by the Lier configurationration file editor。

You can open this editor through their project options dialog box。

The location of the dot Inex section is controlled in the first tab of this editor。

The second tab controls the start and end of rom。As well as the start and end of Ramm。Finally。

 the third tab controls the size of stack and heap。

Let me slightly change the size of the stack to show you how it gets updated in the Lier script file。

But going back to the dot Inveex section， it is defined in the place command at the bottom of the Lier script。

 The section is read only and is placed at the address specified by the variable ICF added Invec start。

All this has important implications for your startup file because you need to make sure that your own vector table is placed in this special dot Invec linker section。

Unfortunately， there is no standard C syntax to place variables in specific sections。

But IeR provides an extension， which looks as follows。

You can specify the section by an ampersand followed by the section name in double quotes。

Make sure that the map file is visible and press F7 to build the project。As you can see。

 the compiler happily accepted your new startup file， but the linker map file looks strange。

The dot inve section is not at address 0 as before。

 but rather has been pushed down to the Ram region。I wonder if you can tell why。Well。

 the problem is that your vector table is a variable that can change。

 so the compiler can't put it into the read only memory。To force the vector table array into the rom。

 you need to define the vector table as a constant。 For this。

 the C language provides the keyword constant。The use of the Kt keyword in C deserves a whole separate lesson。

 but syntactically， it is very similar to the volatile keyword that I explained in lesson 5。

Similar to volatile， the keyword cons can be placed either before。Or after the type， again。

 just like with volatile， I recommend placing const after the type。

When you build the project and check the Lier map file now。

 the vector table is indeed placed in RoM at the correct address0。 Also。

 if you look at the object module that provides the vector table， it turns out to be your file。

 startup Tm4c。0。So congratulations， you have achieved the objective of this lesson。Please note。

 however， that at this point， the vector table is not initialized correctly， so the code won't work。

 In fact， I just found out that the code might hang up your debugger and prevent you from programming your board。

Therefore， just in case， please initialize your vector table with the following safe values。



![](img/37a2cc0e5d7baed5e62431502256c5ec_34.png)

This concludes the lesson about the embedded software build process at replacing the default vector table。

🎼In the next lesson， you will learn how to properly initial the vector table with the correct stack pointer and all interrupts available in your microcontrollers。

If you like this channel， please subscribe to stay tuned you can also visit statemachine。

com/quixstar for the class notess and project file downloads。



![](img/37a2cc0e5d7baed5e62431502256c5ec_36.png)