# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p64 7.2.4 Circuit Interleaving -BV1DZ421E7Yz_p64-

604 students work around the dryer bottleneck by finding a laundromat that has two dryers for every washer。

 looking at the timeline， you can see the plan， which is divided into 30 minute steps。

The washer is in use every step， producing a newly washed load every 30 minutes。

Drier usage is interleaved where der number one is used to dry the odd numbered loads。

 and drier number two is used to dry the even numbered loads。Once started。

 a dryer runs for a duration of two steps， a total of 60 minutes。

Since the dryers run on a staggered schedule， the system as a whole produces a load of clean。

 dry laundry every 30 minutes。The steady day throughput is one load of laundry every 30 minutes。

 and the latency for a particular load of laundry is 90 minutes。

And now here is the take home message from this example。

Consider the operation of the two dryer system。Even though the component dryers themselves aren't pipelined。

 the two dryer interleaving system is acting like a two stage pipeline with a clock period of 30 minutes and a latency of 60 minutes。

In other words， by interleaving the operation of two unppeline components。

 we can achieve the effect of a two stage pipeline。



![](img/969cbfcba6252bf10baf75c1375e0161_1.png)

Returning to the example of the previous section， we couldn't improve the throughput of our pipeline system past one over eight nanoseconds because the minimum clock period was set by the eight nanosecond latency of the C module。

To improve the throughput further， we either need to find a pipeline version of the C component or use an interleaving strategy to achieve the effect of a two stage pipeline using two instances of the unpipelined C component let's try that。



![](img/969cbfcba6252bf10baf75c1375e0161_3.png)

Here's a circuit for a general purpose2 weigh interleaever， using in this case。

 two copies of the unpipelined C component， C0 and C1。

The input for each C component comes from a D latch。

 which has the job of capturing and holding the input value。

There's also a multiplexer to select which C component output will be captured by the output register。

In the lower left hand corner of the circuit is a very simple two state FSM with one state bit。

The next state logic is a single inverter， which causes the state to alternate between zero and 1 on successive clock cycles。

This timing diagram shows how the state bit changes right after each rising clock edge。

To help us understand the circuit， we'll look at some signal waveforms to illustrate its operation。



![](img/969cbfcba6252bf10baf75c1375e0161_5.png)

To start， here are the waveforms for the clock signal and our FSM state bit from the previous slide。

A new X input arrives from the previous stage just after the rising edge of the clock。Next。

 let's follow the operation of the CZ component。Its input latch is open when FSMQ is low。

So the newly arriving x1 input passes through the latch， and C0 can begin its computation。

 producing its result at the end of clock cycle number2。

Note that the C0 input latch closes at the beginning of the second clock cycle。

 holding the X1 input value stable， even though the X input is starting to change。

The effect is that C0 has a valid and stable input for the better part of two clock cycles。

 giving it enough time to compute its result。The C1 waveform are similar。

 just shifted by one clock cycle。C1's input latch is open when FSMQ is high。

 so the newly arriving x2 input passes through the latch and C1 can begin its computation。

 producing its result at the end of clock cycle number3。Now let's check the output of the multipleer。

When FSM Q is high， it selects the value from C0， and when FSM Q is low。

 it selects the value from C1。 We can see that happening in the waveform shown。Finally。

 at the rising edge of the clock， the output register captures the value on its input and holds it stable for the remainder of the clock cycle。

The behavior of the interleaving circuit is like a two stage pipeline。

The input value arriving in cycle I is processed over two clock cycles。

 and the result becomes available on cycle I+ 2。What about the clock period for the inner leaving system？

Well， there is some time lost to the propagation delays of the upstream pipeline register that supplies the X input。

 the internal latches in multipleer， and the setup time of the output register。

So the clock cycle has to be just a little bit longer than half of the propagation delay of the C module。



![](img/969cbfcba6252bf10baf75c1375e0161_7.png)

We can treat the interleaving circuit as a two stage pipeline。

 consuming and input every clock cycle and producing a result two cycles later。

When incorporating an nway interle component in our pipeline diagrams。

 we treat it just like an end stage pipeline。So N of our pipelining contours have to pass through the component。



![](img/969cbfcba6252bf10baf75c1375e0161_9.png)

Here we've replaced the S on Pipeline C component with a two way interleaved C prime component。

We can follow our process for drawing pipeline contours。First。

 we draw a contour across all the outputs。Then we add contours。

 ensuring that two of them pass through the C prime component。

Then we add pipeline Regs at the intersections of the contours with the signal connections。

We see that the contours passing through C Prime have caused extra pipeline registers to be added to the other inputs to the F module。

Accommodating the two cycle delay through C prime。Somewhat optimistically。

 we specified the C prime minimum t clock to be4 nanoseconds。

 so that means that the slow component which determines the system clock period is now the F module with a propagation to lay of 5 nanoseconds。

So the throughput of our new pipeline circuit is one output every five nanoseconds。

 and with five contours it's a five pipeline， so the latency is five times the clock period or 25 nanoseconds by running pipeline systems in parallel。

 we can continue to increase the throughput。

![](img/969cbfcba6252bf10baf75c1375e0161_11.png)

Here we show a laundry with two washers and four dryers。

 essentially does two copies of the one washer two dryer system shown earlier。

The operation is as described before， except that at each step the system produces and consumes two loads of laundry。

So the throughput is two loads every 30 minutes for an effective rate of one load every 15 minutes。

The latency for a load hasn't changed， is still 90 minutes per load。

We've seen that even with slow components， we can use interleaving and parallelism to continue to increase throughput。

Is there an upper bound on the throughput we can achieve？Yes。

The timing overhead head of the pipeline registers and inter leavingaving components will set a lower bound on the achievable clock period。

 thus setting an upper bound on the achievable throughput。 Sorry。

 no infinite speed up is possible in the real world。



![](img/969cbfcba6252bf10baf75c1375e0161_13.png)