# 杜克大学《Java编程和软件工程基础-1｜Java Programming and Software Engineering Fundamentals》中英字幕 - P7：07_01_06_文本格式化与标签嵌套.zh_en - GPT中英字幕课程资源 - BV1gM4m117nk

We'll see here how some simple style tags change the appearance of texts on a page。

Here's an example using the bold tag。The B tag typically makes text bold or standing out from other texts。

 Notice that there is both a start and in B tagag。 Here's another example of bold。

 As we'll see later， the bold text might be displayed differently。 If you have trouble seeing。

 for example， if the web page is used by a screen reader。

 That's a semantics or meaning and helps the screen reader change how text is read to a blind user。

 for example。Here's an example using the emphasizes tag。

And here is an example nesting different tags。Let's see how all of this would be displayed on a web page。

Notice that some of the words are bold and some of the words are emphasized in a slanted text。

These two lines are displayed in bold text。And this line is displayed using a slanted text。

Look what happens when you nest tags。Here we have the bold tag nested inside the emphasize tag。

The outermost tag is the emphasized tag， and the innermost tag is bold。

 The innermost tag applies to the words example of nested。

 and the rest of the words inside emphasized get emphasized with slanted text。

You may be used to editing a word document where you type text。 What you see is what you get。

But with marketable language， what you see isn't what you get。

 The H you write will be displayed in a nicer way。 The way you specify it to be displayed。

