# CppCon【中英⚡CppCon 2024】 p23 P24 Deciphering C++ Coroutines Part 2 - Mastering Asynchronous Control Flow - An -BV1NHEEzdE92_p23-

![](img/2ca3a1f49ce032d87e36244aec568451_0.png)

The presentations， you learn something new even in basic presentations。

 but the hallway track is just indispensable。

![](img/2ca3a1f49ce032d87e36244aec568451_2.png)

![](img/2ca3a1f49ce032d87e36244aec568451_3.png)

Good morning。 Welcome to deciphering C+ plus corros。



![](img/2ca3a1f49ce032d87e36244aec568451_5.png)

嗯。

![](img/2ca3a1f49ce032d87e36244aec568451_7.png)

This is me， this is how you can reach me。And you might remember。

 I was on this stage two years ago with the talk about co routines and。

This is now part two of so a direct sQel to that other talk。

 This talk assumes familiarity with the contents that we covered in part1。

 so you should know about the basic mechanisms of the language mechanism of the core routines already。

 if you're watching this on YouTube right now and you didn't see part1 and you don't know cor routineines yet。

 I suggest that you' pause and go search for deciphering cor routine CPP con 201022 and watch that talk first We'll do a really quick recap of part one now but it will be very brief。

 So hang on tight。So quarantine in the simplest terms is just a function that can be paused in the middle。

 and then you can resume it later and all of your surrounding function state will still be intact just the way that you left it and you will just resume at the same place where you left off earlier。

Theres three major components to the language mechanism that you need to keep in mind。

 The first one is the return type， which is just the return type of the core routineine function。

 This is a user defined type that defines the interface through which the caller can interact with the core routineine。

C+ allows for a wide range of behaviors here and many different kinds of interfaces。

 And one goal of this talk is to help us better understand what the design space for these types is。

So as I said， the return type is just the return type of the function。

 so when we call a core routine， we can really think of that as like calling a factory function that creates this type。

 which is then a handle to the core routine state。Notice how we cannot tell by just looking at the signature whether this function is actually implemented as a core routine。

 this is different from most modern core routineine implementations in other languages where you have to mark each core routine by like an async keyword in the signature。

Since C++ 23， we now have in the standard library one core routine return type that ships with the standard library that's the generator。

 and that allows you to yield values from the middle of the function back to your collar。嗯。

The generator also implements the range concept， so anything that works with ranges can consume the values that are generated by this generator。

 If you don't really see how this is useful， I suggest as a little exercise。

 just try implementing an iterator that traverses a complex data structure like a tree orgraph and you will see that it is surprisingly difficult。

 in particular， if you compare it to the simple solution where you just loop over the elements。

And in part one， we actually showed everything that you need to implement a generator。

 So if you've seen part one， then you should have a rough understanding for how the generator works。

The second part is the promise type and while the return type determines the user facing interface of the co routineine。

 the promise type is part of the compiler facing interface here。

 it provides us means for customizing the behavior invoked during start and shutdown of the co routine so in part one we assembled like this little cheat sheet here that shows us all of the customization points will not go through that in detail again for times sake。

And then the final part that we discovered is the Aable。

 which is also part of the compiler facing interface。

 and this allows us to specify what happens whenever a core routine is suspended or resumed。

You can think of the awaitable， like every type that can be cooed on is an awaitable。

 So a coweight is the operator that puts a function into suspension。

 and the awaitable then customizes what happens there。Again。

 this is the overview that we saw in part 1。 And then we also looked at how all of these different parts are connected and how we can。

Turversese this little graph to like， pass data in and out of a core team。

If you want to use the cheat Chi to follow along the talk。

 you can use this QR code to look at it again， I will leave this up for a second。

So why am I back now here with the part 2？ So at the end of the last talk， I said。

 we now understand everything about co routineines， the language mechanism。

 But that just means we understand all of the boring parts。

 so we can now start doing the really interesting stuff that is exciting about cor routineines。

So this talk is about one of these interesting use cases and one that is of particular prominence。

 namely asynchronous computation， so other languages have dedicated language features for this。

 C sharpharp was probably the first one that popularized this， there's also Python JavaScript rust。

 they all have this async weight feature and if you work with with one of these languages it's very likely that you've seen that before。

嗯。But if you work with this in the other languages and you first encounter corotes in C+ plus。

 then you will notice that like from just understanding these， these basic language mechanisms。

 it's not really clear how you can do something like asy weight with C++ and the goal of this talk is actually to show you how that works to bridge that gap。

I will not have much time to motivate why acing a way it is useful to have in the first place。

 if you don't know it from other languages yet， I suggest a good exercise is just look at the documentation for boost AO。

Start by looking at the blocking E client server example。

 and then it will have a bunch of examples for an asynchronous echo client server for the different language versions like C++ 11。

14，1720 and just look at them in chronological order and by the time you arrive at co routines。

 you will hopefully understand why this is useful to have。If that sounds like too much work。

 there's also this great talk by Andre from Codedive last year。

 which is an entire talk dedicated to motivating this use case。

 It got way too little attention so far so please check it out if you're interested in this。

A word of caution， I will at times lead you astray。

 This is intentional and will hopefully help to deepen your insight。

 I will not be covering multithreading at all in this talk。

 That's an entirely different kind of worms。 and this is also not a best practice talk。

 All of the code that you will see here is very much slide where it's meant to illustrate a point it's not supposed to give like a best practice。

So。Let's start by building a mental model for what we're trying to do here。

So we can think of coroteines and an analog that is often given is coroteines are like cooperative threats。

 So what does that mean， So let's say we want to do some task， right， and it's a complex task。

 So it consists of several functions。 we have an outer function that depends on the partial result that is computed by middle function。

 the middle function in turn called an inner function。

 which retrieves like some data through IO right and the simplest way that we can do this is the inner function。

 We'll just use some blocking IO function。 and of course， this could take some time。

So the obvious problem here is that while the inner function is waiting。

Nobody else really can do any useful work here， right， the entire threat is， is occupied now。

 just waiting for this asynchronous result to come back。So。What if we， instead of just waiting here。

 we could just suspend all of these functions？Put them somewhere on the side and then do some useful work on the main thread in the meantime。

That is our main motivation。 that is what we want to do here。

So one straightforward solution to this is you just own a threat， right。

So you could use theing or you could roll your own mechanism using Jthread in futures。

 And then when you call spawn task， what they will do is it will just spawn up a new thread。

 And then while inner function is waiting， it's just blocking that one thread。

 But the main thread can do other useful stuff in the meantime。So of course。



![](img/2ca3a1f49ce032d87e36244aec568451_9.png)