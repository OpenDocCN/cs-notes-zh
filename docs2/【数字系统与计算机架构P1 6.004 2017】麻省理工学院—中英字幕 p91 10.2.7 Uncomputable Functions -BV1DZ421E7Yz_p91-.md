# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p91 10.2.7 Uncomputable Functions -BV1DZ421E7Yz_p91-

This video is optional， but we wanted to answer the question。 are there uncompatutable functions。



![](img/577c3a81eb6fb74b9541da1f53abae3e_1.png)

Yes， there are well defined discrete functions that cannot be computed by any Tring machine。

 In other words， no algorithm that can compute F of X for arbitrary finite x in a finite number of steps。

It's not that we don't know the algorithm。 we can actually prove that no algorithm exists。

So the finite memory limitations of FSMs。Wasn't the only barrier as to whether we can solve a problem。



![](img/577c3a81eb6fb74b9541da1f53abae3e_3.png)

The most famous uncompatutable function is the so called halting function。

When Turing machines undertake a computation， there are two possible outcomes。

 Either the Turing machine writes an answer onto the tape and halts。

 or the Turing machine loops forever。The holding function tells us which outcome we'll get。

Given two integer arguments， K and J， the halting function determines if the cafe Tring machine halts when given a tape containing J is the input。



![](img/577c3a81eb6fb74b9541da1f53abae3e_5.png)

Let's quick sketch an argument as to why the halting function is not computable。Well。

 suppose it was computable， then it would be equivalent to some Turing machine， say T sub H。



![](img/577c3a81eb6fb74b9541da1f53abae3e_7.png)

So we can use T sub H to build another Tring machine， T sub N。 The N stands for nasty。

Preprosses at single argument in either loops or halts。

T sub n of x is designed to loop if turninguring machine X given an input X halts。

And vice versa tabba N of X halts。 If turningring machine X given input X loops。

The idea is that t sub n of x does the opposite of whatever t sub x of x does。

T sub n is easy to implement， assuming that we have T sub H to answer the halt or loops question。Now。

 consider what happens if we give n as the argument to T of n。From the definition of Tiaban。

T sub n with the argument of n will loop if the holding function tells us that T sub n with the argument of n halts。

And。😊，T of n with the argument of n will halt if the halting function tells us that。

Tiser N with the argument and loops。Obviously， Tsib and of N can't both loop and halt at the same time。

So if the holding function is computable and T sub H exists。

 we arrive at this impossible behavior for T sub n applied to the argument N。

This tells us that T of H cannot exist and hence that the holding function is not computable。



![](img/577c3a81eb6fb74b9541da1f53abae3e_9.png)