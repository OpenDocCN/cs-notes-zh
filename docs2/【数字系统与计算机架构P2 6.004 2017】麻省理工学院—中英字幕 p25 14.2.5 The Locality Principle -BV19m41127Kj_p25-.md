# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p25 14.2.5 The Locality Principle -BV19m41127Kj_p25-

So how can the memory system arrange for the right data to be in the right place at the right time？

Our goal is to have the frequently used data in some fast SRAM。

 That means the memory system will have to be able to predict which memory locations will be accessed and to keep the overhead of moving data into and out of SRAM manageable。

 We'd like to amortize the cost to the move over many accesses。 In other words。

 we want any block of data we move into SRAM to be accessed many times。When not in SRAM。

 data would live in the larger， slower DRAM that serves its main memory。

 If the system is working as planned， DRAM axises would happen infrequently， for example。

 only when it's time to bring another block of data into SRAM。



![](img/5586d06d44d5e1a0f2922daaf0d571c0_1.png)

If we look at how programs access memory， it turns out we can make accurate predictions about which memory locations will be accessed。

The guiding principle is locality of reference， which tells us that if there's an access to address X at time T。

 it's very probable that the program will access a nearby location in the near future。



![](img/5586d06d44d5e1a0f2922daaf0d571c0_3.png)

To understand why programs exhibit locality of reference。

 let's look at how a running program accesses memory。Instruction fetches are quite predictable。

 Exional usually proceeds sequentially since most of the time the next instruction is fetched from the location after that of the current instruction。

Code that loops will repeatedly fetch the same sequence of instructions as shown here on the left of the timeline。

There will of course be branches and subrtine calls that interrupt sequential execution。

 but then we're back to fetching instructions from consecutive locations。Some programming constructs。

 for example， method dispatch in object oriented languages。

 can produce scattered references to very short code sequences。

 as shown on the right of the timeline， but order is quickly restored。

This agrees with our intuition about program execution， for example。

 once we execute the first instruction of her procedure。

 we'll almost certainly execute the remaining instructions in the procedure。

So if we arrange for all the code of a procedure to be moved to SRAM when the procedure's first instruction was fetched。

 we'd expect that many subsequent instruction fetches could be satisfied by the SRAM。

And although fetching the first word of a block from DRAM has relatively long latency。

 the DRA's fast column accesses will quickly stream the remaining words from sequential addresses。

This will amortize the cost of the initial access over the whole sequence of transfers。

The story is similar for accesses by a procedure to its arguments and local variables in the current stack frame。

Again， there will be many accesses to a small region of memory during the span of time where executing the procedure's code。

Data accesses generated by load and store instructions also exhibit locality。

 The program may be accessing the components of an object or struct。

 or it may be stepping through the elements of an array。

Sometimes information is moved from one array or data object to another。

 as shown by the data axises on the right of the timeline。Using simulations。

 we can estimate the number of different locations that will be accessed over a particular span of time。

 what we discover when we do this is the notion of a working set of locations that are accessed repeatedly。

If we plot the size of the working set as a function of the size or the time interval。

 we see that the size of the working set levels off。In other words。

 once the time interval reaches a certain size， the number of locations accessed is approximately the same。

 independent of when in time the interval occurs。As we see in our plot to the left。

 the actual addresses accessed will change， but the number of different addresses during the time interval will。

 on the average， remain relatively constant， and surprisingly， not all that large。

This means that if we can arrange for our SRAM to be large enough to hold the working set of the program。

 most accesses will be able to be satisfied by the SRAM。

We'll occasionally have to move new data into the SRAM and old data back to DRAM。

But the DRAM axises will occur less frequently than SRAM accesses。

Or work out the mathematics in a slide or two， but you can see that thanks to locality of reference werere on track to build a memory out of a combination of SRAM and DRAM that performs like an SRAM。

 but has the capacity of the DRAM。

![](img/5586d06d44d5e1a0f2922daaf0d571c0_5.png)

The SRAM component of our hierarchical memory system is called a cache。

It provides low latency access to recently accessed blocks of data。

If the requested data is in the cache， we have a cache hit and the data is supplied by the SRAM。

If the requested data is not in the cache， we have a cache miss and a block of data containing the requested location will have to be moved from DRAM into the cache。

The locality principle tells us that we should expect cash hits to occur much more frequently than cash misses。

Modern computer systems often use multiple levels of SRAM caches。

The levels closest to the CPU are smaller， but very fast。

 while the levels further away from the CPU are larger and hence slower。

A miss at one level of the cache generates an access to the next level and so on until a DRA access is needed to satisfy the initial request。

Cashching is used in many applications to speed up accesses to frequently access to data。For example。

 your browser maintains a cache of frequently accessed web pages and uses its local copy of the web page if it determines that the data is still valid。

 avoiding the delay of transferring the data over the internet。



![](img/5586d06d44d5e1a0f2922daaf0d571c0_7.png)

Here's an example memory hierarchy that might be found on a modern computer。

There are three levels of on chip SRAM caches followed by a DRAM main memory and a flash memory's cache for the hard disk drive。

The compiler is responsible for deciding which data values are kept in the CPU Regs and which values require the use of loads and stores。

The three level cache and access to DRAM are managed by circuitry in the memory system。After that。

 the access times are long enough， many hundreds of instruction times that the job of managing the movement of data between the lower levels of the hierarchy is turned over to software。

Today we're discussing how the on chip caches work。

In part three of the course we'll discuss how the software manages main memory and non volatile storage devices。



![](img/5586d06d44d5e1a0f2922daaf0d571c0_9.png)

Whether managed by hardware or software， each layer of the memory system is designed to provide lower latency access to frequently accessed locations in the next slower layer。

As we'll see， the implementation strategies should be quite different in the slower layers of the hierarchy。



![](img/5586d06d44d5e1a0f2922daaf0d571c0_11.png)