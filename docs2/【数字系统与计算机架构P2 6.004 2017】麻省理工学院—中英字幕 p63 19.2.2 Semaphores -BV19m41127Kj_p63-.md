# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p63 19.2.2 Semaphores -BV19m41127Kj_p63-

What we'd like to do is to create a single abstraction that can be used to address all our synchronization needs。

In the early 1960s， the Dutch computer scientist Esgar Dyktro。

 proposed a new abstract data type called the Semaphore。

 which has an integer value greater than or equal to0。A programmer can declare a sepho shown here。

 specifying its initial value。The se4 lives in a memory location shared by all the processes that need to synchronize their operation。

The semaphoce accessed with two operations， weight and signal。

The weight operation will wait until the specified semophore has a value greater than0。

 then will decrement the semopho value and return to the calling program。

If the semophore value is0 when weight is called， conceptually execution is suspended until the semophore value is non0。

In a simple， inefficient implementation， their weight routine loops。

 periodically testing the value of the se4 proceeding when its value is non0。

The signal operation increments the value of the specified semaphore。

If there are any processes waiting on that seophore， exactly one of them may now proceed。

We'll have to be careful with the implementation of signal in weight to ensure that the exactly one constraint is satisfied。

 In other words， that two processes both waiting on the same semophore won't both think they can decrement it and proceed after a signal。

A semaphore initialized with a value K guarantees that the I called a signal will precede the I plus k call to wait。

 in a moment， we'll see some concrete examples that will make this clear。Note that in 604。

 we're ruling out semaphos with negative values。In the literature。

 you may see P of S used in place of weight of S and V of S used in place of signal of S。

These operation names are derived from the Dutch words for test and increase。

 Let's see how to use semaphos to implement precedes constraints。



![](img/df3dac9e49758ca47b250394f39da83a_1.png)

Here are two processes each running a program with five statements。

Execution proceeds sequentially within each process， so A1 executes before A2 and so on。

But there are no constraints on the order of execution between the processes。

 so statement B1 and process B might be executed before or after any of the statements in process A。

Even if A and B are running in a time shared environment on a single physical processor。

 execution may switch at any time between processes A and B。

Suppose we wish to impose the constraint that the execution of State A2 completes before execution of statement B4 begins。

The red arrow shows the constraint we want。Here's the recipe for implementing this sort of simple precedent constraint using semaphos。

First， declare a semophore called S in this example and initialize its value to0。

Place a call to signal S at the start of the arrow。 In this example。

 signal S is placed after the statement A2 in process A。

Then place a call to weight S at the end of the arrow。In this example。

 weight of S is placed before the statement B4 and process B。With these modifications。

 process A executes us before with a signal December 4 S happening after State A2 is executed。

Statements B1 through B3 also executed us before， but when the weight S is executed。

 execution of process B is suspended until the signal S statement has finished execution。

This guarantees that execution of B4 will start only after execution of A2 has completed。

By initializing the semaphore S to0， we enforced the constraint that the first call to signal of S had to complete before the first call to weight of S would succeed。



![](img/df3dac9e49758ca47b250394f39da83a_3.png)

Another way to think about semaphorees is as a management tool for a shared pool of K resources。

 where K is the initial value of the semaphore。You use the signal operation to add or return resources to the shared pool and use the weight operation to allocate a resource for your exclusive use。

At any given time， the value of the Semophore gives the number of unallocated resources still available in the shared pool。

Note that the weight and signal operations can be in the same process。

 or they may be in different processes， depending on when the resource is allocated and returned。



![](img/df3dac9e49758ca47b250394f39da83a_5.png)

We can use semaphos to manage our end character 50 buffer Here。

 we've defined a semaphore cares and initialized it to 0。

 The value of caris will tell us how many characters are in the buffer。

So Sen does a signal cares after it is added a character to the buffer。

 indicating the buffer now contains an additional character。

And received as a await cares to ensure the buffer has at least one character before reading from the buffer。

Since cares was initialized to 0， even forced the constraint that the Ith call to signal cares precedes the completion of the Ithe call to await cares。

 In other words， receive can't consume a character until it has been placed in the buffer by send。

Does this mean our producer and consumer are now properly synchronized using the carroemmopho we implemented one of the two precedents constraints we identified as being necessary for correct operation？

Next， we'll see how to implement the other precedent's constraint。



![](img/df3dac9e49758ca47b250394f39da83a_7.png)

What keeps the producer from putting more than end characters into the end character buffer？Nothing。

Oops， the producer can start to overwrite characters placed in the buffer earlier。

 even though they haven't yet been read by the consumer。This is called buffer overflow。

 and the sequence of characters transmitted from producer to consumer becomes hopelessly corrupted。

What we've guaranteed so far is that the consumer can read a character only after the producer has placed it in the buffer。

 In other words， the consumer can't read from an empty buffer。

What we still need to guarantee is that the producer can't get too far ahead of the consumer。

Since the buffer holds it most end characters， the producer can't send theI plus n character until the consumer has read the Ithe character。



![](img/df3dac9e49758ca47b250394f39da83a_9.png)

Here we've added a second sepho， spaces to manage the number of spaces in the buffer。Initially。

 the buffer is empty， so it has n spaces。 The producer must wait for a space to be available。

 when spaces is non0， the weight succeeds， decrementing the number of available spaces by one。

 And then the producer fills that space with the next character。😊。

The consumer signals the availability of another space after it reads a character from the buffer。

There's a nice symmetrymmetry here。The producer consumes spaces， and produces characters。

The consumer consumes characters and produces spaces。

Semaphorees are used to track the availability of both resources， in other words。

 characters and spaces， synchronizing the execution of the producer and consumer。

This works great when there's a single producer process and a single consumer process。 Next。

 we'll think about what will happen if we have multiple producers and multiple consumers。😊。



![](img/df3dac9e49758ca47b250394f39da83a_11.png)