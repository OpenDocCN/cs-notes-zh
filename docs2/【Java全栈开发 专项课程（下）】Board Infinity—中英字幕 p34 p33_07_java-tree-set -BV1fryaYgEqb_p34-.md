# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p34 p33_07_java-tree-set -BV1fryaYgEqb_p34-

![](img/ce9639fe1983b4e886cd268a91eed95c_0.png)

Hi there Today in this session， I will talk about three set。

 which is one of the most important implementation of the sortet set interface in Java that uses a 34 storage。



![](img/ce9639fe1983b4e886cd268a91eed95c_2.png)

Basically， it inherits abstract set class and implements the navigigable set interface。

 So whenever we work with an navigable set interface methods。

 we need to instantiate the treeet only the ordering of the elements is maintained by a set using their natural ordering。

 whether or not an explicit comparator is provided。 This must be consistent with equal。

 if it is correctly implemented by the set interface。

 It access and retrieval times are quick fast because its implementation is3。

 tree set is being implemented using a binary search tree。

 which is selfbal just like a red black tree， Therefore operations such as search remove and add consume biggo logan and time。

 the reason behind this is there in the self balancingan tree。

 And if there is to ensure the tree height never exceed by the logan for all the mentioned operations because it's a half of the。

😊。

![](img/ce9639fe1983b4e886cd268a91eed95c_4.png)

![](img/ce9639fe1983b4e886cd268a91eed95c_5.png)

Entire tree to be itated， an object is set to be itated when it comes to the part of the tree only。

 Therefore， it is one of the efficient data structure in order to keep the large data that is sorted and also do operations on it。

 These are some specific methods that you can use with the tree set along with whatever methods are available in in its extent implemented。

Or extended interfaces in classes you can come across。

Let's try to implement it practically with an example。 Here， I'm creating a set。Of string elements。

Where the class is three set。Set one dot at。Set dot add。Leted see E。Said dot at， let's say be。

Said dot add， let's say see。Here we say set set dot add。

 let's say see one more time and now let's try to print our three set。

So you can see that dlicacy is removed and the natural order is maintained。Moreover。

 if you would like to。Check for the contains， just go and check。

C dot contains whether let me check whether C contains or not this time I'm checking with D。

 whether D contains or not， so it should return false because it does not contains。Now。

 from the tree I drivers， I can check set Dot first。So I can see that it's not coming up。

So you can see that first and last is not coming up， so if you will change it to navigate。Sit。😊，Now。

 all these methods would be available。 So depends by which particular operation you wanted to come up and what's your requirement。

 you wanted to navigate or you wanted to go for the set properties as well。

 You can also get ci out set dot。Higher than a particular value。 let's say B higher than V。

And I can go for set dot。Lower than a particular value。 Let's say B， Let's see what it gets printed。

 So we can see that higher than B is C and lower than B is a。

So this is how the set operations can be worked and if you would like to go for navigable set。

 you can also remove the element with the help of poll first。And pull last， said dot Paul first。

And set dot poll last。 So it will tell you which element is。Removed or pulled out。Internally。

 string buffer also uses the tree set because the moment the tree set is to be implemented。

 It will give me the implementation for duplicate values to not be allowed。

 And that's what the string buffer says that in the natural order is maintained。

 That's the feature of the。Preset says that I now hope you can understand where exactly needs to be implemented this preset in the real time after this discussion。

Until next time， see you soon。🎼。

![](img/ce9639fe1983b4e886cd268a91eed95c_7.png)

![](img/ce9639fe1983b4e886cd268a91eed95c_8.png)