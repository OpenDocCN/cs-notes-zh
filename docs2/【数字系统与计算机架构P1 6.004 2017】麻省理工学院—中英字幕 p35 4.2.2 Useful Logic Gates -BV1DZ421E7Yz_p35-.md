# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p35 4.2.2 Useful Logic Gates -BV1DZ421E7Yz_p35-

On our to do list from the previous section is figuring out how to build and and or gates with many inputs。

 these will be needed when creating circuit implementations using a summer products equation as our template。

Let's assume our G library has only two input gates and figure out how to build wider gates using the two input gates as building blocks。

We'll work on creating three and four input gates， but the approach we use can be generalized to create and and or gates if any desired width。

The approach shown here relies on the associative property of the and operator。

This means we can perform an n and by doing pairwise ans in any convenient order。

The orR and Xor operations are also associative， so the same approach will work for designing wide or and X or circuits from the corresponding2 input gate。

 simply substitute two input or gates or two input X or gates for the two input and gates shown below。

 and you're good to go。Let's start by designing a circuit that computes the and of three inputs， A。

 B， and C。In the circuit shown here， we first compute A and B。And then and that result with C。

Using the same strategy， we can build a four input end gate from three to input end gates。

Essentially， we're building a chain of end gates which implement an nway and using n minus12 input end gates。

We can also associate the four inputs a different way， computing A and B in parallel with C and D。

 then combining those two results using a third end gate。Using this approach。

 we're building a tree of ant gates， which approach is best， chains or trees。 First。

 we have to decide what we mean by best。 when designing circuits， were interested in cost。

 which depends on the number of components and performance。

 which we characterize by the propagation delay of the circuit。

Both strategies require the same number of components since the total number of pairwise ands is the same in both cases。

So it's a tie when considering costs。Now consider propagation delay。

The chain circuit in the middle has a TPD of three gate delays。

 and we can see that the TPD for an n input chain will be n minus1 gate delays。

The propagation delay of change grows linearly with a number of inputs。

The tree circuit on the bottom has a TPD of two gates， smaller than the chain。

The propagation delay of trees grows logarithmically with a number of inputs。

 specifically the propagation delay of tree circuits built using two input gates grows as log base2 of n。

When N is large， tree circuits can have dramatically better propagation delayed than chain circuits。

The propagation delay is an upper bound on the worst case delay from inputs to outputs and is a good measure of performance assuming that all inputs arrive at the same time。

But in large circuits， A， B， C， and D might arrive at different times depending on the TBBD of the circuit generating each one。

Suppose input D arrives considerably after the other inputs。

If we use the tree circuit to compute the and of all four inputs。

 the additional delay in computing Z is two gate delays after the arrival of D。However。

 if we use the chain circuit， the additional delay in computing Z might be as little as one gate delay。

The moral of this story。It's hard to know which implementation of a sub circuitrcuit。

 like the four input and shown here， will yield the smallest overall TPD unless we know the TPD of the circuits that compute the values for the input signals。



![](img/96d28ed1ba2f099a8a307efb61f0bd3e_1.png)

In designing seaAss circuits， the individual gates are naturally inverting。

 so instead of using and in or gates for the best performance we want to use the Nand and nor gate shown here。

Nand and Nor gates can be implemented as a single CMOSs gate involving one pull up circuit and one pull down circuit。

And an or gate require two CMmoss gates in their implementation， for example。

 a Nand gate followed by an inverter。We'll talk about how to build summer product circuitry using NanNs andNs in the next section。

Note that NN and nor operations are not associative。

Nand of A B and C is not equal to the Nand of C with the Nand of A and B。

So we can't build a Nand gate with many inputs by building a tree of two input Nands。

We'll talk about this in the next section too。We've mentioned the exclusive ore operation。

 sometimes called Xor several times。This logic function is very useful when building circuitry for arithmetic or parity calculations。

As you'll see in La 2， implementing a two input X or gate will take many more NFEs and PFEs than required for a two input NN or nor。



![](img/96d28ed1ba2f099a8a307efb61f0bd3e_3.png)

We know we can come up with a summer products expression for any truth table and hence build a circuit implementation using inverters and gates and or gates。

It turns out we can build circuits with the same functionality using only two input Nand gates。

 we say the two input Nant is a universal gate。Here we show how to implement the summer products's building blocks using just two input Nand gates。

 in a minute we'll show a more direct implementation for summer products using only NNs。

 but these little schematics are a proof of concept showing that Nand only equivalent circuits exist。

Two input nor gates are also universal， as shown by these little schematics。

Inverting logic takes a little getting used to， but it's the key to designing low cost。

 high performance circuits and CMmoOSs。

![](img/96d28ed1ba2f099a8a307efb61f0bd3e_5.png)