# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p50 5.2.6 Timing Example -BV1DZ421E7Yz_p50-

![](img/284812c22eeb1509853db69d7b601984_0.png)

Using a D register as the memory component in our sequential logic system works great at each rising edge of the clock。

 the register loads the new state， which then appears at the register's output as the current state for the rest of the clock period。



![](img/284812c22eeb1509853db69d7b601984_2.png)

The combinational logic uses the current state and the value of the inputs to calculate the next state and the values for the outputs。

A sequence of rising clock edges and inputs will produce a sequence of states。

 which leads to a sequence of outputs。In the next chapter， we'll introduce a new abstraction。

 finite state Ma that will make it easy to design sequential logic systems。



![](img/284812c22eeb1509853db69d7b601984_4.png)

Let's use the timing analysis techniques we've learned on the sequential logic system shown here。

The timing specifications for the register and Comal logic are as shown。

Here are the questions we need to answer。The contamination delay of the combinational logic isn't specified。

What does it have to be in order for this system to work correctly？Well。

 we know that the sum of the register and logic contamination delays has to be greater than or equal to the whole time of the register。

 using the timing parameters we do know， along with a little arithmetic。

 tells us that the contamination delay of the logic has to be at least one nanosecond。

What is the minimum value for the clock period T clock？

The second timing inequality from the previous section tells us that T clock has to be greater than the sum of the register and logic propagation delays。

 plus the setup time of the register。Using the known and values for these parameters gives us a minimum clock period of 10 nanoseconds。

What are the timing constraints for the input signal relative to the rising edge of the clock？

For this， we'll need a diagram。The next state signal is the input to the register。

 so it has to meet the setup and hold times as shown here。



![](img/284812c22eeb1509853db69d7b601984_6.png)

Next， we show the input signal。How the timing of its transitions affect the timing of the next state signal。

 Now it's pretty easy to figure out when input has to be stable before the rising clock edge。

 In other words， the setup time for input。The setup time for input is the spell with the propagation delay of the logic。

 plus the setup time for the register， which we calculated at 7 nanoseconds。In other words。

 if the input signal is stable at least seven nanoseconds before the rising clock edge。

 the next date will be stable at least two nanoseds before the rising clock edge。

 and hence meet the register's specified setup time。Similarly。

 the whole time of input has to be the whole time of the register minus the contamination delay of the logic。

 which we calculate as one nanosecond。In other words。

 if input is stable at least one nanosecond after the rising clock edge。

 the next date will be stable for another one nanosecond， in other words。

 a total of two nanoseconds after the rising clock edge。

This meets the specified whole time of the register。

This completes our introduction to sequential logic。

Pretty much every digital system out there is a sequential logic system and hence is obeying the timing constraints imposed by the dynamic discipline。

So next time you see an ad for a 1。7 gigahertz processor chip， you'll know where the 1。7 came from。



![](img/284812c22eeb1509853db69d7b601984_8.png)