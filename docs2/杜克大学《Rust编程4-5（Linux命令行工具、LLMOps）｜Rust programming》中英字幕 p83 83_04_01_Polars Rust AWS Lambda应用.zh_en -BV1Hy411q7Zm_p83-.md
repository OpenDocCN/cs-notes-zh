# 杜克大学《Rust编程4-5（Linux命令行工具、LLMOps）｜Rust programming》中英字幕 p83 83_04_01_Polars Rust AWS Lambda应用.zh_en -BV1Hy411q7Zm_p83-

![](img/6d67b582e401cee5829169d7b6704474_0.png)

Data engineering has a superpower， and it is Ru Pols， the rust Data frame library plus rust AWS Lada。

 A few of the reasons why are that it is low memory。

 so you get a very efficient runtime that doesn't cost you a lot of money。

 Ru is one of the best languages in the world at memory management and in terms of the response time。

 you get millisecond response。 you get both a performance application， but also low build time。

 Finally， the arm target is well supported with rust pulls and rust AWs Lada here。

 So really rust offers significant performance advantages over interpreted languages like Python and Java tools。

 the memory safety alone without garbage collection allows for faster execution and more predictable performance。

 Now， if we take a look at polars here， this is part of the secret sauce in that it's designed to be a fast data frame library implemented in rust and it can outperform many。

Similar libraries in other languages in terms of speed and also if you're running rust on AWS Lambda。

 it allows you to take advantage of the serverless architecture for scalability and efficiency So you can see here these are some of the ways that you can invoke and interact from cargo Lambda invoke to a URL endpoint to an event and you can mix your code together with the core logic in the library and then the Lambda invocation in the main dos and use the excellent cargo Lambda library to deploy it to the AWS runtime Now if you take a look at parallelism as well。

 rest ownership system and the built-in concurrency enables you to have efficient parallel execution and this can be a lot more complex in a language like Python which has the gill and Java which has other issues with concurrency so you're able to harness all of the multi core processors and this can lead to substantial improvement in data。

now in terms of cost effectiveness already talked about this。

 but a few things to remember are that arm based Lambdas are built with optimized rust binaries。

 and these can lead to lower execution times and memory usage and this translates into a much lower cost compared to both Python and Java based solutions。

Okay， we haven't even talked about safety and reliability。

Rust is also well known for strong safety guarantees。

 especially with memory safety and spread safety。 And these errors are much more common in Python and Java。

 Now， in terms of integration and modern tooling。 The rust ecosystem is really one of the best in the world。

 A vibrant growing has tools， again， like cargo Lambda。 The cargo ecosystem alone。

 And Pols is an emerging standard now for data frames， and has extremely fast performance。

 one of the fastest data frame libraries in the world。 And finally。

 Aws Lambda has been around for quite some time。 and it's very successful。 And surprise。

 it's also built in rust using the firecracker open source framework。 So really。

 rust is an emerging superpower for data engineering。 And you owe it yourself to take a look。😊。



![](img/6d67b582e401cee5829169d7b6704474_2.png)

![](img/6d67b582e401cee5829169d7b6704474_3.png)