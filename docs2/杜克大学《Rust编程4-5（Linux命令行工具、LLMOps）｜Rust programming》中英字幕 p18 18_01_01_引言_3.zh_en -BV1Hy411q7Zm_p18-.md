# 杜克大学《Rust编程4-5（Linux命令行工具、LLMOps）｜Rust programming》中英字幕 p18 18_01_01_引言_3.zh_en -BV1Hy411q7Zm_p18-

![](img/5ed3326364a2ba7377dc7c7acd0b29e0_0.png)

Now that we know a little bit of the basics of Python and building some of the command line tools and sometimes with or without dependencies and the same with rust。

Let's see how we can work better with depend penencies and we'll see that for both use case。

 both Python and rust， so this lesson will be divided into half looking into Python and look into building modules and working with imports in a more advanced scenario rather than then just having like a single file that does something which is a typical Python script and then we'll also take a look at how to work with the penencies then we'll take a look at the Python package index。

 which is essentially if you later on will understand how to publish your tools to the Python package index if you want to distribute your tool you want to make it available for your users and then we'll do the same thing with rust willll try to understand working with modules in a more complex way。

 not so simple with maybe the main that Rs or the Lib that Rs files but let's take it a step beyond and see how that works and then we'll take。

Look at crs I O， which is the the same thing as the Python package index is a place where you can publish your tool。

 your library， anything that you build in rust， really to share it out with the world to make it useful for other users so that they can install it in their system in a very straightforward way。

 So let's see what are all those components about。 And let's try， try them out。😊。

