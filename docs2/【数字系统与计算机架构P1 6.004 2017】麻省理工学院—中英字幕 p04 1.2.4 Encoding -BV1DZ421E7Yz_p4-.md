# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p04 1.2.4 Encoding -BV1DZ421E7Yz_p4-

Next， we turn our attention to encoding data as sequences of zeros and ones， a string of bits。

And encoding is an unambiguous mapping between bit strings and the members of the set of data to be encoded。

 For example， S we have a set of four symbols， A， B， C， and D。



![](img/c50a3654f7f457b00c294e3eb6cb37f8_1.png)

And we want to use bit strings to encode messages constructed to these symbol， for example， A BBA。



![](img/c50a3654f7f457b00c294e3eb6cb37f8_3.png)

![](img/c50a3654f7f457b00c294e3eb6cb37f8_4.png)

If we choose to encode the message one character at a time。



![](img/c50a3654f7f457b00c294e3eb6cb37f8_6.png)

Our encoding would assign a unique bit string to each symbol。Since we have four samples。

 we might choose a unique2bit string for each， A could be 00。



![](img/c50a3654f7f457b00c294e3eb6cb37f8_8.png)

B01， C10， and D11。This would be called a fixed length encoing。

 since the bit strings used to represent the symbols all have the same length。

The encoding for the message ABBA would be 00-01-01-00。And we can run the process backwards。

Given a bit string and the encoding key， we can look up the next bits in the bit string。

 using the key to determine the symbol they represent 00 would be decod as a，01 is B， and so on。

We can also use bit strings of different links to encode symbols。

This is called a variable length encoding。So a could be 01， B1， C0，0，0。

 and D001 A BBA would be encoded as 01，1101。We'll see that carefully constructed variable length encodings are useful for the efficient encoding of messages。

 where the symbols occur with different probabilities。

We have to be careful that the encoding is unambiguous。 Suppose we had decided to encode A as 0。

 B is 1。 C is 1，0， and D is 1，1。The encoding for A BB A would be 0，1，1，0。

 Looking good since bad encoding is shorter than either of the two previous encodings。Now。

 let's try to decode this bit string， oops， using the encoding key。

 we can unfortunately arrive at several decodings。 A BB A， of course， but also AA or ABC。

 depending on how we group the bits。This attempt is specifying and encoding has failed since the message cannot be interpreted unambiguously。

 Gally， we can represent an unambiguous encoding as a binary tree。

 labeling the branches from each tree node with 0 and  one。

 placing the symbols to be encoded as the leaves of the tree。



![](img/c50a3654f7f457b00c294e3eb6cb37f8_10.png)

![](img/c50a3654f7f457b00c294e3eb6cb37f8_11.png)

![](img/c50a3654f7f457b00c294e3eb6cb37f8_12.png)

If you build a binary tree for a proposed encoding and find that there are no symbols labeling interior nodes and exactly one symbol at each leaf。

 then your encoding is good to go。

![](img/c50a3654f7f457b00c294e3eb6cb37f8_14.png)

![](img/c50a3654f7f457b00c294e3eb6cb37f8_15.png)

For example， consider the encoding shown on the left。

 It takes just a second to draw the corresponding binary tree。

 The simple B is distance 1 from the root of the tree along an arc labeled 0。

 A is distance 2 and C and D are distance 3。If we receive an encoded message， for example， 0-1111。

 we can decode it using successive bits of the encoding to identify a path from the root of the tree to sending step by step until we come to a leaf。



![](img/c50a3654f7f457b00c294e3eb6cb37f8_17.png)

![](img/c50a3654f7f457b00c294e3eb6cb37f8_18.png)

Then repeating the process， starting at the root again until all the bits in the encoded message have been consumed。

So the message from the sheep is0 takes us from the root to the leaf B。

 which is our first decoded symbol。 Then 1，1 takes us to a， and the next 1，1 results in a second a。



![](img/c50a3654f7f457b00c294e3eb6cb37f8_20.png)

The final decoded message， B AA is not totally unexpected， at least from an American sheep。



![](img/c50a3654f7f457b00c294e3eb6cb37f8_22.png)