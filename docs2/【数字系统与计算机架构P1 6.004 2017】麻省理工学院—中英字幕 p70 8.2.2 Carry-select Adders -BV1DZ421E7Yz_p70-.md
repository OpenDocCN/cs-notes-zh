# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p70 8.2.2 Carry-select Adders -BV1DZ421E7Yz_p70-

The most straightforward way to improve performance is to reduce the propagation delay of a circuit。

 Let's look at a perennial performance bottleneck， the ripple carry adder。

To fix it we first have to figure out the path from inputs to outputs that has the largest propag delay。

 in other words the path that's determining the overall TPD。In this case。

 that path is the long carry chain following the carry in to carry out path through each full adder module。

To trigger the path， add minus1 and1 by setting the A inputs to all ones and the B input to all zeros except for the low order bit。

 which is1。The final answer is zero， but notice that each full ladder has to wait for the carry in from the previous stage before it produces zero on its sum output and generates a carryout for the next full ladder。

The carry really does ripple through the circuit as each full ladder in turn， does its thing。

The total propagation delay along this path is n-1 times the carry in to carry out delay of each full adder。

 plus the delay to produce the final bit of the sum。

How would the overall latency change if we say doubled the size of the opera， In other words。

 made N twice as large。It's useful to summarize the dependency of the latency on N。

 using the order of notation to give us the big picture。Clearly as n gets larger。

 the delay of the exorgate at the end becomes less significant。

 so the order of notation ignores terms that are relatively less important as n grows。

In this example， the latency is order n， which tells us that the latency would be expected to essentially double if we mate n twice as large。



![](img/cca9ffe8de9378392c15c312180e10a7_1.png)

The order of notation， which theoreticians call asymptotic analysis。

 tells us the term that would dominate the result as N grows。

The yellow box contains the official definition， but an example might make it easier to understand what's happening。

Suppose we want to characterize the growth and the value of the equation n squared plus 2 n+3 as n gets larger。

The dominant term is clearly n squared， and the value of our equation is abounded above and below by simple multiples of n squared。

 except for finitely many values of n。The lower bound is always true for n greater than or equal to 0。

And in this case， the upper bound doesn't hold only for n equal to 0， 1， 2， or 3。

 for all other positive values of n， the upper inequality is true。

So we'd say this equation was order n squared。There are actually two variants for the order of notation。

 we use the theta notation to indicate that G of n is abounded above and below by multiples of F of n。

The Big O notation is used when G of n is only abounded above by a multiple of FFM。



![](img/cca9ffe8de9378392c15c312180e10a7_3.png)

Here's our first attempt at improving the latency of our audition circuit。

The trouble with the Rple carry adder is that the high order bits have to wait for the carry in from the low order bits。

Is there a way in which we can get the high half of the adder working in parallel with a low half？

Suppose we wanted to build a 32 bit ater。Let's make two copies of the high 16 bits of the adder。

 one assuming the carry in from the low bits is0， and the other assuming the carry in is1。

So now we have three 16 bit adders， all of which can operate in parallel on newly arriving A and B inputs。

Once the 16 bit additions are complete， we can use the actual carryout from the low half to select the answer from the particular high half adder that use the matching carry in value。

 This type of adder is appropriately named the carry Select Adder。

The latency of this carry select add is just a little more than the latency of a 16 bit Ripple carry edition。

This is approximately half the latency of the original 32 bit ripple carer。

So at a cost of about 50% more circuitry， we half the latency。As a next step。

 we could apply the same strategy to have the latency of the 16 bit adders and then again to have the latency of the8 bit adders used in the previous step。

At each step， we have the adder latency in add a mug delay。After logbase2 of N steps。

 the N will be1 and we're done。At this point， the latency would be some constant cost to do a1 bit edition。

 plus logb 2 of n times theMX latency to select the right answers。

So the overall latency of the Car Select Add is order log n Note that log base to of n and log n only differ by a constant factor。

 so we ignore the base of the log in order of notation。

The Car Select add shows a clear performance size trade off available to the designer。



![](img/cca9ffe8de9378392c15c312180e10a7_5.png)

Since adders play a big role in many digital systems。

 here is a more carefully engineered version of a 32 B curory select adder。

 You could try this in your AU design。The size of the adder blocks has been chosen so that the trial sums and the carry in from the previous stage arrive at the carry select mugs at approximately the same time。

Note that since the select signal for the Mues is heavily loaded。

 we've included a buffer to make the select signal transitions faster。

This carry select adder is about 2 and a half times faster than a 32 bit ripple carry adder at the cost of about twice as much circuitry。

A great design to remember when you're looking to double the speed of your AOU。



![](img/cca9ffe8de9378392c15c312180e10a7_7.png)