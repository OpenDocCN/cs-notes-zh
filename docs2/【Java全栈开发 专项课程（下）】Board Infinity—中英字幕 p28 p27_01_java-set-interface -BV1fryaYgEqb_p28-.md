# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p28 p27_01_java-set-interface -BV1fryaYgEqb_p28-

![](img/f7e260757d15bb2a34841b7995dab40e_0.png)

Today in this session we will discuss about set interface in Java with its implementation so let's get started。

 Basically， the set interface of the Java collection framework provides the feature of the mathematical sets in Java that extends the collection interface。

 Unlike the list interface sets cannot contain the duplicate elements。



![](img/f7e260757d15bb2a34841b7995dab40e_2.png)

This is how the set extends the collection interface。Since set is an interface。

 we cannot create object from it and in order to use functionality of the set interface we can use the following classes such as Enum set。

 hash set， link hash set and the tree set。These classes are defined in the collection framework and the implementation of the set interface。

The set interface is also extended by the sub interfaces。

 such as the sorted set and the navigational set。 These are the very basic methods that you can use for the set interface includes these methods of the collection interface。

 Its because collection is a superet of set。 You can see here。

 add helps to add the specific element to the set where add all helps to add a complete collection of the set to the specific set。

 Itraator helps in ittrating， remove and remove world methods to remove one or all the elements。😊。

Retain all， retain all the elements in the set。 They are also present in another set It by comparing Cons will just return true or false。

 depending the elements available in the set or not contains all will check for each and every entry in the set。

 and two array will return an array containing all the elements of the set as well。

 So these are the specific set methods to iterate with for further followed by。

 we have a set operations just like union intersection and subset， Union is to return。😊。

Or get the union of two set where each element will becomes once。

 intersection is just like a common elements to extract from two or more sets。

 Subet is to check if excess a subset of y by checking the contain or using the container method itself。

 So let's get started by practically implementing the couple of examples for the set operations。

 Here we are creating a set。Indiger。Set one。New hashet。Of type inte。

This should be taken up from the u package。 It's always available in different packages。

 so please check it wisely in which package your collection is available。

 So I'll just get it from the U package。 I will add the elements set1 dot at。

I just entered the inte values 00。Set one dot at 200。Set one dot at 300。Set one dot at 400。

Once it is done， you can try printing your set as well。

That's how your four elements are available in the set。 You can use a iterator。Htator。

From the u package。Iitator of integer。Irate equals to set1 dot ittator。Whileile。I trade has next。

And it becomes true， then sis out。I trade I trade dot Next value has to be printed。

 After each next value， I'm just putting a space or comma。

So you can see that the things have been printed。I think if an even I do not print the comma。

 it should it in the next line。 If you do not want to print in the next line。

 just write only print method。That's how the basic set elements to be added and itrated with the help of itrator。

 or you can also use a for each method for more set implementation and its interfaces in classes。

 please stay tuned。 See you in the next session。

![](img/f7e260757d15bb2a34841b7995dab40e_4.png)

![](img/f7e260757d15bb2a34841b7995dab40e_5.png)