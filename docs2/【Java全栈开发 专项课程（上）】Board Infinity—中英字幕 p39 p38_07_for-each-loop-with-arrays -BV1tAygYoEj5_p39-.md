# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p39 p38_07_for-each-loop-with-arrays -BV1tAygYoEj5_p39-

![](img/7e8779b8e27b159d5c0396fd62e40376_0.png)

Hi there。 Today， In this session， I will discuss about4 each loop in Java。😊。



![](img/7e8779b8e27b159d5c0396fd62e40376_2.png)

Basically， for each slope is used to itrate through elements of arrays and collections。

 As I discussed earlier， it is another。Way to traverse the elements like for loop while loop and do while loop。

 also known as for each loop or enhanced loop。Instead of declaring and initializing a loop counter variable。

 you can declare a variable that is of the same type as the base type of the aerial collection is there。

😊，And we use a column along with that， for example， our array is of integer type。

 so we declare an integer type of variable until the array has the value one by one will take up the value from the array and I it。

In the loop body you can use the loop variable you created rather than using the index variable that is what I said you can use a VA variable。

Don't consider where as a key word here。 Let me help you out with the help of an example。

Considering I have a spring type of array。Which contains certain names。

 I'm going to initialize my string array here only。At the time of declaration。

Where the first name is King。Second is Sccher。Third is Sarah。Food is bowling。Now， to i this array。

 I can either use。For look to ittrate over a collection。Until the name start length is there。

 I should come inside it and print names at index I。Otherwise， I can lose a afford each loop。

 which is the enhanced loop。There I will be declaring a string variable name。

Will take the value from the names， E。And print inside the fo。So as I said。

 whatever type of array you have that is names， the same type of variable is declared until the names array is having the values one by one will take the names from the names array and print it along。

Both will print the same output。 Just iteration is slight different。King cocher， Sara baling。

 King kcher， Sara baing。I just wanted to help you out that you need to understand where should you use for each loop and where not to。

If you are using for loop， then there are certain limitations because it moreover helps in iterating or traversing the elements rather than working onto the index。

For each loops are not appropriate when you want to modify the array。

 it just helps in traversing or iterating because to modify the array you need to work on the index element。

For each loop do not keep the track of index so we cannot obtain the error index using the ford loop in that case you need to go for traditional for loop for each slope only iterates in the forward direction。

 but in this case of forward loop you can start from the length and go till the0 and say I minus minus your array can be traverse in reverse direction。

For each cannot process two decision making statement at once。

 but in the case of for loop we could do so please understand where exactly so moreover for each lo tips to be used in the case of traversing for each lo also has some performance overhead over the simple iteration。

But in the case of iterrating the collections or array 4 each is always suggested。

I hope the concept is clear to all of you。 I will frequently be using foliage Lo in my collections or in the real time applications。

 See in the next session until next time， Stay tuned。 Thank you。🎼。



![](img/7e8779b8e27b159d5c0396fd62e40376_4.png)