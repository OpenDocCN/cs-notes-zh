# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p42 4.2.8 Worked Examples： Gates and Boolean Logic -BV1DZ421E7Yz_p42-

Instead of building all of our logic functions directly in seamoss。

 it is easier for us to create a higher level of abstraction known as Boolean gates。

 which represent seamos gates。Each gate is assigned a symbol which can then be used in schematic diagrams that combine multiple logic gates together。

In order to be able to understand what function any combination of logic gates will produce。

 we will begin by reviewing the basic gates together with the truth tables that define their logic。

We begin with an inverter。An inverter is a gate that has a single input and a single output。

 The output is simply the inverse of the input。 When a equals 0， y equals 1。 and when a equals 1。

 y equals 0。Next， we examine and and ore gates。The and function expects all of its inputs to be true or one in order to produce a one as its output。

So its truth table is for aB equals 00， y0 for aBs01， y equals0， for aB equals 10， y equals 0。

 and for AB equals 11， y equals 1。The or function just expects at least one of its inputs to be true in order to produce a one as its output。

So for A B equals 0，0， y equals 0， but for the other three combinations， y equals 1。

Nand and Norgate simply produce the inverse of and and ore。

The reason that we like to work with Nand and nor gates is because they are inverting gates and they can be implemented as a single sea moss gate。

 whereas the and and or gates cannot。Finally， the last basic gate is an exclusive or。

 The exclusive or produces a one output， if exactly one of its two inputs is a 1 and0 otherwise。

 So for A B equals0，0， y equals 0 for A B equals 01 y equals 1 for A B equals to 1，0。

 y equals1 and for a B equals to 11， y equals 0。

![](img/e05b4f6e3127d77a6dc0cb2d7bb9ae9b_1.png)

The output of one booleion gate can be used as an input to another Boolean gate。

 so multiple gates can be used to generate more complex functions。For example。

 here we have a circuit that consists of two inputs and six gates， which are an inverter。

 an ans gate and or gate， two norer gates and one Nand gate。

In order to figure out what this combination of gates produces as its output。

 we can work incrementally through the circuit。

![](img/e05b4f6e3127d77a6dc0cb2d7bb9ae9b_3.png)

We begin by enumerating all our choices of inputs for A and B。

We know that the output of the inverter is the complement of B。

The and and or gates use the A and B inputs directly。

 so since we just reviewed what and and or gates produce， we can fill in these columns as well。Next。

 we want to see what our first Norgate produces。 Its inputs are A and the complement of B。

We walk through each of the input combinations and determine the corresponding output for that gate。

For input 0，1， the output is 0 for 0，0， the output is 1。For inputs 11。

 the output is 0 and for input 10， the output is 0。In the same manner。

 we evaluate the outputs for our second Nor gate。Its inputs are the outputs of the first Nor gate and the and gate。

Again， we simply walk through each combination of inputs and specify the outputs produced by the second Norgate。

When both inputs are0， the output is， when one input is 0 and the other is a1， the output is a 0。

Finally， we take those outputs together with the outputs of the ore gate and generate the final output of the circuit at the output of the Nand gate。

Here， when both inputs are one， we get a0 output。 Otherwise， the output is a1。

This Nand column of our truth table is the resulting output H。



![](img/e05b4f6e3127d77a6dc0cb2d7bb9ae9b_5.png)

Now that we have evaluated what our output H is equal to for each combination of A and B。

 we can represent the circuit as a single truth table whose inputs are A and B and whose output is H。

At this point， we can express the function H as a combination of all the cases that make H equal to1。

This occurs if A and B are both equal to0， or if a equals 0 and b equals 1。

 or if A and B are both equal to 1。This can be expressed as a Boolean logic expression as follows。

H equals not A， not B， or not A， B or A B。 This notation is called a sum of products notation。



![](img/e05b4f6e3127d77a6dc0cb2d7bb9ae9b_7.png)

![](img/e05b4f6e3127d77a6dc0cb2d7bb9ae9b_8.png)

Any sum of products can be converted into a simple gate representation of the function。

 using only inverters and and or gates by having one large ore gate that receives as input。

1 and gate per product term。 inverters are used as needed to complement the inputs。To the an gates。

One thing that's interesting to note about this combination of gates is that it can easily be converted into a circuit that consists purely of Nn gates。

 The way to do this is to realize that if you take the output of a gate and invert it twice。

 you produce the original output。This means that we can add two inverters between each and output and or input。

We can draw these inverters as bubbles， which represent inversion。

 where replace one bubble on the output of the an gate and the other bubble at the input to the ore gate。

We know that an anand gate， followed by an inversion， is simply a Nand gate。

We also know that an inverter is equivalent to a Nand gate with both of its inputs tied together。

In addition， using de Morgan's law， we know that not A or not B is equivalent to not of A and B。

This means that the bubbles followed by the ore gate can also be replaced with a Nand gate。

 thus arriving at our equivalent circuit representation， consisting purely of Nand gates。

The advantage of using Nan gates to implement the circuit is that Nan gates are。

Inverting logic where each gate can be implemented as a single CMmoss gate。

All functions can be expressed as a combination of nan gates。

 so a Nand gate is considered a universal gate。Nor gates are also universal and can be used to express any function。

Furthermore， any circuit that can be used to implement any other function is also considered universal。

To determine if a gate G is universal， one needs to check if one can convert G into either a Nand or nor gate by simply using one or more copies of G together with low and high constant inputs。



![](img/e05b4f6e3127d77a6dc0cb2d7bb9ae9b_10.png)