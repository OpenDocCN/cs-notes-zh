# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p37 15.2.5 Exceptions and Interrupts -BV19m41127Kj_p37-

Now， let's figure out how exceptions impact pipeline execution when an exception occurs because of an illegal instruction or an external interrupt。

 we need to store the PC plus4 a value in the XP register and load the program counter with the address of the appropriate exception handler。

Exceptions cause control flow hazards since they are effectively implicit branches。

In an unppelined implementation， exceptions affect the execution of the current instruction。

 We want to achieve exactly the same effect in our pipeline implementation。

So first we have to identify which one of the instructions in our pipeline is affected。

 then ensure that instructions that came earlier in the code complete correctly。

 and that we annu the affected instruction and any following instructions that are in the pipeline。

 since there are multiple instructions in the pipeline， we have a bit of sorting out to do。



![](img/ab9bb5651e7f74b0793a434531aeb7b4_1.png)

When， during pipeline execution do we determine that an instruction will cause an exception？

An obvious example is detecting an illegal op code when we decode the instruction in the RF stage。

But we can also generate exceptions in other pipeline stages。 for example。

 the AOU stage can generate an exception if the second operaand of a div instruction is0。

Or the MEM stage may detect that the instruction is attempting to access memory with the illegal address。

 similarly the IF stage can generate a memory exception when fetching the next instruction。

In each case， instructions that follow the one that cause the exception may already be in the pipeline and will need to be annulled。

The good news is that since register values are only updated in the right back stage。

 analling an instruction only requires replacing it with aO op。

 we won't have to restore any changed values in the register file or main memory。



![](img/ab9bb5651e7f74b0793a434531aeb7b4_3.png)

Here's our plan。 If an instruction causes an exception in stage I。

 replace that instruction with this beie instruction， whose only side effect。

Is writing the PC+4 value into the XP register。Then flush the pipeline by annuuling instructions in earlier pipeline stages。

And finally， load the program counter with the address of the exception handler。In this example。

 assume that load will generate a memory exception in the MEm stage， which occurs in cycle 4。

The arrows show how the instructions in the pipeline are rewritten for cycle 5。

 at which point the IF stage is working on fetching the first instruction in the exception handler。



![](img/ab9bb5651e7f74b0793a434531aeb7b4_5.png)

Here are the changes required to the execution pipeline。

 we modify the Moes in the instruction path so that they can replace an actual instruction with either No opP if the instruction is to be annulled or B anE if the instruction costs the exception。



![](img/ab9bb5651e7f74b0793a434531aeb7b4_7.png)

Since the pipeline is executing multiple instructions at the same time。

 we have to worry about what happens if multiple exceptions are detected during execution。

In this example， assume that load will cause a memory exception in the me stage and note that it is followed by an instruction with an illegal op code。



![](img/ab9bb5651e7f74b0793a434531aeb7b4_9.png)

Looking at the pipeline diagram。The invalid op code is detected in the RF stage during cycle 3。

 causing the illegal instruction exception process to begin in cycle 4。But during that cycle。

 the mem stage detects the illegal memory access from the load instruction and so causes the memory exception process to begin in cycle 5。

Note that the exception caused by the earlier instruction load overrides the exception caused by the later illegal op code。

 even though the illegal op code exception was detected first， That's the correct behavior。

 since once the execution of load is abandoned。 The pipeline should behave as if none of the instructions that came after the load were executed。

If multiple exceptions are detected in the same cycle。

 the exception from the instruction furtherthest down the pipeline should be given precedence。



![](img/ab9bb5651e7f74b0793a434531aeb7b4_11.png)

External interrupts also behave as implicit branches。

 but it turns out they are a bit easier to handle in our pipeline。

We'll treat external interrupts as if they were an exception that affected the IF stage。

Let's assume the external interrupt occurs in cycle 2。

This means that the sub instructionion will be replaced by our magic B& E to capture the PC+4 value。

And will force the next PC to be the address of the interrupt handler。

After the interrupt handler completes， we'll want to resume execution of the interrupted program at the sub instructionstruction。

 so we'll code the handler to correct the value in the saved XP register so that it appoints to the sub instructionstruction。

This is all shown in the pipeline diagram。Note that the ad。

 load and other instructions that came before a sub in the program are unaffected by the interrupt。



![](img/ab9bb5651e7f74b0793a434531aeb7b4_13.png)

We can use the existing instruction pathmes to deal with interrupts since we're treating them as IF stage exceptions。

We simply have to adjust the logic for IR sourceur IF to also make it one when an interrupt is requested。



![](img/ab9bb5651e7f74b0793a434531aeb7b4_15.png)