# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p25 p24_06_java-sortedmap-interface -BV1fryaYgEqb_p25-

![](img/387cb7bb00299c812b17b81bda5b401d_0.png)

Hi there。 Today， In this session， I will talk about sorted map interface and its methods。😊。



![](img/387cb7bb00299c812b17b81bda5b401d_2.png)

So let's get started。Sorted map is an interface in Java that is a sub interface of map interface and allows the data to be arranged in ascending order sorted according to their keys。

 It ensures that the entries are kept in ascending order as this interface name itself is sorted。

It allows very efficient manipulation of map subsets， which is cannot create objects from it。

In order to use the functionalities of the sorted map interface。

 we need to use the class pre map that implements it。This is how this syntax looks like。

The main characteristics of a sorted map is that in if orders the keys by their natural ordering or by the specific comparator。

 so there is a comparator method that can be used to order the keys in a map。

So considering using a tree map when you want a map that satisfies the following criteria when null key or null value is not allowed or permitted。

The keys are sorted either by natural ordering or by the specific comparator。

This is how the tree looks like map interface being extended to the sorted map interface and then implements to the tree map class。

 so you need to instantiate the tree map class。Also。

 this sorted map also implements to the congruent skip list map class that has a different perspective。

 These are the advantages of sorted map Arrange the element to be sorted order。

 Search performance becomes easy。Predictable iteration in order is so efficient and itated。

 These are the sorted methods that you can use here。

 the methods are quite implemented and different as compared to the map interface。

 The map interfere interface methods can be used here。long with that。

 These are the more abstract methods being added to the sorted map comparator， first key。

 last key head map， tail map and sub map。 So let's get started with the practical implementation。Yes。

Here Im creating a sorted map。Reference variable。Where the key is string and the value is in teacher。

Numbers equals to need to instantiate for the tree map。I need to insert number， start put。Do。Yeah。

Number taught birth。one。Post that would like to print my sorted map。 That's called numbers。

If I would like to get。Number start first key。There is a method。

If I would like to get numbers taught last key， there is a method。

If I would like to remove the element， then we have a method numbersumb dot remove。

And we can pass the key。1。That's also， we have。So these are the methods to be iterated let me just try printing Moreover methods are there Moreover you will be elaborating the sorted map it has all the methods implemented from the map interface along with the sorted map interface so you can practice it accordingly I hope it's clear to all of you。

So see you in the next session until next time stay tuned， thank you。



![](img/387cb7bb00299c812b17b81bda5b401d_4.png)

。

![](img/387cb7bb00299c812b17b81bda5b401d_6.png)