# 《高级数据结构｜6.851 Advanced Data Structures, Spring 2012》中英字幕（deepseek - P17：-17-17. Succinct Structures I.zh_en - GPT中英字幕课程资源 - BV1FDFVzdEBA

The following content is provided under a creative Commons license。

 Your support will help M I T Open Coseware continue to offer high quality educational resources for free。

To make a donation or view additional materials from hundreds of MI T courses。

 visit Mi T OpenCourseware at O C W dot M I T dot E Du。



![](img/a5301df0dbdb71cac9519e3fc56fa9a4_1.png)

![](img/a5301df0dbdb71cac9519e3fc56fa9a4_2.png)

Alright， today， we start a new section of data structures。 This is gonna be two lectures long。

 So this week of succinct data structures， where the goal is to get really small space。

And first thing to do is to define what small means。 Most succinct data structures are also static。

 although there are few that are dynamic。 We'll be focusing here on static。Data structures。

And so in general， the name of the game is taking a data structure that you're familiar with。

 We're going to talk about essentially two today。 One is binary tries。

 which has a killer application of doing binary suffix trees in particular。

 So it could be a compressed try， whatever。 But we'll assume here， the alphabet is binary。

 A lot of this has been generalized to larger alphabets。 I'll tell you a little bit about that。

 But to keep it simple in lecture。 I'm gonna to stick to binary alphabet。

And another data structure we're gonna to look at is a bit vector。 So N bits in a row。

 And you want to do interesting operations like。Find the I 1 B in constant time。

 These are things that are easy to do in linear space， where linear space means order N words。

Like if you want to implement a try， you have a pointer at every node， two pointers at every node。

 That's easy。 bit factors。 you could store an array of all where all the ones are。

 And so it's easy to do linear space， but linear space is not optimal。

And there are three senses of small space that we'll be going for。One is。

 the best version is implicit。An implicit data structure means you use the very optimum number of bits。

Plus， a constant。But here were， we're focusing on bits， not words。

 And if you translate a linear word data structure order N words is order n times W bits。

 and to start something like an n bit vector， you should really only use order N bits not order N W bits。

Okay， now what would be ideal is if you use n bits plus a constant。

 The plus a constant is essentially because sometimes the optimum number is not an integer。

 and it's hard to store a non integer number of bits。 So you add a constant。 Yeah， I mean。

 I wouldn't mind too much if you added order log n bits or something。 But typically。

 the goal here is constant。Sometimes you can really get zero。But。

The next best thing would be a succinct data structure。

Where the goal is to get optt plus little O of optt。Bs。

So the key here is to get a constant factor of one in front。And then， the。

The worst thing in this regime is still pretty good， it's order opt bits。So， for example。

 if you're storing it an n bit vector。If you use order n bits。

 that's still better than order N W bits， usually compact is a savings of at least a factor of W over what you'd normally call a linear space data structure。

 So this is the big savings factor W。 But there's a constant here。

 Sometimes you like to get rid of that constant， make it one。 And it's not so bad if you have。

 say another， I don't know。 square root of n or something little O of n。Bits of extra space。 But。

 of course， the ideal would be to have no extra space。 And that's implicit。 Now。

 it's a little confusing。 I usually call this area succinct data structures to mean all three。

 But the middle one is called succinct。 That's typically the goal is to go for succinct because implicit is very hard and compact is kind of like a warm up towards succinct。

 So this is the usual goal in the middle。And we're gonna do this for binary tries and rank and select。

 So let me tell you a little bit about what's known。Oh， sorry。 one quick example。

 you've seen implicit data structures。 You may have even heard this term in the context of heaps。

 binaryary heaps are an example of a dynamic， implicit data structure。 They achieve this bound。

 They have no extra space。In the appropriate model。 and another one would be asorted array。

 Sored array supports binary search。 can't really update it。 So it's a static search structure。

 It achieves the optimal number of bits。 You're just storing the data。

 and usually implicit data structures just store the data。

 though sometimes they reorder the data in interesting ways。

 like sorting sorted array is one way to reorder。Your data items。So here's a short survey。

 this is definitely not exhaustive。But it。covers a bunch of the main results。

So one place where this area really got started。Well， there are a few places， actually。

My academic father， one of my PhD advisors， Ian Monroe， sort of the one of the fathers of this field。

 and he started looking at succinct data structures at the very early days。

 and one of the problems he worked on was dynamic search trees。

 So if you want to do static search trees， you can just store the items in distorted array。

 easy login search， no extra space。What if you want to do inserts and deletes also in log n time per operation。

 just like a regular search tree， but you want to do it implicitly。NowThis is tricky。

 and there's an old result that let you get log squared for update and query。

 which essentially encoded。 you can't afford pointers at all here。

 But the idea was to encode the pointers by permuting enough items。 if you take a log n items。

 then the permutations among them is roughly login log log n bits。

And so you could encode bits by just permuting pairs of items。

And so you could read a pointer in like log n operations。 You end up with log square。

And then Ian Monroe got down to little bit less than log squared。

And then the then there's a series of improvements over the last several years。

 And the final result is。Log in。Worst case。Insert。Deelete。And predecessor。

And this is by Franschini and Garrosi。And furthermore， it's cache oblivius。

 You can get log base P of N。Cash movies。So this has been pretty much completely solved。Implicitly。

 you can do all the good things we know how to do with search trees。 Now。

 this is not trying to solve the predecessor problem using venom debo and such tricks。 that's。

 I believe， open。 but for a basic log n performance， it's solved。Okay， before this got solved。

 another important problem is essentially the， the equivalent of hashing。

 So you w a succinct dictionary。 wantna be able to do。 this is gonna be static。

So there's no insert and delete。It's just， is this item in the dictionary。

 So I have a universal of size U。 I have N items in the dictionary。 So first question is。

 what is the optimal number bits。And this is actually usually very easy to compute。

 You just take what are the possible， the set of possible structures you're trying to represent。

 which is。And items out of a universe size U。How many different ways are there to have n items in a universal size U。

Come on。 It's easy combinators。Somebody。十分。Bg you sorry。That's how many bits you'll。 Yeah。

 log you choose n is the number of bits you'll need。

 The number of different possibilities just you choose n。 You take log base two of that。

 That's how many bits you'll need to represent this。 Now， this is not necessarily an integer。

Becauseuse it's got a log。 That's why we would have plus order1 if we're doing implicit。

 It's not known how to do implicit。 It's known how to do succinct。

 So this is gonna be plus little O of that thing。And。Actually， I have the explicit bound。Rar。

I don't know how exciting this is， but you get to know how little low it is。Lock， clock and。Squared。

Over a log and。So this is slightly smaller。 So this is roughly something like。You log n depends If。

 if n is small， this is like you log n。 If n is big， this is like。Sorry， N log U， I should say。

 you can always encode a dictionary using n log U bits just for every item。

 you specify the log U bits。 But when n is big close to U。

 then you can just use a bit vector and use U bits。 And so this is little of n。

 So it's always smaller than whatever you're encoding over here。It's only slightly smaller than N。

 Lo log n squared over log n， but it's a little low of one。And that's， I believe， the best note。

So this is Broneck and Monroe， and then improved by PC。

And the point is you get constant time membership query。In general。

 the name of the game is you want to do the queries you're used to doing in something like a dictionary。

 same amount of time， but with less space。Okay， next one is。呃。Maybe I'll go over here。Binary try。

 which is what we're。Goon to work on today。 This is， there's various results on this。

 but sort of one of the main ones is by Monroe and Raman。Again。

 now this is a little heart of a question， how many binary tries on n nodes are there？

The answer I will tell you is the anth Catal number。Which is a quantity we've seen before。

Two and choose in over n plus1。As we mentioned last time， this is roughly four to the end。

Thiss kind of interesting。 We saw this。 We saw the catalan number in a different context。

 which is we were doing in direction and using lookup tables at the bottom on all rooted trees on end nodes。

 number of rooted trees on end nodes was also Catalan number。 This is a different concept。

 binaryary tries。 If you've ever taken a combinators class。

 You see Catalan numbers all over the place。 There's thezillion different things。 all。😊，All of them。

 the number of them is Catalan number， and we will actually use that equivalence between binary tries and rooted trees at the end of today's lecture。

 So we'll see why they're the same number。Okay， so we take log of that。 That's 2 n Bs。

 So you need2 n Bs to represent a binary try and。Indeed， you can achieve 2 n plus little of n bits。

So that's a succinct data structure， and our goal will be to be able to do constant time traversal of the tree。

 So left child， right child， parent。And for fun， another operation we might want to do is compute the size of the current subte。

Again， think of a suffix tree。You're at you start at the root and you want to be able to go along the left child every time you have a zero bit in your query string。

 you want to go to the right child every time you have a one bit。 parent we don't really need。

 but why not。 And then subrecise would tell us how many matches are there below us。

 You could count either the number of nodes below you or the number of leaves below you。

 It's roughly the same for a compact try。And so this lets you do substring searches and we'll actually talk more about that next lecture。

 how to actually do a full suffix tree。 but this is a component of a full of a binary suffix tree that has the same performance。

 but uses optimal amount of space。 and this is a big motivator originally for doing。

Doing compact or succinct data structures at University of Waterloo。

 they were doing this new OE project where Oxford English Dictionary was trying to go online or digital。

 And this concept of having a Cd rom that could have an entire dictionary on it was crazy And Cd roms are really slow。

 So you don't want to just scan the entire Cd to do a search。

 You want to be able to do a search for arbitrary substrings。 That's what suffix trees let you do。

 but you really can't afford much space。 And so this was that was the motivation for developing these data structures back in the day。

Things are a lot easier now space is cheaper， but still。

 there's always going be some giant thing that you need to store that if you want to store a data structure。

 you really can't afford much space。Cool， so that's static。 There is a dynamic version。

 That's a more recent result from just a few years ago。 You can do constant time， insert and delete。

Of a leaf。And you can do subdivision of an edge。These are operations we saw for dynamic LCA。

So same operations as dynamic LC， you can do these in constant time and still maintain this succct binary tri representation where you can do all these traversal operations。

 so we won't cover that。 We will cover the static version today。Then that's for binary alphabet。

 let me tell you what's known about。Larger alphabet， this is a problem I worked on a while ago。

 though our result is no longer the best。So here。It's a little complicated， but the number of tries。

Number of k are tries， so K is now the size of the alphabet。Is。K， N plus1， choose N。

Over can plus one。And so it's succinct， meaning you， we can achieve log of that plus little of that。

bits。And the queries we can achieve are constant time。Child with label eye。Handpar and subreci。

So we can do all the things we were able to do before the analog of left childil and right child now is I have a character in my pattern P。

 and I want to know which child has that label， so it's not the same as finding the Ithe child of a node the edges are labeled by by their letter and the alphabet and you can achieve that we had an earlier result that achieved like log log or something and then finally was brought down to constant by Farn and Monroe。

So。ACole more。 Why don't I just mention what they are。Its getting a little。Tedious。

 a lot of other things you might want to store。You can store succinct permutations。

 although there's some open problems here。 So you want to store a permutation using log n factorial bits plus little of n。

 If you want a achieve succinct， the best known， oh。

 and the interesting query is you want to be able to do the k power of your permutation。

 So see where an item goes after K steps， best query known for that is log n over log log n。

 if you want succinct， if you only want compact。Then it's known how to do constant time。

 So an interesting open question is， can you achieve succinct constant time queries。you。

 if you relax either， then it's known how to do it。 There's a generalization of this to functions。

Where one of those results is known， the other one isn't。You can try to do abelian groups。

There are finite anbelian groups。 There aren' too many different ones。

 And you can represent an entire obbelian group on n items using log n bits。

 which is pretty crazy order log n bits。And you can represent an item in that group in log n bits and do multiplication inverse and equality testing。

 There's other results on graphs， which I won't get into those are a little harder to state。

 And then another interesting case is integers。So you want to store an integer and you want to be able to increment and decrement the integer。

And you want to do as few bid operations as possible， worst case， for example。

 if you have lots of ones in your bit string you do an increment， you don't want to pay linear cost。

 linear number of bit updates to do it。And so you can achieve implicit。

 So just a constant number of extra bits of space。If I have an n bit integer。

Then I can do it an increment。Or a decrement。And。Order， log n。Bitreads。And constant bit rights。

And this is Ramen and Monroe from a couple years ago。So this is pretty good。 Of course。

 idea would be to do a constant number of， Well， constant number of bit reads or write would be optimal。

 I guess I personally be interested in getting a constant number of word reads and write。😊，So。

 but that's an open problem， I believe。So， these。There's only order and bit reads。

 If they were all consecutive， that would be constant number of word reads。

 but they're kind of spread out。It be nice to get constant number of word。Operations。

So that's a quick survey of what's known。Let's do some actual data structures now。

 So we're going to be focusing on。This。Sccinct binary tries。And we're gonna do two versions of it。

 One of them is。Level order。And the other will use a balanced parentheesis representation。So。

Let's start with the level order 1。 This is very easy。

So I'm going to just loop over the nodes in my try in level order。

 So level by level and write 1 bit to say whether there's a left child and 1 bit to say whether there's a right child。

 Let's do a little example。So， here is a。Binary try。And I'm gonna to write。Bit string for it。

So first time， I'm gonna look at the top level。 I have the node A has a left child to B。

 right child to C。 So I write 1，1。And this corresponds to B， and this corresponds to C。Okay。

 then next level is B。 It has no left child。And it has a right child， which is D。

I'm just writing down the label so I don't get lost。 Then we have node C。Which has。

A left child and a right child， E and F。Then we have no D， has no left child。Ass the right child。

 then we have， which is G。Note E has no children。No F has no children。 No G has no children。Okay。

 so there is a 2 n B string。 This is obviously 2 n Bs。For end nodes。

So this is one way to prove there's at most for to the N tries。And。Well。

 we'll talk about how useful it is。 I want to give you another representation of the same thing。

Which is we take， if we take these nodes and add on wherever there's an absent leaf。

 I'm going add what we call an external node。As you sometimes see in data structures books。

One way to represent a null pointer say， oh， there's a node there that has no children。

This unifies things a little bit because now every node either has two children or no children。

Another way to think about the same thing。And it turns out。If you look at this bit string。

And add a one in front。So put this one in parentheses。

Then what this is encoding is just for every node in level order。

 Are you a real node or are you an external node， Are you an internal node or an external node。

 right A here。Is internal。 B is internal。 C is internal。 This is an external mode。

Then this is internal， internal， internal， then external， G， external， external， external， external。

 external， external。Right， the zeros correspond to external nodes， because those are absent children。

 So same thing。Alright， equivalently。One equals an internal node。And 0 equals an external node。

Of course， to do this， we need one more bit。And we're gonna。

 I'm gonna take this view primarily because it's a little easier to work with。

 doesnn't really make much a difference。 Just just everything over by one。

And I'd like to write down the indices here。 So we have 1，2，3。4，5，6，7。Into this array。

Because now our challenge is， allright， great。 We've represented a binary try。

 but we want to be able to do constant time left child， right child， parent。

 We're not gonna be able to do subree size。 levelvel order is really not good for subte size。

 but left child， right child and parent we can do in constant time。

And the reason we can do it in constant time。Itca there's a nice lemma， kind of analogous to。

Binary heaps。So the claim is， if we look at the I internal nodes。 for example。

 C is the third internal node。So internal node number three。

Then we look at positions  two times 3 and two times 3 plus1。 So 6 and 7 in this array。

 And we get the two children， E and F。So that works，6 and 7 are E and F。

 or this one would be the fourth internal node。 D is a fourth internal node。

 And so app positions A and 9 should be this external node and G。8 is an external node，9 is G。

So it works。Thiss dilemma。You can prove it pretty easily。By induction on I。So。The idea is， well。

 if you look at。Let's say， the I internal note。And the I minus first internal node。

This one has two left children。 don't know whether they're internal or external between them。

 They're on the same level。 and we we're in level order。

 So anything in between here is an external node。 so they have no children。

Which means if you look at the children of I， they're going to appear right after the children of I -1。

Because。We're level order。 So we have these two guys， the next children。

 the next nodes at this level are gonna be these two guys。So。This one appeared at。2 I -2 and 2 I -1。

 This one appear at 2 I。And。2 I plus one。This is if I and I-1 are on the same level。

 but it also works if they're in different levels。So if P -1 is the last node on its level， again。

 it's going to have two children here。Which will be the last nodes on this level。

And then right after that will come the children of I again， at position 2 I。And 2 I plus1。Okay。

 so that's essentially the proof that this works out。 It's lots of ways to see why this is true。

 But I think I'll leave it at that。Okay， so this is good news。 It says。😊。

If we have the E internal node， we can find the left and the right children。

 But these are in different namespaces， right， On the one hand， we're counting by internal nodes。

 On the other hand， we're counting by position in the array， which is counting。

 position by internal and external nodes。This counts both zeros and ones。 This only counts ones。

So we need a mechanism for translating between those two worlds。

 between translating between indices that only count ones and indices that only that count zeros and ones。

And this is the idea of rank and select。So in general， I have a string of n bits。

I wanna be able to compute。The rank of a bit。Which is the number of ones。At or before position I。

 So I'm given a position like 6。 and I wan to know how many ones either there up to 6。

 which would be 5 in the full。Full array here。Or I'm given a query like 8 number of ones is 6 up to position 8。

And then the inverse of rank。Is select。Which gives you the position。Of the J 1 B。

So this lets you translate between。These two worlds of counting just the ones。Is rank？And。

 or going to the J 1 B， thats select。So this lets you compute the left child as just being at position twice the rank。

Because the rank tells you this value I， which is which internal node are you。That's your rank。

 You multipied by  two。 and that was the position of the left child。Right child。

I's going to be that plus one。Parents。Is going to use select。 So if we want the parent a I。

This is gonna be。Select。Of。I over 2 with a floor。So that's just the inverse of left and right child。

 If I divide by 2 and take the floor， I get rid of that plus 1， I get the rank。

 and then I do select sub 1。 And that gives， that's the inverse of rank。Okay。

 so that lets me implement。 if I have rank and select in constant time。 now I can do left child。

 right child parent in constant time。 The remaining challenge is， how do I do rank and select。

 And that's what we're gonna do next。Any questions about that before we go on。All right。

So now we do real data structures。 This is gonna be some integer data structures， some fun stuff。

It's gonna use some techniques， we know。But in， in a different setting。

 because now our goal is to really minimize space。I're going to use indirection and look up tables in a new kind of way。

These are going to be word Ram data structures。 I want to do both rank and select in constant time。

And little， and the amount of space I have is little of N。 I want to sink。 I。

 I'm gonna store the bit vector。So then in addition to the V vector。

 all allowed for rank and select is little low of N space。 That's the cool part。So rank。

Its one of the first succinct data structures is by Jacobson， 1989。So first observation is。

 what can we do with the lookup table。Suppose I wanted to store all the answers。But， I can't afford。

Much space。Well， if I had， let's do sort of worksheet。 If I had space X， if。

 or if I looked at all bit strings of length X， and then I wanted to store them。

That's gonna cost our store a lookup table for each of them。It's gonna cost， well。

 there's two to the X different bit strings of that length。Then for each of them。

 I have to store all possible answers to rank and select queries。

So there's order X different queries。 You could query every bit， every bit。And then for each of them。

 I have to write down an answer。 So this is gonna be。Log X bits。

 because the answer is a value between 0 and x -1。 So it takes log X bits to write it down。

So this is how much space it's going to be to store the answer for all bit strings of length x。

So what should I set x to。I'd like this to be a little low n。

So anything that's a little bit less than log n is going to be okay。 and in particular。Half log n is。

Good enough choice。If we use half log n bits， this is gonna be root n。Log in。 Lo log in。

Which is a little low n quite small as succinct data structures go。

 we're going to use more space than root N。The point is。

 if we could get down to bit strings of logarithmic size， we'd be done。

 but we have a bit string of linear size。So how do we reduce it？Something like indirection。So。

Here's our。The funny thing here is we're gonna do redirection twice kind of recursively。

 but stopping after two levels。The first level of indirection。

Is going to reduce things down to size log squared。So we're gonna take our n bit string。

 divide into chunks of size log squared n。 So there's n over log squared n chunks。They look。

Something like this。Log squared n。And the idea is right now， we're trying to just do rank。 Okay。

 so rank number of ones at or before given position。

 So what I'm gonna to do is that each of these vertical bars。

 I'm going to store the cumulative rank so far。Okay， why log squared， basically。

 because this is what I can afford to store that cumulative rank。Is login bits。Could be， I mean。

 this rank is gonna get very big。 By the end， it'll have most of the1 Bs。

 So it could be potentially linear。 So I'm gonna need log n bits to write that down。

But how many of these vertical bars are there will only n over log squared n of them。So。

I have n over log squared n。Things I need to write down。 Each of them is log n bits。So。

Some cancellation。This cancels with that。We have an over log n bits overall。

Which is slightly little event。 And that's what we're， that's the bound we're going to achieve。Okay。

 of course， now we have to solve the problem within a chunk。

But we've at least reduced to something a log squared。 Unfortunately。

 we need something a half log n before we can use lookup table。 So there's a bit of a gap here。

 So we're gonna use the in direction a second time。This time we can go all the way to half log n。

So I use red vertical bars to denote the sub chunkunks， each of these is half log n。

Overall size of a chunk here is log squared n。So every one of these chunks gets further divided。 Now。

 how could this help， Why didn't I just subdivide into chunks of size half log n before， I mean。

 why I couldn't do it as clear if I did n over log n of them， each of them stores log n bits。

 I'd have and over， I'd have linear number bits。 I can't afford linear number bits。

 That would only be compact， not succinct。How does it help me to first reduce to this and then reduce to this。

Well， what I want to do at each of these red vertical bars is store。The cumulative rank。

 but not the overall cumulative rank。 I only need the cumulative rank within the overall chunk。

Not relative to the entire array。Okay why does that help me？Need less bits。

 These ranks can't get too big because the overall size of a chunk is just log squared。

Log of log squared is log log n。 So I only need log log n bits。To write down。Those cumulative ranks。

And so。Total size here is gonna be n over log n。Times， log log again。bitits。

Becauseuse there's an over log n of these red vertical bars。

 Each 1 I only need to write log log n bits。 And this is slightly little of n。

 It's actually a little bit bigger than this， but still little O n。So we can still afford this。

And now we're， we're done because these sub chunkunks are size half log n。

 so I can use this lookup table and solve my problem。So。呃。I mean。

 step four is just putting everything together。To compute the rank of a query。

 first thing you do is figure out which chunk you fall into。

 which you can do by division and in division。 you， these things are stored in an array。

 So you just compute。 what is that cumulative rank。So you take the rank of that chunk。You add on。

The rank of the subchunk within the chunk。And then you add on the rank of the element。In the subtunk。

So rank of the chunk is stored in the array known as2。

 the rank of the subchunk within the chunk is stored in the array known as3。

 and then to compute the rank of the element in the subchunk， you use the lookup table。

Which is essentially telling you for every possible subtunk what the answers are。So。

Three times a constant is a constant。And we get ranked constant time。And。

 and log log n over log n space。If you're concerned that n times log O n over log n is not very subline。

 you can do a little bit better。Using fancier tricks。Namely， you can achieve。And over log to the K。

And space。This is a result of Petrashku from 2008。I'm not gonna go into how it's done。

 but if you're interested， it's a little bit less， it would be nice to do better。

 But my guess is there should be a lower bound that with constant。 So this is for any constant K。

It would be nice to do better， like a square root n or something。

 But my guess is there's a matching lower bound。😊，I don't think that's known。Okay， so that was rank。

Our next challenge is to do select the inverse and select is a little bit harder， I would say。

Don't have a great intuition， why。But， it is。And we're going to be able to use the same kind of technique。

 So， again， we can use lookup table。And。So I do that first。So just like before。

 if we have bit strings of length at most half log n。

Then we're only going need something like root n space。 It's root n。 Again， times log n。

 log log n space。Becauseuse just like rank， there at most n possible queries actually fewer because there maybe be fewer1 bit。

 but at most there are n1 B to query。 An answer is now an index。

 which is within a thing of size half log n。 So I just have to write down an index of that size。

 So ass log log bit， log log n bits to write it down。Cool， so that's the same。 Now。

 the challenge is about getting down to half log n bits。

 We're gonna use the same technique of two levels of indirect， but they work differently。

 There's an extra thing we need to deal with。In select。

There will be two cases depending on whether your array has lots of ones or not so many ones。

And those two cases can vary throughout the string。So。What we do。First of all。Is。呃。

So maybe I'll go over here。I'll stick here。So we're back to an end bit string。So we're looking at。

 we want the analog of。This structure of this structure of chunks。 Now， we can't just say， oh。

 take the bit string divided into chunks of equal size because then given a query， you know。

 we want to do selective of J。We need to know。Where J belong， which are these chunks J belongs to。

 So instead of making them equal size， we're going to make them have an equal number of 1 B。

 So then we can just take J divide by the size of these chunks， which is log n， log log n。

You could probably do log squared as well， but log n， log log n， well。Is a slightly better choice。

And so。We just divide every log n， log log n1 B。 put a vertical bar that way， given J。

 we divide by this thing， take floor。 That tells us which chunk we belong to。Okay， so it's different。

 decomposing by one space instead of 0，1 space。And so for those guys， we just store an array。

 If your query happens to have a0 mod this， then you have your answer。 Otherwise。

 you still need to query within the chunk。In some sense， the。Aray has gotten divided。

Something like this。So the number of one bits in here is always the same。Loggan。Lg log N once。Okay。

 so you can now teleport to the appropriate chunk。 And the issue is， how do I solve the chunk。

 But now chunks have different sizes， which is kind of annoying。 That's why we need this extra step。

Which is within。A group。Of。Log in， log， log in。1 B。 I'm calling them groups now， instead of chunks。

So each of these groups has different size。 Let's suppose it has。Size R。So say it's our bits long。

Over here。Hey， R is going be different for each chunk， but we'll you do this for every chunk。

Every group。Then there's two cases if R is big。We're done。How big。

WellIf it's at least the square of the number of1 Bs， that means it's very sparse。

 Only square root of the bits are ones。 The rest are all zeros。

Then I can afford to just store all the answers。I'm just gonna store a lookup table of all the answers。

 if it's very sparse， because then。Then I claim I only need this many bits in order to store all of these answers。

 So if I， I do this for all groups that have a large number of bits。 I store this lookup array。

How many。How many， if I sum up the size of all these arrays， how much do I pay。Well。

The lookup array has this kind of size。 They're log n log log n1 bits。

 and each of them I need to store an index for them。 Now。

 this could cost log n bits because potentially one of these groups is very large。

 It could be almost linear size。 So I need log n bits to write down a position in there。

 There's log n log log n 1 Bs to write down a position 4。 So this is the size of one of these arrays。

Now， how many of these do could I possibly need to store。 Well， I know that this group has log n。

 log log n squared bits in it。So the maximum number of such groups is n over that。N over log n， log。

 log n squared。And now。We get to do some cancellation。So this two cancels with this log n log log n。

 And then this log n cancels with this log n。 And so we get。And over。Log， log n。bitits。

Which is slightly little low than。Okay， again， it is possible to get an over log to the case space。

 but we won't do that here。 We'll be happy enough with an over log log again。Okay。

 but we're not done， unfortunately。 So we've now， we've reduced into groups。

 And I've only given you one case。 This is when R is large。 The other cases is R is small。

 meaning the number of bits in the group is at most log n， log log n squared。

That's a good case for us， because that's pretty similar to rank。 Here。

 we got chunks of size log squared。 He gets slightly larger than log squared。

 but only by a polylo log factor。And that would correspond to this step 2 in rank。

 If You do step 2 and step 3 here。Then we get step 2 of rank。 We've reduced to polylo sized chunks。

By getting rid of this case。 And so we have to do it again because we have polylo size groups。

 but we need to get down to groups of size log。Half lock。

So we need to do another layer of interactiondirect。So we get to do steps 2 and 3 again。

This is what I'll call step four。Repeat steps 2 and 3。On I sorry， I didn't say。I need an else clause。

Else， I'm going to call this bit vector a reduced bit vector。So， reduced to order。Login。

Log log n squared bits。And so， step 4 is on。All reduced strings。All reduced bit strings。

I want to do steps 2 and 3， again。Let me do it quickly。呃。My goal is to further reduce。To。

Poly log loggan。Right， I took n bit strings， and I got down the log poly log bits。 I do it again。

 I should get down to poly log log bits。 And indeed， I can。 And this is plenty small log。

 polylylo log。 It's way smaller than half log。So we don't even need that much of the lookup table。

Fine， so I'll call this two prime。I want to make this explicit because they are slightly differentcause now everything's relative to this or to the reduced string。

 which is polylo。This gets hard to pronounce， but every log， log n square1 bit。

 we're gonna write down the relative index within the reduced string of size log n。

 So writing down the relative index only costs。Log， log n bits。

Because we're in something of size log n。 So writing now that index is short。

We write it down for all of these。 So we end up paying。And。Over log， log n squared。

That's the maximum number of these indices that we need to store。 Each of them。 we pay logla again。

 So here I'm summing over all the reduced bit strings。 This is an overall size。

 It's at most n over log again squared that we need to store。

 could be fewer if there aren't many reduced bit strings。 But worst case。

 everything ends up being reduced。So we have this many times that many， times that many bits。

 and we get n over log log n bits。Okay， this is roughly following the pattern of step 2 over here。

 Step 2 over here didn't just have the log term。 It also had an auxiliary log log term。

 So if you felt like it， you could make this log log n times log， log log n。

 but it'll actually give you worse space bound。 So this is slightly better。Okay。

 then we apply step 3 prime。Which is we look at each of the groups that we've identified and either it's big。

 either it's big and it has lots of 0 bits， or it's not big。And in either case。

 we're going to be happy。So， if a group。Of。Log， log， and。Squared 1 B。Has our bits。

We look at each of them individually。And if R is at least the square of that。 So log。

 log n the fourth power。we're losing constants in the exponents， but it's not a big deal。Then。Store。

啊。Relative。I mean， saw are all the answers， but now as relative indices。Okay， let's go over here。

So how much do these relative indices cost， Again， it's at most order log log n bits。

To write them down。We don't know that a group is any smaller than log n。

 but it's at most the original size of log n。 It's only log log n bits to write each of them down。

 And now we get to say， oh， well， we had to write down log log n squared 1 bits。

 But this could only happen and over log log n to the fourth many times。 So the space is。And over。

Log log n to the fourth。 That's the maximum number of these。

 I guess you call them sparse bit vectors， Sprse groups。

 there could beca each of them is at least this big total number of them is at most n divided by that for each of them。

 we have to write down log log n squared different indices for array。

 and each of those indices cost log log n bits to write down。

So this is three T log n to the third power。 This is log n to the fourth power。 So again。

 this is n over log log n。 You can tell I've tuned all of these numbers to come out to n over log log n bits。

Okay。That was the if case。 There's the else case， which is that you have reduced to polylo log size。

 namely， then in the， in the dense case， you have R's at most log log n to the fourth。

So at this point， the。So， else。You are further reduced。When you're further reduced， you have at most。

Log， log in。The fourth bits。And at most log log n， squared of them are 1 B。

 but we don't really care about that。 Once we're down to a bit vector of poly log log size。

 we can use our lookup table。And we're done。So that's select。 If you wan to do a select on an。

 on an index。 first， you figure out。Which group it's in by dividing by log n， log log n。

 taking the floor。You teleport to the appropriate group using this array。Then， within that group。

There's a bit saying whether it was sparse or dense。If it was sparse， so lots of zeros in it。

 then you have a lookup table that gives you all your answers。For the remainder of your query。

If it's dense， then you go over here。You know that this thing will be stored。

 And so you figure out which subgroup you belong to by dividing by log log n squared。

 taking the floor。There's an array， this thing that teleports you to that。Group。

Sorry to that subgroup。And then you apply。Then there's a bit there saying whether it was sparse or dense。

 If it was sparse， there's a lookup table giving you the answer。 If it was dense。

 there's an index into the number one lookup table。

That tells you what this bit string is because there there's only log log into the fourth bits。

 So just by， in fact， that is the index。 Just what those bits are lets you look up into this table and solve your query in constant time。

 In all cases， constant time。 But here is a little bit more branching。

dependepending on the situation。As I said， select is a little more complicated than rank。

 But in the end， constant time little event space and over log log n， which can again be improved。

By for trashq and not an over log to the K and for any constant K question。

 Can quickly remind us how the two and three changed。Okay。

 how did 2 and 3 change You don't actually really need to change them。

 The big change is that you're storing only relative indices， not indices。

So before we're storing an array of indices of every log log n，1 bit。 these were you know。

 global pointers。 But now after  two and3， we've reduced something of size log n or poly log n。

 We need to exploit that here。 And so that we were only storing log log n bits。 We didn't do that。

 This would be order n bits and then be too big。That's really the only thing you need to change。

 The other thing I changed was this value。 If you follow that plan， it would be。啊。W。

There's also a square。 So we didn't have to add a square here。

 You could also add a log log log n term here， but it won't matter。 Basically。

 you do something that works。The square was necessary to cancel out this guy， for example。

 you didn't do the square。 Well， you So instead of this， you could have done log log n times log。

 log log n without the square。 Then here you would have gotten n over log log log n。

So you could have followed the same pattern。 You just get a slightly worse space bound。

 I tuned it here here we needed here。 we could not have afforded to go to log Square if I recall correctly。

 though you can check that。Maybe it's a good P set question。There's lots of choices that work here。

 But this is the 1 I find the cleanest that gets a decent bound， not the best bound， but。

Reasonable other questions。I think that's all that I changed。 The other。

 the sparsity definition was still a squared thing。So it was squared over here。

 and it was squared over here。 It's just the thing we were squaring was a little different。Okay。

One more thing I want to talk about。So。At this point。

We just finished this level order representation of binary tries。

Becauseuse we already saw left child， right child and parent reduced to rank and select。

 We just solve rank and select in little of N bits。 So at least statically。嗯。We can do left child。

 right child parents in a binary trial now and constant time for。2 n plus little of n bits of space。

 The two n bits are to store those two n bits that we wrote down before。

So that's succinct binary tries done。One mention， there are some dynamic versions in particular。

There are dynamic versions of rank， and select。But the best versions that are known to do dynamic rank and select。

Achieve something like log over log log。Time for operation。If you're interested in dynamic。

So this is kind of annoying if you wan to go to dynamic。

 either you pay more time or you don't use rank and select。

 but I'm not gonna worry too much about dynamic。 Stick to rank and select。

 But there's one more thing on this list， which is。A different way to do succinct binary tries。

 And this different way is gonna be more powerful， more useful for things like suffix trees。

 which is what we're gonna do next class。 So I want to tell you a little bit about。Of this。

 the level order representation is kinda like a warm up。 It motivates， rank， and select。

But it does not let us do subtree size。 Sub size would be nice to do because you care about how many matches you have after you do a search down suffix tree。

Love order just ain't going to cut it for that。So we're gonna use different representation。

 We're still gonna use rank and select a lot。And， and of generalized forms of rank and select。

But it's going be a little bit more handy。Essentially。

 I want to do more like a depth first search of the try。Less like a。ALes level order。

 So more depth first。Okay， here's our friend， the binary try。Same one as before。

We had our binary new representation of it。 I'm not going to draw that here。

First thing I want to do is say， hey， look， this is the same thing as a rooted， ordered tree。

Carity mentioned that there's the same number of them。 There's Catalan of these。

 and there's Catalan of these。 So a rooted ordered tree has a node and has some number of children and then more nodes。

 The children are ordered， but they don't have labels on them。 So it's a tree， not a try。Okay。So。

I claim these two things are equivalent。 and there's a nice combinatorial bijection between them。

She may have seen before。It's kind of， a classic。 But here we're gonna use it。For handy stuff。

 basically。So binary tries distinguished between left and right。 roototed ordered trees do not。

 They just have order。So to clean that up， I'm going to look at the right spine of the try。

Distinguish that because right and left meat make a difference here and then recurse。

 So this is the right spine of down here。 This is the right spine of this sub tree。Now。

 every node lives in some right spine。And then I'm just gonna rotate 45 degrees。Counterclockwise。

 So I have AE G。That's my first right spine。 I'm going think of them as children of a new root node。

And then they have children below that， which correspond to the right spines that hang below。 So A。

 for example， has this right spine， B C， D。这 b c。😔，D。Here。E has a right spine of F hanging off of it。

She has no right spine hanging off of it。So you need to prove that this is a real bijection， every。

Bary try can be so converted into rooted ordered treat， and it's unique。

 So if it's different over here， it'll be different over here。

 and you can convert backwards as well if you just delete the super rootot and turn all the children into a right spine or recur。

 They're really the same thing。 This is why there's Catalan of each of them。Okay。Now。

 what I'd really like to get to is balance parentheses。

And while it's a little unclear how to represent a binary try with balanced parentheses。

 these things it's really clear how to represent a binary represent with balanced parentheses here。

 I just do an orderer tour， which was a depth for search visiting these things。

 and every time I start a note， I'll write an open pre， Every time I finish a note。

 I write a close print similar to representation we talked about before。So， this would be。

gonon need more space。Thiss gonna be an open paren for star。 It I write that。

 make that one really big。We start here。 Then we open the a chunk。Then we do B。

 which has no children。Then we do C， which has no children。Then we do D， which has no children。

And that finishes a。Okay， then we start E。Then， we do F。Then we finish F。 we finish E。Then we do G。

And then we're done with star。Okay， so that's very easy transformation。 Again。

 there are Catalan many of these balanced prints。 You think， oh， there's two to the end of them。

Cause each， each parent could be open or close， but they have to be balanced。

 So it's a little bit more constrained than that。And so it ends up being Catalan of n over 2。

 if there's Npars。Fcause that there's two per ends here for every node over here。

 This is gonna be our bit string。Open friendss are zeros。 Close friends are ones。

This has roughly 2 n Bs，2 n plus 2， I guess， for the star。relativeative to this n。So basically。

 nodes here。Correspond to nodes here。Which correspond to an open paren closed parnne pair over here。

Now， we're not， we can't afford to store these labels。 Those are just guidelines。

 Think about what you need。So let's think about there are three things we really want here。

 left child， right child and parent。This is the。 This is the thing that we care about。

 But this is what we're going to store。 So I want to translate from here to here to here。

 This is an exercise in translation。So what does a left child mean here， Left child over here。

Corresponds to。呃。Well， I guess it goes in general， the left child goes to this branch。

 which is like all of these children pointers from A。 But really， if you follow the left child。

 you get to B， not any of the other things on the right spine。

 You always get to the top of the right spine。 top of the right spine is the leftmost node in the spine here。

 In other words， it is the first child。Of a node， first child of a node。

 If there is one is gonna be the left child over here。Right child is like following the spine。

 That's like going this way。So right child is what I would call next sibling。

The next sibling to the right， If there is one， that's going to correspond to the right child because we're just following a right spine。

Okay， parent？Is a little trickier。Parent is the reverse of these。 So either。

You take your previous sibling， but if you're here and there is no previous sibling。

 then you take your actual parent。Because parent should walk up here。

 this is like going left previous sibling， previous sibling， parent of this guy though。

 is the actual parent over here。So this is going to be previous sibling if there is one。

Or if there isn't one， you go to the parent。Okay， so that's the easy translation。 Now。

 we need to convert these pictures into balanced parentheses pictures。

 which is also gonna be easy in itself。 But the jump all the way from binary tries to balance per ends would be pretty confusing。

So that's why we have this intermediate step。So we want first child here。If I have a parent。

 like look， I'm looking at a。So a corresponds to this paren and this paren。

 I'm gonna represent the node。 let's say by the first paren。

Then the first child is just the very next character。Right。

 you put the first child right after that open parent。

So this is really the next character if we want to find the first child。

 This is if it's an open parent。It could be the very next， like， if you're doing B。

 the very next character is a closed pre。 that means there are known children。Okay。

 but that's how you can tell whether there's a child。

 if there's an open parent right after your open parentn， that's your next child。

That's your first child， I should say。Now， what about next sibling， So let's say， again， I'm at a。

And I want to know the next sibling。 Next sibling is E。

So that's like I go to the closed parentend for a， and then I go the next character。So。

 this would be。呃。Go to the close per end for where you are right now。

And then go to the next character。This is， again， if it's an open print， if it's a closed print。

 then you have no next sibling。 So again， you can tell whether this operation fails。

 What we need is an operation。Given a bit string representing balanced parentheses and given a query position of a left paren。

 I need to know what is the matching right per。And I'll just wave my hands and claim that can be done with the same techniques as rank and select。

It's not easy。 It's quite a bit harder， but you do enough of these recursions。 Eventually， you。

 you can solve it。Okay， last operation is parent over here。

 which corresponds to previous sibling or parent over here。Which corresponds to。呃。

There are two cases。 we want to move backwards。 So here we're always ending with next character。

 So the first thing we do is go to the previous character。And there are two cases。

 if it's a close parent。 So let's say we're here at E。We go to the previous character。

 If it's a closed parn， then a is our previous sibling。

 So we want to do the previous sibling situation。 We again find the match。

 We hit percent in V I and's the corresponding thing in Eax。 I forget。

 you go to the matching close parn。 sorry， open par。 And then there you go。

 you've got your previous sibling。 So if it's a close parn。

 then you go to the corresponding open parenten。😊，If the。

 if the previous character is an open parent， then you're done。 That's your parent。 So that here。

 if you're at a， you go to the previous character and it's an open parent。

 Then you just found the parent of A。 There was no previous sibling。So in either case。

 you end up with an open print corresponding to either your previous sibling or your parent。

So that's left child， right child parent。 If you have this in matching parent operation。

 you can do all of these in constant time and。Little O N space beyond the two n Bs to write down that bit string。

That's not so exciting because we just reinvented the same results we had before of doing left。

 right and parent in constant time。 But what we buy out of this representation is we can now do subree size。

😊，So。嗯。That's a little bit。Trickier， let me go to another board。

But whereas with level representation， it was impossible now it is possible。

And we're going use subt size， I think， next class， when we do suffix， compact suffix trees。So。

Subre size is what we want。In the binary try。In the rooted ordered tree。

 it's a little tricky becauseuse subrees no longer correspond to subtrees。 For example。

 the subtree of C。Consists of this subt and this subt。So it's really C over here。

 It's C and all of its right siblings。So， this is。Size of the node。Plus， size。Of right siblings。

However many you have。Okay， so in the rooted order tree， it's actually kind of messy。

 Turn out in the balance parent， it's pretty。Clean。

Cause all your right siblings correspond to parent groups that just follow each other。

 And you want to know， So these are a bunch of siblings here。Verying size。 And we're given。

 say this sibling， we w to know for this sibling up to all， all the ones to the right。

 So there's an enclosing parenthesis here for our。Parent in this representation。

We want to know the length of these。So it's just。We want to take the here we are at this left parent。

 We want to compute the distance。To。The enclosing。Close per in。So that's here。

That's our enclosing close print。 So here's a new operation。 given a per end pair。

 I want to compute the enclosing per end pair， these guys。

That can also be done in constant time with rank and select like techniques。

 And then you just measure this distance and you divide by 2。

 That will give you the number of nodes in here。 It's half the number of prints。

That will give you subre size。 And we have a couple extra seconds。

 So another bonus is suppose you want to know the number of leaves in a subtre。If I recall correctly。

 that's something like。Instead of doing this distance to the enclosing peren， you do something like。

Rannk of。So just rank of that。 Those are the number of leaves。Of。In closing。Close par。

This is getting notationally confusing minus the rank。Of。Here。Okay。

 so I just want to compute how many open per closed pre are there from here to here。

And so I just take the rank here， subtract by the rank here。

 That gives me the number of leaves in that range。 So this is a generalization of rank before we did rank of just a single bit。

 This is rank of a 2 bit pattern。But 2 Bs is not much harder than 1 B。

 You can very easily adapt the rank structure we saw to do any2 B pattern instead of just the 1 B。

So that gives you the number of leads in the subree， which corresponds to the number of matches。

 So you can do lots of fun things like this。 This representation is super powerful。

 and we'll use it next time。😊。

![](img/a5301df0dbdb71cac9519e3fc56fa9a4_4.png)