# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p64 p63_03_method-overloading-in-java -BV1tAygYoEj5_p64-

Today， in this session， I'm going to talk about method overloading。In Java。

 that is a very important concept to be discussed when it comes in polymermorphm。In Java。

 it is possible to create methods that have the same name。

 but different parameter list and different definitions。



![](img/dd7e3882d3bec73627573880965eaaa5_1.png)

So method overloading allows to declare multiple methods for the same name but different parameters in the same class and allows to more methods that have the same name in the parameter list。

😊，When an overload method is invoke， Java uses a type。

 number or sequence or arguments as a guide to determine which version of the overloaded methods to call。

This is how method overloading comes into the picture。 We have four fun methods。

 The first fund method has0 arguments。 The second is having one argument。

 The third is having one argument， But you can see in the second and third， the type is different。

In the third funk method， we have two parameters or arguments that is different from the above3。

So here funk methodthod is overloaded four times。 These methods have the same name。

 but different function arguments。 As I said， Java uses the type。

 or number or sequence of arguments to be different。



![](img/dd7e3882d3bec73627573880965eaaa5_3.png)

Basically， overloading needs to be used when different objects are required to perform a similar set of tasks but use different input parameters。

It is used when different objects are required to perform the similar set of tasks。For instance。

 in e-commerce platform， there are different modes of payments like we have a CD option or a U option or bank transfer option。

All of which are overloaded to perform a single function and when we call overloaded method in an object Java matters of the method named initially。

 and then the method decides the number and type of parameters of the definition to be executed。

There are different ways， as I said， by changing the number of arguments or the type of the arguments。

 if the number of arguments and the type needs to be remain same。

 you can change the sequence of the arguments to differentiate from other definitions。

These are the advantages over the top of two use method overloading concept in your real time projects or the applications。

 it increases the readability helps in maintaining the application consistency helps in code reusability。

 backward compatibility and flexibility。So this is how these things needs to be taken care of the overloaded methods can also change the access modifier and can change the return type that does not impact onto the method signature overloaded overloading is used when objects are required specifically the similar tasks for the different input parameters。

 Let's try implementing method overloading in Java。Considering， I have。A class calculation。

There we have。Public。Static in teacher。Is a return type edition， edition of two numbers。

 number one and number2。return， the sum of number one plus number2。Public static inte edition。

 This time， I'm expecting three parameters， number one。Number 2， and number 3。

Number one plus number two plus number 3。This time， I want to take。

Different parameters such as public static， float addition。Earlier， I had two integer parameters。

 but I this time， I wanted to take two floating parameters or decimal values。

So return type is as per the type of the parameter that is number one in number two。

And same thing gets happened。Let's say I wanted to take double addition。And this time。

 I wanted to take one in teacher number one and one。Float number。 So I just take the higher one。

 That's a float。 I can also take it up。Floty。So I just want one integer and one food。Return。

 number one plus number2。And in the next iteration I wanted to take once again。

 I wanted to take one integer in one float。 but this time， I just wanted to change the sequence。

 but because two methods cannot have seen signature exactly。 so you can change the sequence。

 So as I said， if I am seeing number of parameters can be different。 you can see that。

The first addition method has two parameters， and the second one is having three similar parameters。

When I'm changing the type， saying the type should be different than first edition method has two integer values。

 but the third。Addition method has two float values When I am just saying the sequence can be different。

The4 and fifth number will have。One intesger and one float and one float and one intesia。

 so that's how it looks like So when you are going to call these methods。

I just call directly inside the says out。Calculation dot， addition。

 all the overloaded methods are coming。 So if I am supposed to call by two integer numbers。

 I say  hundred and 200。Again says out， if I am saying calculation。Dot addition，3 integer numbers，00。

200 and then 300。If I'm saying。Calculation， dot addition。2 float values，00。50 F。200。30th。

If I'm saying。One in teacher and one float。Then， again， says out。1。In teacher and another float。

I need to pass these values inside。Calculation door。At。Hundred and 200 point。540 f。If I'm saying。

Calculation， dot addition。This time I am passing first number as floating and the second number as inte。

 So this is how the overloaded method needs to be created and called up。

Let me just run this program right away。I'll just restore my console all the methods are called in a nice implementation。

So， this is how。Overloading comes into the picture， when。Objects are required to perform different。

 similar tasks， but with the different input parameters。

 I hope it really helps and concept is clear to all of you。

See you in the next session until next time Stay tuned。 Thank you。



![](img/dd7e3882d3bec73627573880965eaaa5_5.png)