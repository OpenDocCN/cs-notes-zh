# 杜克大学《Rust编程2-3（数据工程、DevOps）｜Rust programming》中英字幕 p86 86_04_07_数据科学公共数据集使用.zh_en -BV11y411z7Dn_p86-

![](img/eb8740a9d44ce8687d2770afcb4c2ff3_0.png)

Yeah。A very common way to build machine learning systems is to use public datasets。

 Let's talk through a few of the common public datasets that are available。

 a very popular merg public data as the hugging face datasets and you can use it to finetune a model。

 So let's say you get a pretrained model from Huging face and you use an environment that has GPU enabled like Giub code spaces or an Amazon SageMake environment with GPU enabled。

 you can then take that huggingface data。 and finetune it based on the new data that's available and then create a new model and put it either into production or back into Huging face。

 Likewise with Amazon S3 It's a very common scenario to have a big public dataset and you can pull that dataset into。

 let's say a Jupyter notebook do exploratory data analysis on it。

 find out what it is you're trying to build and then create a model based on that S3。Data set。

 another common public data set is CAGle。And there are many examples of public dataset sets that have also had many feature engineering components added to it so you could build off of that。

 use that feature engineering solution to then create your own custom model。

 so there are many examples， these are just three of public datasets that are useful in then building models that you can use either in production or for experiments that lead to further models either as a hobbyist or a production machine learning engineer。



![](img/eb8740a9d44ce8687d2770afcb4c2ff3_2.png)