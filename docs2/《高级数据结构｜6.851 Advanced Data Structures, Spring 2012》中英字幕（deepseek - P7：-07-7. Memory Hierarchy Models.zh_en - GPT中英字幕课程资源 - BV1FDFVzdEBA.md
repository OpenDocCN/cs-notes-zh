# 《高级数据结构｜6.851 Advanced Data Structures, Spring 2012》中英字幕（deepseek - P7：-07-7. Memory Hierarchy Models.zh_en - GPT中英字幕课程资源 - BV1FDFVzdEBA

The following content is provided under a creative Commons license。

 Your support will help M I T Open Coseware continue to offer high quality educational resources for free。

To make a donation or view additional materials from hundreds of MI T courses。

 visit Mi T OpenCourseware at O C W dot M I T dot E Du。



![](img/10ac5475d606569f928e958b4ce9c10d_1.png)

Alright， today， we start a new section advanced data structures and it's data structures for the memory hierarchy。

 So the idea here is while tradition， almost all data structures think about memory as a flat thing。

 And in the RA model， you've got a giant array， you can access the I element in your giant array in constant time。

The memory hierarchy， you admit the reality of almost all computers since， I don't know，80s with。

 which have caches。 The ideas you have your。CPU。Connected with a very high bandwidth channel to a relatively small cache。

Which is connected via a relatively narrow bandwidth channel to a really big memory。And in。

 in real computers， this keeps going。 You've got level 1 cache， then level 2 cache。

 then level 3 cache， maybe， then main memory， then disk， then network， or whatever。

 Usually the disc has a cache。 So you have this very deep hierarchy。

 which is typically growing exponentially in size and also growing exponentially in slowness in latency。

So， you'd like to。Design data structures that most of the time are working at the cache level。

 because that's really fast。 And as little as possible， you'd like to go to the deeper levels。So。

We're gonna define two models for working with memory hierarchies。 first model。

It's called external memory model。Okay。It's also called the I O model。

And it's also called the disk access model， I think just so that it could be called the dam model。

I usually call it the external memory model。 and this is sort of the simplest model。

 It's also the most well studied， I would say， and it's going to relate closely to the other models that we're gonna look at。

 And the idea with the external memory model is， yeah， okay。

 a real computer has many levels in this hierarchy。

 But let's just focus on the last one and the next to last one because typically that last one is going be way。

 way slower than all the others。 And so you really want to minimize how many memory transfers happen here。

😊，So I want to count。The number of memory transfers。Between。Two levels。

And so this model just thinks about two levels。 I'll call them cash and disk。

We think of disk as infinite。And slow， we think of cash as not only fast， but infinitely fast。

 It takes zero time to access something in cache。 All we're thinking about is how many transfers between the left and right have to happen。

 So computation here is free。 Access over here is free。 And we just pay for this。

 That's the external memory model。 You can also think about running time。

 make sure running time is still optimal， and that will guarantee this part's good。

 But usually the hard part is to minimize this part。 minimize number of memory transfers。 Now。

 to make this problem really interesting。😊，We need to。Add one more assumption。

 And this is also the case in practice that when you request。

 when you transfer an item from disk to cash， you get not just that item。

 but entire block of items at the same speed。So， in fact。

 disk we're going think of as divided into blocks。Block size here。Is B。Capital B。Also over here。

 caches are divided into blocks of size B。And we have a limited number of them。

We're going to suppose we have M over B。诶。Cash lines are usually called at the cache level。

 And so the total size of the cache is M。M stands for memoryory。Sorry， it little this。

 this notation is slightly confusing for historical reasons， but that's the way it is。

 So B is block size。 M is cache size。Total cache size。 There's M over B blocks the size B。

Dis has infinitely many blocks of size B。There's lots of motivations for this。

 But basically the main cost you're paying here is latency。 You have to send a request。

 And then the request， the answer has to come back。

 Think of that as the disk is far away from the CPU。

 And so it takes a long time to get there for the request to get there and for the data to get back。

 So if you're sending data back， you might as well send。

 it's just as fast to send a lot of data as a little bit of data。 In fact。

 typically you set this block size so that the amount of time it takes for the data to arrive equals the latency。

To balance those two costs with disc， it's especially the case because you have this latency of moving the disk head。

 Once you move the disk head， you might as well read the entire track， the entire circle of that。

 if you're dealing with hard drives。Yeah， it's just as fast once the head gets there。Okay。

 so that's the model。 And so now we want to minimize the number of block memory transfers。

 This happens。 And the blocks make things a lot more interesting because that means when you read an item。

 you really want to use all the items right around it。😊，Soon， before it gets kicked out of cache。

 obviously， cache has limited size。 So when you bring something in， you've got to kick something out。

 that's the block replacement strategy in the external memory model， you can do whatever you want。

 You have full control over this cache。So。Let's see。 Let me write some obvious bounds。

And then I'll tell you some the main results in this model。

So a number of memory transfers is what we care about。And。

It's going to be at most the running time on a Ram。So this comparison between two models。

 right at most， every operation you do on a Ram data structure incurs one memory transfer。

 That would be the worst case。 So that's what we wantanna beat。There's also a lower bound。

So this is also， at least。The number of cell probes。Divided by B。

I think we've talked briefly about the cell probe model， cell probe。

 I think in retroactive data structures， cell probe model is this model where all you count is you just count how many words did I have to read in order to do this data structure operation。

 So computation is free。 We just count number of word reads from memory。😊。

So it's it's a lower bound on Ram data structures。 And in this model。

 we're only counting memory axises， but where cell probes you count word word numbers here。

 we're counting blocks。 And so at best， we could improve by a factor of B。So this is a lower bound。

 This is an upper bound。 So there's roughly a s of a factor of B here。

 We want to do closer to this bound。Than this bound。

 This is the obvious thing wanna get down to here， whenever we can。

So let me give you some examples of that。So sort of a bunch of basic results in this model。

Result zero。Is scanning。So if I have an array of items。And I just want to。

Say add them all up or do a linear scan to search for something。

 Anything that involves accessing them in order。This will cost。I guess。Ciling of an over B。

If the array has size n。Number of blocks in that array is ceiling of N over B。And so。啊。

That's how many block x does it cost。 So the， the annoying trick。

 the annoying part here is the ceiling。Okay， if N is smaller than B， then this is not great。 I mean。

 you have to pay one memory read。 So it's optimal， but。Anyway， that's scanning， pretty boring。

Slightly more interesting， although not that much more interesting is search trees。

If you want to be able to search for an item in this external memory model。

 what do you do in this external memory model， what do you do。Beeries。Conveniently named bee trees。

 We want the block， the branching factor of a bee tree。To be B。Anything that's theta B will do。

 But exactly B is great。 If you can read all of these pointers and read all of these key values。

 if that fits in B， then we're happy。😊，啊。So。Yeah， typically we assume the branching factor here is like B plus1。

So in particular， if B is1， you still have a branching factor of two。 So make it a reasonable tree。

 And so now in a constant number of block reads， you can read all the data you need to decide which child pointer you follow。

 you follow that child pointer。 And so it takes log based B plus one of n。To do a search。

This is counting memory transfers。So this is not as big and improvement here。

 we've improved by a factor of B， roughly here。 we're improving by a factor of basically log B。

So not as good as you might hope to do。Dividing by B。 But it turns out this is optimal。 you need。Log。

Base B plus1 of n to do a search。诶。And。Comparison model。In the same way。

 you need log base two of n time to do a search in the comparison model。 in this model。

 you need log based B of。 Let me talk a little bit about proof of that。

It's a pretty simple information， theore argument。So。How we prove this lower bound。

The idea is the following。 you want to figure out。Among an items here。Where your item fits。

 let's say it fits right here。 So you want to know it's between this element and this element。

How many bits of information is that， Well， there's， I guess。

 n plus one different outcomes of where your item could fit。If you allow it to exactly match。

 it's maybe twice that。 but there's theta n。Options。For your answer。And so if you take logs， that is。

诶。Loggan。Bits of information。And this is how you'd normally show that you need log n comparisons。

 You need log n comparisons，cause each comparison could at most give you one bit of information。

 Yes or no。And there's login bits to get。 So that's the lower bound on binary search。

But now we're in this model where we're not counting comparisons。

 even though we're in the comparison model， we're counting number block transfers。

So when I read in some data， I get B items for my array。Far from something。So， when I do。

A block read。All I can do in the comparison models can compare my item to everything that I read。

So you might as well compare all of them。 but the information you learn。Is only log。B plus one。Bits。

Because what you learn is among these B items， where do I fit？And in the best case。

 that is log of B plus1 bits of information， it could be worse if these guys are not well distributed among your N items。

It most that。 And so you take the ratio。And you need。Log。And over log B plus 1。Block reads。

And that's log based B plus1 of n。Yeah， question。Plus one bit when you。Alright。

 so the claim is in the comparison model， you compare your item to everyone in here。

But if your array is presorted。Then all you're learning is， I mean， or information theoretically。

 all you're learning is where your item fits among these B items。So。There's， you know。

 order B options of where you fit。 And so only log B。Bits of information there。Okay this is。

 you might consider this a vague argument。You can do a more formal version of it。

 but I like the information theoretic perspective。Cool， so that's。

The best thing you can do for search in this model。Now。

 you might also wonder about insertions and deletions。

 B trees support insertions and deletions in log based B plus one of N time， but that is not optimal。

 We can do better。So， for example。If you want to。Sort。Right answer for sorting。This is crazy bound。

N over B， which is really good。 Thats linear time in this model times log base M over B of n over B。

This is a slide。 This is roughly log based M of N。The over bees don't make too big a difference。

So this is an improvement from normally sorting takes n log n， but the base2。

 we're improving the base of the log to M over B instead of 2。

And we're improving were the overall running time by factor of B。

 So this is actually a really big improvement。We're。

 we're going faster than the Ram time divided by B。

Because to sort in the cellpro model just takes linear time。 You read all the data， you sort it。

 put it back。 So we can't quite get N over B， but we get this log factor。So this is really good。

 And it's something that would not result from using bee trees。

This is a little funnyca in regular Ram model， if you do。

 you insert everything into a balanced binary your search tree， then do an in order traversal。

 That's an optimal sorting algorithm。 It's give you n log N here， it's not optimal。

You throw everything into a beach where you get n times log base B of N。

So there's two things that are bad。 One is that the base of the log is slightly off。

It's B versus M over B。 That's not so big a deal。 The big thing is， it's n over B versus N。

We don't want to pay N。 We want to pay N over B。There's also a matching lower bound in the comparison model。

 which we will not cover here。By next lecture， we'll know how to do sorting optimally。At first。

 I want to tell you about， about a bunch of。Different。Wass or different problems and results。So。

 a funny problem。Which actually does arise in practice， but。It's interesting just to think about。

 instead of sorting。 sorting is， I give you an array。 I don't know what order it's in。

 You have to find the right order and then put it into the right order。 There's an easier problem。

 which is called permuting。 Should I give you an array。 I give you a permutation that I'd like to do。

 I just want you to move the items around。So there's no。

 you don't need to do any comparisons to get here。 I tell you what the order is。

 You just have to physically move the items。 This is harder than it sounds because when you read you read blocks of items in the original order。

 And so it's hard to just do an arbitrary permutation。And there's two obvious algorithms。 One is。

 you could sort。Your item。 so you could go to every item in order and just write down its desired number in the new permutation。

 write down the permutation， then run a sorting algorithm that will take n over B log M base M over B of N over B or。

The other obvious algorithm is to take the first item， put it where it belongs in the array。

 Take the second item， put it where it belongs in the array in， you know， a new copy of the array。

 And that will take it most and memory transfers。Each one， a completely separate memory transfer。

 And the claim is the optimal thing to do is the men of those two。So you just check。

 how does N and B and M compare do the better of the two algorithms。啊。Again， there's a lower bound。

In a model called the Indivisible model， which is assuming that。Each word of your。

 of your data doesn't get cut into sub wordss。So this is assuming there's a lower bound， assuming。

Your words do not get cut up。I believe it's still unsolved， whether you could do better otherwise。

Okay， the last result。This model I wanted to talk about is called buffer trees。 Bffer trees are。

Pretty cool。They're essentially a dynamic version of sorting。Which is what we lack。

 what bee trees do not give you。So they achieve the sorting bound divided by n per operation。

So this is optimal。 Having't told you what the operations are。 The bound is amortized。

 It has to be amortized because this bound is typically little O of one。Think of this as1 over B。

 There's a log factor， but assuming that log factor is smaller than B。Which is often the case。

 This is an order one over B。 It's a little o of1 cost， which a little fun。 But， of course。

 every real cost is an integer。 It's an integer number of block transfers。 So but amortized。

 you can get a little o of1 bound。And what it lets you do。Is delayed queries and batch updates。

So I can do an insert， and I can do a delete。 I can do many of them。

 And then I can do a query against the current data structure。I want to know。

 I don't know the successor of an item， but I don't get the answer right then。 It's delayed。

 The query will sit there。 Eventually， the data structure will decide to give me an answer。

 or it may just never give me an answer。 But then there's a new operation at the end， which is flush。

 which is give me all the answers to all the questions that I asked。

 And the flush takes this much time。So if you， if you do n operations and then say flush。

 then it doesn't take any extra time。 It's going to cost one sort of pass through the entire data structure。

 one sorting step。诶。Yeah。The one thing you can get for free is a zero cost find min。Alright。

 so buffer tree， you can do fine min instantaneously。 It maintains the min all the time。

 So this lets you do a priority queue。 You can do insert and delete min in this much time。Online。

And the。But if you want to do other kinds of queries， then they're slow to answer。

 you get the answer much later。Okay， so this is a dynamic version of sorting。

 It lets you do sorting in particular。 lets you do priority queuees， Other great things。

 We will see not quite a buffer tree， but another data structure achieving similar bounds in the next lecture。

Today's lecture is actually gonna be about bee trees。

 which seems kind of boring because we already know how to do bee trees。

 But we're gonna do them in another model， which is the cachebilius cacheshbivius model。

So let me tell you about that model。Alright， so the cache flow based model is much newer from。1999。

And it's from M I T。 originallyin， Charles Lyisterson and his students invented it back then in the context of 6。

0，4，6， I believe。And it's almost the same as this external memory model。There's one change。

Which is that the algorithm doesn't know what B or M is。Now。

 this is both awkward and cool at the same time。 So the assumption is that the computer looks like this。

😊，The algorithm has no and the algorithm knows that fact。

 but it doesn't know what the parameters of the cache are。 doesn't know how the memory is blocked。

 Now， as a consequence， it cannot manually say， read this block， write this block。

 It can't decide which block to kick out。 This is gonna be more like a typical algorithm。 I mean。

 the algorithm will look like a regular Ram algorithm。 It's going to say， read this word。

 write this word to memory。 you know， do an addition， whatever。So。Algorithm。Looks like。

A RAM algorithm。But the analysis is going to be different。So this is。

Connient because algorithms are I mean， how you implement an algorithm becomes much clearer in this model。

 And this is， now we're going to assume that the cache is maintained automatically。In particular。

 the way that blocks are replaced。Becomes automatic。So the assumption is。

 when you read a word for memory， the entire block comes over。

 You just don't know how much data that is。And when something gets kicked out。

 the entire block gets kicked out and written to memory。

 But you don't get to see how that's happeningca you don't know anything about B or M。

We we need to assume something about this replacement strategy。 And in the cacheshlivius model。

 we assume that it is optimal。This is an offline notion saying what block we're going to kick out is the one that will be used farthest in the future。

So this is very hard to implement in practice because you need a crystal ball。

 but good news is you could use something like LRU。

 least recently used kickbackey out or even very simple algorithm first and first out。😊。

The most recent， the， the oldest block that you brought in is the one that you kick out。

These are actually very close to optimal。They're constant competitive。Againt。Or。

Given a cache of twice the size。So this is from the early days of competitive analysis。

Says if I compare offline optimal with a cache size M versus。

 or let's say offline optimal with a cache size M over  two versus L R U or FiIO。

 which are online algorithms on a cache size M， then this cost will be at most， I think。

 twice this cost。Now， the good news is you look at all these bounds。 you change M by a factor of2。

 Nothing changes， assuming everything is at least some constant and M and B are not super。

 super close。😊，Then changing M by a factor of2 doesn't affect these bounds。

 doesn't these ones don't even have M in them。 So it's not a big deal。

So we're going to assume this basically for analysis。It's not really that big a deal。

 You could also analyze with these model， with these models。 The point is。

 they're all the same within a constant factor。For the algorithms we care about。

What other fun things？嗯。If you have a good cachelivious algorithm。

It's good simultaneously for all B and M。 That's the consequence。 So whereas B trees， for example。

 need to know B， they need to have the right branching factor for your value of B。

 the cachelivious model， you have to have。Your data structure has to be correct。

 has to be optimized for all values of B and all values of M simultaneously。

So this has some nice side effects。 in particular。 You essentially adapt。

To different values of B and M。So two nice side effects of that are that if you have a multi levell memory hierarchy。

 each level has its own B and its own M。If you use a cache oblivious algorithm and all of those caches are maintained automatically using one of these strategies。

 then youre， you optimize the number， maybe a should drop picture。

Here's a multi level memory hierarchy。You optimize the number of memory transfers between these two levels。

 You optimize the number of memory transfers between these two levels。

 between these two levels and between these two levels。So you optimize everything。

Each of them has some， if you just think of this as one big level versus this level or versus all the levels to the right。

Then。That has some B and some M。And you're minimizing the number of memory transfers here。Also。

 if B andM are changing。Lots of situations for that。

 If you have multiple programs running at the same time。

 then your cache size might effectively go down if they're really running in parallel on the same computer or you know pseudo parallelel。

Block size could change if you， if you think of a disk and the block size is the size of your track。

 then closer to the center。 the tracks are smaller， closer to the rim of the disk。

 than blocks are bigger。 All these things basically in the cachehe bous model。

 you don't care because it will just work。 Now， there's no nice formalization of adapting to changing values of B andM。

 that's kind of an open problem to make explicit what that means。

 But we're gonna assume in the cachehelubius models， you have one fixed B。

 one fixed am analyze relative to that。 But， of course。

 that analysis applies simultaneously to all B's in all Ms。And so， it's。

Feels like you adapt optimally， whatever that means。In some sense。

 the most surprising thing about the Cash oblivious world is that it's possible。

You can work in this model and get basically all of these results。So， let me。Go over these results。

So in the cachehelivius model， of course， Scans are still good。 Scans didn't assume anything about。

Block size here， except in the analysis。 scanning was just a sequential read。 In general。

 we can do a constant number of scans。 some of them could go to left。

 Some of them could go to the right， Some of them could be reading。 Some of them could be writing。

 It's a general form of scan， but all of that will take。啊。Order N over B ceiling。Okay， search。

This is what we're going to be focused on today。You can achieve。Order log。Base B plus one of n。嗯。

Search。Insert and delete。Just like bee trees， but without knowing what bee is。That's。

We're going to do most of that today。What's next sorting， sorting， you can do the same bound。

That we'll see you next lecture。啊。Okay， next is permuting。Here there's actually negative result。

Which is that you can't do them in。Doesn't say exactly what you can do。In particular。

 we can certainly permute as quickly as we can sort and we can permute。

 and we can achieve this min in a weird sense and that we can achieve n and we can achieve the sorting bound for permutation in the Kehelus model。

 either one， but not both with the min around them。Because to know which one to apply。

 you need to know how B And M relate to N。 and you don't hear。

And so there's a lower bound saying you really can't achieve this bound even up to constant factors。

But it doesn't explicitly say anything more than that。So a little awkward。 Fortunately。

 permuting isn't that big a deal。 I mean， and most of the time， sorting。

 the sorting bound is much faster than the end bound， so。Not so bad， but。Slightly awkward。

 So there' are some things you can't do。 This is the first negative result on cachesh bovius。

 Another fun negative result is actually in search trees。😊，You might ask。

 what is this constant factor here。In front of the log base B plus1 of n。And for bee trees。

 the constant factor is essentially one。You get one times log base B plus1 of n。

For in cashshmblivious world， you can prove that one is unattainable。

And the best possible constant factor is log base 2 of E， which is like 1。44。

So that is the right answer for the constant。 Today， we'll get a constant here of4。

This is for search。um。And， but4 has since been improved to 1。44。

 So a tiny separation there between cache oblivious and external memory。Let's see。One more thing。

Continuing over here。Is priority Q。Cash oblivious。We can do in the optimal time1 over B。

 log base M over B。Of an overby。Only catch with that result。

Is it assume something called the tall cash assumption？See what I want to say here。

We'll say M is omega。B to the one plus epsilon。Ugly looking omega。Certainly， M is at least B。

 If you have one cache line， you have M equal B， but we'd like M to be a little bit bigger than B。

 We'd like the cache to be。If M was at least B squared。

 then the height of the cache would be at least as big as the width of the cache。 hence tall cache。

 But we don't even need M equal to B squared。 just M to the M equaling B to the 1。

001 would be enough。Then you can get this result。 And there's a theorem that shows for cashsh oblivious。

 you need to assume this tall cash assumption。 you cannot achieve this bound without it。

But this is usually true for real caches。Life is good。Alright。

 so that's a quick summary of cache obli Ca oblivious results。 Any questions about those。

Then we proceed。Into achieving。Search trees。Log based， B event performance。

Cash obliviously make it interesting because external memory。

 we already know how to do it with a bee tree。So we did both models。Cashlious beries。What remain。

This was the first result in cache bolivious data structures。

 I'm going to start with the static problem。Cash oblivious。Static。Search trees。

This was actually in an M thesis here by Harold Prokup。So the idea is very simple。

 we can't use bee trees because we don't know what bee is。 So we'll use binary search trees。

 Those are those have served us well so far。 So let's use a balanced binary search tree。

 because it's static， might as well assume it's a perfect。😊，Balanced binary search tree。

So let me draw one over here。T my favorite one。So how do you search in a binary search tree。

 binary search， that's our algorithm is fixed。 So the only flexibility we have。😊。

Is how we map this tree into。The sequential order in memory， Memory is an array。

So for each of these nodes， we get to assign some order， some position in the。Aray of memory。

 And then each of these child pointers。You know， that changes where they point to。And now。

 cacheliously， you're going to follow some route to leaf path。 You don't know which one， all of them。

You are visiting nodes in some order， which looks pretty random。In the array of memory。

 it's going be。Some sequence。Like this。We would like the number of blocks that contain those nodes to be relatively small。

Somehow。So that's the， the hard part。 Now， you could try level order。 You could try in order。

 pre order， post order， all of them fail。The right order is something called venom de Bois order。

So let's say there are end nodes。What we're going to do。Is。Carve。The tree。

At the middle level of edges。So。We have a tree like this。We divide in the middle。

Leavs us with the top part and many bottom parts。There's going to be about square root of n nodes in each of these little triangles。

Roughly speaking， the number of triangles down here is's going to be roughly square root of n。

I think I have written here square of n plus1。It's a little overestimate because I multiply those two numbers bigger than n。

 But it's roughly that if there were root n nodes up here。

 then there would be squared of n plus1 children and square root of n plus one things down there。

So roughly root in different things， each of size root in。Then what we do。Is recursively。Layout。

Those triangles。Of size， roughly square root of n。And then concatenate。So a very simple idea。

Let's apply it to this tree。So I split in the middle level。

And I recursively lay out this triangle up here。Try it as a box。 then I recursfully lay out this。

 then I recursfully lay out this and this。And this。So up here， I mean， to lay this out， I would。

 again， split in the middle level。Lay out this piece。 So this guy will go first。 then this one。

 then this one。 Now I'll go over and recursively do this。 So it's 4，5，6。 Then this one，7，8，9。1011，12。

1314。15， that is the order in which I store the nodes。 I claim that's a good order。For example。

 if B equals 3， this looks really good because then all of these nodes are in the first block。

And so I get to choose which of these four children trees I should look at。 I mean。

 I happen to go here and then maybe here。 I look at those two nodes， look at their keys。To decide。

 oh， I should go into this branch。But I only pay one memory transfer to look at all those notes。

 So this， this looks like a tree tuned for B equals 3。

 But it turns out it's tuned for all bees simultaneously。 If we do this recursively。

 It's hard to draw a small example where you see all the bees at once， but。That's the best I can do。

Okay。Let's analyze this thing。So the order of the nodes is clear。 I didn't。

 if you know what the van de Duvo's data structure is。

 then you know why it's called this Otherwise C lecture。10 or so in the future。

 Forge exactly when we're covering it。 So this order was not invented by venom Dubos。

 but it looks very similar to something else that was invented by ven and debo。 So we call it that。

But that's the name of a guy。Peter。So analysis。Order is clear， the algorithm is clear。

 we follow our route to leaf path。What the what we claim is any route to leave path。

An any value of B。The number of blocks visited is order log base， B of N。So to do that。呃。

In the analysis， we get to know what B is。 The algorithm didn't know algorithm here doesn't know B and doesn't need to know B。

 But to analyze it to prove an order log base B of N bound， we need to know。What B is， So the。

 while the algorithm recurs all the way down。You know。

 keeps dividing these blocks into smaller and smaller pieces。

We're going to look at a particular level of that recursion。I'm gonna to call that a level of detail。

See， I call it straddling。B。So my picture is going to be following。

These little triangles are going to be sized less than B。

But these big triangles are size greater than or equal to B。 at some level of recursion。

 that happens。Bym honestnicity， I guess。I could keep going。 I mean， this。

 this tree is going to have substantial height。 I don't really know how tall is。

 or that's sort of the， the central question。D on some pointers here。

But just look at if you subdivide， you subdivide all the big triangles till you get to the small triangles that are sized less than B。

 then you stop recursing。Now， the algorithm， of course， will recursively lay that out。

 But what we know， we recursively lay things out， and then we concatenate。

 meaning the recursive layouts remain consecutive。We never like， interleave to lands。So whatever。

 however this thing is laid out， we don't really care because at that point， it's gonna。

 it's going be stored in in memory， which is this giant array。

 It's going to be stored in interval of size less than B。So at worst， where are the block boundaries。

 Maybe draw that in red。Block boundaries are size B。 We don't know exactly how they're interspersed。

 Let's say these are the block boundaries。So it could be that this little subart falls into two blocks。

 but at most two blocks。So the little triangles of size less than B。诶。Live。😔。

And at most two memory blocks。So to visit each of these little triangles。

 we only spend two memory reads at most。So all that' left is to count how many of those little triangles do we need to visit。

On a route to leave path。I claim that's going be log based B of N。So I guess the first question is。

 what is the height。Of one of these little triangles。Well， we start with a tree of height log n。

 Then we divide it in half， and we divide it in half and divide it in half。

 and we take floors and ceilings， whatever。 But we repeatedly divide the height in half。

And we stop whenever the number of nodes in there is lesson than B。

So that means the height is going to be theta。Log B。In fact， it's gonna be in the range half log B。

To log B。Probably with a p that way， it doesn't really matter。

Becauseuse you stop as soon as you hit B， you're dividing the height in half repeatedly。

 you might overshoot by it most a half。啊。That's it。So height is log B。

So if we look along a root to leaf path， each of these things we visit， we make vertical progress。

 log B， Theta log B。And so， and our total， the total vertical progress we need to make is log N。

And so it's log N divided by log B。So。I guess， four times log n。2。Well， I can over log B。诶。Triangles。

We reach a leaf。It's total progress we need to make， divided by half log B here。

Progress per triangle。 So we get two times log base B of N。 And then for each triangle。

 we have to pay two memory reads。 So this implies。At most4。Log。Base B of N。Memory transfers。

And I implicitly assumed here that B is not one。 Otherwise need a B plus one here。But。

To be interesting。As long as B isn't1， then it doesn't matter。Okay。

 so that's the log based B of event analysis。 actually really simple。Essentially。

 what we're doing here is binary searching on B。In a kind of weird way。

 using divide and conquer and recursion。 we say， well， maybe B is the whole thing。

 Then we're B equals n， Then we don't have to do anything。 Everything fits in a block。 Otherwise。

 let's imagine dividing here and supposing B is root n。And then we need two accesses。So。

 but that might， be might be smaller than that。 So we keep recursing， keep going down。

 And because we're dividing in half each time。啊。Sorry， we're not binary searching on B。

 We're binary searching on log B。Which is the， the height of a tree with B nodes in it。

And that's exactly what we can afford to do。 And conveniently。

 it's all we need to do because to get log based B of N， correct。

 you only need to get log B correct up to a constant factor。All right。Now， that was the easy part。

 Now， the hard part is to make this dynamic。 This is where things get exciting。

 This works fine for a static tree。😊，And this is how we do search。

 but we can't do inserts and deletes in this world easily。啊。But I'll show you how to do it。

 nonetheless。So this is what's called a Cash oblivious bee tree。We're gonna do it in five steps。

And the first step， unfortunately， is delayed till next lecture。 So I need a black box。 It's。

A very useful black box。It's actually one that we've briefly talked about before。Think think。

 in the time travel。If you remember in time travel。

 there is one black box which we still haven't seen， but we will do finally next lecture。

 which was you can insert into a linked list and then given two items in the linked list。

 know which one comes before which other one in constant time per operation。

Closely related to that is a slightly different problem called order file maintenance。

 Here's what it lets you do。You can store n elements， and words。In specified order。

 So just like it's a linked list。But we don't store it as a linkedist。

 We want to store it in an array。Of linear size。So there's going be some blank spots in this array。

 got n items， an array of size， say 2 n or 1。1 times n， anything like that will suffice。

The rest we call them gaps， items， gaps in the array。And。The updates。

 So this is physically what you have to do。 And you can change this set of elements by either deleting an element。

Or inserting an element between two given elements。So this is just like the linked list updates。

 I can delete somebody from a linked list。 I can insert a new item in between two given items in the linked list。

 But now it has to be stored in a physical array of linear size。Now。

 we can't do this in constant time。But what we can do。Is。Move。Elements around。Okay。

 so we me draw a picture。So maybe we have some elements here。They could be in sorted order。Maybe not。

There's some gaps in between。I didn't mention it， but all gaps。Willll have constant size。

So this will be constant size gaps。And we're able to do things like insert a new item right after 42。

 let's say we insert 12。 So 12， we could put it right here。 No problem。

 Now maybe we say insert right after 42， the number 6 and we're like oh there's no room to put 6。

 So I've got to move some stuff， maybe I move 12 over here。And then I put6 here。And then I say， okay。

 now insert right after 42， the number 7。And like， oh， I could， you know。

 move these guys over one and then put 7 here and so on。Okay。

 obvious algorithm to do this takes linear time， linear number of shifts in order to do one insertion claim is you can do an insertion in log squared amortized。

Its pretty cool。 So a worst case， you still going need to shift everybody。

 but it just leaving constant size gaps will be enough to reduce amortized cost of log squared。

 And this is conjectured optimal， though no one knows how to prove that。😊。

And that's order file maintenance。 Now， we're gonna assume this is a black box。 Next lecture。

 will actually do it。But， I wna。First， show you how。It lets us get cachebuvious， dynamic beries。

Log base B of N， insertt Dele search。Okay。We're gonna use an ordered file maintenance data structure。

To store our keys。Just stick them in there。Now， this is good。 It's， well。

 it's really not good for anything。Although it's a starting point。

 it can do updates in log squared of N。 I didn't mention this part in a constant number of scans。 So。

 I mean， think of these as this kind of shift。 I have to scan left to right to copy 23 to left。

42 to the left and so on。Scans are things we know how to do fast and then over B memory transfers。

 So if you're gonna do log squared， if you're gonna update an interval。

 log squared guys using constant number of scans， this will take log squared and over B。😊。

Memory transfers。Not quite the boundary we want。 We want log base B of N。

 This could be larger or smaller than log base P of N。 It depends how big B is。

But don't worry about it for now。 Okay， this is still。

 at least as polylogue updatesates are kind of vaguely decent。 We'll improve them later。

On the other hand， search is really bad here。 I mean， we're maintaining the items。

 I didn't mention it， but we will actually maintain the items in sorted order。So， yeah。

 you could do a binary search， but binary search is really bad cash up obviously。

 because most of the time you'll be visiting a new block until the very end。 So binary search fun。

 Exerse is log n minus log B。 We want log N divided by log B。😊，So we can't use binary search。

 We need to use fanom devos layouts， this thing。So we just plug these together。

 We take this static search tree and stick it on top of an order file maintenance data structure。So。

Here we have a van M de Boas La static tree。On top of。This thing。Which is an ordered file。

And we have cross linking。Really， here we have gaps。And we don't store the gaps up here。 Do we， Yeah。

 sure we do。 actually， Store them all。 Put it all in there。Alright。

 I'm gonna I have an actual example here。Do it。3。Flash。7，9。Slash。16。21。42。Okay， cross linkss between。

 I won't draw all of them。 Then we store all the data up here as well。

And then the internal nodes here are going store the max of the whole subte。 So here， the max is 3。

The max is 9，16。42。42，42。And9。 if I store the max of every subree， then at the root。

 if I want to decide， should I go left or should I go right， I look at the max of the left subte。

And that will let me know whether I should go left or， right。Okay， so now I can do binary search。

I do a search up here。I want to do a search。 I search for the tree。

 This thing is laid out just like this。 And so we pay log based B of N。To search up here。Plus one。

 if you want。And then， you know， we get to a key， and we're done。That's all we wanted to know。

 So search is now good。Updates。2 problems。 One is that update。

 updating just this bottom part is slow。 Second problem is if I change the bottom thing。

 and now I have to change the top thing。But let's look at that part。 First。

 it's not so bad to update。 I mean， this structure stays the same unless n doubles。

 then we rebuild the entire data structure。But basically， this thing will stay the same。

 It's just data is moving around in this array。 And we have to update which values are stored in this structure。

 The layout of the structure， the order of the nodes that all stays fixed。

 It's just the numbers that are being written in here that change。 That's the cool idea。So。

That's step 2。Let's go to step 3。Actually kind of wanted that exact picture。It's okay。

I need to specify how updates are done。So let's say I want to do an insertion。啊。

If I want to insert a new key， I search for the key。 Maybe it fits between 7 and 9。

Then I do the insertion in the bottom structure in the ordered file。 So maybe you wantan to insert 8。

These guys move around。 Then I have to propagate that information up here。

And the key thing I want to specify。ItAn update does a search。Then it updates the ordered file。

That's going to take log squared N over B。And then we want to propagate changes into the tree。

In post order。Post order traversal。That's the one thing I wanted to specify。

Is it actually matters in what order I update the item。 So， for example。

 suppose I move 9 to the right， and then I insert 8 here。Okay， so then this thing。Let's do。

Let's do it。What changes in that situation。Well，9 will change to an8 here。

Which means this value has to change to an eight。And this value needs to change to an eight。

And this 1， I can't quite update yetcause I need to do the other tree。

 So here I change this value to。9。This one doesn't change that this is okay。 This is okay。

 This is okay， Just taking the maxes as I propagate up。 So the red things are the nose that I touch。

And compute the maxes of。 And the order I did it was a post order traversal。

 I need to do a post order traversal in order to compute maxes。

 I need to know what the max of this subree is。 And this subree before I can recompute the max of the root。

Okay， so it's really the obvious algorithm。Now， the interesting part is the analysis。

So searches we've already analyzed， we don't need to do anything there。 That's log base B of N。

But updates are the interesting thing to analyze。And again， we're gonna look at the level of detail。

That straddles B。Just like before。So let me draw a bit of the picture。

It draw it slightly differently from how I drew it last time。Okay。

What I'm drawing here is actually the bottom of the tree。So real tree is like this。

And I'm looking at the， the bottom where the leaves are。

 because I care about where I connect to this ordered file。So there's the order file at the bottom。

I'm refining each triangle recursively until their size less than B。

 They're gonna be somewhere between root B and B。These big triangles are bigger than B。Okay。

So that's where I want to look at things now。When we update in the ordered file。

 we have this theorem that says you move elements in an interval。So what changes is an interval。

 The interval has size log squared n amortized。So。We are updating。Some interval here。

Maybe something like this。啊。And so all of these items。Change。

 let's say now this is not going to be too big。 It's going to be log squared and amortized。

And all of the ancestors of those nodes have to be updated， potentially。 So， you know。

 all these guys are gonna get updated。 Some of this tree， this path。

All of this stuff is gonna get updated。 Some of this stuff。A path up to here。

 the path up to the root gets updated。In general。And all these nodes get touched。 Now。

 I claim that's cheap。No more expensive than updating down here。Let's prove that。Oh。Okay， first。

 first issue is， in what order do I visit the nodes here， I want to think about this post order。Okay。

 maybe I'll use another colour。Draw that。 So let's say we， we start by updating this guy。 You know。

 we update this value。And we go， I'm doing a post order traversal， this tree。

 And Once I've completely finished here， I can go up here， maybe go up here。

 visit over down this way。 you know， fix all the maxes here。 Then I go up， fix all the maxes here。

 fix all the maxes。 all the maxes。 When I come up， then I can fix the maxes up here。And so on。Okay。

 think about that order。If you just look at the big picture。

 which is which triangles are we visiting， what we do is we visit this triangle then this one。

 then this triangle then this one， then this triangle than this one。

 Or I guess this is the better picture。 This triangle， left one， this triangle， next child。

 this child this parent， next child， this parent， next child。 and when we're done。

 we go over to the next big triangle。Within a big triangle， we alternate between two small triangles。

As long as we have。four。Blocks of cash。This will be good because there's at most two blocks for this little triangle。

 at most two blocks for this little triangle。 So jumping back and forth between these is basically free。

So we assume。诶。chalk。Weume that we have at least four blocks of cash。Post order traversal。

Altternates。Between。Two little triangles。So this part is free。

The only issue is how many little triangles do I need to load overall？Sorry。

 I should say I'm just looking at the little triangles in the bottom two levels。So， this is。

In the bottom。2。Levels。Sorry， I forgot to mention that。

 As soon as you go outside the bottom big triangle， let's not think about it yet。

 worry about that later。So just looking at these bottom two levels that I drew。

 we're just doing this alternation。 And so the only issue is how many little triangles do I have to load。

So there could be these guys。 these guys are good because I'm touching all these nodes。

 and the number of nodes here was bigger than B。 So if I take。

 if I take this divided by B and the ceiling， the ceiling doesn't hurt me because the overall size is bigger than B。

These guys have to be a little bit careful here。 The ceiling might hurt me。

Cause I only read some of these nodes。 It may be much less than B could be as low as root B。

 I divide by B， Take the ceiling。 I get one。 So I I have to pay one at the beginning， one at the end。

 But in between， I get to divide by B。So the number of little triangles in the bottom two levels。

I visit。Is going to be。啊。1 plus。Log squared n over B。

Amortized the size of the interval is log squared an amortized。So this would be， if I'm perfect。

 but I get a plus one because of various ceilings。Okay， and also。The number of blocks。It's the same。

Because each of these little triangles fits inside。Atmost two blocks。

 So it's going to be order  one plus log squared n over B。Okay。

 so I can touch all those in this much time。Because。

 because I assume that I can alternate between two of them quickly。

 And once I finish one of these leaf blocks， I never touch it again。

So I visit each block essentially only once。As long as my cache is this big。Cool。

 so that deals with the bottom two levels。 I claim the bottom two levels would only pay this。

 And we were already paying that to update the ordered file。 So it's no worse。Now。

 there's the levels above this one that we can't see。I claim that's also okay。So。

 also need number of memory transfers。Its equal to that。Okay， one more part of the argument。Levels。

Above。The bottom two。Nice thing about the levels above the bottom， too， is there。

There aren't too many triangles。Allright， well， there aren't too many nodes involved， let's see。So。

Down here， there were like log squared n nodes。 There was a lot of nodes。 Once I go up to levels。

 how many of these nodes can there be。These roots。Well， there's log squared n。At the bottom。

And then I basically shaved off a factor of B here。

So the number of these roots is at most log squared n over B。That's small。For those nodes。

 I could afford to do an entire memory transfer for every single node。😊，so the number of nodes。

That need updating。Those are ancestors。Is order log squared n over B。It's not。

 That's the number of leaves down there。 At some point。

 you reach the common ancestor of those leaves。 That tree。 So here， draw a big picture。

Here's the bottom。Bottom  two layers。Okay， we're shaving that off。Up here。The number of nodes here。

These roots that we're dealing with， that's the log squared n over B。

 So if we build a tree on those nodes。Total number of nodes in here is only log squared n over B times 2 because it's a binary tree。

But then there's also these nodes up here， this is potentially log n nodes。

So a number of nodes that need updating is at most this plus log n。Okay。

 so what's going on here is we， we first analyze the bottom two layers。

That was okay because of this ordering thing that we're only dealing with two triangles at once。

 And therefore， we pay this size divided by B， which log squared n over B。

 Then there's the common ancestors of those nodes。 That forms a little binary tree of size log squared n over B for these nodes。

 this triangle up here。 We can afford to pay one memory transfer for every node。 So we don't care。

That we're efficient up here。Which is， I mean， we may， well， let's not worry about whether we are。

 It doesn't matter。 So for these nodes。We pay at most one。Memory transfer。Each。

And so we pay at most logs word N over B for them。 That's fineca we're already paid that much。

And finally， we have to pay for these nodes up here。

 which are from the common ancestors of these guys up to the root that has linked that most loggan could be smaller if this was a big interval。

So， for these guys。We pay。Order log base B of N。Why， because this is a route to node path。

And this is the static analysis。 We already know that following a route to node path would pay at most log based B of N。

Okay， so we pay log base B of n plus log squared n over B for this。

 plus log squared n over B for this。 plus log squared n over B for the。Ordered file。

So overall running time。For an update is log base B of n plus log squared n over B。

We actually paid log basease B of n twice once to do the initial search。

 I said an update first has to search for the key， then it updates the ordered file。

 That's where we've paid the first log squared N over B。

 And then to propagate all the changes up the tree， we pay log squared n over B for this part。

 and then another log base B of n for this part。ho。Okay， that's updates with a bad bound。

But we're all， turns out we're almost there。 So at this point。

 we have searches optimal because searches are just searching through the Ven andbo thing。

 That's log base B of N updatesates not quite optimal。I mean， for not quite matching a bee tree。

 I should say， this is not an optimal bound raming for we're just aimming for bee trees。

We'd want to get this bound， same as searching。But。We have this extra term。Which if B is small。

 for example， if B is log n。This term will be bigger than this one。

The break point is when B equals log n times log log n。Okay， so for small B， we're not happy。

 and we want to be happy for all B simultaneously， so。WWanna fix this。

We're gonna fix it using a general technique。Great technique。Called indirect。 So this is step 5。

In direction。Idea is simple。We want to use。Previous the previous data structure。On。

And over log n items。And what， we need to store n items。 So where do we put them on the bottom。

So I'm going to have theta log n nodes down here。Theate dial n nodes next to them。Or items。

 I should say。Theta log n。Okay， and then one of these items， let's say the min item。

Gets stored in this tree。 And the mint item over here gets stored in this tree。

 and min item here gets stored。Up here。Okay， so this item only this。

 the data structure we just developed。 I only store an over log n of the items。

The rest are that fit in between those items are stored down here。 Now I use standard B tree tricks。

 actually， if I， if I want to do an insertion， Okay， how do I do a search。

 I follow a root to leaf path and then I do a linear scan of this entire structure。

 How much does it cost to a linear scan of log n。 only log n divided by B。

 This is smaller than log base B n。 So the hard part of search is the log base B N。Okay。

 how do I do an update？ Well， first， I search for the guy。

 I find the block that it fits into and just insert into that block。Done。

As long as this block doesn't get too big， I can do， how do I insert into a block。

 rewrite the entire block。It's a scan。 It only takes log n over B。

 So it's basically free to rewrite this entire block every time as long as it has size at most log n。

 If it gets too full， then I split it into two parts that are half full。So in general。

 each of these blocks will maintain the size to be between one quarter log n。And log n。

So if it gets bigger than log n， I split it into two guys of half size。

 If it gets smaller than one quarter log n， then I'll merge it with one of its neighbors。

 and then possibly have to resp it。or you could think of it as stealing neighbors from your siblings。

 that's what you do in bee trees。 But with a constant number of mergers and splits。

 you can restore the property that these guys have sizes very close to half log n。

 And so if they end up getting full， that means you inserted half log n items。

 If they ended up getting empty。 that means you delete half you deleted a quarter log n items。

And so in both cases， you have log n items to charge to whenever you do an update up here。

 So we update up here every time we split or merge down here because this guy has to keep track of the blocks。

 not the items in the blocks。So we effectively slow down。The updates in the top structure。

By a factor。Login。Which is great because we take this update bound， and we get to divide it by log n。

 which removes this square。😊，So， we end up with。So we used to have log base B of n plus log squared n over B。

 we get to divide that by log n。Plus， we have to do。This part at the bottom。

plus we're doing log n over B at the bottom。So basically， this log cancels with this square。

 So this is log n over B as well。 This thing gets even smaller。

So we end up being able to do updates in log n over B。Not really， though， because at the beginning。

 we have to do a search to figure out where to do the update。 If you knew where you had to update。

 you could update a little faster。 You could update and log n divide it by B。

 If you don't know where to update， you've got to spend logways B of N to search initially。Okay。

 but that's the fun part。 We can now update this data structure fast。

 just with this one layer of indirect with log n chunks at the bottom。

 speeding this data structure up by a factor of log n for updates。 searches don't take any longer。

 Just pay this extra term， which is already much smaller than what we were paying before。😊，Oh。

That's cachelvious be trees， other than ordered file maintenance， which we'll do next class。



![](img/10ac5475d606569f928e958b4ce9c10d_3.png)