# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p48 p47_07_encapsulation-and-abstraction -BV1tAygYoEj5_p48-

![](img/9f9222fbc549184fa5ce630602b7122f_0.png)

Hi there。 In this session， we will discuss about encapsulation and abstraction in Java。😊。

With the help of implementational scenarios。

![](img/9f9222fbc549184fa5ce630602b7122f_2.png)

Basically， encapsulation is defined as data hiding or wrapping up the data under a single unit。

If you。Ask in a one word what is encapsulation so encapsulation is data hide。

It prevents outer classes from accessing and changing the fields and methods of a class。

 That's what we use access modifiers to encapsulate the data。

 As I told you if we have a class if we make our class members public。

 this will be accessible outside the class， but if you want to hide the data。

 you can keep it most restricted private， then you can extend it as per the need。

 you can keep it default or protected， but if you don't want to hide your data by any chance。

 keep it public in Java encapsulation help us to keep related fields and methods together which makes our code cleaner and easy to read as well。

 for example， the student class has the following members first name middle name。

 last name full name courses and the methods like save， subscribe and get subscribed courses。😊。

You can see here first name， middle name， last name and full name are the data members courses is also a data member。

And save， describe， subscribe， get subscribed courses or methods。

So if you want these data members to be used inside the methods or member functions。

 you can keep them private so that you can hide it from accessing outside the class definition。 Next。

 we have abstraction。 abstracttraction is to abstract the members of a class。

As a essential details to be exhibited to the user。

Can be achieved with the help of abstract classes or interfaces。

 Also with the help of access modifiers。 Ab class is a restricted class that cannot be used to create object。

The child class has to access the data members or member functions。

 which needs to be accessed from the parent class by its own object。

 an abstract method which does not have a body， So anyhow the child class needs to abstract the pro the abstract methods of a particular parent class into the child one so the body will be provided and inherited from considering a real example。

 the user only knows that in ATM machine first enter the ATM card。

 then enter the pin code of the ATM card and then whatever amount he or she wants to withdraw。

Will get the money， and the user does not know how the internal mechanism is there。

The user just know how to operate the ATM machine but don't know how internally it works。

 that is known as abstraction。So stay tuned to learn more about encapsulation and abstraction。

In the next demo， I'm going to talk about how getrs and setters helps in encapsulating the data and accessing the encapsulated data from outside the class definition see you in the next session。

🎼。

![](img/9f9222fbc549184fa5ce630602b7122f_4.png)

![](img/9f9222fbc549184fa5ce630602b7122f_5.png)