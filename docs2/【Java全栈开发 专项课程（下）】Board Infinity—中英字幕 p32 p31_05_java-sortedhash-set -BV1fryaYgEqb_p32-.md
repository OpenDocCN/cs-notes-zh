# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p32 p31_05_java-sortedhash-set -BV1fryaYgEqb_p32-

![](img/0047c0ad7c263bf994a711efbf80b3d5_0.png)

Hi there。 Today， In this session， I will talk about sorted set interface in Java and its methods with the help of examples。

 So let's get started。 Basically， the sorted set interface presence in the Java dot u package and implements the mathematical sets。

 This interface contains all the methods inherited from the set interface。😊。



![](img/0047c0ad7c263bf994a711efbf80b3d5_2.png)

And adds a feature that stores all the elements in this interface to be stored in a sorted manner。

Here set interface extends to the sorted set and then sorted set extends to the navigable set interface and implements into this three set class。

 So you need to use a three set class to it the sorted sorted set interface。

In order to use the functionalities of the sorted set interface。

 we need to use a three set class as I told you extreme bottom class is a three set。

 other other interfaces。The class， which implements the sorted set in the navigable set is a three set。

 which is an implementation of a sub self balancing tree。 Therefore。

 this interface provides us with a way to navigate through the tree itself。

These are the some common methods to be used， such as the comparator first headset， last。

 splittraator and the subset。Let's get started to practically understand how does this sorted set with the implemented methods works in Java。



![](img/0047c0ad7c263bf994a711efbf80b3d5_4.png)

And， I'm creating a sorted。Set。Of type string。Generic type string。嗯。Then I'm going to say。

 said tot at。India。Seid do at Australia。Seid dot， add。South Africa。Said dot at Japan。P that。

I'm trying to add one more element， said dot at India one more time。Now， the moment I will。

Print my set。What I observe is。India is coming only once， first of all。

 it will remove the dlicacy and Australia is the first element and South Africa is the last。

 so you can see that the elements are sorted in ascending order。Now， let's try to it it。Itraator。

The string elements， Itraator。Seg dot ittraator。Whileole。Itraator dot has next， is there。

Just print the next value。So you can see that still the elements are sorted。

And duplicate values are removed。Post that you can use whatever methods。

 which I haven't used as of now， I can tell you here。For example。Just wanted to。User contains method。

Sis out。Whether the said dot contains。India or not。 So it will return either the true or false。

So you can see that it returns to you。If you would like to get the first value， just go and check。

 said Dot first。Wanted to check the last value， just go and check。Said do last。

So here the first value is Australia， and the last value is South Africa。Apart from that。

If you would like to remove any element， as you always know。

I check said dot remove will tell you whether the element is removed or not。

 You can try printing your element once again。If you would like to remove all the elements。

 just write set dot remove all。And all the elements gets removed from the set。

So both are returning through because one element is deleted and removed and after that all elements are removed as well。

 so that's how your sorted set works in Java still a couple of more set implementations are left。

 so stay tuned to learn more about them see you in the next session。



![](img/0047c0ad7c263bf994a711efbf80b3d5_6.png)

。

![](img/0047c0ad7c263bf994a711efbf80b3d5_8.png)