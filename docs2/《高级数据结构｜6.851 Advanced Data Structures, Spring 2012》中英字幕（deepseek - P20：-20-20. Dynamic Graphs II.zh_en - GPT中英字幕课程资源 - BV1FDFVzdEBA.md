# 《高级数据结构｜6.851 Advanced Data Structures, Spring 2012》中英字幕（deepseek - P20：-20-20. Dynamic Graphs II.zh_en - GPT中英字幕课程资源 - BV1FDFVzdEBA

The following content is provided under a creative Commons license。

 Your support will help M I T Open Coseware continue to offer high quality educational resources for free。

To make a donation or view additional materials from hundreds of MI T courses。

 visit Mi T OpenCourseware at O C W dot M I T dot E Du。



![](img/7a6712daca2225696d0bad091f659505_1.png)

![](img/7a6712daca2225696d0bad091f659505_2.png)

Today we continue our theme of dynamic graphs， this is the second of three lectures。

 and this will be the main lecture about upper bounds for general graphs。

 Last class was about link countries and we saw essentially how to solve a problem called dynamic connectivity。

Which is where you can insert into delete edges and you want to know what's connected to what。

For trees， we solved it， we're going to solve it again for trees in an easier way because we need a slightly simpler data structure we can augment in different ways called order tour trees and then we'll solve trees yet again in a setting where you can only delete edges。

 then we can get from log n time to constant time。So we'll still talk about trees for quite a while。

 but then we'll finally get to general graphs and we'll do a log squared end solution for general graphs so just another log factor more you can generalize from trees to general to undirected graphs and then I'll give you a flavor of what's out there。

 dynamic graphs is a big worlds as lots of results and I'll give you some idea of what other problems have been studied in dynamic graphs。

 both undirected and directed， for directed graphs things get pretty slow。And yeah。

 next class will be about lower bounds， which will prove that this log stuff is actually necessary。

In case you were wondering because a lot of the time we've been able to get better than log in this class in dynamic graphs。

 you can't。 They' are log lower bounds。So let's start out with definition of the problem。

We'll be focusing today， mostly on this dynamic connectivity problem。So。Maintain an undirected graph。

Subject to。Adge insertion and deletion。And can also insert and delete degree0 vertices。

We're not really going to worry about vertices。 it's easy to add vertices that have no edges to them。

 and also easy to delete them。Be more about the adding and removing edges incident to those vertices。

And then the query， connectivity query。It's given to vertices。Is there。A V to W path。

So this is asking are two vertices V andW in the same connected component？

That's a kind of strong kind of query。 In some sense。

 a weaker kind of query is just to know globally is the graph connected。

That's another interpretation。Of dynamic connectivity。

 It turns out both of these problems are interesting and useful for solving problems。

 but they also turn out to be more or less equivalent。

 All the upper bounds we know for one apply to the other and vice versa。

 So doesn't seem to make much difference。 Which kind of query you want。 So might as will take both。

We'll focus on the VW query。Okay。So that's the problem。In general， and I want to。

 just to introduce some terminology。Kind of like when we had partially in full persistence。

 there's also partial and full。Dynamicness。So fully dynamic data structure is one where you can do inserts and deletes。

Of edges。And a partially dynamic data structure。Is one where you can do inserts or deletes。诶。So。

You can only do inserts or you can only do deletes throughout the lifetime of the data structure。

And these have specific namescause usually use different data structures for just insertions or just deletions。

 Just insertions is called incremental。And just deletions is called decremental。

idea of incremental algorithms is definitely not a new one， but in dynamic graphs。

 it always makes sense。 So in general， dynamic graph problem is defined usually by insertion and deletion of edges。

And either fully dynamic or partially dynamic。 And then the query is what can vary。

 We're going to focus on connectivity queries， but you could do others。

 So this explains for decremental， we're going to solve trees in constant time for fully dynamic。

 We're going to solve general graphs in log squared end time。Okay， before I cover those。

 that's gonna be， well I guess these three are。Sor of the meat of the lecture。

 But before I go into that， I wan to tell you where this dynamic connectivity results fit into the literature in general。

On dynamic connectivity。So。This is part of our survey。But just on dynamic connectivity。Well。

For trees， this is the best we know。 I mean， this is the best possible。 really。

 you can do log n in general， and then decremental。You can do constant。

We actually already know how to do trees with link cut trees。 not totally obvious， but you can do。

 you can use links to simulate edge insertions and you can use cuts to simulate edge deletions。

 It's not totally obvious because the。The link operation requires that one of the vertices is the root of its tree。

So。Not so trivial， but we'll see an easy way to do that in order two or trees。Later on。

So this we kind of already know how to do， But we're gonna see another simpler way。

The constant time amortized decremental we're going to do today。So we're also gonna do this today。

Incremental， I don't think it's known how to do constant time。

 but I'll tell you another result it can get almost constant time。

Another special type of graph you could consider is a plain graph。

 This is a graph with the planar embedding。 So as you're inserting edges or adding vertices and whatnot。

 you say which phase they live in。 And so you have a fixed planar embedding。

 It's being constructed as you go along。 These can be are easy to maintain。 you can get order log in。

 I guess in some sense， that generalizes trees because they also have pretty obvious planar embedding。

 So that's。Something you can do。In log n， but the big question is for general graphs。

Can you do log n preparation？That's an open problem。

Al be the central open problem in dynamic connectivity。But we， we're not so far away from that。

 as I said， you can get log squared。 Let me say what the log squared result is a little bit better than just log squared。

 You get log squared update。But query is actually subloarithmic。Get a。Log n over log log n query。And。

Another result is you can do a little bit better than log squared。 You can do log times。Log。

 log and cubed。Update， so roughly log n update， but there's some polylo log。Term。

And query slows down very slightly。 It becomes log n over log， log。Login。

Thatll sort of make sense why。In a moment。 So these are sort the be。

 the lowest update time known is this one。 log times， log log cubed。

Big open question is whether you can get。Lock。For update。And any kind of reasonable query time。

I idealally， log for both。So that's for fully dynamic。 If I don't say any otherwise。

 fully dynamic is always the default。Results the case you usually care about。嗯。Cool。

 but if you wan to do incremental。You can achieve。An alpha bound。

 because incremental dynamic connectivity is really the union find problem。

And you find you have a bunch of sets of elements。 you can take two of them union them together once they're union。

 they can never be split apart and find is tell me what set I'm in。

 So if I do a fine on two vertices， I can find which connected components they're in and determine check whether they're the same that will give me connectivity query。

 insertion corresponds to merging two connected components unless they were already together。

And it known， you can get an alpha amortized bound。 And that's optimal for that problem。

 So I guess it's actually a theta。So that's sort of well known stuff。's it's a complicated analysis。

 but already been done。So in particular， that's， I believe the best way we know how to solve incremental connectivity in trees though。

Curious whether you can do constant incremental。Deremental， there's essentially a log n solution。

Not quite， but。For dense graphs， there's a login solution。

So I'm stating instead of a bound per operation， this is a total bound。 if you do。

 let's say you start with an M edge graph and you delete all the edges。

 then you pay log n for each edge， but you also have to pay this n poly log n。Cost， so F M is is。

A bit bigger than n than this dominates。 And so it's log n preparation。

 But if you have a sparse graph， it's still polylo per operation。So decremental。

 we can kind of get log for general graphs。Another goal you might have is what if I wanted to get worse case bounds。

 Everything I've said so far is amortized。 You you want to get worse case， Not much as known。

 Big open question is。Can I get。Polyloggue updating query。嗯。

So it's actually a pretty big breakthrough to get polylo whatsoever。

 This first result is like from 2001 or so after people had worked on dynamic graph for probably a decade。

 So it took quite a while to find this any kind of polylo that still hasn't been made worse case。

 So the best results are sort of the。State of the art at the time before the polylo came out。

 which is square root of N update。Constant query。um。And。One other result for worst case。

 which is if you want to do an incremental solution。 so you're just inserting edges。

 then there's a bound。 There's actually a whole trade off between updates and queries。If you have。

Updates that take over X time， Then you get queries that take log n over log X time。

So log base x of n。And that's optimal。 These are theta。 There's matching lower bounds。So， I mean。

 this is， this is， again， really union find。 And so union find has been well studied。

 even in the in the worst case setting， can't do as well as alpha， basically。Okay， then finally。

 wanna talk about lower bounds。So there are two complementary results。But maybe first。

 I'll summarize by saying， you need omega log n。Update or query。 One of them has to be， at least。

Login。To do dynamic connectivity。啊。As you see， queries can be sublogarithmic。

 We don't know how to make update subloarithmic and get any reasonable query time。But。

One of them has to be at least log， and here's the specific theorems that imply that。Yeah。

It's actually similar to this result， not quite the same。There's a log on the left hand side。

And these results hold for amortized。Solutions。hoops。Key words missing here。This is an update bound。

 and this is a query bound。So these are basically symmetric results。

 says if you have super logarithmic update。So you have some x bigger than one times log n update。

 then you get log n over log X query。 That's a lower bound。 If you have x super logarithmic query。

 So let's say it's x times larger than log n， then you get you need at least a log n over log X update time。

So this result is is the one sort of of relevance because we don't know any subloarithmic updates。

 but we do know subloarithmic que。 So this is sort the regime that we have essentially matching upper bounds。

Let me show you。This result。This result。And this result， even。Are in some sense。

 tight against this trade off， this update query trade off because if you look at。

 so this update is log n times log n。 So x is log n。 And over here， we get log n over log log n。

So that's tight against that here also we have here X is log log n cubed。

 So you take log of log log n cubed。 you get log log log n。

 So that's why there's a log log log n down here。 That's the best you could hope for given given that update time。

 that's the best query time you can get Sam over here。 given an update time of root N。The best， well。

 of course， best you can know for constant query。 But indeed that it works outca you're taking log n divided by half log n。

 you get constant。So these results are， I mean， they may sound suboptimal， but in a certain sense。

 they're optimal。 Of course， these are just single points on the trade off curve。

 We still really care about the situation of log n。 ors the open problem here。 Lo n update and query。

 That would still kind of be ideal。But these are not as bad as they look as the point。 Of course。

 it would be better if this wasn't a3。 It was only a one， but they're still， in a certain sense。

 tied against this， against the query bound that you get。Okay， another open problem here is。

 is whether this the reverse situation is ever relevant。 Can you， can you have。

A sub logarithmic update time and get any polylo query time。So thiss another open problem。呃。

Little O of log n query。That's sorry， little login update。And let's say polylogue query。I mean。

 the lower bound kind of predicts that something like this ought to exist。

 but it may be it's just impossible。 And there's a different reason。

 a different lower bound that makes it impossible。Okay。Some。Theta I wanted to add here。Okay。

Another fun thing about this lower bound is that it holds， even if your graphs are paths。😊。

So in particular， that tells us that this dynamic tree results， we covered last class， say。

 you need login。If you're going to do both operations in log N， that's optimal。

Even if your trees are paths， which is like the really easy case。

 And we're gonna prove this theorem next class。So if you want， that' would be our。

Our last class and our last lower bound。Next time， tune in next time。 But today。

 we're gonna focus on upper bounds， namely these two。And this one。Okay， so let's do them。

First one is， are there two trees？Which is another way to do the login。Found it's something we need。

We'll need it for doing the log squared solution。For a reason， we'll see in a moment。

So earlier two or trees go back to 1995， Heninger and King。They're simpler dynamic trees。

 simpler than Ly cut， even though they're more newer。And。

The key difference compared to link cut trees is that they let you do stuff with subtre of the tree instead of paths of the tree。

 Li cut trees are great。 We could compute the man or the max or the sum of a bunch of weights on any path that we cared about。

But for various reasons， we're gonna be able。 we want to do the same thing on subtes。

 And this turns out to be easier。So what do we do， are there two or trees？We take an order tour。

 Remember or tours from I remember lecture 15。I don't quite remember what that lecture was。

When do we do other tours。 doesn't matter。 Do you remember the idea of an oil tour is just walk around the tree do a depth for search and。

 and keep track of every time you visit a node。So you visit some nodes multiple times， in general。

 the degree of the node is the number of times you visit it。Every edge gets visited exactly twice。

 so it's a linear number of visits total。I just wantan to take that。Linear order visits。You。

 pullll it out straight， store that in a balanced binary search tree。 That's other two trees。

 They're very simple。So。Store the node visits。By the O tour。And a balanced spinr search tree。

What's the order， We're storing them in order by the， the order that the other tour visits。

The visits。 So this will be the leftmost， the men in the tree。 this will be next and the next。

 So if you do it in order traersal the tree， you should get the visits in this order。Okay， this is。

 of course， a way to balance a tree in some sense。 I drew a balanced tree as our thing we're trying to represent。

 But if， again， it could be a totally unbalanced thing。 when you do an eer tour。

 you get order and visits and you throw them into a balanced binary shirt tree。 It's balanced。

 Of course， doesn't really， it's， it's a bit of a weird thing because you're not really preserving the structure in a。

In an obvious way。 But there's one， one more thing we store。

 which will let us do essentially whatever we want。Each node。Stores。Two pointers into this structure。

To the first and last visit。So like this node has a pointer to its first visit and its last visit along the。

 along this structure doesn't keep track of any middle visits  because there could be a lot of those。

 We just want to have a constant number of pointers。

 This is gonna be a pointer machine data structure， just like link cut。Okay， so let's see。

How do we solve our operations， inserting theleting of edges and connectivity， Actually。

 I'll start with just phrasing it like link cut trees。

 We can do most of the link cut tree operations。 Remember， there was find route link and cut。

Where our main operations， then there was aggregation， those that will have to change。

But if we want to do find root。啊。So we're given one of these balanced binary search trees and it is the node V。

Well， actually， it's a little more。A little weirder here。

 but we have some node V in our tree and really we have our pointers into this this is a balanced binary search tree here。

We have pointers to the first and last visit in here。 I don't care。 Just take any visit。

 anything in here。 Let's say the first visit of V， then walk up the tree。

 then walk down the tree on the left。Left spine。That is the min。

And that's going to be the first visit of the root node。

And so this has a pointer in turn to the root node。Boom， you found the root of your tree。Again。

 we're maintaining forest of trees。 And so we just need to find which tree V is in。 walk up。

 walk down。 That's where to log n because this thing is balanced。And we found the root。收下试试。

Turs out the other operations are not too easy。If you can split and concatenate your tree。

So let's start with cut。a bigger diagram。So here's V。Here's the parent of V。 I'll call it W。

 Here's the edge and our goal。And a link cut operation or sorry。

 And a cut operation is to delete that edge。Separate this tree from that tree。Okay。

 so what I'm gonna， what I need to do is isolate this subt of V。But conveniently。

 if you think of the O tour， the Oer tour， you know， and comes does some stuff here。 Eventually。

 it follows this edge， Visits V for the first time here。Then if visits all the stuff down here。

 then if its V for the last time。AndThen it follows this edge and then does other stuff。

So the V subree is a contiguous interval of the order tour。 So we just cut it open， cut it apart。

So we split the BST。At the first and last visits to the。Which is exactly what we know。

We have a pointer from the。To its first and last visits in the order tour。 So split the tree there。

 split the tree there。 you know， split in a binary search tree， you're given a particular key value。

 let's say X， and you split into everything less than x and everything greater and equal to x。

 something like that。That's a split operation。 It can be done in log n time and pick your favorite balance binaryner search tree。

 red black trees， A VL trees。 they can all do this and maintain balance。 these two things。

 even though they might be very different sizes， they'll both be log height in their own relative to their own size。

Okay， this is obviously not quite enough because what we've done is cut。

 basically cut here and cut here。 Now we have three trees。

 We have the one we want for V V's sub tree that will correspond to or two of exactly that thing。

 So this is the represented tree I'm talking about。 And this is the balanced binary search tree。

But the rest of the tree is in two parts。 There's the parts the left of V part before we visited V。

 the part after we visited V。 those we just have to stick back together so we can catnate them。啊。

Yeah。So I'll call them the before V tree and the after V tree。

There is actually a tiny thing that has to happen here。 I just remember。

 which is we need to delete somewhere we need to delete one occurrence of W。Of the parent of the。

Cause it used to be， okay， we visit W。 Then we visit V， blah， blah， bla。 Then we visit W again。

 If we cut out this part， we're gonna have two visits to W in a row。 We really just want one visit。

 So this is a minor thing。 but we need to do a delete in there。So each of these is log n time。

 So the overall time is log n。 easy。 This is a cut and paste kind of argument， all pretty cheap。Okay。

 now let's do link。Concatetnate is a reverseers of split， by the way， you have two trees。

 All the things in the left are are smaller than all the things in the right。

 and you just want to join them together。Like a very restricted kind of merge。Okay， so join。

Recall is you have a node V。And you want to it， you want to make it a new child of W。

W might have other children。It lives in some bigger tree。We want to add V as a child of W。

 So we're assuming here V is a root for now。So what do we do。But sort of the same thing。

 We sort of know we want to put V in here somewhere。

 It actually doesn't really matter where we put it。 You know。

 we're not keeping track of the order of nodes in here。 So I'm going to simplify my life and say。

 let's put V at the end。 I want to make V the last child of W。Like this。Okay， so we。

 we can find the last child。 in some sense， that's the last after we visit the last child。

 we come back and do the last visit to W。So I'm going to look at the last visit of W。

 cut the tree open there and stick this part in。Cut the Euler tour。In half there。 So split。呃。W is3。

At W's last visit。诶。And then we do some mega concatenate。We're gonna concatenate。Basically。Before。

W's last visit。We're going to concatenate a single occurrence of W'ca there's a new occurrence of W。

 Now。 we do it before and after V。 So we add another W。 Then we add V's。Or their tour。These BST。

 And then we do after。Whatever used to be there after W's last visit。Lets say。After and including。

Including that W visit。Okay， so just the symmetric of this here we had to delete one occurrence with W here。

 we have to add one back in， but you just cut paste in the thing you need and rejoin everything。

 So again， very easy。 It's a constant number of log n time operations。

 If you can do split and concatenate in log n time。So。You see this this like， wow。

 why do we spend so much time with link cut trees。 This is really easy。

 But they have their different applications。 If you want to compute aggregates on paths。

 you need to use link cut trees。 the best we know。 This structure will not do aggregates on paths。

 but it will do aggregates on subtrees because subtes are represented by these intervals in the order tour trees。

 So if you have a weight on all the nodes in here， say。😊。

You can easily find this interval and do a range query there， say take the min of all those things。

 if you have subree mins， you can take the min of log n things and in log n time。

 compute the min in an interval or min of min over a subte rooted at a node or max or sum or whatever。

 So here you can do subt aggregation in log n time and we're going to need that for the log squared solution。

呃。One other thing I wanted to maybe mention。So here we did join。

 and join assumed that we V was the root of its tree。 What if V is not the root of its tree。

 And we still want to insert an edge because insert。When we're maintaining an undirected graph。

 there is no rootedness。 That rootedness is just a tool。 you could say at the first step here。

 just root the tree somewhere so that we can have a well definedfin beginning and end of the oer tour。

 Of course， Oer tour in its most natural form is actually a cycle。

 and it would only have one visit here。So you can think of it that way。 I mean。

 I'd still like to have the two visits conceptually because that makes even the subree rooted at the root an interval between the beginning and the end。

But the nice thing of the cycle view is if I wanted to change who the root is。

 say I want this node to be the root。That's just a cyclic shift of everything。 right。

 It just means I want to start the cycle here and end it here。

 So I'd basically duplicate this copy of the vertex。You， you know， Sify this one。 So's only one copy。

 That's a constant number of inserts and deletes。 and now rotate。What does rotate mean， Well。

 before was a sequential order starting here， ending here。 now I wanted to be a sequential order。

 say， starting here， continuing this way。And then ending back here。

 So if you look at what that requires， maybe use another color I want。This part。That's。

 that's an interval of the old tree， of the old。Sequence， and then I want this part。

So there's two pieces of the old tree。 And I just want to change their order。

RightIt's a cyclic shift。cyclic shift is just a cut and then rejoin the other way。

So I can reroute a tree in log n time as well。So this is why in this world。

 join is equivalent to insert。 If I want to do a general insert。 first， I reroute these tree。

 I do a cyclic shift with one split， one concatenate。Like this。 And I get V to be the root。

 Then I can do the join。So， again， constant number of splits and concatenates。

So it's another operation we can do。And log n。Everything here is login。Cool。

Any questions about Otler tour trees。That's all I'll say about them。We will next move on to。

Next result。Can link countries also do a reroot， I haven't thought about that。诶。I would guess。So。

But it's going require some more thoughts。 So let's see link cut tree。 we can do a little aside here。

 link cuttry。 we do an access on V。 And so now we get that V is up here right at the root of the tree of ox trees。

 And we've got other trees of ox trees hanging off here。But in particular。

 this thing represents a path。啊。It represents the root to V path。It actually has no right child。

 so it's a little bit emptier like that。So now we want to flip things around and say V is the root。

 We basically want to reverse the order of all the nodes in this tree。

So reversing the order of nodes in a binary search tree， you can't regularly do。

 But if you augment your data structure to say， I could basically mark these nodes as all inverted。

 treat left as right and right as left。 It's basically the dyslexic approach。

 you have a bit saying from now from here， down to here， mark everything is dyslexic。

 And so you invert left and right。 And so I think with this appropriate augmentation， you can。

Reroot the tree in log end time。 But of course， there's some details to check。

It might even be that's in the original link cut paper， but I don't remember offhand。

 So I'm pretty sure it works and therefore gives you dynamic connectivity。

 But if it doesn't order two or trees do it， too， so。We're covered， either way。Okay。

So that's our to our trees。 Next on our list is decremental connectivity in trees。

 So we're going to solve a weaker problem， which is just deletions only instead of inserts and deletes。

We're going to solve it faster。Constant time per operation。This is a just a fun little result。呃。And。

It's fun because it uses some techniques we know。So bound is constant amortized。

 assuming all edges get deleted。We need to assume this。

 essentiallycause we're amortizing against the future， so。We need to get all the way to the end。

 So overall， over and operations it's gonna take order end time。Okay， this is， again。

 a series of refinements or a bunch of pieces of solution that get combined together。

 not a sequential algorithm。So the first observation is， of course， we can do order login。 We know。

 now， maybe two ways to do it。 but you can use or two or trees and only do cuts。

 Life is easy Link cut trees。 You could also just do cuts。 You don't even need rer。 Reroing。

 Reroing was only for joints。Okay， so that may seem obvious， but。

How do we reduce the log n to a constant？In general， in this class。もと警応。

Indirection and maybe look up tables。 Yep， how do we do indirect in a tree。Leaf pruning。

 leaf trimming， yep， whatever you wanna call it。Should call it， I don't know。

 the Edward scissor hands approach or something。So leaf trimming。 What I want to do is cut below。

Maximally。Deep nodes that are roughly log n。Say， with greater than log n。Decendants。

So we have our top tree。And then hanging off some node here， we're going to have some bottom trees。

These each have umost log n nodes。These have bigger than log n nodes below them。

 so we can charge every leaf in the structure to the log n nodes below it。 And so up here。

 we have at most。Sorry。N over log n。Leaves， not nodes。 Nos could still be linear。

It's a graph of a path， but at most N over log n leaves。

So we have at most on over log n branching nodes up here。

So what we need is a structure for dealing with log pads up in the top。

 a structure for dealing with log n size things down here。

 and a structure for combining the paths together。That's the easy part。

So if we treat each long path as a single edge， basically。

 we look at the compressed top tree in the sense of compressed tries。

 but it's not tree instead of a tri。 I guess it's a tri， whatever。

 we look at the compressed tree up here that will have size n over log n。

 And then we can afford to use structure 1。Why， because in some sense。

 there will only be an over log and operations performed。

 So it's not a space issue that we're trying to save。 It's a time issue。

 So a little different from the past。There will， I mean。

 there are only n over log n times that you can destroy a path up here because there's only n over log n paths Each time you destroy a path。

 I do an operation up here， I pay log n。 But if the total number of operations is n over log n。

 total cost is linear。 so constant amortized against the future。So I'm going to use structure 1。

On the compressed top tree。And what remains is what do I do in the bottom trees。

 What do I do in the paths。 And then how do I combine all those results together。

 Let's first talk about how to combine the results。 There are a couple of different cases。

It could be。So we're doing a query on VW。 and we w to do connectivity query。

So it could be that V AndW are in the same。Bottom tree。In that case， we just need to。

Do a query within a bottom tree。 So as long， as long as we can solve bottom tree case quickly。

 we're happy。 Constant time， I guess。That will turn out to be pretty easy。That's the easy case。Now。

 otherwise， they're gonna be， well， they could be in different bottom trees。

 So it could be these down here。And。W is in some other bottom tree。

 So then we have three queries we need to do。 One is like this。1 is like this。

We need to test a single edge， but that's trivial。 And then we need to do a query in the top。

Can you get from here to here。 Now， these， these nodes are a little bit special because they are leaves。

 They will be at the ends of paths。 So this is really just a query we can do using structure 1。😊。

Becauseuse we only care about。I mean， we take either an entire path or no path at all。So we can。

 we can look at the compressed tree， and that's enough because the leaves exist in the compressed tree。

 That's not always the case。 a different situation。Is this we might have， for example， V up here。

 W up here and， you know， nothing V and W are not down here。 So these are irrelevant。

 We just want to know， in the top tree， can I get from V to W。Now。

 this is a little more awkward because V is going to live on some path。

W is going to live on some path， but it might be in the middle of a path。

And so there's now three queries we need to do。 One is， can I get this to the top of my path。

And then can I get from one path to the other。 So these are branching nodes here。

 and can I get from this branching node to that branching node， It might not always be going up。

 Sometimes you have to go down。 But point is a constant number of queries to path structures to compress structures。

 This is， again， the one structure and a constant number of calls to a bottom structure will suffice to answer the  query。

 There's a few more cases that I haven't drawn。 if you could be half of this in half of this。 but。

Can all be done as long as we can solve bottom and paths。 So let's solve paths first， I think。Oh no。

 bottom pad。 bottom tree is fine。So part3 of the solution is solve the bottom tree。

Here we can do it in constant worst case。So。This is essentially the lookup table thing。

 although it's a pretty simple lookup table， What we do is store So bottom tree has only log n nodes so we can represent 1 B per node in a single word。

And what we'll do store a bit vector。Of。Which edges。Have been deleted。

In what order I don't really care。 Just pick some order， some fixed order on this。

 on the edges down here。And store the bits in that order and say every edge knows its sequence。

 So you could do like a depth first search at the beginning to to label the edges in some canonical order。

 And then every edge knows what its bit position is in the vector。 So when I go to delete an edge。

 I just set that one bit using an or operation。um。Cool， so I can delete edges。

 I could actually even insert edges down here that used to exist。 I could undlete an edge。

 but that only works in the bottom structure， so。Not generally useful。 Okay。

 so it's clear how to do an update， how to do a delete。 I just mark a bit。 How do I do a query。

 I want to know given two nodes V and W， one of them could be the root of the tree， I want to know。

 can I get from V to W Or was there's only a single path that might exist。

 So it's a matter of has any edge along this path been deleted。So a general way to write that。

Is I have。I have W。 I look at V's path to the root。 I look at W's path to the root。 At some point。

 we reach the LCA。What I want to know is have any of these edges been deleted。 If yes。

 I can't get there。 If none of them have been deleted， I can get there。 It's an if and only if。

 I don't care about these edges up here above the LC because I only need to go to the LC and back down。

So what do I do， each vertex。Sttors， a bit vector of its ancestors。The ancestor edges。Well， what a。

And so strategy is kind of weird。 It sorts of bit factors representing its。A path to the root。

That's this red stuff。 I want to know for vertex V。

 what are the edges along the path to the root of this bottom tree。I just pre processed。

 You can build this thing as you do the depth first search and store it。 Again。

 it's one word per vertex so I can fit it， fit it with constant space。ItConst space overhead。

 And now if I have two vertices， I take the X or of those two paths that will give me this part。

And then I use that as a mask into the bit vector。Say have any， or any of these one。

How do I do th just mask？ And if the result is the zero word， then I know none of them are one。

 Otherwise， I know some of them was one， some edge was deleted and unscrewed。So I take X or。Of。

Vs path。And W's path。And then， I mask。This thing which stores which edges were deleted。

And then I check whether that word equals 0。If it equals 0， yes， I can get there。

 If it doesn't equal 0， I can't get there because some edge was deleted。Ands so very easy。

Because we can fit。Login， bits an award。Okay， that's the bottom tree structure。 Next， we need。

A path structure。Here we can use do constant amortized with a similar trick。We're going to use。

 So here's our path。We're going to use essentially indirect again。 when we have a sequential thing。

 Again， we know how to solve this log again。When we have a sequential thing。

 our usual way of indirection is to split into chunks。Each chunk here， I need to be about login。

In size， I'm just make it exactly log n in size。 So as's N over log n chunks。So if I。

 if I store a summary vector， we can think of these as being 0，1，0，1， whatever。

 where the ones mean the said' has been deleted。0 means it hasn't been deleted。

 So let me write an over log n chunks。Each log n edges。

And I'm going to store each chunk as a bit vector。Bit factor has log n bits。 W is at least log n。

 so I can do the same sort of things I could do before。 If I delete an edge， I just mark， I add。

 I set a1 bit。In the chunk bit vector。Okay， now that's good for local queries。

 If I want to do a long distance query， I need to basically summarize， are any of these bits。

 Are any of these edges deleted。 If so， put a one up here， any of these edges delete， if not。

 put a 0。And now I want to structure over this， you might call the summary vector like In de Bos。

 done it before within direction as well。 Now， this summary vector has size n over log n so I can afford to use our log n solution。

That we started with。So， use one。On。Summary。Fctor。A chunks。Because， again。

The first time I set one of the bits in here， I have to do an update up here。

 But only then once it's been done once， I don't have to update again。

 So there'll only be n over log n update so I can afford log n per update。

 It'll still be linear time total， constant per operation amortized。So this is my solution。

 If I have a query， let's say I want to know between here。And here。I first check。

 can I get to the right endpoint with locally， which is a query within the chunk， which is， again。

 I just mask out these bits， I say， and now do I have the zero vector in which case nothing I care about has been deleted and I can get there。

 Or if I don't have the zero vector， I can't get there。I wantan to know。

 can I get to the left from here， Again， it's a mask and a check against 0。 And then I want to know。

 can I go over this。Interval of chunks。 And that I can use the summary vector for。And again。

 that is a mask of a sub intervalterval and then checking whether there has zero。

 So I take the end of these three results。And I've done。That gives me a query over a path。

So it's kind of fun。 We use two levels of indirection， One to get to reduce the number of leaves。

And then these were sort of trivial。 And then within the structure， we could only。

 we could only afford things on the non branching part。 So we had to deal with paths separately。

 And there we use another level indirect。But in the end， we get rid of all of our logs。

 and it's constant amortized。For deletions in a tree。Questions about that。That was our。Second result。

IOne more。Okay。Last result we're going to talk about is log squared and update。

 log over log log query。For general graphs， finally， we graduate from trees to general graphs。

This is a result by Hol De Lichtenburg and Thorpe。2001 is the journal version。Okay。

 so we want to solve dynamic connectivity。 We want to understand in general。

 when two vertices are in the same connected component or not。That sounds tricky。

 We're gonna do that in a pretty simple way at a high level。

 High level is we want to store a spanning forest。I mean， you know what a spanning tree is。

 spanning forest is， well， your graph might be disconnected。

 That's the whole point of the data structure。 If it's not disconnected， you answer yes all the time。

When it's disconnected， you say， okay， I'll have a spanning tree for this connected component。

 spanning tree for every connected component。 together， we call that a spanning forest。

That's like the maximal connectivity you could get， but represented as a tree。 Now。

 we have a great way to represent trees or there two trees。

And if you somehow connect it together two components。

 that is an insertion of an edge in an order tour structure。Great。😊。

So we can maintain all these connected components and merge them if we have to using an insert in order to structure。

And you can do， to do a query connectivity query。 I don't think I mentioned this。

 You do find root on V And W， see whether they're the same root because root is like a canonical name for the connected component。

So you can solve connectivity using find route in constant time。 Well。

 constant extra time log for the find route。Okay， so everything looks great。 I can do connectivity。

 I can do insertion。 What about deletion。😊，If I delete an edge that is not in my spanning forest。

I'm happy。 I have exactly the same connectivity as before as proved by the spanning forest。😊。

The trouble is when I delete an edge that's in the spanning forest。Then it's like， maybe。 so here's。

 here's， you know， spanning tree。Whatever。And now let's said， I delete this edge here。Then there's。

 there's a couple of possibilities。 It could be。This is only， you know， it was a graph。

 It's not a tree。 So there could be some other edge that connects those two trees。

 And then I have to find it。I'm gonna find it。 That's going be annoying。

 or it could be there's no such edge。 Then then I'm fine。 Then it's just a cut。

But distinguishing those two cases is going be our challenge。

 And that's where we're gonna lose another log factor。But only another log factor。

To only lose another log factor。 what we're going to do is not just store one spanning forest。

 but we're going to kind of we will store the spanning forest。

 but then we're going to hierically decompose it and say， well， yeah， there's there's this big tree。

 but I'll just sort of some of the edges I'm going to put in the next level down。 some I won't。

 So some subset of this forest will be at the next level down。 And there's going to be log n levels。

 That's what we lose our log factor。And the weird thing is， I mean。

 there's no real reason to put things down， except we'll use it as a charging scheme。

We'll prove that an edge can only go down log n levels， and then it has to get deleted。

Before it becomes relevant again。 So it will let us charge only log n times per。Per edge。Okay。

 that's about as good as I can give you a high level overview。 Now we have to see the details。

 It's gonna be， it's kind of amazing that this works。😊，It does。

So we're gonna talk about the level of an edge。This is not a definition， per se。

But it's a changing quantity over time。 As you do edge deletions。

 some edges are going to decrease in level。They all start。At log n。

 log n is going to be the top level。 That's the whole spanning forest。But they can decrease。

 They will only decrease its monotone。And they can never get below 0。 So they started log n。

 They could go down， and the lowest value is 0。 Okay， now G I， G is my graph。And G。

 I is gonna be the sub graph of。Lower level edges。诶。So say level less than or equal to I。Okay。

 so in particular， G log n is the whole graph。So we always include lower level edges。

 So level 0 is gonna appear in all the G Is。 But if we， if we。Look at G0。 It's only level 0 edges。

 G1 is level 0 and 1。 And so this is sort of a hierarchical decomposition of the graph。

 We have fewer edges at the bottom levels。Okay。And there's gonna be two key invaris we have over these structures。

Invariant 1。It's going to be every connected component of G I is。Small。

It's going to be size at most2 to the I。This is really what's going to let us charge against something。

 Whenever you go down a level， the max size of a connected component goes down by a factor of 2。

 So at level 0， all components have size 1。There are no edges， a little0。So I kind of lied。

 I guess max， the lowest level is one。At level 1， you can have two vertices。

 So there can be isolated edges in G I， but that's it。

 can't have a path of length 2 in G1 and so on up the tree at G log N， you can have the whole graph。

 whole thing could be connected， but this will let us。Charge to something as we go down in the tree。

As we go down in levels。Should say。Okay。So， next thing we need。Is a spanning forest for each GI。

F I is going to be a spanning forest。Of GI。So it's going to maintain the connected components of GI。

And we're going to store that。Using an order tour or tree。There's this issue of pluralization。

' say trees， because F I is disconnected。 Each connected component you use。

 you store using an order to tree。Yes， and check whether it's an order to forest， I suppose。Okay。

 so that way， we can do a query。 And so given to nodes。

 we can know whether they're in the same connected component。In F I。

 just by seeing whether they have， they live with within the same， under the same root。Okay， well。

 in particular， that means that F log N， which is the spanning forest of G log n。

 which is everything。That will let me solve queries。This is the desired。pananning forest。

That will let me ask connectivity ques。 So all this infrastructure is in order to support deletes efficiently。

But queries are easy。 We just look at the top forest。 That's the one we want。Okay。

 now second invariant。This is where things get interesting。Al，2。The forests have to nest。

So F log n is， of course， has the most edges。 F 0 is gonna have the fewest edges。

 but I want them to be contained in this nest structure。

 All this is saying is that there's really only one spanning forest。 F log N。 F I is just F log n。

But restricted to。The edges of GI。Okay， so really， we're trying to represent this forest。

 But then as we look at lower levels， we just forget about the higher level edges。

 restricted lower edges of level less than equal to I。 That's our smaller forest。 This is。

 in some sense， the hierarchical decomposition of the forest。

So there's really only one forest that will make our lives way easier。Fun fact。

Is that that forest then is actually not just any spanning forest。 It's a minimum spanning forest。

With respect to level。Okay， you've probably heard of minimum spanning tree。

 minimum spanning forest is just the analog for disconnected graphs。呃。

So we're defining the weight of an edge to be its level。

And so F log N can't just be any spanning forest。 It has to prefer lower level edges。

 Otherwise this nesting structure won't be true。Now， this。

 its only that doesn't uniquely define the forest or anything because maybe all the levels are log n。

 And then every spanning forest is a minimum spanning forest。

 but it's a constraint on spanning forest。Cool。Let's go over here。Okay。

Let me quickly say how to do an insert and how to do a query。 These are really easy。

The lead is really where the action is。 But just to make sure we're on the same page。

And to introduce a little bit of notation and say a little bit about what we do。

 we are going to store incidence lists。Which is for every。Vertex。

 we have a list of the incident edges。In like a linked list。Okay， so in constant time。

 we can add our edge to B And Ws instanceless。 that's not maintaining any order or anything special。

Then， we also set。The level of the edge to the log n。That's what I said。

 every edge starts at level log n。And then there's one more thing， which is if。V andW。

Our disconnected。In F log n。So we can tell that because we can do a connectivity query on F log N。

 We have that as another to tree。 so we can see whether V and W are in different components。

 If they are， we have to merge them。 So we merge them。We， this is。You call an edge insertion。

So this is an order two or tree insertion。That we know how to do in log n。

 we reroot and we do a join。So， that's cheap。And that's it。Okay。

 so insertion is easy to do in log end time， actually。It's deletion that's going to be。Painful。

 actually， we're going to charge to the insert。 so it'll end up being log squared amortized。

 But worst case， it's login。Great， I want to say a little bit about connectivity。 Now。

 we know how to solve connectivity already。 We do find root on V And W。😊，And in login time。

 we determine whether they're in the same component。 But I actually claimed maybe erased by now。Yeah。

 but I claim not a log and query。 I claimed a log over log log query。

 Turns out that's really easy to do。You just。Change the top level minimum spanning for， slightly。So。

 I want to make。F log n。Equal to B3。Actually， it's a bunch of B trees， one per connected component。

Of branching factor。Loin。I guess's a B tree。 So I have to have some slop Theta log n， let's say。Okay。

 usually we， we said， we said with all other two or trees， it was a balanced binary search tree。

 I'm going to make this particular forest use a log n way Be tree。This is gonna slow down updates。

 but it actually speeds up queries。So。To do a fine route。Is now a little bit easier。

Find root should just be log。Log based log n of n， which is log n over log log n。Because find route。

 you just need to go to your parent to your parent to your parent， go to the top。

 And then you have to go to the leftmost place。 But it's easy in a Bre to go always go to the leftmost place。

 If I had to do a search with them， then know that would be annoying。 but going to the leftmost。

 That's easy。 So I only pay log over log log for query。 So I got my desired query time。

 And I claim the update is kind of okay。 because its flowed down。

 But we're already gonna pay log squared eventually。 And here were doing log。

 So we don't need to deal with we don't need to be that fast。嗯。Update is now gonna be。Let's see。

The height of the tree。Times the branching factor。If we touch nodes in a root2 leaf path。

 there's log over log log of them for each one。 we have to rewrite the whole node， so we pay log。

 but this this is less than log squared。So this won't end up hurting us because we only do this at the top forest level。

 We don't do it at all the levels。 If we did at all the levels， we would lose another log factor。

 or log over log log factor。 But here we do this once at the top， pay log square over log log。

 then we have to update log n other levels each paying logs。

 So we're already paying log squared for the lower level。 So if we increase the top level a bit。

 Not a big deal。So that's how you improve connectivity queries。Slightly。now， finally。

 we get to deletes。 This is the moment you've been waiting for。

How do we do a delete in this structure？What are all these levels good for？Okay。

So we're deleting an EE， it's endpoints， are V andW。

First thing we do is remove E from the incidence list。Okay。

 if every edge stores a pointer to where it lives in the instance list。

 you can do that deletion in constantine。Great， so。As I said。If E。Is not in。This forest。

What if it's in a lower level of forest？Oh， I see it right。I forgot。In verying2。

Varian too says the forests are nested。 So if it's in any forest， it's gonna be in the last one。Okay。

 so if it's not in the last one， that means it's not in any of the forest。 That means we're done。

 We do nothing。 That's the easy case。We preserve， I mean。

 we didn't destroy any connectivity because the forest represent maximal connectivity。

 They're spanning。But if it's in the forest， then something change。 I then we need to determine。

 like in this picture， is there a replacement edge or is there no replacement edge。

In which case when there's no replacement image， we basically don't do anything。

 we have to do a bunch of deletes in the forest。But yeah。Is that what I wanna say next。Yes。

 we always do that。We're going to delete E。 We have to delete E。

 So we're going to recursively delete it。呃。From。F。Sub e dot level。Up to。F log n。

E dot level is the earliest force that appears in。 we'd have to delete it from all those。

 Each of those is an order two or tree deletion or cut。

 And so each of them pays log n total cost log squared in。 is where the log squared is coming from。

That's great。 Now we've successfully deleted the edge。 But now we need to know， is there a placement。

😊，And at what level is the replacement now， we know by。invariant， too。

That there could be no replacement of lower level。Right，The point is E was。

 sort of this is a minimum spanning tree。 So E was the lowest level edge that could connect the two sides。

 These two connected components of the forest。So if there's any replacement。

 it's at these level or higher。Might not， there might not be a replacement at E dot level。

 but there might be a replacement at higher level。 We want to find the smallest level for which there's a replacement that will preserve invariant。

 too， that we have a minimum spanning forest。So that's what we're gonna do。 Lo over the levels。

 try to find a replacement edge。So we're going to start at。E dot level。Loop potentially up to log n。

 call the level I。Okay， then。I want to identify at level I， the two sides of the edge。

 let TV and T W。Be the trees。Of F I。Containing。V and W， respectively。Okay。

 we just deleted the edge connecting those two sides。 So we know that they're in two different trees。

 TV and T W。And I， one of them is smaller than the other。

 I'd like to relabel V And W so that the size of TV is less and or equal to size of T W size in terms of number of vertices from there。

In the tree。Okay， so swapped V and W if necessary。Here's the fun thing。 If we imply。

 if we apply in variant1。Then， we learn。These sizes are not so big。Claim it most two to the eye。

To realize it's the most to the eye， you have to imagine the moment before this deletion happened before we did deleted the edge because at that point。

 TV and T W were actually one tree。 They were connected by E E。We've just deleted them。

 But the moment before we deleted them invariant one held。 And so there was a connected component。

 it should have size at most2 to the I。 Now we split it， but that's all we've done。

 So the sum of those sizes has to be at most two to the I。

 because that used to be a connected component。Kuo。So that means that size of TV。Is half that。

At most two to the I -1， because TV is is less than equal to T W。 So it's at most half of the total。

The TV is even smaller。In particular， what that tells us now we have TV and TW's kind of separate components temporarily。

 We don't really know that they're separate at level I， but we know at level I -1。

 they are separate at level I -1， there is no replacement edge by the minimum spanning forest property。

So。What we could do at this point is take TV， take all over the edges internal to TV and push them down to level I -1。

We could afford that。What do we mean by we could afford that。

 We wouldn't destroy invariant one because we're taking2 to the I -1 vertices。

Pushing all those edges down。 we don't have to push all of them。

 We could push some subset of the edges down。 Whatever connection component we make at that level will be of size at most two to the I-1。

 And so invariant one will be preserved。So great。We can't afford。In a certain sense of a。To push。

All of TV's edges。2。Level。I am-1。Great。So。We don't actually need to do this。

But what we're going to do is use it to pay for stuff。We have this scary goal。

 which is we want to find， is there a replacement edge。😰。

I don't know a good way to find a replacement edge。

 except to do basically a depth first search and look at all the edges。

That's going to take a lot of time。But the good news is， if we search from TV。

 every edge that's useless。😊，We can just decrease its level。

And whenever we decrease the level of an edge， we basically get a free coin to continue。

So you get a free life every time you push a level down an edge down by one level。

 because overall number of pushes can be a number of edges times or a number of insertions times log n because every edge can only be pushed log n times。

So whenever we push down an edge， we get a free free bonus life so we can keep doing our search。

So here's how the search works。And say。For each。诶。Thiss a little bit tricky to implement。

 I'm gonna first tell you what we want to do and why that's okay。

 And then we'll see how we actually do it。So I want to search from every vertex in F V。

 I want to look at all the outgoing edges from there of level I。I never mind how to find those edges。

 Just pretend you could find it in constant time per edge。It's going to be log in time for。

 but that's okay。It's two cases。Either Y is in TW。Otherwise。Why is going to be in TV。Right，cause we。

 weve have TV here。Of T W， we just deleted this edge E。Sadly， we want to find a replacement edge。

 So if we look at all the edges coming out of a vertex in TV， this is， I guess， X。

It could be it's an edge that stays within TV， or it could be it's an edge that goes to TW。

 There are no other options because of our invariance。If it's in TV。It's kind of annoying。

 which doesn't help us。 But then we can just set the level of that edge E prime。To be i -1。

Because we know we could afford to push all these edges down by one level。

 So that pays for this round of the loop。Great， what about the other case。

 The other case is the good case。 That means we found a replacement edge for E。😊。

If we have an edge from TV to T W and at level I， that was we did these levels in order。

Did I say that。 Yeah， we're doing levels in order。 So if we can find this is the lowest level replacement edge。

 So we do it。 We insert。That edge。Into FI。Using an order tour or tree insertion。And then， we're done。

So we stopped the algorithm。The moment we find a desired edge， we're happy。 Now。

 maybe there are lots of edges。 So we've got to stop when we find the first one。

Then we've restored our minimum spanning force property。 We have maximal connectivity。

 all these great things。 As long as we don't find an edge。

 we can push things down a level and pay for this round。 So it's constant time or。

 you know log in time to do the last step。 but all the other steps are paid for by a decrease in level。

Okay。So the claim is， overall， we pay log squared n。

Because we had to do log square n for these deletions。Plus。呃。Loin。Times the number of level。

Decreases。That's for this step。 I claim that each of these， each round of this loop cost log n。

So that's why we get log n times number of level decreases。

 but number of level decreases is at most number of edge insertions。Times log n。

 because each edge can only decrease in level log n times by definition of levels。

 And so we can charge to the insert operations at a factor of。Log n。 So this。

 there's two log ns here。 So inserts now cost log squared amortize as well。

Delettes cost log squared advertised， and we get log squared overall。One last issue， though。

 which is， how do we do this step， Actually， there's another step that's not so trivial。 this one。

We need to know the size of TV and the size of T W。 Well， that's easy。

 I just store subtree sizes at every node。 So then I look at the root， the root of the BST。

 and it tells me how big that that tree is。 And so I can see which is bigger and switch accordingly。

 So that's trivial。 This one requires a different augmentation for or their tour trees。 And again。

 both of these situations we're using the fact that we can augment on subtrees not on paths。

 That's why we need order tour trees， not link cut trees。So， what we store。In F。What are red FV。

I mean。TV here。So in F I， this for each eye， but at the forest at level I。

What I'm going to store is that every node in or tour tree。

I want to know in the subt rooted at that node。So here's are there tour tree。 here's a node V。

 This is in a tree in F。 I want to know in this subte in here。Is there。

Any node that has an out that has a level eye edge incident to it。So I want to know。

 are there any level eye edges。If there aren't any level I edges。

 then for the purposes of this search， I should just skip that subre。

Its as if the suby was was erased。So I have some tree。 It has height log n overall。

 I era some of the subtrees according to this bit， which is an easy thing to。Keep track of。

 it's an augmentation。constantstant factor overhead。

Then I basically wantna do an in order traversal of this tree， but skipping the stuff below。So。

 I mean， one way to think of it is you just repeatedly call successor like successor in a binary search tree。

 But whenever I see that there's no level edges below me， I just skip。Alright， so in log in time。

 you can basically find the next place where there's an outgoing edge。

 I guess also the instance lists have to be stored separately for each level。 So you can say。

 what are your outgoing level I edges from a vertex。 This lets you find the vertex quickly。

 But then you have to find the edges quickly。 So the instance lists are not just stored as one single linked lists。

 one linked list per level。But that's easy to do。 So then from this vertex。

 we can find all the outgoing level I edges。And we can do this for loop efficiently。

And whenever it's slow， we get to charge。Okay， in the remaining negative two minutes。

 Let me tell you briefly about other problems that have been solved。 So this finishes log squared n。

 fully dynamic connectivity。 I wasn't planning to spend much time on this anyway。

 There are notes if you're interested in specific problems。 I'll just tell you what the problems are。

Generalization of connectivity is K connectivity。 I don't just want to know。

 is there one path from V to W， I want to know， are there K disjoint paths from V to W。

So they could be edge disjoint or vertex disjoint paths might be your goal。In that case。

Polylo is polylylog solutions are known for two connectivity， edge or vertex。

 This is a result actually I used in my very first graph algorithm in this papers where I first learned about dynamic graphs in log to the fourth end time。

 you can maintain two edge connectivity。It's open for K equals。Three。

 even whether you can achieve that bound， so kind of lame。Another problem is minimum spanning forest。

 So you don't just want to maintain some spanning forest。

 Maybe you actually have weights on the edges， and you want the minimum spanning forest。

 That it turns out you can solve with a couple extra log factors。 So in log to the fourth end time。

 you can reduce to with an extra log squared factor。

 you can reduce to the problem of dynamic connectivity。 And so log to the fourth overall。

 you can maintain minimum spanning forest。Another problem is planarity testing。

 You want to insert or delete edges and at all points， know whether your graph is planar。

And that can。There's no great results known for that。 It's like n to the two thirds。

Diirectctorgraphs， things get really hard。Two problems I've surveyed here is probably more。

 but sort the most fundamental questions are the analog of connectivity is what I call dynamic transitive closure。

 which means I given to vertices V and W， I want to know。

 is there a directed path from V to W instead of undirected at the moment， a bunch of results match。

A trade off curve， which should be update。Times query。

Is roughly M times n a number of edges times number of vertices。 This is big。

 Okay but there are a bunch of results that achieve this trade off。 There's。

 I don't think I'm matching lower bound， but it's also open whether this entire trade off curve could be achieved。

 There's various points on the curve where you pay square root of n for an update。

 Then you pay like M times square root of n for the query or vice versa stuff like that。

then a harder version of this is you have weights on the edges。

 And now you want to query is not just find a path， find the shortest path。

This is what I call dynamic， all pair shortest paths。😊，This is even harder。

 There are some results where you can do， like。Roughly n squared time updates constant query。

 So it's maybe similar type of trade off curves， not really known when the graph。

 when all the weights of the edges are one， you just want to find the shortest path in terms of number of edges。

 you can do， not really that much better。 Still， this is the product。

 But at least more of the trade off curve is it has been solved。

So that's a quick overview of dynamic graphs more generally。

 just in case you dynamic graphs come up all over the place。 obviously。

 a network where the links are changing or。Facebook， you want to maintain connections。

 but people friend and defriend all the time。 So you want to maintain whatever structure you care about。

Anyway， that's dynamic graphs upper bounds。 Next class will be lower bounds， the log n。



![](img/7a6712daca2225696d0bad091f659505_4.png)