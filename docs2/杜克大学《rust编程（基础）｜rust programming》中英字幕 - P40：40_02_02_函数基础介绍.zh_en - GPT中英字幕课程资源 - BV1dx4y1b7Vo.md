# 杜克大学《rust编程（基础）｜rust programming》中英字幕 - P40：40_02_02_函数基础介绍.zh_en - GPT中英字幕课程资源 - BV1dx4y1b7Vo

![](img/374527a871a1a786590ff2a4b5468dd8_0.png)

When we've been adding loops， conditions， bear assignments in rust。

 we've already been using this special function， which is the main function in rust。

 it's kind of like the entry point for executables in rust but now we're going to see functions with a different perspective now we're going to be able to build them。

 we're going to see some of the functions that are called unit functions which are functions that are just executing some code。

 they're not returning anything， they're doing some work we're calling them they're doing some work and then that's it and those are called unit functions and we'll see how those work but we'll also see functions that are actually returning values in how those work and finally we'll take a look a little bit about panic it's a special a special call syntax in rust。

 it's a special macro that allows us to stop all the execution in the rust program and we'll see how that works。

And even though you might not necessarily see it a lot in production code。

 we'll see in what situations it makes sense to use panic versus some others that perhaps is not such a good idea to have panic。

