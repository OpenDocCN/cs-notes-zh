# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p42 p41_02_spring-containers-overview -BV1fryaYgEqb_p42-

![](img/87e24070d443feb6b19620fbe8439e3f_0.png)

Hi there。 Today In this session， I will talk about spring IOC containers。😊。

How it works and how to create spring IocC container and how to retrieve beans from the spring IocC container。

 all the things in this lesson， so let's get started。Basically。

 if I talk about spring IocC container， the spring container is responsible for instantiating。

 configuring and assembling the spring beans。

![](img/87e24070d443feb6b19620fbe8439e3f_2.png)

The container gets。Instructions on what object to instantiate。

 configure and assemble by reading the configuration metada。

The configuration metadata is actually being represented either with the help of Xzel or Java annotations or Java code。

As I discussed， it stores the meta information and the responsibilities that what it has to do。

There are two types of spring IOC containers， one is a beam factory container and another is a application context container。

The bean factory container is the root interface of spring IOC container that supports the dependency injection defined in spring framework do Be dot factory do bean factory interface。

And the application context is the beam factory implementation and which is a child interface of beam factory。

 which is having more enterprise specific functionalities like we have a specific methods like get beaman method and all to instantiate the singleton beaman when the container is started。

😊，It does not wait for get dream method to be called。

 It just instantiate whenever the beam is invo for the first time。

Application container container includes all the functionality of the beam factory container。

 as I said， it is a child interface of the beam factory。

So stay tuned to learn more about how to create this configuration metadata。

 either by Xl or annotation based or Java based configuration and invoke it。

 See you in the next session until next time。 Stay tuned。 Thank you。



![](img/87e24070d443feb6b19620fbe8439e3f_4.png)