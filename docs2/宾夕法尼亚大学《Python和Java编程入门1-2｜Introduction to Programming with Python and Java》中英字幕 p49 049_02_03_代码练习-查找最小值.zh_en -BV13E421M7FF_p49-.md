# 宾夕法尼亚大学《Python和Java编程入门1-2｜Introduction to Programming with Python and Java》中英字幕 p49 049_02_03_代码练习-查找最小值.zh_en -BV13E421M7FF_p49-

Let's write code that finds the minimum value of a list of numbers。

We're not going to use the built in min function to do this。

We're going to instantiate a numbers list， which will contain the proper values。

Iterate over that list and find the minimum value。

![](img/89fe0ceb0fb900724891e3e45fe6d2c3_1.png)

And then print the minimum value。So first， let's define our list。

Numbers equals a list with the values。5，3，8， negative 1。Negative 2。2 and 0。Those were our numbers。

We're also going to define。And keep track of the minimum number。 and we'll initially set that， too。

The first value or the first item in the list。So， define。Min number variable。😔。

So the minimum number initially is the first item in the list。

 So we're just going to say that 5 is the minimum number。 Then we're going to iterate over the list。

4our number in。😔，Numbers。If the number。Is less than the minimum number。Set the men number。

To be that number。Finally。Print。Min number。😔，Is the min number。So what are we doing。

 iterating over the list？Check if the。Number is less than。The defined min number。That's above here。

If the number is less than that number。Reset。Min number。And then finally， just print them in number。

Negative 2。2 is the min number。

![](img/89fe0ceb0fb900724891e3e45fe6d2c3_3.png)