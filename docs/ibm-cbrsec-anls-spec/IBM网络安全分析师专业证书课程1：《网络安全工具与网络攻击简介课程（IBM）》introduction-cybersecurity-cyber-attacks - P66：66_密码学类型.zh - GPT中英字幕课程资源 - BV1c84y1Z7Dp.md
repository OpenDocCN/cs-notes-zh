# IBM网络安全分析师专业证书课程1：《网络安全工具与网络攻击简介课程（IBM）》introduction-cybersecurity-cyber-attacks - P66：66_密码学类型.zh - GPT中英字幕课程资源 - BV1c84y1Z7Dp

![](img/7a32d8cd45e02736865efe34772a00b5_0.png)

Yes。In this video， you will learn to describe the three encryption types used in modern cryptography。

 including typical uses and advantages of each symmetric。

 asymmetric hash this section we'll discuss three main type of cryptography or modern encryption types。



![](img/7a32d8cd45e02736865efe34772a00b5_2.png)

They're the symmetric， the asymmetric and the hash functions。Gmetric and Christian。

 there are several crosss of it。 Our strengths include those include the speed and the。

And the fact that the cryptographic is strain。Increases by deep of the key。

 I meaning if we have a bigger key， the cryptographic will be more secure。

The problem withsymmetric Christian is that the key needs to be shared using a secure method。

 sometimes an auto of bank method， meaning we cannot send a key plain text to the third party using an unsecured channel。

Because as we discussed previously， the whole exchange or the encryption algorithm or the algorithm is based on just one key and that key needs to remain secret at all times。

 D tripleD and AES are just some of the examples that we currently use and their AES is one of the most modern symmetric encryptions that we currently use as of today on the other hand。

 asymmetric encryption uses two keys。We Phil Diffy and Martin Holman are the ones that created the Diffy homeman algorithm。

 They're considered the pioneers or the modern asometric encrypt。On a as youmetric encryption。

 we have two keys。 as I mentioned before， one key can be made publicly called public key。

 and the other one needs to be kept private at all times。 This is called a private key。

Since we have two keys， one key is used to encrypt and the other one is used to encrypt。So basically。

 whatever is's encrypted with the。Public key can only be deced with the private key and the other way around as well。

 whatever is' encrypted with the private key can only be deced with the public key。

Aometrictry encryption is used in digital certificates。 It's used in the publicly infrastructures。

 It uses the one way algorithm to generate the two keys。

It's based on math like fing drug numbers and discrete logarithm。

This is basically used for generating the two set of keys。

 It's actually a lot slower than symmetric encryption。

 And this is a reason why whenever we use assymmetric encryption。

 most of the time we're using symmetric encryption as well。 to put in an example。

 whenever you visit a website， a Htps website or a secure website。

 you're using assymmetric encryption first， to exchange the。

Key for the symmetric encryption to be used from that point forward。So basically。

 as we discussed earlier。Since we need to be able to exchange that key for the symmetric encryption in a secure matter。

 assymmetric encryption permits that we exchange one key。Between two notes or two parties。

 And from that point forward， we'll be able to use Ssymmetric in cushion。

 which is a lot faster in a secure manner。The hash functions provide encryption using a oneway algorithm in no key This means that any length or a variable length plain text is hash into a thick length hash value this is often called a message digest or simply a hash this is used like we talked before for integrity if a message or a plain text changes and we can determine this by hashing that same plain text and the checking the previous hash and the hash that we just did and in other words。

 we generated a plain text。And we send it to somebody else with the corresponding hash。

And somebody changes that claim text in transit， then we can determine if something changing on that message using the previews has generated and then the hashingary FDA message arrived C1 and MD5 or older algorithms。

 they are prone to collisions。In chat two is a newer and recommended alternative。

Haash functions are prone to collisions。 Or that's one of the issues。

 Sha one and N5s or the algorithms， all the algorithms that are more prone to collisions。

 A collision means two different plane texts。Having the same hash。At the end。

 since we have a limited amount of characters that the hash actually outputs， let's say， M5。Outputs。

 15 characters or more。There's a possibility that two plain texts can have the same exact hash value or message digest。

Those are called collisionitions。

![](img/7a32d8cd45e02736865efe34772a00b5_4.png)