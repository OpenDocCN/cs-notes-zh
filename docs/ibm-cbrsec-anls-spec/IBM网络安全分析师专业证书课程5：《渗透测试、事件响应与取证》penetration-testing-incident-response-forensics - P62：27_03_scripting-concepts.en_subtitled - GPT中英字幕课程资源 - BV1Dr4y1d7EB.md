# IBM网络安全分析师专业证书课程5：《渗透测试、事件响应与取证》penetration-testing-incident-response-forensics - P62：27_03_scripting-concepts.en_subtitled - GPT中英字幕课程资源 - BV1Dr4y1d7EB

Welcome to scripting concepts brought to you by IBM。



![](img/bb17d87f7cf2b072a6c6e30b25da8a2a_1.png)

In this video， Raoul will be taking us through the different concepts related to scripting languages。

 Let's get started。Descriptive basically a program。There is a different。Well。

 a basic difference in which a script is not translated to machine code it depends on an interpreter program that will run a script line by line。

 but other than that it has the same functions procedure it can have external calls an external call is。

Reference to another program， in this case， another script。That will be run from the original script。

And we'll add its。Output。The the script that we're running and the standard variables。And constantss。

Let's see some examples。Most the scripts。Depends on variables to be able to perform its output。

A rule of thumb is mostly scripts just as almost every program languages。我声再见。

A rule of thumb is that most interpreters。Just the same as most program languages。

Need its variables defined。Now， that doesn't happen in script languages like bash。But in Windows。

 yes， you need to。😮，Declare the type of the variable you're using。What does it mean。

 What does mean declaring。It means that if you're going to store text。Endor variable。

You need to tell the。As scripting interpreter that you're using it for text。Otherwise。

 if you are using。AVariable for numbers。And if I that text。

You won't be able to use any mathematic functions on that part， it will give you an error。Now。

 for space efficiency。Most variable types have already。Aet memory size。That happens， foremost。

Programming languages and mouse interpreters。Bash uses。Poined a memory type。 So it doesn't use。

Size or is variable。But that's an exception。没有。These variables， what can I do to them。

 I can assign a value。I can reference， manipulate placed。Or at the end of the program。

 delete the bar。 Now， there is a special。Kind of variabless called arguments。Or parameters。

 What happens with this is exactly why the scripts are so interesting。 When we're calling a script。

 we can tell name of the script。 Let's call it backup。And then in the common line， we can tell it。

Back up this database。 Now， inside of the script， we are going to program。

That variable that comes after the name of the script as a parametermeter。Basically， prometies。

Something we had。At the beginning of the script execution。Part of this script calling。

That we will be using。To perform the script。It will be either the target or it will be one of the commands。

Or operations that we are going to perform on the target。Now， the E statement is。

The lesser definition of a decision inside of a script。

He will give us an structure to decide if we're going to perform one operation or another。

Inside of a script。The normal definition I have if a statement will be。If。

And then we're going to establish a condition that would be one state or another。For falses。

Then what is going to be performed if that condition comes true？Or else。That is。The text I mean。

 the code that will be executed。If the condition comes as false。

And the end of the Eve estate statement。When performing repetitive tasks。

We are going to face situations when a task has to be repeated。Byu a number of times。

 Sometimes we will know how many times we have to repeat the task。Sometimes he will come as a。

For sometimes we will have a list of items。会。Don't know。

 We don't know when we are writing the script。 How many times is it going to be。Repeat it。But。

There is a variable or a list of variables。So will determine that。 So we will create a loop。

The three。Loops defined in almost every。Programming language。Our for loop is going to be。

Evalated at the beginning of the look。He will perform the loop for。

And already determined number of times。Or determined by part。The definition would be。four。jo子。都。

limit。读。The code that is going to be repeated。And end。The next one is the while。A wide loop。

Its said while a condition is true。So it could be said。A sense or then。While sensor is called， doom。

A series of tasks to heat up the sensor。And。So this look will be repeated while。

The condition is true。And the empty loop。为持。It has been deprerated。Where is dial。

The until loop is evaluated at the end of the loop。Trying to determine if I。

If a condition is true at the end of the loop。So there's nothing and until loop can do that a wild look cannot do。



![](img/bb17d87f7cf2b072a6c6e30b25da8a2a_3.png)