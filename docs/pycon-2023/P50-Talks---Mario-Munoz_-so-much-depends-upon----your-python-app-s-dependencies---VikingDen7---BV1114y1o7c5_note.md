# Python依赖管理：P50：Python应用依赖管理指南 🐍

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_1.png)

在本节课中，我们将要学习如何管理Python项目的依赖关系。依赖管理是软件开发中的关键环节，它确保项目能够稳定运行，并且便于团队协作与部署。我们将从基础概念入手，逐步介绍核心工具和最佳实践。

## 概述

Python应用的构建很大程度上依赖于外部库和包。有效的依赖管理能确保项目在不同环境中的一致性，避免“在我机器上能运行”的问题。本教程将引导你理解并掌握这一过程。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_3.png)

---

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_4.png)

## Python依赖管理：P50-1：理解依赖关系与虚拟环境

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_5.png)

上一节我们介绍了依赖管理的重要性，本节中我们来看看什么是依赖关系以及为何需要虚拟环境。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_7.png)

一个Python项目通常会使用许多第三方库，这些库就是项目的**依赖**。例如，一个Web项目可能依赖`Flask`和`requests`库。直接在所有项目中共享这些库会导致版本冲突。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_9.png)

为了解决这个问题，我们使用**虚拟环境**。虚拟环境为每个项目创建一个独立的Python运行环境，包含其专属的Python解释器和包目录。这样，不同项目可以使用同一库的不同版本而互不干扰。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_11.png)

创建虚拟环境的常用命令是：
```bash
python -m venv myenv
```
激活虚拟环境后，所有通过`pip`安装的包都将只存在于该环境中。

---

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_13.png)

## Python依赖管理：P50-2：依赖管理工具与`requirements.txt`

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_15.png)

理解了虚拟环境的作用后，我们需要一种方法来记录项目具体依赖了哪些库及其版本。这就是`requirements.txt`文件的用途。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_17.png)

`requirements.txt`是一个文本文件，它列出了项目所需的所有第三方包及其版本号。这是一个示例：
```
Flask==2.3.2
requests>=2.28.0, <3.0.0
pandas
```
*   `==` 指定精确版本。
*   `>=` 和 `<` 指定版本范围。
*   不写版本号则安装最新版。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_19.png)

你可以使用以下命令将当前环境中已安装的包生成`requirements.txt`文件：
```bash
pip freeze > requirements.txt
```
当其他人或部署服务器需要搭建环境时，只需运行：
```bash
pip install -r requirements.txt
```
即可一键安装所有指定依赖。

---

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_21.png)

## Python依赖管理：P50-3：进阶工具：`pipenv`与`poetry`

虽然`requirements.txt`是基础工具，但在处理复杂的依赖关系（如下游依赖冲突）或需要区分开发与生产依赖时，它显得力不从心。因此，出现了更强大的工具。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_23.png)

以下是两个主流的现代Python依赖管理工具：

*   **`pipenv`**： 由Python官方推荐，它结合了`pip`和虚拟环境管理，并引入了`Pipfile`和`Pipfile.lock`来确保依赖树的确定性。
*   **`poetry`**： 一个更全面的工具，它不仅管理依赖，还能帮助你打包、发布项目，并采用`pyproject.toml`作为配置文件，这是现代Python项目的趋势。

这些工具能自动解析依赖冲突，并生成锁文件，确保在任何地方安装的依赖树都完全一致。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_25.png)

---

## Python依赖管理：P50-4：依赖管理的核心原则与最佳实践

掌握了工具之后，遵循一些核心原则能让依赖管理更加高效可靠。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_27.png)

以下是依赖管理的最佳实践列表：

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_29.png)

1.  **始终使用虚拟环境**： 这是隔离项目依赖的基石，避免污染系统全局Python环境。
2.  **精确锁定版本**： 在生产环境中，务必使用锁文件（如`Pipfile.lock`, `poetry.lock`）或固定`requirements.txt`中的版本，以实现可重复的构建。
3.  **区分依赖类型**： 使用工具将仅用于开发（如测试框架、代码检查工具）的依赖与项目运行必需的依赖分开管理。
4.  **定期更新依赖**： 定期检查并更新依赖至新版本，以获取功能改进和安全补丁，但更新后需充分测试。
5.  **审查依赖项**： 了解项目引入了哪些第三方代码，使用安全工具扫描已知漏洞，避免引入不必要或存在风险的包。

遵循这些实践，你的项目将更加健壮、安全且易于维护。

![](img/0ec22d4e0cc12cb45b9033e28d6a2a35_31.png)

---

## 总结

本节课中我们一起学习了Python应用依赖管理的完整流程。我们从理解依赖关系和虚拟环境开始，介绍了使用`requirements.txt`记录依赖的基础方法。接着，我们探讨了更强大的现代工具`pipenv`和`poetry`。最后，我们总结了确保依赖管理有效的五项核心最佳实践。良好的依赖管理是专业Python开发的标志，它能显著提升项目的稳定性和团队协作效率。