# 信息论、模式识别和神经网络：15：使用神经网络进行数据建模（一）：前馈网络

![](img/2471e5004fce99c2a3d81a0d555558b7_0.png)

![](img/2471e5004fce99c2a3d81a0d555558b7_0.png)

欢迎来到第15讲。我们现在将开始讨论一个全新的主题，即神经网络。

首先，我想提一下我们在书中的位置。我们将关注第38、39和42章。那里还提到了其他一些章节，你也可以看看。

我想说明一下我们为什么应该对神经网络感兴趣。我将从大脑开始谈起，这是动机之一。我们并不了解大脑的工作原理。因此，研究它们的理论模型是很有趣的。

神经网络也有工程应用。所以，即使你不关心神经科学，你也可能对使用神经网络方法解决机器学习问题感兴趣。

让我们先谈谈大脑。关于大脑的第一个惊人之处，我们将在神经网络的第二讲中谈到，是我们执行内容寻址记忆的惊人能力。这是一件非常了不起的事情，我们习以为常，因为我们一直在做。让我提醒你一下内容寻址记忆的含义。

我可以给你一些嘈杂、模糊、只有少量像素的图像，它们可能会让你想起一些东西。我们通过向你展示记忆的一个片段、一个不完整的部分内容，来获取内容，获取你回忆起的记忆。这与标准计算机的工作方式截然不同。在标准计算机中，内存就像文件柜里的抽屉，要获取内存，你需要知道它在哪个文件柜编号和哪张卡片上。然后你去那里找到它。而这里我们说，嗯，里面有一张卡片，上面有这些孔。你有点神奇地找出了那张卡片。

你明白我的意思吗？你能看出我在说什么吗？让我们再给你一个词或两个词，以及另一张黑白图像，不是全彩的。一些带有数字0、4、9的东西。我不知道为什么，但你有点概括。你说，嗯，我听说过那个。我听说过0、07。那只是00，7的平方。所以我给你一些小提示。你实际上从这些提示中得出了整个记忆。这是大脑做的一件了不起的事情。对我来说，这确实是神经网络最令人兴奋的地方，大脑可以做到这一点。我们有一些模型具备一点这种能力，我们将在下一讲中讨论。

让我再举一个例子。我会告诉你一位奥斯卡提名女演员，但不会透露她的全名，她在一部非常出色的乔治·卢卡斯电影中饰演参议员。尽管我刚才给出的信息中有一条是不正确的，但我预测你正在想娜塔莉·波特曼。

这是另一个例子。我将向你展示一本书的封面，标题中有几个词。我将把标题全部涂掉，只留下标题中的两个字母。我将把封面几乎全部涂掉。我会把那个黑色补丁放在作者的眼睛上。作者在封面上，因为当你把黑色补丁放在某人的眼睛上时，就很难认出他们。然而，这本书的标题是什么？《时间简史》。很好。就是这样。这就是内容寻址记忆。

我们是如何做到的？我们能让机器做到吗？大脑是如何做到的？对我来说，这是一个非常引人入胜、有趣且令人兴奋的问题。我们今天不会讨论这个，但这是研究神经网络的动机之一。

那么今天，我们将用神经网络做一些基础工作，也许看看它们的一些工程应用。这将是神经网络部分的巅峰。我们将回到这个问题：我们如何实现内容寻址记忆？我将向你展示一个使用神经网络的解决方案。

让我再给你一个对大脑和神经网络感到兴奋的动机。这是基于1989年进行的一些实验。实验对象被展示了725张图像，屏幕上显示了其中四个示例图像。其中100张被标记为正面图像，625张被标记为负面图像。实验对象被要求记住标签“加号”或“减号”。所有图像在特征上都相当相似。几个月后，实验对象可以记住所有正面和负面例子，并能将它们与实验中早期未见过的其他图像区分开来。甚至12个月后，他们仍然能正确回忆所有正面和负面标签。

这让我印象深刻，因为实验对象是鸽子。鸽子可以学习随机的二进制标签，与图像相关联的完全任意的二进制标签。它们也能学会识别特定的人。实际上，那里的四张图像中，右边的两张都有同一个长头发、穿靴子的女孩。这就是其中一些实验中的一些图像的工作方式。所以，无论是随机标签还是有意义的标签，鸽子都能学会。它们可以在几分之一秒内做出反应，并正确地说出，例如，我以前见过这张图像吗？这种识别熟悉事物的能力，我们现代计算机仍然难以做到。

所以，我将花更多一点激励性的时间来说，鸽子和超级计算机之间有什么区别？

首先，我们来谈谈硬件差异。我在这里的基本观点是，鸽子在识别和极快响应图像这类任务上胜过超级计算机。那么，是因为鸽子实际上拥有更多的硬件吗？我们来数一下。

鸽子大脑中的设备数量大约是10^11个神经元。每个神经元的突触数量可能在10^3左右。所以可能有10^14个突触。你可以把神经元或突触看作是基本设备，有点像晶体管。超级计算机中的设备数量，比如说，10^10位内存。如果你有1GB内存，当然，它们的内存远不止这些。但我不想不必要地不公平，因为所有这些内存都只是闲置在那里，完全没有被使用。CPU本身呢？CPU。也许一个CPU里有一百万个左右的设备。所以你可以说，是的，鸽子打败了超级计算机，因为它有更多的设备。但是鸽子的设备速度慢。所以我们最好考虑一下时钟频率。

对于一台超级计算机，时钟频率大约是1000 MHz。这意味着我们处于大约每秒10^15或10^19次设备操作的量级。我刚刚查了一下关于超级计算机的最新消息，那是拥有大量CPU和海量内存的超级计算机。它们每秒可以进行10^16次浮点运算。这是量化超级计算机能力的两种不同方式。一种是硬件方面，每秒有多少硬件设备操作。另一种是输出方面，整个超级计算机每秒能进行多少次浮点运算。今天的顶级超级计算机每秒可以处理10^16次浮点运算。

![](img/2471e5004fce99c2a3d81a0d555558b7_2.png)

那么鸽子呢？时钟频率对应10毫秒的时钟时间。我大致这么说，因为单个神经元在被激发时每秒可以放电100次。你可以认为硬件的某些部分可能有更快的时钟时间。也许，你知道，我支持突触可以进行一些非常快速、有趣的事情的观点。但我们就用这个数字吧。这意味着如果你把神经元作为你的操作设备，你每秒有10^13次操作。或者，如果你把突触看作是基本设备，每秒有10^16次操作。

就每秒操作次数而言，超级计算机比鸽子更有优势。就实际输出每秒浮点操作次数而言，如果你把突触看作是进行浮点运算，它们与单个鸽子大脑中的超级计算机处于同一水平。所以鸽子大脑具有惊人的计算能力，但就这些数字而言，它实际上并不比超级计算机大。

问题是，字长是多少？所以你是说，对于这些浮点运算，超级计算机中的浮点运算有多大？可能是一些类似于32位整数乘以另一个32位整数之类的操作，并且做得绝对完美。而一个突触，如果幸运的话，可能每次操作处理2或4位。我猜，在10毫秒的时间尺度上，你很难得到超过2或4位的信息，因为一切都受到泊松噪声的支配，通道是打开还是关闭，而且你只有少量的整数个通道。所以，是的，超级计算机或今天的超级计算机的操作精度或字长比鸽子大。它们处于同一量级。并不是说鸽子拥有远多于它的资源。

![](img/2471e5004fce99c2a3d81a0d555558b7_4.png)

![](img/2471e5004fce99c2a3d81a0d555558b7_5.png)

也许鸽子在这种图像识别任务上表现好得多，是因为它以不同的方式组织其硬件。超级计算机本质上是标准串行计算机，只是以略有不同的方式连接起来。它们有一个或多个CPU。如果你想在标准计算机中存储一个新记忆而不丢失任何旧记忆，你必须有一些新的、未使用过的硬件。旧记忆和新记忆之间完全没有联系。你只是把它们放在另一个地方。你需要知道它们在哪里。与此同时，鸽子大脑是并行的。它们具有高连接性。一个典型的神经元可能连接到大约1000个其他神经元，而超级计算机中的一个典型晶体管可能连接到大约3个或10个其他晶体管。计算，无论它是如何完成的，肯定是分布式的。当鸽子学习一些新记忆时，新记忆存储在它用于其他所有事情的同一硬件中。我们不知道是如何做到的，但它们存储在相同的硬件中。而且，重要的是，鸽子大脑对硬件损坏具有鲁棒性。所以你可以拿走鸽子的大脑，或者你的大脑，你可以损坏成千上万个神经元，第二天你仍然可以继续运作。好吧，每当你喝酒时，你就在这样做。你杀死了一些神经元，但你仍然继续下去。相比之下，超级计算机，如果你把手伸进去，说，哦，去摧毁这台超级计算机中1%的晶体管，它会没事的，不是吗？你会发现它并不好。

![](img/2471e5004fce99c2a3d81a0d555558b7_7.png)

所以这里有一个假设。假设是，也许鸽子和超级计算机在这种现实世界图像识别任务上性能的差异，是由于计算风格的差异。所以也许计算风格是关键。所以，如果我们对解决计算机仍然无能为力的问题感到兴奋，也许我们应该摆脱串行计算，也许我们应该转向真正的并行计算。也许我们应该研究使用具有高连接性、分布式且以完全不同的方式工作的硬件的方法。

所以在下一讲中，我们将回到存储和回忆记忆的任务，并展示一种使用简单神经网络模型实现它的方法。所以，我刚才花了15分钟来激励你们为什么应该对并行分布式处理感兴趣，这是一本古老的神经网络圣经的名字。

我要讨论的这种并行分布式处理，是使用我们称之为神经元的基本设备进行的并行分布式处理。所以我们将有一个单一的神经元。它将是一个有输入和输出的东西。然后我们将以各种方式将它们连接起来。一种连接方式是前馈，看起来像这样。你有一些输入进入。一些神经元。然后它们连接到更多的神经元。我们可以在这些边上加上箭头来显示方向。然后可能还有另一个神经元，然后输出一些东西。这就是一个前馈网络。它描述起来很简单，因为它是确定性的。你只需输入一个输入，这些神经元可以计算，这些可以计算，这些可以计算，然后你就完成了。

另一种连接方式是反馈网络，你说，让我们允许每个人的输出成为其他人的输入。就像那样。然后动态取决于你如何定义所有神经元相互作用的方式，也许会发生更令人兴奋的事情。

所以，让我们告诉你更多关于单个神经元的信息。然后，在今天的讲座中，我将向你介绍前馈网络。下一讲将讨论反馈网络。

那么，单个神经元是如何工作的呢？单个神经元有输入、输出和一些参数。这些参数通常称为权重。参数潜伏在输入和神经元主体之间。它是这样工作的，非常简单。如果权重是 W1, W2, ..., Wk，输入是 X1, X2, ..., Xk。神经元所做的是使用它自己的权重将其输入的加权和相加。它再加上一个叫做 W0 的数字，你可以把它看作是一个额外悬挂的权重，连接到一个始终设置为1的虚拟输入。这个权重有时也被称为神经元的偏置，这是当所有输入为0时它的激活值 A。

计算完激活值 A 后，它通过一个函数计算其输出，有时也称为神经元的活动。这个函数可以是 1/(1 + e^{-a})，看起来像那样。或者可以是双曲正切函数，看起来几乎完全一样。或者可以是一个阶跃函数，像那样。我们可以顺便注意到，我们在不知不觉中已经遇到过神经元了。它们出现在上一讲中，当时我们讨论了自旋系统的变分方法，那里有一些叫做 a 的量，它们是自旋平均活动的加权和。所以 X 是自旋指向上的程度，a 是激活值，然后我们把它通过 tanh 函数来确定指向上的程度。所以实际上我们已经见过这个了，在自旋系统的变分自由能最小化中。

我们之前也见过这个。我可能在讲座中问过的一个问题，当然书里也有，是如果有两个高斯分布，你不知道一个数据点来自哪一个。它出现在 X1, X2 空间中。假设这个高斯分布标记为类别1，这个标记为类别2。如果我给你一个来自这两个高斯混合分布的数据点 X，请告诉我，你认为 C 等于1的概率是多少？这个问题的答案是 f(a)，其中 f 是这个函数，a 正是这种形式，一些 WkXk 的和加上 W0。所以它正是这种形式。这就是问题的答案：我处于哪个类别的推断？答案是，嗯，如果你在这条线上，你有50%的机会在类别1，在这条线上机会更高，在这条线上机会更低，等等。所以问题的答案随着 X1 和 X2 的线性等高线而变化，假设这两个高斯分布具有相同的协方差矩阵。所以我想我们在早期讨论聚类时做过这个练习。

所以，我们已经遇到过神经元了。我们当时只是没有称它们为神经元。我们称它们为简单的数学函数，这正是它们。

所以，让我们熟悉一下这里使用的新术语。让我们玩一下一个神经元。

![](img/2471e5004fce99c2a3d81a0d555558b7_2.png)

![](img/2471e5004fce99c2a3d81a0d555558b7_9.png)

![](img/2471e5004fce99c2a3d81a0d555558b7_11.png)

那么，这是一个神经元的输出图像。顶部的标题，我们可以调暗灯光。标题告诉你这个神经元的三个权重是什么。Y3，嗯，这是因为我们正在玩的这个预测神经元有两个输入。是的，我们将玩一会儿只有一个神经元的前馈网络，它有两个输入，一个输出。和往常一样，这里有一个小的偏置悬挂着，你可以把它看作是一个权重 W0，连接到一个始终为1的输入。所以 W1 看这里，W2 看这里。作为 X1 和 X2 的函数，这就是神经元的输出。权重按顺序设置为 -15, 2 和 1。我使用的函数是 1/(1 + e^{-a})，有时称为逻辑函数。现在让我们调整权重。让我们改变偏置的权重。当我们改变神经元的偏置时，函数只是来回移动。等高线的方向没有改变，但函数在移动。现在，让我们改变权重 W2。那会做什么？嗯，它不会改变函数与 X2=0 轴相交的位置，但它确实使函数围绕那个点旋转。现在，让我们改变权重 W1。这将使它围绕红色曲线与 X1=0 轴相交的点旋转。所以现在你可能会想到它围绕屏幕外的一个点旋转。

这是曲面图视图。让我们用等高线图重新做我刚才做的事情。这是等高线图。嗯，让我们调整到合适的大小。嗯，好多了。我正在展示与刚才权重设置为 -15, 2 和 1 时完全相同的函数的等高线图。这里是 0.5 等高线。这里是我忘记的等高线，可能是 0.2 和 0.8 之类的。白线是垂直于红色等高线的表示。所以，如果你愿意，它指向权重向量的方向。这仍然是 X1 轴，这是 X2 轴。我展示了一个向量，在这个方向上与2成比例，在这个方向上与1成比例。所以它与权重向量成比例。严格来说，这些向量，输入空间 X1, X2 和权重 W1, W2，它们彼此处于对偶空间。所以我们不一定应该把它们画在同一个屏幕上。但这就是为什么我拉伸坐标轴，让它看起来是垂直的。好的，让我们重复刚才做的三个权重调整操作。我将调整权重 W0，偏置，这使等高线像这样移动。接下来，我们将调整权重 W2，第三个权重。这使等高线像这样移动。最后，调整权重 W1，这将使整个图形围绕红色线与垂直轴相交的地方旋转。

好的，接下来我要做的是同时改变所有三个权重。我将按一个因子放大或缩小所有权重。我想确认你理解，如果我把这个神经元的所有三个权重都加倍，等高线图会发生什么？和你的邻居讨论一下。希望这一切都非常简单。好的，有人预测如果我把所有三个权重都加倍，等高线图会发生什么吗？有人吗？等高线会移动吗？红线会去哪里？中间等高线会去哪里？移动，保持静止？好的，有点难想出来。它保持静止。只是变得更陡峭。好的，让我们回到演示。

![](img/2471e5004fce99c2a3d81a0d555558b7_7.png)

所以，缩放权重不会改变激活值为0的地方，也就是红线所在的位置。是的。好的，这就是玩一个神经元。接下来我们要做的是讨论仅由一个神经元组成的神经网络的学习。学习意味着你获取一些数据。数据的形式是一组输入-目标对。所以目标大致指定了你希望神经元或神经网络在响应输入时说什么。我将有一个标签 N 来遍历这些数据，学习的想法是我们调整参数，也就是权重，使得当输入 X_n 时，神经网络的输出 Y 接近目标 T_n。

显然，当有人说要让这样的事情发生时，你会对他们说，嗯，等等，请告诉我目标函数，然后我会最小化它。所以我会那样做。目标函数衡量权重有多好，我们称之为 G。开始时，G 将有 n 项，每一项衡量 T 与 y 的接近程度。我的接近度度量将是一个相对熵。

![](img/2471e5004fce99c2a3d81a0d555558b7_13.png)

好的，其中 y_n 是我的简写，表示当你输入 X_n 且权重设置为 W 时的 Y。所以 W 的依赖性潜伏在这里的 y 中。这样，这个目标函数是各项的和。每一项要么看起来像这样，要么看起来像这样，取决于目标是0还是1。所以如果 y 与目标匹配，目标函数给你0。如果 y 与目标不匹配，你会得到一个越来越大的惩罚，你离得越远。你可以把 G 看作是数据的信息内容，从神经元的角度来看，神经元将其输出 y 视为数据的正确概率分布。所以它是数据的信息内容，或者等价地，它是数据的描述长度，假设你将 y 视为正确的概率。

当我说数据时，我有点不精确。我的意思是一组 T。所以如果你已经被告知数据输入在哪里，X 是什么。然后有人说，现在，我要告诉你 T。如果你使用完美的算术编码器或其他压缩方法，使用这个神经元对它们进行编码，这就是描述长度。好的，让我们展示一个例子。

所以这是我们的单个神经元。这是一些数据。我们可能正在谈论的数据类型可能是我们试图经营一个蔬菜分拣工厂。鸽子罢工了，因为它们意识到自己比超级计算机强，它们希望获得与超级计算机相同的工资。所以现在我们试图用一个简单的神经元来替代鸽子。鸽子的工作是区分土豆和胡萝卜，或者葡萄干和鹅卵石，无论我们在做什么。我们有一些标记数据。所以我们测量了10个物体的 X1 和 X2。其中5个是葡萄干，5个是鹅卵石。所以类别在那里用颜色标出。我们知道这10个东西各自属于哪个类别。我们有 X1，我们有 X2。我刚才在输入空间 X1 X2 中向你展示的是神经元如何产生一个函数，它是一个斜坡，一个软的 S 形斜坡。

那么什么是学习？学习就是关于 W 最小化像这样的目标函数。那涉及到什么？嗯，如果你对最小化东西有任何理智，你会说，计算梯度容易吗？答案是，是的，计算梯度很容易。所以你得到梯度，我们称之为 G，也就是 dG/dW。然后你基于梯度做一些合理的下坡操作。你可以用梯度做的最简单的事情叫做梯度下降。这不是一件明智的事情，因为它不满足你想要的协方差，即向量空间及其对偶的适当处理。所以这混淆了向量空间及其对偶，这是一种犯罪，但很多人这样做。所以你只需计算出梯度，然后你说，让我们沿着梯度的方向迈出一步。所以，那就是梯度下降。这个参数在神经网络界被称为学习率。这是一个你必须以某种方式设置的临时参数。

很容易证明，梯度可以写成所有示例的 (T_n - y_n) * X_n 的和。你可以把这看作是神经元当前在第 n 个示例上犯的错误乘以输入。所以为了计算梯度，你输入每个输入，查看输出和目标之间的误差，然后将误差乘以输入得到梯度，最后把它们加起来。这个通过微分得到的简单操作有时被称为反向传播。所以反向传播是神经网络界对微分的称呼。反向传播是一种在神经网络中高效进行微分的特定算法。

好的，如果我们这样做会发生什么？我们从一些随机选择的权重开始神经元，权重会改变。这是梯度下降在起作用。我向你展示三个权重值，它们都爆炸了。在 W1 W2 空间中发生的事情看起来像这样。权重向一个方向漂移，然后向另一个方向漂移。所以当下坡时，下坡的方向改变了。发生了什么？嗯，这是数据。这是我随机选择的初始权重向量。所以它在区分蓝色和黄色方面做得不是很好。你进行梯度下降，权重改变，红线所在的位置改变，它旋转，然后它改变，再旋转一些。权重变得越来越大。经过40000次迭代，权重就到了那里。

好的，这就是学习。你可能会说，嗯，我不满意。也许我可以做得比那更好，因为刚才，在我们达到这个最终的优化权重设置之前，我们设法找到了一个可以放置悬崖完全分隔黄点和蓝点的地方。刚才，当我们进行到一半时，答案看起来更合理一些。这个现在说，好吧，你给我另一个例子。我会为你分类。无论它出现在哪里，你都会说，我99.999%确定它是蓝色或黄色，因为我们有了这个极其陡峭的悬崖。你可能会说这很愚蠢。我们不喜欢这个结果，它设法最小化了目标函数，但也许我们选错了目标函数。

所以，如果我们不喜欢这个，我们该怎么做？明智的人会改变目标函数。所以现在我们要做的是带有正则化的学习，它说我不喜欢这些巨大的、陡峭的、尖锐的函数变化，或者在神经网络语言中，我们称之为权重衰减，因为当权重大时，你会得到陡峭的函数。所以我们添加一个额外的项，目标函数被改变。所以它不仅仅是 G。新的目标函数 M 是 G 加上一个额外的项，它说我不喜欢大的 W。所以目标从“请尽可能好地拟合数据”变成了“请拟合数据，但如果权重大，就加上惩罚”。所以 E_W 被定义为 1/2 * 所有 W_k 的平方和。好的，我们这样做。

现在发生的是这个。我设置了这个参数 alpha，如果你来自统计学界，它要么被称为正则化器，要么被称为正则化常数。E_W 被称为正则化器。或者如果你在神经网络领域，alpha 被称为权重衰减率。当你加上那个，你的梯度会发生什么？嗯，现在只是一个额外的项。所以 G + alpha * E_W 的梯度是这一堆加上 alpha 乘以你的权重。所以在那上面下坡意味着你有一个负的 alpha * W 项。

当你进行最小化时发生的事情在这里显示为权重的变化。它们过去会爆炸。但现在它们稳定下来，不会爆炸，我们没有得到一个不断变陡的函数。所以所有权重都稳定下来了。这是过去发生的情况。那时我们没有正则化器。橙色曲线显示一切都在爆炸。这是在 W1, W2 空间中发生的情况。我们下坡，下坡，然后基本上沿着轨迹停止。所以我们在权重空间中遵循几乎相同的轨迹。但我们在这个新目标函数的最优点处停止。这是我们过去经过40000次最速下降迭代后要去的地方。现在，当我们打开权重衰减时，我们下坡并最终到达这个地方。

我该如何调整这个的大小，让它看起来合理？好的，你可能会看着那个说，好，那更好。因为现在它不会做出完全不合理的、过于自信的答案。例如，如果你说，对于另一个到达这个位置的点，它在蓝色类别中的机会是多少？它会说大约90%，而不是说我100%确定。如果你在这个黄色点得到另一个，它会说50/50。你可能会说，是的，基于我们这里关于鹅卵石和葡萄干的数据，这不是一个糟糕的答案。

你可能仍然不满意。如果你不满意，请记住这一点。我们稍后会回到这个问题。

![](img/2471e5004fce99c2a3d81a0d555558b7_9.png)

现在，我们将转向更大的网络。然后当我们有更多想法时，再回到单个神经元。

![](img/2471e5004fce99c2a3d81a0d555558b7_11.png)

让我再告诉你一些关于单个神经元的事情，然后我们再回到这个学习业务。

首先，我可以给你一个使用单个神经元解决实际问题的例子。我谈到鹅卵石和葡萄干。这里是一个手写体问题。你可以获取一个手写数字的数据集，这些数字已被数字化为黑白点，在256维空间中。所以你可以获得几千个“2”和“3”的例子，并使用我刚才描述的算法来得到一个单个神经元，它有256个输入，257个权重，其输出可以被视为我正在看的东西是“2”或“3”的概率。当你这样做时，这就是优化后的权重看起来的样子。你制作的分类器的错误率约为10%。所以在这个手写数字数据库上，它正确分类了90%。所以它并非完全无用。因此，即使是单个神经元，也可以是解决一些简单推理问题的有趣、快速且粗糙的方法。

![](img/2471e5004fce99c2a3d81a0d555558b7_15.png)

![](img/2471e5004fce99c2a3d81a0d555558b7_17.png)

你可以问关于单个神经元的一个问题是，你可以将其视为一个通信信道。这只是一个小插曲。如果你感兴趣，书里有一整章关于这个。思考这里发生的事情的一种方式是，哦，我并不真的相信神经元的输出是正确的概率分布。所以忘记那个想法。但我确实相信神经元是打包数据内容的有用方式。所以，如果你给我一个数据集，包含大量手写数字的例子，我可以将所有内容打包成257个小数字，比如说，一个单个神经元。然后我可以把神经元发送给某人。所以我可以发送权重 W。然后其他人可以尝试重建所有标签是什么。我假设他们可以获得相同的输入。当他们获得这些输入时，他们可以使用神经元来重建输出是什么。

![](img/2471e5004fce99c2a3d81a0d555558b7_19.png)

所以这是一种信息论的观点，关于单个神经元。你可以问这个问题：如果我将神经元视为一个信道，那个信道的容量是多少？一个神经元可以存储多少比特？你可以形式化这个问题。书中的章节做了这个。有一个相当有趣、非平凡的答案，它看起来像这样，假设这些 X 处于一般位置。所以它们要么是随机分布的，要么至少彼此不是有理共线的。如果 X 处于一般位置，那么很有可能你可以用你的神经元正确学习世界提出的任何随机标记。很有可能你可以学会那个，只要标签数量 N 小于或等于输入数量的两倍，也就是参数数量的两倍。

我刚才说了什么？我说一个具有 K 个输入的单个神经元几乎肯定可以记住 2K 个随机二进制标签。这意味着一个神经元在权重中可以存储多少比特的容量。每个权重 W1 到 Wk，每个都是一个实数。但有效地，如果你以这种方式使用神经元，只是学习一些示例上的标签然后重现它们，你可以可靠地通过该信道通信每连接2比特。所以对于许多神经网络风格的应用来说，这是一个非常方便的经验法则，如果你使用神经网络，那么也许你可以在该网络中学习的有效比特数大约是网络中参数数量的两倍。这对于单个神经元是精确正确的，对于其他类型的神经网络也可能正确。但你需要谨慎使用这个想法。

好的，这就是作为通信的学习以及单个神经元的容量结果。我现在想做的是转向稍微更现实、更大的网络。我将向你展示一个神经网络，它有一堆我们称之为隐藏神经元的东西。为简单起见，我将向你展示一个只有一个输入的神经网络，鼠标现在指向的地方。这是偏置单元，如果你愿意的话。这是输出。所以它是一个具有一个隐藏层的多层网络。你从输入开始，这里只是一维的。经过许多神经元。然后你到达你的输出，经过了所有这些神经元。

所以如果 H 是隐藏神经元的数量，这个东西中的参数数量大约是 3H。因为每个神经元有两个输入。然后这个神经元大约有 H 个参数输入。可能更准确地说，是 3H + 1。好的，所以我想更多地探索像这样的神经网络的函数是什么样的。这是具有一个隐藏层的多层感知机的最简单例子。这种多层感知机仍然被广泛使用。所以理解它们很有趣。让我们切换到另一个演示。

![](img/2471e5004fce99c2a3d81a0d555558b7_13.png)

我在这里要做的是展示 H=3 的情况，我将创建一个随机网络。我设置所有权重，有多少个？6，10。我将这个网络的10个权重设置为从高斯分布中抽取的随机值。然后我将更新这些权重，我们将看到随机函数发生了什么。所以你可以得到一大堆函数。有输入变量的函数，这里显示。所以我在水平轴上显示输入变量 X1。垂直轴是来自这个多层网络的输出 Y。好的，那是一堆函数的小电影。我们在做什么？我们只是把三个 tanh 函数相加。它是三个双曲正切函数的加权和。蓝色的东西就是那个。当你对三个任意的双曲正切函数进行加权和时，你得到的就是一个例子。

所以现实世界中使用的大多数神经网络的另一个名称是双曲正切函数的加权和。但这听起来不那么性感。好的，这是一个有10个隐藏神经元的。蓝线是那10个双曲正切函数的和。所有那些在 -1 和 +1 之间变化的东西本身就是双曲正切函数，是那10个神经元正在计算的。好的，我们随机化权重，它变得起伏不定。这向你展示了单输入单输出神经网络的典型情况。增加到25。这是一整堆随机函数。它看起来像什么？它看起来像一根曲线板。所以思考神经网络的另一种方式是，这些前馈多层网络只是曲线板。然后当你带来一些数据并说，请将这个神经网络拟合到数据，你只是在说，请通过数据画一条曲线板。你知道我所说的曲线板是什么意思。它是一种弯曲的东西。所以你买一些弯曲的、有弹性的东西，它会给你一条平滑的曲线。我不知道，也许现在不卖了。但它们过去在高中很流行，这样你就可以画出一条弯曲平滑的曲线。

![](img/2471e5004fce99c2a3d81a0d555558b7_21.png)

![](img/2471e5004fce99c2a3d81a0d555558b7_23.png)

好的，接下来我们能做什么？我刚才向你展示的有大约25个隐藏单元的演示，我从高斯分布中随机抽取权重。我没有告诉你标准差是多少。现在，我将向你展示那些标准差。刚才它们都相等。这个屏幕现在向你展示输入权重、偏置权重和输出的标准差，它们现在被涂成红色、紫色和灰色。三条蓝线向你展示了从高斯分布中抽取的三个随机样本。好的，你可以看到那三个函数。现在我要做的是调高和调低标准差，这样你就可以看到缩放随机权重、使它们变大或变小对随机函数的影响。首先，让我们调高输入权重的标准差。你看到函数变得更起伏，它们有点收缩了，因为我们把所有双曲正切函数都变得更陡峭了。

好的，如果我们降低所有输入权重的标准差，你会得到更宽的函数，局部看起来就像直线。好的，现在让我们调整偏置。偏置，我们可以有很大的标准差。现在我们有很多起伏，或者我们可以有很小的标准差。现在我们得到的东西看起来像是大约3或4个双曲正切函数的和，即使它是一个25个函数的和。所以偏置的标准差决定了你得到多少起伏。但它不影响起伏的特征长度尺度。那是由输入的标准差控制的。最后，如果我们调高或调低输出的标准差，这是微不足道的。函数只是变得更高，垂直方向，或更低。

好的，这就是那三个标准差的作用。接下来我们该做什么？让我们给自己一些数据。就像单个神经元一样。我们计算梯度，并对该梯度进行最速下降，最终我们能够完美地区分五个黄色鹅卵石和五个蓝色葡萄干。我们可以对看起来像这样的数据集做同样的事情。这里有五个数据点。我们可以定义一个新的目标函数，它不是这里的 G，而是实数的一个度量，衡量输出与目标的接近程度。所以我们有一个像这样的神经网络函数，称为 Y。你有一些像这样的目标值。这些是误差。这是 T_n，这是 Y_n。我们可以定义 E_D 等于 1/2 * (T_n - Y_n)^2 的和。你还能有什么其他误差度量？嗯，显然，还有其他度量。但这是一个标准做法。人们经常求和平方。这取决于 W，通过 Y_n，它们都依赖于权重。

![](img/2471e5004fce99c2a3d81a0d555558b7_25.png)

![](img/2471e5004fce99c2a3d81a0d555558b7_27.png)

好的，我们用它做什么？我们说这是我们的目标函数。请使输出尽可能接近所有目标。你计算梯度 G，根据定义是 dE_D/dW，很多人对该目标函数进行最速下降。所以他们沿着梯度下坡。好的，请调暗灯光。

经过一次最速下降迭代后发生的情况是这样的。我从一个由蓝线表示的25个隐藏单元神经网络开始。我们开始下坡。经过一次迭代，它看起来已经很有希望了。然后你进行另一次迭代，进行2000、3000、8000、9000次迭代。你最终得到这个屏幕上的东西，它完美地穿过了所有五个数据点。当你说，哈，太棒了。我学会了这个函数。从五个数据点，或者也许你会说，不，垃圾。这很愚蠢，因为也许你正在处理一个噪声问题，实际上你认为真正发生的是有一个真实的基础函数，而数据点与它相差一些加性噪声。然后你会对有一条完美穿过噪声每一个细节的绿线感到相当恼火。

如果你以这种方式不满意，你会怎么说？嗯，你会说，我想做带有正则化的学习。所以你再次改变目标函数。你说，我需要某种正则化，因为我不喜欢这个起伏的函数，因为它比我预期的更起伏。对它不会超级起伏的期望，也许可以用一个正则化器来表达。你可以回到我们刚才有的那个好的旧正则化器 E_W。你可以加上 alpha 乘以它，或者也许你更复杂一些。你说，实际上，我应该把权重分成几类，因为记住刚才，这个典型函数的起伏。它的长度尺度是由输入权重的标准差决定的。它的起伏数量与偏置的标准差有关。所以实际上，一个典型函数中的起伏数量大约是 sigma_bias / sigma_in。它有一个垂直长度尺度，与 sigma_out 有关，也许乘以 sqrt(H)。所以，由于输入权重的大小、偏置和输出以如此不同的方式影响事物，为这些东西中的每一个设置三个正则化器可能是有意义的。你可以定义 E_W1 为权重的平方和，对偏置求和。E_W2 为权重的平方和，仅对输入权重求和。E_W3 可以是 1/2 * W_k^2 的和，仅对输出权重求和。这将是一个维度上更有效的做法。然后你可以说我的正则化器是 c 从1到3求和，alpha_c * E_Wc。然后你就在做维度上有效的事情，而不是假装它们都有相同的正则化常数。

当你这样做时，请调亮灯光。数据误差不会像在没有正则化器时那样变得越来越小，因为你设法完美地拟合了数据，权重爆炸了。你可以加上，你可以设置所有 alpha，比如说为1，作为开始。所以 Alpha1, Alpha2 和 Alpha3 都设置为1。现在你下坡。第一次迭代看起来和上次完全一样。但你继续下坡，它只是静止不动。你进行10000次迭代，它没有爆炸，也没有变得非常起伏来拟合所有噪声的每一个细节。你可能会说，也许那更好。但你仍然可能有点不满意，因为，你，这真的是问题的答案吗？问题是什么？嗯，无论如何，我们现在最小化的目标函数称为 M。M 等于 E_D 的倍数加上所有 Alpha_c * E_Wc 的和。这就是我们现在最小化的目标函数。那个目标函数已经下降并稳定下来。我们找到了它的一个最优点。权重没有爆炸。

好的，接下来是什么？我怀疑接下来是对我们刚才所做的事情的解释。让我们看看。是的，好的，让我们回到单个神经元。所以，单个神经元看起来像那样。我想鼓励你对这个感到不满意，并对我们刚才有的那个东西也感到不满意，然后我们会解决你的不满意。

为什么你应该对这个结果感到不满意，即训练了一个单个神经元作为替代鸽子来区分鹅卵石和葡萄干？嗯，想一想，嗯，一个不满意的来源可能只是，等等，这完全取决于 alpha 的值。当你将 alpha 设置为不同的值时，你会得到不同的答案。这里我将 alpha 设置为0.1，你得到一个不同的解，这里你将 alpha 设置为1，你又得到一个不同的解。所以一个问题是如何设置正则化常数。但另一个问题，即使你对用某种魔法将其设置为此值感到满意，你可能会说。嗯，我愿意相信这里关于的预测与这些数据相当吻合。我认为，你知道，说20%是共享的，80%是共享的，看起来是可读的。是的，那没问题。但我不相信这里的这个外推。所以如果你看这些点，称为 A 和 B。你对这个想法感到满意吗，即我们同样有信心 A 大约有90%的可能性属于黄色类别，B 也有90%的可能性属于黄色类别？这合理吗？如果你不满意，你可能对作为推断的学习观点感兴趣。

所以我们讨论了作为通信的学习。现在我们将讨论带有正则化和权重衰减的学习，作为推断。所以如果有人最小化某个东西，我倾向于将其指数化，并把它解释为一个概率，不管你喜不喜欢。所以如果有人取 M(W)，即 G(W) 加上 alpha * E(W)，并最小化它。我会说，好的，我将以以下方式解释它。给定你做出的隐含假设，权重的后验概率是 e^{-M(W)} / Z。这等于给定 W 的数据概率与给定你所有假设的 W 的先验概率的乘积。所以这是给定数据和其他假设的后验分布。这需要用某个东西进行归一化。隐含地，如果你最小化这个东西，你就是在给定这些假设下找到最可能的 W。给定 W 的数据概率是 e^{-G(W)}。W 的先验概率是 e^{-alpha * E(W)} 除以某个东西。

所以这是对人们已经在做的事情的一种解释。它没有改变任何东西，但可能增加一些有用的视角。所以如果有人最小化这个目标函数，他们隐含地假设确实存在一个基础函数 Y，通过抛掷一个偏置为 y 的弯曲硬币来生成数据。所以这就是对这个的解释。数据来自 y，使用一个偏置为 y 的弯曲硬币。用于惩罚巨大权重的正则化器，因为我们决定不喜欢巨大的权重，被解释为关于权重的先验信念，它说，如果 E(W) 是 1/2 * w^2，例如，它说 W 的先验概率是一个均值为0、标准差 sigma_w^2 = 1/alpha 的正态分布。所以这个参数 alpha，刚才还是一个令人头疼的任意正则化常数，现在被解释为先验分布的方差。

这种解释可以被忽略，也可以被利用。利用它说，好吧，让我们从字面上理解它，然后我们可以做一大堆新事情。例如，我们实际上可以问，将推断转化为预测的正确方法是什么？如果有人要求预测，你应该怎么做？嗯，如果你相信这个模型是正确的，并且被要求预测我们刚刚在位置 X_{n+1} 观察到的下一个东西属于类别1的概率是多少？给定我们到目前为止看到的数据和所有其他假设，屏幕上这个新点，比如说 A 或 B 或任何地方，它是土豆或胡萝卜或葡萄干或鹅卵石的概率是多少？嗯，这个问题的答案不是通过说“请找出最小化这个的 W，然后用它来做预测”来找到的。答案是通过边缘化来完成的。所以，如果你相信所有假设，正确的答案是你应该对 W 的后验概率进行边缘化，乘以预测，也就是在 X_{n+1} 处使用参数 W 评估的 y。所以这实际上是你应该做的。如果你相信你一直在隐含做出的假设。

![](img/2471e5004fce99c2a3d81a0d555558b7_29.png)

![](img/2471e5004fce99c2a3d81a0d555558b7_31.png)

![](img/2471e5004fce99c2a3d81a0d555558b7_32.png)

这是一个简单函数在讨厌的红色分布下的平均值的例子。我们知道有三种方法来捕捉这头大象。我们可以使用蒙特