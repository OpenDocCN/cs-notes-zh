# 宾夕法尼亚大学《Python和Java编程入门1-2｜Introduction to Programming with Python and Java》中英字幕 p87 087_03_02_代码练习-最大值最小值函数.zh_en -BV13E421M7FF_p87-

Let's do an exercise with tus。Create a max and min function that returns a tuple containing the max and min of a given list。



![](img/30333ab671cd12ab7ce9694b46dd3a9b_1.png)

So first， let's create the max and min function itself。 So define max and min。

It's going to take a given list。Returns。Max and men of given list。

And all this does is get the max from the list using the max function。

 Python's built in max function。 get the min。From the list using pythons built in min function and return both of these at the same time inside of parentheses。

 And what this does is it creates a tuple because it's inside of parentheses。

 The first value in the tuple is the max of the list。

 The second value in the tuple is the min of the list。The entire thing is inside of parentheses。

 This returns a tuple。Return tuple containing max and min of。List。Now to run this。

 we're going to create a main function in our program。To call this function， we need a list。

 So let's create one。List 1 equals。10 negative1。😔，Negative 34，56。

Then we're going to call Max and Min。With the given list。That function， Maxim Min returns a tuple。

So I'm going to assign that to a variable， max and min。This says the return value from max and min。

 Let's print the type of that print type of max and min。First， take a look at the type。

To run our main function。If the name is main。Renmin。



![](img/30333ab671cd12ab7ce9694b46dd3a9b_3.png)

Let's run this。So I see the type of that return value， Max and min is a tuple。

Now I need to get the values from within that tuple。



![](img/30333ab671cd12ab7ce9694b46dd3a9b_5.png)

So let's first get the max。 We'll say max is equal to max and min。Index 0。 So this is a tuple。

 I know Max and min is a tuple。 I'm going to get the first value inside of the tuple， using index 0。

Get Max from Tple。And then we'll get the min， which is max and min index 1。This is get min from Tple。

Let's print those， max。An min。So this will print the max and min from that tuple。



![](img/30333ab671cd12ab7ce9694b46dd3a9b_7.png)

So 56 and negative 34。 So when I call Max and min。With this given list，10 negative 1， negative 34，56。

 the max is 56。That looks correct。And the min is negative 34， also looks correct。



![](img/30333ab671cd12ab7ce9694b46dd3a9b_9.png)

Another way to do this。Is when calling the function。So we'll say another way to do this。

Double variable assignment。We can do something like Max and min。With the given list。We can say M。M X。

Version 2， MI N， version 2。Equals and what is this。 This is a double variable assignment。

 I'm basically assigning the tuple values directly to variables。

 So max version 2 is going to be assigned to the first value inside of the tuple that's returned from this function and min version 2 is going to be assigned to the second value inside of the tuple that's returned from this function。

These should look the same。So we'll say max version 2， min version 2。

So this is double variable assignment。To get tuple values directly。



![](img/30333ab671cd12ab7ce9694b46dd3a9b_11.png)

Sort of eliminating a step here。Again，56 and negative 34 by assigning the variables directly to the tuupple values。



![](img/30333ab671cd12ab7ce9694b46dd3a9b_13.png)