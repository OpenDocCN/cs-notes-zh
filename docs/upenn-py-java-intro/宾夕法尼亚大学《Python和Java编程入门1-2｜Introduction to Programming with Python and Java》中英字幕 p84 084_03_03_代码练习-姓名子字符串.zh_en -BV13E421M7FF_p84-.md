# 宾夕法尼亚大学《Python和Java编程入门1-2｜Introduction to Programming with Python and Java》中英字幕 p84 084_03_03_代码练习-姓名子字符串.zh_en -BV13E421M7FF_p84-

Let's do an exercise。Set a variable name to the value of your first and last name。

Then print a sub string containing just your first name without counting the letters in your first name。

As a hint， use the built in string index method to locate the single space between your first name and your last name。



![](img/455342ef0f07a17f5f1426b2130a4999_1.png)

So let's create a variable name。And I'm going to store my name。Brrandon space。Krarkowski。

And then I want a sub string that only contains my first name。

 So I need to identify the index of this single space。Get index of single space between first name。

And last name。So we'll say name dot index。 What is the index of the first single space。

We'll store that in a variable first space， and then I'll use that in my slice。

 So get substring of name using slice。Print name， square brackets。 I want to slice of the string。

 I'm going to start at character 0。That's the first character in the string， all the way up to。

The index first space。And this should be。First， name only。We'll run that。 And we're left with。

Brandon。

![](img/455342ef0f07a17f5f1426b2130a4999_3.png)