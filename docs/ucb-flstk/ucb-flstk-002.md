# 002：HTML基础入门

![](img/c8707bd7bb5380f1164d5044997b9e6b_1.png)

在本节课中，我们将学习网页开发的基础——HTML。HTML是构建网页的骨架，负责创建网页上的文本、图片、链接等所有可见元素。我们将从HTML的基本结构开始，逐步学习各种常用标签的语法和用法，并通过一个动手实践环节来巩固所学知识。

---

## HTML概述与基本结构

HTML，全称超文本标记语言，是网页开发的三大基石之一。它的核心作用是定义网页的结构和内容。CSS负责样式和美化，JavaScript则负责添加交互功能。

一个标准的HTML文档包含以下基本骨架结构：

```html
<!DOCTYPE html>
<html>
  <head>
    <title>页面标题</title>
    <!-- 元数据，如页面描述、关键词等 -->
  </head>
  <body>
    <!-- 网页的主体内容在这里编写 -->
  </body>
</html>
```

*   `<!DOCTYPE html>`：声明文档类型，告诉浏览器这是一个HTML5文档。
*   `<html>`：根元素，包裹整个HTML文档。
*   `<head>`：头部，包含页面的元信息（如标题、字符集、引入的CSS/JS文件链接），这些内容不会直接显示在页面上。
*   `<body>`：主体，包含所有会在浏览器中显示的内容，如文本、图片、列表等。

在VS Code等编辑器中，输入 `!` 然后按Tab键，可以快速生成这个基础结构。

---

## HTML标签语法与常用文本标签

上一节我们介绍了HTML文档的整体结构，本节中我们来看看构成内容的基本单元——标签的语法，以及如何创建文本。

HTML标签通常成对出现，由开始标签和结束标签组成，内容写在两者之间。语法如下：
`<标签名>内容</标签名>`

以下是创建文本的常用标签：

*   **标题标签 (`<h1>` 到 `<h6>`)**: 用于定义标题，`<h1>` 最大，`<h6>` 最小。
    ```html
    <h1>这是一级标题</h1>
    <h6>这是六级标题</h6>
    ```
*   **段落标签 (`<p>`)**: 用于定义段落文本。
    ```html
    <p>这是一个段落。</p>
    ```
*   **换行标签 (`<br>`)**: 强制换行，是一个自闭合标签（没有结束标签）。
    ```html
    第一行<br>第二行
    ```
*   **水平线标签 (`<hr>`)**: 创建一条水平分割线，也是自闭合标签。
    ```html
    <hr>
    ```

---

## 创建列表

在网页中，我们经常需要展示项目列表。HTML提供了两种主要的列表类型。

以下是创建列表的标签：

*   **无序列表 (`<ul>`)**: 项目以圆点等符号开头。
    ```html
    <ul>
      <li>项目一</li>
      <li>项目二</li>
    </ul>
    ```
*   **有序列表 (`<ol>`)**: 项目以数字或字母顺序编号。
    ```html
    <ol>
      <li>第一步</li>
      <li>第二步</li>
    </ol>
    ```
*   **列表项 (`<li>`)**: 用于定义`<ul>`或`<ol>`中的每一个项目。

![](img/c8707bd7bb5380f1164d5044997b9e6b_3.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_5.png)

---

## 插入图片与创建链接

现在我们已经可以创建文本和列表，接下来学习如何让网页内容更丰富——添加图片和链接。

### 图片标签 (`<img>`)

![](img/c8707bd7bb5380f1164d5044997b9e6b_7.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_9.png)

`<img>` 标签用于在页面中嵌入图像，它是一个自闭合标签。有两个重要属性：

![](img/c8707bd7bb5380f1164d5044997b9e6b_11.png)

*   `src`: 指定图片的来源。可以是网络URL，也可以是本地文件的相对路径。
*   `alt`: 提供图片的替代文本。当图片无法加载时显示，也对屏幕阅读器用户友好。

![](img/c8707bd7bb5380f1164d5044997b9e6b_13.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_15.png)

```html
<!-- 使用网络链接 -->
<img src="https://example.com/image.jpg" alt="图片描述">
<!-- 使用本地相对路径 (假设图片与HTML文件在同一目录) -->
<img src="myphoto.jpg" alt="我的照片">
```

![](img/c8707bd7bb5380f1164d5044997b9e6b_17.png)

**关于相对路径**：如果图片放在名为 `images` 的文件夹中，而HTML文件在上一级目录，路径应写为 `src="../images/myphoto.jpg"`。`../` 表示返回上一级目录。

![](img/c8707bd7bb5380f1164d5044997b9e6b_19.png)

### 链接标签 (`<a>`)

![](img/c8707bd7bb5380f1164d5044997b9e6b_21.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_23.png)

`<a>` 标签（锚标签）用于创建超链接。核心属性是 `href`，用于指定链接的目标地址。

```html
<a href="https://www.example.com">访问示例网站</a>
```
默认情况下，点击链接会在当前窗口打开新页面。如果希望在新标签页打开，可以添加 `target="_blank"` 属性。
```html
<a href="https://www.example.com" target="_blank">在新标签页打开示例网站</a>
```

![](img/c8707bd7bb5380f1164d5044997b9e6b_25.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_27.png)

你可以将任何内容（包括文本、图片甚至按钮）放在 `<a>` 标签内来使其可点击。

---

![](img/c8707bd7bb5380f1164d5044997b9e6b_29.png)

## 表单与输入控件

![](img/c8707bd7bb5380f1164d5044997b9e6b_31.png)

表单是网页与用户交互的关键组件，用于收集用户输入的数据，如登录信息、搜索查询等。

![](img/c8707bd7bb5380f1164d5044997b9e6b_33.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_35.png)

### 表单容器 (`<form>`)

![](img/c8707bd7bb5380f1164d5044997b9e6b_37.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_38.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_40.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_41.png)

`<form>` 标签用于定义一个表单区域，内部可以包含各种输入控件。

![](img/c8707bd7bb5380f1164d5044997b9e6b_43.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_45.png)

### 输入控件 (`<input>`)

`<input>` 标签是最常用的表单控件，通过 `type` 属性定义其类型。它是一个自闭合标签。

![](img/c8707bd7bb5380f1164d5044997b9e6b_47.png)

以下是几种常见的输入类型：

![](img/c8707bd7bb5380f1164d5044997b9e6b_49.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_51.png)

*   **文本输入 (`type="text"`)**: 单行文本字段。
    ```html
    <input type="text" placeholder="请输入用户名">
    ```
    `placeholder` 属性提供输入提示。
*   **密码输入 (`type="password"`)**: 输入内容会被隐藏为圆点。
    ```html
    <input type="password" placeholder="请输入密码">
    ```
*   **单选按钮 (`type="radio"`)**: 用于多选一的情况。相同 `name` 的 radio 为一组。
    ```html
    <input type="radio" name="gender" value="male"> 男
    <input type="radio" name="gender" value="female"> 女
    ```
*   **复选框 (`type="checkbox"`)**: 用于多选多的情况。
    ```html
    <input type="checkbox" name="hobby" value="reading"> 阅读
    ```
*   **提交按钮 (`type="submit"`)**: 用于提交表单数据。
    ```html
    <input type="submit" value="提交表单">
    ```
    `value` 属性定义了按钮上显示的文字。

![](img/c8707bd7bb5380f1164d5044997b9e6b_53.png)

### 按钮标签 (`<button>`)

`<button>` 标签用于创建可点击的按钮，通常与JavaScript配合实现功能。也可以放在 `<a>` 标签内作为链接按钮。

![](img/c8707bd7bb5380f1164d5044997b9e6b_55.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_57.png)

```html
<button>点击我</button>
<!-- 作为链接按钮 -->
<a href="https://example.com"><button>去往示例网站</button></a>
```

![](img/c8707bd7bb5380f1164d5044997b9e6b_59.png)

---

![](img/c8707bd7bb5380f1164d5044997b9e6b_61.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_63.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_64.png)

## 动手实践：构建你的第一个HTML页面

![](img/c8707bd7bb5380f1164d5044997b9e6b_66.png)

![](img/c8707bd7bb5380f1164d5044997b9e6b_67.png)

理论部分已经介绍完毕，现在让我们通过一个完整的练习来巩固所学知识。请跟随以下步骤，在本地创建一个简单的HTML页面。

1.  **创建项目文件夹和文件**
    *   打开终端，进入桌面：`cd Desktop`
    *   创建一个新文件夹：`mkdir html-practice`
    *   进入该文件夹：`cd html-practice`
    *   创建HTML文件：`touch index.html`
    *   用VS Code打开该文件夹：`code .` （或直接从VS Code界面打开）

2.  **编写基础HTML结构**
    *   在 `index.html` 文件中，输入 `!` 然后按Tab键，生成基础代码。
    *   将 `<title>` 中的内容改为“我的第一个网页”。

![](img/c8707bd7bb5380f1164d5044997b9e6b_69.png)

3.  **在 `<body>` 中添加内容**
    *   添加一个一级标题：`<h1>欢迎来到我的网页</h1>`
    *   添加一个水平线：`<hr>`
    *   创建一个无序列表，列出你的爱好：
        ```html
        <ul>
          <li>听音乐</li>
          <li>阅读</li>
          <li>运动</li>
        </ul>
        ```
    *   添加一个换行 `<br>`，然后创建一个指向你常用网站（如百度）的链接：
        ```html
        <a href="https://www.baidu.com" target="_blank">访问百度</a>
        ```
    *   再添加一个换行，然后插入一张图片（可以从网上找一张，使用图片链接）：
        ```html
        <img src="图片链接地址" alt="风景图片">
        ```
    *   添加一个水平线，然后创建一个简单的登录表单：
        ```html
        <form>
          <input type="text" placeholder="邮箱"><br><br>
          <input type="password" placeholder="密码"><br><br>
          <input type="submit" value="登录">
        </form>
        ```

![](img/c8707bd7bb5380f1164d5044997b9e6b_71.png)

4.  **在浏览器中查看效果**
    *   保存 `index.html` 文件。
    *   在文件管理器中找到该文件，双击用浏览器打开。
    *   尝试与页面交互：点击链接、在表单中输入文字等。
    *   修改代码并保存后，在浏览器中刷新页面即可看到更新。

![](img/c8707bd7bb5380f1164d5044997b9e6b_73.png)

---

![](img/c8707bd7bb5380f1164d5044997b9e6b_75.png)

## 总结

本节课中我们一起学习了HTML的基础知识。我们了解了HTML在网页开发中的角色，掌握了文档的基本结构，并实践了多种核心标签的用法：
*   **结构标签**：`<html>`, `<head>`, `<body>`
*   **文本与排版标签**：标题 (`<h1>-<h6>`)、段落 (`<p>`)、换行 (`<br>`)、水平线 (`<hr>`)
*   **列表标签**：无序列表 (`<ul>`)、有序列表 (`<ol>`)、列表项 (`<li>`)
*   **媒体与链接标签**：图片 (`<img>`)、链接 (`<a>`)
*   **表单与交互标签**：表单 (`<form>`)、各种输入框 (`<input>`)、按钮 (`<button>`)

![](img/c8707bd7bb5380f1164d5044997b9e6b_77.png)

记住，HTML构建的是网页的“骨架”和“内容”。在接下来的课程中，我们将学习CSS来为这个骨架穿上漂亮的“外衣”，让它变得更加美观。