# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p48 p47_03_spring-vs-spring-boot-vs-spring-mvc -BV1fryaYgEqb_p48-

![](img/1ff2b4114054e23d62d5209253c8bfce_0.png)

Hi there。 Today in this session， I will talk about the differences between spring。😊。

Spring boat and spring embassy。Firstt of all， I'll talk about spring and spring boat。



![](img/1ff2b4114054e23d62d5209253c8bfce_2.png)

As I said， spring is a framework which is the most popular application development framework of Java。

😊，The main feature of the spring framework is dependency injection or IocC with the help of spring framework。

We can develop a loosely coupled application， and it is better to use if application type or characteristics are purely defined。

Whereas the springboard is a module of spring framework allows us to build a standalone application with very minimal or zero configuration and it is better to use if we want to develop a simple spring based application or restful services。

These are the few differences， as I said， spring is a open source lightweight rate framework widely used for developing the enterprise web applications。

Where the spring boot is used to develop the rest APIs。

 most important feature of spring is a dependency injection， but in the spring boot。

 we use it because the rest APIs can be created with a bare minimum piece of code and the auto configuration。

Spring create a loosely coupled application where the springboard create a standalone application。

To run spring application， we need to set server explicitly。

 but Springboard provides the embedded server such as Tomcat or jetty。😊。

String does not provide support for in memoryory database， but string boot does。

Developers boiler plate code for smaller task needs to be implemented by manual integration。

 But spring boot， there is varied reduction in boiler plate code， as I told you。Configuration itself。

 is bare minimum。Apart from that， so many other things are there， for example。

Developers manuallyly define the dependencies for the spring project in Pro dot XimL but Springboard comes with the concept of startutter。

 so when you generate your spring application with spring starter。

 the dependencies are added as in J internally to take care of the downloading。



![](img/1ff2b4114054e23d62d5209253c8bfce_4.png)

Next， if I talk about spring versus spring embassy， spring boot versus spring embassy， as I said。

 spring Bo makes it easy to quickly bootstrap and start developing the spring based rest APIs and it avoids a lot of implementation of the code。

 hides a lot of complexity behind the scene so that developer can quickly get started in develop the spring based application easily。



![](img/1ff2b4114054e23d62d5209253c8bfce_6.png)

Where I talk about spring emVC， which is a Web emVC framework for building Web applications。

 contains a lot of configuration files for various capabilities。For various capabilities。

 and it is an HtTP oriented web application development framework。

Spring boot and spring emss exist for different purposes， so it is not fair enough to compare them。

 but as they are the part of spring spring framework， these are the modules of spring framework。

 so lets just compare them。So spring boot is for packaging the spring based application for creating rest API。

 but spring embassies took create a complete embassy based web application。

String boot provides the default configuration to build spring powerful framework where the spring embassy uses the in house or inbu feature for developing a web application。

 no need to build the configuration manually in the case of Spring boot。But in spring embassy。

 everything gets started from the dispatcher so late you need to configure by your own。

There is no requirement of deployment descriptor manually， but you can have it It is your choice。

 but the spring ems will always require a deployment descriptor。

It avoids the boiler plate code and wraps the dependency together in a single unit。

 But in spring emy， everything gets handled by each dependency separately。😊。

Springboardat reduces the development time and increases the productivity where the spring MSC takes more time to achieve the same。

So stay tuned to learn more about springwood。Rest API's development in the next sessions。

 See you in the next session。

![](img/1ff2b4114054e23d62d5209253c8bfce_8.png)