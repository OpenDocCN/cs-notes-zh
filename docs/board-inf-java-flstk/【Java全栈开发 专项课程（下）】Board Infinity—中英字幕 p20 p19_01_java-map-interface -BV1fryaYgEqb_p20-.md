# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p20 p19_01_java-map-interface -BV1fryaYgEqb_p20-

![](img/d700b94113eebfa5a1afb6b8cb3d451e_0.png)

Hi there。 Today In this session， I will talk about map interface in Java。😊。



![](img/d700b94113eebfa5a1afb6b8cb3d451e_2.png)

Basically， map contains values on the basis of key that is key and value pair。

 just like a dictionary， where each key and value pair is known as an entry and map contains always unique keys。

 You can it the keys and entries individually with the help of some methods available in the map interface。

 map is useful if you have to search update or delete the element on the basis of key。

 There are two interfaces for implementing map in Java that is map and sorted map。

And three classes along with that is hash map linked hash map and tree map。

 A map doesn't allow the duplicate keys。 As I told you， a map contains always unique keys。

 hash map and linked hash map allow the null keys and values。

 which is saying that the itration of null keys and values are nondup。

 But3 maps doesn't allow any null values， and the null keys itself。 This is the map hierarchy。

 There are two interfaces。 As I said。 One is the map。

 and one is the sorted map Navigd map is a subitration of the sorted map。

 They are being extended or implemented to the hash map linked hash map and three map classes。😊。

These are some common map methods which are allowing us to implement and work with the map on the basis of keys and its values where the put inserts an entry in the map。

 put all insert the specified map in the map entry。

 put if absent will check the insertion value is there or not if it is not then it will insert otherwise it will skip。

Remove deletes the entry for the specific key。 Gett will return the key for the specific key contains key will check whether the particular key exists in the map set or not entry set will check for the complete entry set available in the dictionary or map set。

 Gett key will only get the keys and the get value will only get the value。

 These are some map exceptions。 You might get a no such element exception。

 when no item exists in the invoking map or entry。 Class cast exception will occur when an object is incomplete with element in the map to be assigned。

 null pointer exception can occur if an attempt is made to use a null object and nu is not allowed in a particular map type that you are going to use。

 unsupported operation exception will attempt is made to change a map which is unmodfiable。

 So you need to take care of these。Exceptions while working with map， according to that。

 you need to modify your operations。 Stay tuned to learn more about map interface and its sub classes in my upcoming session。

 See you in the next session。🎼。

![](img/d700b94113eebfa5a1afb6b8cb3d451e_4.png)