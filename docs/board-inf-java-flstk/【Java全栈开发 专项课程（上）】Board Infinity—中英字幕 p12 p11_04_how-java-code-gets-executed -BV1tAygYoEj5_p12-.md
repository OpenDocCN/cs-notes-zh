# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p12 p11_04_how-java-code-gets-executed -BV1tAygYoEj5_p12-

![](img/d2e6da8363224352a0198289bc4d620e_0.png)

Hi there。 today In this session， we will discuss。 how does the Java code gets executed。

 As I discussed in my previous session， there are three components that comes into the picture， J VM。

 JD K and J R， that plays an important role。And running any Java code。

And produces the output as per the expectation or the requirement。



![](img/d2e6da8363224352a0198289bc4d620e_2.png)

First of all， as you know， we write。A dot Java program that is known as a Java Se code。

 otherwise we wouldn't be able to run the program you need to save it with the dot Java extension that is known as a source code。

 the machine cannot directly understand the source code。

 you need to compile that source code into the Bte code。That is a programme dot Cs。

So the bytecode that's being generated cannot be understood by the program JVM。

 the machine cannot understand it。 So the virtual machine known as a JVM will load your bytecode into it and executes and understands and executes JVM includes a Java interpretator that converts the bytecode to the target computer machine code so that we can produce or get the output。

 JVM is a platform specific， which means that each platform has its own JVM each operating system has its on JVM。

 So Java virtual machine， as I said， which is an interpretator that creates all the statements through step by step from the Java bytecode and which will further convert it to the machine level language。

 However， once proper JVM is installed onto the machine。

 any bytecode can be run So bytecode once convert it into the target machine code then runs。

 but the bytecode。When you， as I said， Java code is portable。

 you can port it to any other machine and that specific machine based JvM can run your Java by This is how the things gets happen。

First of all， you write your dot Java code that dot Java code gets compiled into the bycode。

 That is a dot class。 Then the Java bytecode is moved locally or through network to the runtime environment。

 The Java virtual machine verification starts for the bytecode。

 The JVM has a bytecode very that is due to maintenance of the specific。

Security of the program and this bycode verifies the code only and only after the classes have been loaded onto the runtime。

 which is the JVM。It makes sure that the bikecodes are valid and accessible。

 It also saves the computer from various viruses and unseeccurure websites。

 As I said that JVM also includes the libraries inside it， whatever the programme needs to run。

As I said that on your machine hardware， there is an operating system being saved and under onto which the JVM resides。

So once your class gets loaded。Over the Java virtual machine， Java program execution gets started。

 so JVM interprets your piece of code step by step with the as an interpretator and nowadays the modern JVMs are much faster as they use JIT compilation units。

JVM can execute various tasks At the same time。 We also call them hotpot compilers as they are used by JVM to find out the hotspots in the byte code as well。

So this is how your Java code gets executes onto the machine。

One more thing you can understand here is Java。Virtual machine once loads your application。

 We most of the time gets confused that is this happen at once。 So guys， internally。

 it's a complete sequence gets happen。 First， the compilation gets happen。

 then class gets loaded onto the JVM， and the JVM as an interpretator converts it into the machine understandable code。

 most important question we get at this point is， is Java a compiler or a interpret based language。

 So。Concept is pretty clear when we talk about compiler。

 we mean that a tool that convert the source code into the byte code。

Making it easier for the computer to interpret。 And that is called a compiler。

 But when we say Java interpretator。A interpreter is a programming tool that converts the source code or a byte code into the machine understandable code。

 That's a J。So we can conclude here that we consider Java to be both compiler and interpreted based language because the source code first gets compiled。

Into the by code or a muhi binary code。And then the same binary code runs onto the JVM。

 and this is mostly a software based interpretator。

And that's for Java is a compiler and interpreter based language， booth。

I hope I answered a very most frequently asked question here。Now。

 you are all set to write your Java programs and gets them executed as per your requirements。

 As per your real time case studies。 So see you in the next session until next time Stay tuned。

 Thank you。

![](img/d2e6da8363224352a0198289bc4d620e_4.png)

。

![](img/d2e6da8363224352a0198289bc4d620e_6.png)