# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p51 p50_02_constructors-in-java -BV1tAygYoEj5_p51-

Hi there。 Today In this session， Im going to talk about constructors in Java。



![](img/19f0d681f964d16e3b291993c87cb2c6_1.png)

![](img/19f0d681f964d16e3b291993c87cb2c6_2.png)

Basically， when we talk about constructor， it is a special kind of method being used in creation of the object。

And helps in initializing the data members of a class。 It is a block of code like a method。

 It looks like a method。But as I said， it is a special method that is being to initialize the state of an object during the time of object creation。

😊，The constructor is initialized implicitly at the time of creating an object。

 you don't need to call it or invoke it by your own。

Whenever you do not have any constructor or a special method inside a particular class， As I said。

 each and every class is being inherited from the object class in Java。

 So object class constructor invokes to initialize the default values to the data members。

 Another iteration is the construct name should be same as that of the class name。

And it does not have any return type。 Let's say if your class name is student。

 the constructor name itself needs to be student。 And as I said， it does not have any return type。

 So constructor should not have any return type。 It is helping us to initialize the data members If a class cannot be declared as final and synchronized。

Can invoke another constructor of the same class。 That concept is known as constructor chain。

 and the access specify of the constructor can be private， default， protected and public。

As private helps in restricting by creating the instance of the class。

 that is why we keep it private by default default you can make it protected and public as per the accessibility。



![](img/19f0d681f964d16e3b291993c87cb2c6_4.png)

As I said， constructor is a special method so you can compare the constructor with method。

 Constructors must not have a return type， but methods should have a return type always the name of the constructor must be same as that of the class name。

 but method name must not be same。 you can give it any meaningful name to the method。

 Constructor is used to initialize the data members of the class。

 but method expose the behavior like calculation except details and display details。

 Constructor is invoke implicitly， you do not need to take initiative to call the constructor。

 but methods always needs to be called as for the requirement either from the main method or from the another method Cho is yours。

How does the constructor work so basically as I told you in my previous session when we say student object creation equals to new student at the time of saying new the memory gets allocated to the data members of a class and at the same time the constructor is invoked so as I said you do not need to take the initiative to call the constructor by your own。

Stay tuned to learn more about constructors in Java。 See you in the next session。



![](img/19f0d681f964d16e3b291993c87cb2c6_6.png)

🎼。

![](img/19f0d681f964d16e3b291993c87cb2c6_8.png)