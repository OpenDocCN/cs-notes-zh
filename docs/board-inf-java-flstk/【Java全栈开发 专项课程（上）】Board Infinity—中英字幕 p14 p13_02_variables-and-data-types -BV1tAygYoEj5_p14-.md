# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p14 p13_02_variables-and-data-types -BV1tAygYoEj5_p14-

![](img/81b859afe5a39ab0884c8b99e3e65c59_0.png)

Hi there。Today in this session， I will discuss about Java variables。😊。



![](img/81b859afe5a39ab0884c8b99e3e65c59_2.png)

And data types。A variable is basically a memory location which are reserved to store values。

 and those values needs to be used throughout the program execution。To indicate the storage area。

 each variable should be given a unique name。 and with the help of that name。

That value can be used throughout the program execution。

Variable needs to be declared with a specific data type before use。As I said， data type。

A variable can be declared using any specific data type， whether it is primitive or non primitive。

As I am talking about local primitive data types。We have8 specific primitive data types。

 Priitive data type is nothing。But a type which storess the values directly inside it and not the reference。

 So we have8 primitive derivative types by short int long float， double care or boolean。

Byite short in and long stores the integral values。

 non floating non decimal values with their specific range。

 You can evaluate the range of the data to be stored into this type， as per their memory location。

If I say one bite， two short2 bite in teacherger 4 B and long。8 bite further。

 we have float double that stores。The floating or decimal values care stores the character。

Java is a unicord based programmingming language。 That's what care stores2 by or a boion that stores to and falls。

Or 0 or1。The value stored can be changed during the program execution。

 Make sure your variable should not be declared with the cont key。There are three types of variables。

One is a local variable， another is the instance variable that means created at the class level。

And the third one is a non instance， also known as class variable or static variable that can be accessed with the name of the class。

 I will talk about instance variable or the class variables later today。

 I will focus more on to the local variable。This is the syntax to declare the variable first you define the data type。

 what type of data you wanted to store and then the variable name equals to the value that you want to store inside it。

Separating by comma， you can declare more than one variable inside it。For example。

 I want to store any integer value， let's say H， so I will say integer and then variable name a or h。

 so a is the variable name and de type this integer it stores the integral value without decimal precisions。

Similarly， you can declare more than one variable inside a statement within a statement。

Or you can just declare the variables and assign the values later。

 or you can do the initialization means at the time of declaring the variable。

 you can also assign the value So this statement is known as initialization of a variable further。

 we have data types， when we say data types， as the name suggests。

Data type specifies the type of the data that can be stored inside the variables in Java。

Each variable in Java has a specific type， which tells you the size。😊。

Of memory that needs to be assigned for that particular variable。As I said。

 we have primitive data types and non-priitive data types where the primitive data type are those data types that are predefined in the Java programming language。

 as I said they are directly used to store values inside it and there are total 8 primitive types the size of the primitive data type does not change with changing the operating system because Java programming language is platform independent of all the operating system。

I already discuss what are the primitive data types by it short integer long， float。

 double care or bullying。These are the size being allocated to the primitive types。 As I said。

 by long， short and integer at the number without floating decimals and float and double are used to store the floating decimals。

 Also， Java is based upon Uniicor standards or encoding。 That's what the character contains2 byte。

 If you ever heard about the Asi code that is C programming where the character is1 Bte。

 And then there is a boolean 1 B that stores true or false true says one and false says0。

While declaring the primitive data type the variables with the primitive type always should begin with a lower case letter。

The table demonstrates you how to declare or initialize the primitive data types。

What are their memory size in the while declaring it with a specific type and the range of the data that you can store inside it。

You can go as per your requirement。Next that we have is the non primitive data types as I discussed。

 we have primitive and non primitive。😊，Non primitive data types are the types that are not predefined always you can also define as per your need that is known as object oriented classes。

Not only object oriented classes， we do have the predefined classes like springs。

 string arrays and class that you can declare as per your requirement。

 we do have other classes like AWT has a point class。

Date class in the U package that are reciding into the specific packages before using them into your program。

 you can definitely import and use it。A referenceference type does not have a specific size or a byte range。

 whatever data you store inside it as per that the memory gets allocated。

This is one of the examples given how you can declare the string is basically non primitive type。

 but the most common type looks like the primitive initialization。

 but internally it is a reference type I am going to tell you what are the all non primitive types we have and we can use it in our program stay tuned to learn how practically the variables and data types can be used in a Java program。

See you in the next session until next time， Stay tuned。 Thank you。



![](img/81b859afe5a39ab0884c8b99e3e65c59_4.png)

🎼。

![](img/81b859afe5a39ab0884c8b99e3e65c59_6.png)