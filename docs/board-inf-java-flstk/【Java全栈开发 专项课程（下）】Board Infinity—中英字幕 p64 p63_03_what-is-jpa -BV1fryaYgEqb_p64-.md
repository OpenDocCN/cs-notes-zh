# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p64 p63_03_what-is-jpa -BV1fryaYgEqb_p64-

Hey guys， today in this session we will talk about J， which is Java Perence API。

 so let's get started。Basically， as I discussed， the JPA is specification of Java。

It is used to persist data between Java object and relational object。

 we say that in relational database just like hibernet we discussed。

 the data is stored in the form of object and communicates in the form of objects but in the relational database it stores in the form of table。

 so when you say JP your entity or object communicate with the table in the form of objects。

For example， employ is a class in Java object， but in a relational database it is a table。

So Jpa acts as a bridge between object relational model and the relational database system。

As JP is just a specification， it does not perform any operation by itself it just。

Communicate between the database tables as well as the objects， which are the Java objects。

 ORM tools like hibernet toppling Iase implements the JP specification for Java persistence。

There are some advantages of JpaA to be used in real time。

It reduces the code complexity because very easy to itate or annotate the entity with the help of writing a rate table or maybe arate I。



![](img/116de7b26a67bfbf341bd87b5a969212_1.png)

![](img/116de7b26a67bfbf341bd87b5a969212_2.png)

And code gets works。 increasere the productivity。 We can easily use one。

Java object for multiple operations improve the performance data independence as tomorrow if you need to change the database settings you just change it once the entire configurations handled by the GPA will be modified and the better error handling。

 obviously in the entire Java iteration by creating the custom exception or the predefined exceptions that we have under the Java persistence API。

I hope a little brief is clear to all of you stay tuned to learn more practically gets implemented。

See you in the next session until next time Stay tuned， Thank you。



![](img/116de7b26a67bfbf341bd87b5a969212_4.png)