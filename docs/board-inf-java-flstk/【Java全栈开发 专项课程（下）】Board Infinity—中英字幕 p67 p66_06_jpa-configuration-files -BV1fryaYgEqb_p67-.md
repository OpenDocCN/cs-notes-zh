# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p67 p66_06_jpa-configuration-files -BV1fryaYgEqb_p67-

嗯。Hey guys today in this session I will talk about Java persistence dot Xml file that is Java Jpa configuration file that plays an important role where the entire configuration goes on the moment we talk about this persistentence dot Ximl。

As I discuss， persistent context is set of entity instances where for any persistentence entity identity。

 there is a unique entity instance entity manager。And lot more details。

 it is usually located in the metata INF directory of your application。

 or you can keep it into your resources package。

![](img/216143afc2befe6d992ee4f4b075475c_1.png)

![](img/216143afc2befe6d992ee4f4b075475c_2.png)

I will be demonstrating you how exactly this Jpa configuration file needs to be placed。

 so let's get started。So as you can see that this is a springboard， my SQL JPA project。

 I will tell you each and everything about it， but first of all I am talking about this configuration that I have kept inside this project。

You can see that in this project I have segregated， first of all。

 you know that the springboard project needs to be created first and once the project is generated by your spring starter。

 the project will become up here here I should name change the name of this as application dot Java I have not changed anything here。

 You can see that。Just like a main method was kept inside it earlier while I was demonstrating you the rest for Athes。

 the same main method is kept here。I have segregated the controller folder， exceptions folder。

 models， folder， repository folder services folder because I have integrated JA。As I have told you。

 we will not be talking about spring Embassy March here the concept is all about the spring boot and the very bare minimum itration would be required for the integration。

In the models folder， you can see that employed or Java is created in employed or Java I have used my J。

 you can see that Jakarta Perence API。So here you can see that this is the table。

 this is the class I wanted to convert this into the entity so I used the Ad entity JPA annotation。

 converted this entity to the table where the table name would be employees。Here， as I discussed。

 every hibernate model class needs to have a unique ID that I have used at the ID for the same。

I want to generate the value as identity， which is auto in。It's completely your choice。

 If you would like to change the column name， just keep the writing column， first name。

 last name or whatever renaming you want。 If you don't want。

 you can still miss aggregate column by default。 they will come inside the column。

You can also make some conditions which will be converted into the database as conferences。

Please try to create this dot Java file， whatever entity you are creating as a complete Java file where these properties are private so you need to have getter and setter one default method and one one default constructed and one constructor with all the fields required and don't miss to have two string method so that in case you need to print your object you can just print the object and your object will return whatever strings that you will annotate here by default as I told you you can generate by going to the Se property it will be generating and giving you the entire object details and whatever properties you have inside。

I will tell you more about the controller repository and the service that being implemented by the GPPA in my next session。

Until next time， stay tuned， Thank you。

![](img/216143afc2befe6d992ee4f4b075475c_4.png)