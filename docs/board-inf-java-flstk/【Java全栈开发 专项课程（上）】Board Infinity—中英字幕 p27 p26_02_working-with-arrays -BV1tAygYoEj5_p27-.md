# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p27 p26_02_working-with-arrays -BV1tAygYoEj5_p27-

![](img/8d14ce12a40c52c24ee881b9730d463e_0.png)

Hi， there。Today， in this session， we will discuss about arrays in Java。😊。



![](img/8d14ce12a40c52c24ee881b9730d463e_2.png)

In this digital world， storing information is very crucial task to do as a programmer。😊。

Every little piece of information needs to be saved in the memory location for future use。

For the same purpose， we also use array， which is a data structure to store elements of the same data type instead of storing hundreds of values in different hundreds of variables。

Using an array， we can store all these values in just a single variable to use the data frequently。

 we must assign a name which is done by the variable。In array。

 each element can be accessed directly by using its index number where considering if the size of the array is 5。

 then index starts from0 and go still and minus5 that is 4 an array is a homogeneous non primitive data type used to save multiple elements into a particular variable。

The idea is to store multiple items of the same type in the array。

 That's what we call it as a homogeneous。 This makes it easier to calculate position of each element by adding an offset search to a base value。

Assume you have to write a code that takes marks of five subjects。5ive students。

 instead of creating five different variables， we will store and access the data from only one variable that's error variable called marks。

There are a couple of advantages to use arrays over the top of variables。

 It helps in less writing less code。 That's a code optimization。

Random access with the help of the index， easy to traverse data。

 easy to manipulate data and sort data。There are two types of arrays that one is the single dimensional array and another is the multi dimensionmenal array。

If you would like to store the data in a particular row， that's a single dimensional array。

 as I said， marks of five students name of five students or something。

 if you wanted to store multiple dimensions that each object each element in the array will have a student object and each object will have couple of properties associated to it。

This kind of data you can store in multidimensional array as well。

 also if you are supposed to work on any of the calculation that relies on matrix table or vectors。

 you can talk about multidimensional error as well。

Stay tuned to learn more about arrays in Java with the practical implementation。Stay tuned。

 see you in the next session。

![](img/8d14ce12a40c52c24ee881b9730d463e_4.png)

![](img/8d14ce12a40c52c24ee881b9730d463e_5.png)