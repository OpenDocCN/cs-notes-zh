# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p131 p59_10_functions-and-scope -BV1tAygYoEj5_p131-

Hi there in the previous video we learn looping structures now in this video we will learn functions and scope in JavaScript。

 so let's get started。Functions are a fundamental part of JavaScript。

A function is a block of code that performs a specific task。

 You can use functions to organize your code into reusable pieces that can be called from different parts of your program。

😊，In JavaScript， you can define functions using the function keyword。

Let's look at the syntax of function。So here we have a keyword function that is used to define a function in JavaScript。

 then it is followed by a function name， this is actually the name you give to your function。

It should be descriptive and indicate that what function does。Then we have parametermeter 1。

 parametermeter 2。 These are the parameters that function accepts parametersmeter are optional。

 and you can have as many or as few as you need。 Permeters are separated by commas。

Then we have a block of code that needs to be executed。😊，It is executed when you call your function。

 it can contain any valid JavaScript code， including other functions， loops and conditioners。Last。

 we have written value。 This is an off statement that returns a value from your function。

 If your function does not return a value， it will return undefined by default。



![](img/dba95744ce517b636d74b5b1468a767a_1.png)

Let's look at example。So let's come here to the VS code and here I have a file name function star Gs。

So in order to create a function， we have to write a function keyword。

 then let's give the name of the function as add numbers。And here we will pass the parameters。

 we will pass number1 and number two， these are called parameters。And here we would say， let sum。

And it would be number one。Plus， number two。And then we will return the sum。

Now where this number one and number2 are coming from。

 So for that we do function calling or so we write the name of the functions and we call it like this。

 this is called as function invocation or calling of a function。

This number one and number two will come from here。 So let's say we are passing 5 and 10。

 so5 will be number1 and 10 will be number two when you pass。

The values as the function is getting called， these are calledless arguments。 and when you create it。

 these are called as parameters。So 5 will go to number1，1 will go to number2。

And let's console the output。So let's say console dot log。And let's run this program。

 So I will open up the terminal。And let's say， note。Functions are Js， and you get the orpress 15。

This is how you create functions。Let's talk about scope。



![](img/dba95744ce517b636d74b5b1468a767a_3.png)

So scope is the set of rules that determine where variables and functions are accessible in your program in JavaScript there are two types of scope we have global and we have local scope。

So global scope refers to the variables that are declared outside of any function or block。

 These variables are accessible from anywhere in your program。Then we have local scope。

And that you can create it Agile in a functions or in a block。

 So let's also see an example for that part。

![](img/dba95744ce517b636d74b5b1468a767a_5.png)

So here what I will do is to understand them， let's create a variable。So let's say let。

 and let's name it as。Global variable。And let's say this is， or I am a global。The日本。

Then we can create function。 and let's call this as。不认。Bat。Variables。At inside this。

 I will create a local variable。So local scope refers to the variables that I declare inside a function or a block。

And these variables are only accessible within that function or a block。 So we are。

Creating a local variable here。It could worth any name。

 but just for readability purposes and just giving it local variable。I am a local variable。

Let's try to output both of them file a console do lock。Global variable。

 And then let's also say console dot log。Local variable。We will call this function here。

 so this is function invocation and after function invocation， I am printing out the values again。

And let's see what happens。So if I run this program。

You will see that the first output is I am a global variable。

 that means global variable is access inside the function。

 And then the second is I am a local variable。 Of course， it is created locally。

 That's why it is able to access it。😊，But next console statements。

 you can see that I am a global variable is printed again because the global variable was declared in the global scope。

 But the last one， you can see that global scope cannot have access to the。😊，Local scope。

 That's why it gives uncord reference error that local variable is not defined。

So local is for that particular function or block only。



![](img/dba95744ce517b636d74b5b1468a767a_7.png)

That is the difference between global and local scope。Let's try to summarize this。

So in JavaScript functions are blocks of code that can be called and executed at any point in the program。

They can take in parameters and return values。Function can be defined with global or local scope。

 Sccope in Javascript refers to the。Point where a variable is accessible in your program Global scope refers to a variable that are declared outside of any function or block and are accessible from anywhere in the program。

Whereas local scope refers to the variables that are decorated inside a function or a block and are only accessible within that function or a block itself。

UnderUnderstanding scope is very important for writing clean， efficient and bug free code。

This is all for this video。 See you in the next video。 Thank you。操。🎼。



![](img/dba95744ce517b636d74b5b1468a767a_9.png)