# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p10 p9_02_what-are-jvm-jre-and-jdk -BV1tAygYoEj5_p10-

![](img/a55131878aa1fd7b9ecff92f3125de5f_0.png)

Hey everyone， today in this session I will be discussing the three specific and most important components of Java that is JDK。

 JRE and JVM。We must understand the differences between JDK JR and JVM before proceedceding it up further because they exactly will help you to understand how a Java program actually runs。

 I'll first talk about GR JRE is basically a Java runtime environment also return as Java RTE Java runtime environment provides all the libraries。

 the Java virtual machine and the other components used to run the application return in Java It is used to provide the runtime environment and JVM is also actively released by Sun microcrosystem。



![](img/a55131878aa1fd7b9ecff92f3125de5f_2.png)

And also will help you to understand how the installation of the packages needs to be provided at the runtime environment。

 so GRE is equals to the JVM plus all the set of libraries and other additional files required to run your program GRE does not contain tools and utilities it just contains the libraries and the JVM inside it such as compilers or debuggers for the developing outlets and applications at the utilities it does not contain by the GRE。

GR is just a simple JVM plus all the class likes。

![](img/a55131878aa1fd7b9ecff92f3125de5f_4.png)

Whenever we write any Java Se code， which will be saved with the extension of dot Java that needs to be compiled into the Java bytecode。

 bytecode is a dot class。As if you remember， I told you， Java is portable。

 We write the code once and run anywhere。 So whatever sales code you write， compiled。

And convert it into this by code。 So this by code is actually portable。

 You can run this by code anywhere， and this by code will help you to load it onto the GVM。😊。

Where the three layers resides hardware operating system and over the top of the operating system。

 all the Java virtual machine libraries resides which gives you the runtime environment to execute your bytecode。



![](img/a55131878aa1fd7b9ecff92f3125de5f_6.png)

Next as I discussed Ill tell you all the components is JDK JDK is nothing but a kit that provides the environment to develop and execute the Java program。

 It is a kit that includes two things。One it includes the development tools to provide an environment to develop your Java program and another is the GRE So JRE equals to JDK+ libraries and JDK equals to JRE+ development tools and JDK is equals to JBM plus set of libraries plus other additional files plus development tools So JDK contains the Java runtime environment also interpretator to interpret your Java into the bytecode and the compiler that is Java C the and many more it also contains some toolkis integration libraries。

 other base libraries， Lng and u based libraries another these are the components that GR are needs to be stored but as I told you JDK equals to the JR+ development tools So all libraries are provided to the JD。

By the GRE so this is how the JDK contains the GR GR is equals to JVM plus class libraries plus the development tools like compilers debuggers and Java docs this is the integration of JVM plus JDK and GRE this is Java standard edition API JDK is equals to whatever tool we required for development integration libraries。

 other based libraries and the language and u based libraries， but。



![](img/a55131878aa1fd7b9ecff92f3125de5f_8.png)

![](img/a55131878aa1fd7b9ecff92f3125de5f_9.png)

You can see here this is all combination of JDK plus the development toolkits as well。

 so see you in the next session to tell you more about how a Java code is executed and writing our first Hall program until next time stay tuned Thank you See you in the next session。



![](img/a55131878aa1fd7b9ecff92f3125de5f_11.png)

🎼。

![](img/a55131878aa1fd7b9ecff92f3125de5f_13.png)