# 【Java全栈开发 专项课程（下）】Board Infinity—中英字幕 p26 p25_07_java-navigablemap-interface -BV1fryaYgEqb_p26-

![](img/bed4d1f623cd338ff2291e21973eaa92_0.png)

Hi there Today in this session， we will learn about the Java navigable map interface and its method with the help of the example so let's get started。

😊。

![](img/bed4d1f623cd338ff2291e21973eaa92_2.png)

The navigable map interface of the Java collection framework provides the feature to navigate among the map entries and it considered as a type of sorted map。

The performance and behavior of ascending operations are likely to be faster and efficient than that after descending order。

 and it also provides the way to create the sub map and an existing map in Java。

It has various implemented features like head map， tail map and subm， which allows us to more， more。

 do the focus on the navigation。This is a syntax of navigable map。 and here its easily mentioned。

 the K is the unique identifier used to associate each element and the value element associated by the keys in a map。

 This is the iteration map extends to the sorted map sorted map extends to the navigable interface。

And then it's implemented by the tree map panel。 So in short， if you would like to it it。

 you again need to use a tree map class to work on the operations。

 These are the specific methods associated to that selling entry， descending key sets。

 descending map， first entry， floor entry， last entry。

 poll first entry and the poll last entry along with。



![](img/bed4d1f623cd338ff2291e21973eaa92_4.png)

So let's get started by practically make it implement。

Here I just get this navigable map from the U package。

 which will take a string as a key and integer as a value。Again。

 we'll take the numbers equals to new tree map。Numbers taught birth。1。Numbers start put to。

Numbers to。3。嗯。Then， I would like to print this。Numbers， navigable map。Here。

 if you would like to get numbers dot first entry， then there is a。Methd。

 if you would like to get numbers stored last entry， then。Usually get。If you will get numbers do。

Poold first entry。Number start。本。Last entry， then there is a method。Let's try make it implemented。

So you can see that the first entry is the one。2，1，3 and 2。 I think the sequence is quite changed。

 So you can see that it's 1，2， and 3。First entry and the last entry is。First and last。

But you will pull the first entry that will be take one and the last entry that will be take two。

So that's how the values gets iterated in my implementation。

Because the first entry returns and remove the first entry of the map and the same gets with the port last entry that returns and removes the last entry of the map。

 See you in the next session until next time。 Stay tuned。 Thank you。



![](img/bed4d1f623cd338ff2291e21973eaa92_6.png)