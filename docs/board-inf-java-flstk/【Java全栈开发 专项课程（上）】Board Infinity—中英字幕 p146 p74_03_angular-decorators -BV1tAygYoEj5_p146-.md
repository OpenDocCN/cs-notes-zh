# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p146 p74_03_angular-decorators -BV1tAygYoEj5_p146-

![](img/cdc240e18b29587dcdc3e8250c99b9da_0.png)

Hi there in the previous video we learned about angular life cycle now in this video we will understand angular decorators。

So let's get started。In Angular， there are several types of decorators that are used to enhance and modify the behavior of classes。

 methods， properties and parameters。Let's look at some main types of daators in Angular。

Gla they个 it us。So for that， we have component， it is used to define an angular component。

Then we have directive， it is used to define an angular directive。

Then we have injectable it is used to define or declare a class as injectable or dependency injection。

Then we have Nng module it is used to define an angular module。That he would see in the modern slu。

Then we have pipe。😡，It is used to define an angular pipe。

 so for all these we have different different types of glass decorators。



![](img/cdc240e18b29587dcdc3e8250c99b9da_2.png)

![](img/cdc240e18b29587dcdc3e8250c99b9da_3.png)

Next， we have something called property decorators。So let's look at some of the important ones。So。

 we have input。Decorator， it is used to define an input property on a component or directive。

Then we have output。Property decorative that is used to define and output property on a component or directive。

Then we have host binding that is used to bind a property of a host element to a directive property。

And then we have host listener that is used to define an even listener on a host element。

Next we also have some types called as method decorators。

In this we have a view child that is used to query a child component or element。

 then we have content child that is used to query a content child component or element。

Then we have host that is used to specify the provider to be used when injecting a dependency and then again here we have host listener that is used to define an even listener on the host element。

Now， all of this seems confusing， but these are just。The decorators that you can use for different。

 different use cases。

![](img/cdc240e18b29587dcdc3e8250c99b9da_5.png)

Lastly， but not the least， we have。

![](img/cdc240e18b29587dcdc3e8250c99b9da_7.png)

Parameterters， decorators as well in that we have inject that is used to specify the dependency that you have to inject。

Then we have optional parameter decorator that is used to indicate that a dependency is optional or not。

Then we have self that is used to limit the search for a dependency to the current injector。

 and then we have skip self that is used to skip the current injector and search in the parent injectors。

 and then we have attributes that is used to retrieve the value of an attribute on an element。

Let's try to see how we can include some of them or some of the important decorators。



![](img/cdc240e18b29587dcdc3e8250c99b9da_9.png)

So let's go to the V code here I have a basic。Tempate ready for Angular and currently I am in the app dot component dots。

So here。As you can see， we have add the red component。So this is a glass decorator。Inside die。

If you want， we can have some more decorators like。Input here。So， we can see input and。Like this。

 and then we can save data。Should be spring， in this case。

And then we can go here and then we can also use output decorcorator。

And we can give it some patterns。And we can just give it a string， as well。

So this is how you can include decorators。For your different use cases。

 if you go to app or module dot Ts you will see we have Nng module as well and it can take up its own metadata。

And you can say both decorators。So if you analyze the pattern here。

All decorators in angular are represented with the add the rate symbol， for example。

 this add the rate component and add theate Nng module here。And also。

 you can see we have add the rate input and add the rate output。So we can define class decorators。

 input decorators， and a lot more depending upon the use case。

Then we have many event decorators as well that is host listener and so on。

And decorators are just the way to pass metadata to the angular application。



![](img/cdc240e18b29587dcdc3e8250c99b9da_11.png)

So all these decorators can be combined and used together to customize the behavior of angular elements。

 provide metadata and configure dependency injection and much more。

They play a crucial role in building angular applications and enablingling the framework's powerful features。

This is all for this video in the next video we will learn about angular components。

See you in the next video， Thank you。

![](img/cdc240e18b29587dcdc3e8250c99b9da_13.png)

![](img/cdc240e18b29587dcdc3e8250c99b9da_14.png)