# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p162 p90_03_angular-forms -BV1tAygYoEj5_p162-

H there。In the previous video， we learned about angular pipes。

 now in this video we will learn angular forms。 so lets get started。

So forms are an essential part of many web applications， allowing users to input and submit data。

 Angular provides robust support for working with forms。

 including template driven forms and reing forms。So let's discuss about them in detail。

 The first is temper driven formss。So temperatureative driven forms in angular rely on directives within the HTML template to create and manage the form。

The structure of the form is defined directly in the template itself。

Angular inferences the form controls based on the directives used such as N model or two way data binding and N form for form handling。

 Tt driven forms offer a simplest syntax and require less code compared to reactive forms。

 They are well suited for simple forms with basic validation requirements。

The form logic is primarily defined in the template itself， making it more declarative。

 T driven forms are commonly used in scenarios where quick form implementation is required or where the form structure is relatively straightforward。

 such as contact forms or login forms。They can be a good choice for smaller， less complex formss。

 Next， we have is reactive formss。Reacting forms in Angular provide a more flexible and programmatic approach to working with forms。

They are built using the form control form group and form area classes provided by angulars forms module。

 Now with reactive forms， the form structure is defined programmatically in the component code rather than directly in the template。

This gives you fine grain control over the form structure and behavior。

You can dynamically add or remove form controls， apply complex validation rules and handle form submission programmatically the active forms are well suited for complex forms that require more advanced features such as dynamic form controls。

 conditional validation， cross field validation or forms with complex data dependencies。

They offer better scalability and maintainability for larger forms or forms that require extensive validation and customization。

 It's important to note that both template driven forms and reactive forms provide powerful features such as form validation。

 data binding and handling form submission。 Let's discuss about。

In which cases to use what kind of forms。So use template drivenwin forms。4 contact forms。

So these forms are simple forms with basic validation requirements such as name。

 email and message fields。You can also use it for login forms。So forms。

 these are forms with standard login credentials validation such as email and password fields。

You can also use it in quick data entry forms。That are the forms with a small number of fields that don't require complex data processing or validation。

You can use reactive forms for multiple cases as well。For example。

 you can use it in multi step forms。So these are the forms with multiple steps or sections that require conditional validation and complex navigation。

 You can also use it with forms with dynamic controls。

So these are the forms where the number or type of controls may change based on user input or data conditions。

You can also use it in custom form validation。So these are the forms with custom validation rules that involves multiple fields or complex data dependencies。

We can also use it for advanced form handling， so these are the forms that require programmatic control over form submission handling from events or complex data processing。

In both the approaches， you can perform form validation， data binding and handle form submission。

 The choice between temperature when forms and reactive forms depends on the complexity and customization needs of your form。

So temporary driven forms are suitable for simpler forms。

 while reactive forms offer more scalability， flexibility and control for complex forms。

So you need to consider the specific requirements of your forms， the complexity of your application。

 and the level of control and customization needed when choosing between template driven forms and reactive forms。

So this is all for this video。 In the next video， we will learn about template driven form。

See you in the next video。 Thank you。

![](img/4f254e54abaae4d54ff76202155c7a05_1.png)

。

![](img/4f254e54abaae4d54ff76202155c7a05_3.png)