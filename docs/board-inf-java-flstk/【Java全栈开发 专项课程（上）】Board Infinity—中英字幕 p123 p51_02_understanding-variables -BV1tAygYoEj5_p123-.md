# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p123 p51_02_understanding-variables -BV1tAygYoEj5_p123-

Hi there。 In the Swie， we will learn variables in ja。So let's get started。

So let's see that you have a box of crayons and you want to organize them by color。

You could use different compartments in the box to store each color separately。

Each compartment would be like a variable and the crayons would be the data stored in those variables。

For example， you could have a compartment for red crayons， a compartment for blue crayons and so on。

The number of crayons in each compartment could vary depending on how many crayons you have of each color。

In JavaScript， variables work in a similar way。 you can think of variable as a container that holds a piece of data like a crayon。

The variable has a name that you can choose， just like the compartment in the crayon box has a label indicating the color of crayons it holds。

You can then use these variables to perform calculations or manipulate the data stored in them。

Just like you can move crayons from one compartment to another。

 you can change the data stored in a variable as needed。So most of the time。

 a JavaScript application needs to work with some information。Let's see some examples。

 The first is a social media app。In this case， the information might include the user's name。

 age and number of followers。The second is a music app In this case。

 the information might include the user's favorite songs， playlist and artists。

 so variables are used to store this information。To create a variable in ja。

 you can use let where and con keyword。Let's understand when to prefer what through some examples。

So let's move to the VS code and here I have Intextort HTMLml ready with us。



![](img/75dcfb91b3d353a627ed64639e7e4b07_1.png)

We can just create a new。File， and let's call it as variables taughtt Js。You can use where lead con。

But when to use what， let's see that。Before that， let's use let keyword and let's say let name。

To be joined。So this is how you create a variable Here you have two process First is that。

You are saying let name that means you are declaring a variable。

Then you are assigning the value that is drawn in this case， like this。

So this is variable declaration， this is variable initialization。

You can in fact do the initialization and declaration in the same line like this。

So we will use in this fashion。So let is the preferred way to declare a variable that can be reassigned later in the code。

It is block scope， which means that it can only exist within the block of code where it was defined。

For example。Let's create a variable by the name of count。And we can give it a value of0。

Then we can say count to be1。And then we can say console， tot lock。高。And now we can run this。

So let's clear up the terminal。 And here we can say。Let's save。 and let's say node。Variables do G S。

 and you can see one is printed here。Next we can look at cont。

 so cont is the preferred way to declare a variable that cannot be reassigned。It is also B scope。

For example， let's say if you want to use some constants like。Let's say pi， so we can say con。

PI and normally the value of pi is approximately equal to 3。14。And then we can say console dot lock。

Pi， and if I click on save if I run this code， you will see that we get the value。 But now。

 if someone tries to do this。历史。4 or something。In that case， it will give an error。

 as you can see here， that you cannot assign it to a constant variable。

The third one that is left is Van keyword。So where is the older keyword for declaring variables in JavaScript。

While it can still be used in modern jascript， it is generally considered outdated because it is not B scope。

 This means that variables stick with where are available throughout the entire function or global scope。

 which can make it harder to reason about your code and avoid bugs。Now。

 all these terms functions and global scope you will be looking at。In fellow videos， in detail。

For now， let's also see how we can create a variable using where。So， we can save back。Don。

Equals to 0。And we can use if condition here again。

 we will see if conditions in detail in the conditional part。So， I can see if。Through。Let's say。

 where count。To be one。 And then we can say console dot lock。够。And in the town， if you will say。

 console lo lock。Count as well。 So let's clear up the terminal。 and let's see the output。

So here you can see that in both the cases， Ed is one that means it is not block scope。

It has kind of shadowed the previous count。At this point。So earlier it was 0。

 even though it is in a block that is these curly brackets， it has overridden the previous count。

 and now you can see that in both the consoles。

![](img/75dcfb91b3d353a627ed64639e7e4b07_3.png)

It is printing one and one， each time。So let's summarize this。Overall。

 variables are a way to store and manage data in your code。They allow you to refer to a data by name。

 which makes your code more organized and easier to read。To create a variable in JavaScript。

 you can use let wear and consquiver。Let en the preferred keywords for declaring variables in modern ja because they are blocked scope and offer better control over variable assignment。

While where can still be used， it is generally considered outdated and should be avoided in new code wherever possible。

This is all for this video In the next video， we will see data typess in ja。

 See you in the next video。 Thank you。

![](img/75dcfb91b3d353a627ed64639e7e4b07_5.png)