# 【Java全栈开发 专项课程（上）】Board Infinity—中英字幕 p133 p61_02_understanding-the-document-object-model-dom -BV1tAygYoEj5_p133-

Hi there in this video， we will learn Dom and HTML。So let's get started。

Them stands for document object model。The document object model is a programming interface for web documents。

It represents the HTML or Xml document as a tree like structure where each node in the tree corresponds to a different part of the document。

The dom is a way for web developers to interact with the contents of a web page using JavaScript。

 and it allows them to add remove or modify elements dynamically。

You can imagine a web page as a tree where each branch represents an element in the webage such as a paragraph or an image and each leaf represents a property or attribute of that element such as a text inside the paragraph or the source URL of the image This is essentially what dom is JavaScriptscript can manipulate the dom by selecting nodes and changing their properties or adding and removing nodes altogether。

For example， you could use JavaScript to change the text inside a paragraph tag。

Or add a new paragraph or image to a page。😡，Remove an element from the page entirely or maybe change the attributes of an element。

 such as its size， color or position。 This is how a tree structure would look like。

 This tree diagram represents the hierarchical structure of the dom for a given web page。

 Each node in the tree corresponds when HTMLl element or a piece of text within that element。

 The nodes are arranged in a parent child relationship with parent node higher up in the tree and child notess branching off from their parent nodes below。

 For example， document is the parentry of this HTMLl， The HTMLml is a child of document。😊。



![](img/bda2397144c1272eec6cafdbfcea039f_1.png)

In this particular diagram， the root node is the document object。

 which represents the entire web page。😡，The first level of the child notes corresponds to the top level HTML elements。

 such as HTML， then we have heard， then we have body as well。

 Each of these nodes have one or more child notes depending on the structure of the webage Head has further child notes。

 body has further child notes。For example， the body node might have child notes corresponding to a header section。

 a main content area and a foot section。Each of these child notes might have further child notes such as paragraphs。

 images or other STs。For example， here we have H1 in the body and this H1 has a further child node of。

😡，Text that is welcome to my page。😡，This three diagram is useful for visualizing the structure of a web page and understanding how different elements are related to one another。

By traversing the tree and selecting different nodes。

 developers can use JavaScript to modify the contents of the webage and create dynamic and interactive effects。



![](img/bda2397144c1272eec6cafdbfcea039f_3.png)

Let's summarize this， so the document object model is a programming interface for web documents that represents the HTMLml or Xml document in a tree light structure of nodes。

Each node in the tree corresponds to a different part of the document。

 and jascript can manipulate the dom by selecting nodes and changing their properties or adding and removing notess altogether。

 The Dom is a powerful tool for web development， and it allows developers to create dynamic interactive web pages。

This is all for this video in the next video we will learn how to access and actual manipulate the dos with JavaScript See you in the next video Thank you。

🎼。

![](img/bda2397144c1272eec6cafdbfcea039f_5.png)