# Meta《数据库工程师（数据库简介／Git／MySQL）｜Meta Database Engineer》中英字幕 - P57：10_更改目录和列出内容.zh_en - GPT中英字幕课程资源 - BV1Vw4m1Z7tb

I've opened up the command line。 and first I want to check what directory I'm currently in to do that。

 I run the PwD command。 PwD is short for print working directory。 I type PwD and press the enter key。

 The command returns a forward slash， which indicates that I'm currently in the root directory。

 This is the top level directory within the operating system。

 If I want to check the contents of the root directory， I run another command called Ls。

 which is short for list。 I type L S and press the enter key。

 and now notice I get a list of different names of directories within the root level。

 In order to get more detail of what each of the different directories represents。

 I can use something called a flag。 Fls are used to set options to the commands you run。

 Use the list command with a flag called L。 which means the format should be printed out in a list format。

 I type L S space dash L。

![](img/0fab21cc55ba74420deeb04ac9c054f3_1.png)

Press enter， and this returns the results in a list structure。

 Let's focus on some items in this list。First， you need to know what's the difference between the link file。

 a directory， and a standard file。The link file is always represented by the L and it's always going to be at the very start of the output the T directory has an arrow beside it。

 which points to TMP this means TP is the same link to the actual directory TMP。

The next item is the bin directory and it's represented as a D that means that's just the standard directory and that you can use the change directory command to actually open the directory and check its contents Now let's focus on the ETC directory。

To change directory， use the CD command。I type C ETC to change to the ETC directory and press enterter now I type LS to check the contents。

Notice that the contents are completely different from the route。

 the command returns what's inside the ETC directory。To verify that you're in the ETC directory。

 run the PWD command and it confirms that you are in ETC again， if I want to change the output。

 type LSL and it returns a printed list format in list structure。

Let's cover a standard file like a text file or a configuration file。

The association or the symbol for it is the hyphen In this case， it represents the file Resolve。

com understandingstand the different symbols and different name conventions is important when you're trying to find specific files notice that there is a route and that these just represent the owner and the group that it's associated to。

So if I want to move back from the ETC directory， there are two ways to do it。

One is by typing in CD dot dot， which means that I go back up to the parent directory， press enter。

 and then I type PWD notice I am now back in the root directory to step back into ETC， type CD ETC。

To confirm that I'm back there， type PwD and enter If I want to use the other alternative。

 you can do an absolute path type in C forward slash and pressent Then I type PwD and press enter you can verify that I am back at the root again to step through multiple directories use the same process type C ETC and press enter check the contents of the files by typing LS and pressing enter。

Let's try another directory， for example， the SSH directory， type C， SSH。And press enter。

Then type LS and press enterter again you'll notice the different output from each one to move back up to the previous directory。

 I can use the CD dot dot and I should now be in the ETC directory and then again back to the root using CD dot dot which will take you back to the root itself Finally I can confirm that I am in the route by typing the PwD command and pressing enter you have now learned how to navigate and change directories。



![](img/0fab21cc55ba74420deeb04ac9c054f3_3.png)

![](img/0fab21cc55ba74420deeb04ac9c054f3_4.png)