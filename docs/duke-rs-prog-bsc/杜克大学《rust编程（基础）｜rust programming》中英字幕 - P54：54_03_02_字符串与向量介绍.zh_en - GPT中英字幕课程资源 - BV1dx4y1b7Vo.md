# 杜克大学《rust编程（基础）｜rust programming》中英字幕 - P54：54_03_02_字符串与向量介绍.zh_en - GPT中英字幕课程资源 - BV1dx4y1b7Vo

Strings and vectors are two of the data structures that you're going to probably see a lot in or the most that you're going to see in rust Now strings they're used interchangeably but there are two primary types of strings。

 one is string slices and we'll see how those look like and the other one they are just called strings sometimes rust developers use the string definition interchangeably to refer to these two different types of string representations Now we'll see what is exactly the difference when would you use one versus the other and how to work with both actually and then we'll take a look at vectors which is actually a collection of items almost like a list if you're coming from ja create or Python it's a collection it's a place where we can it's a data structure that allows us to put many。



![](img/f85bddf3ab4815ecd1a788b2c6271850_1.png)

And grow it and make changes to it and we'll see how to do that， how to add values to it。

 how to modify a vector， and how to retrieve values from a vector if we want to find specific values that are contained inside the vector。

