# 杜克大学《rust编程（基础）｜rust programming》中英字幕 - P41：41_02_03_演示：简单单元函数.zh_en - GPT中英字幕课程资源 - BV1dx4y1b7Vo

![](img/d183e9753b21cb9549f586ea3fd8c812_0.png)

We're going to be working with functions and doing more with functions rust is almost like a functional programming language I would say it is a functional programming language and it allows you to work a lot with functions so we're going to going to interact more with functions in here we have a main function that is calling another function here called process numbers。

 These functions that you're calling them and they're not returning something these are called unit functions。

 these are not returning a value and they're doing some processing and in this case we're going to pass a slice。

 these looks kind of like a list in our programming languages in this case we're passing a slice three numbers and we're going to do some processing so if we run these we're going to be able to see the result the sum of the numbers is6 the sum is even so we're seeing execution details of that function that is doing some work so。

This process， this workflow where we from one function we're calling some other function that does some work and we're not necessarily expecting a value or expecting anything back it's called a unit function。

 It's kind of like a fire and forget you call this function。

 It doesn't work and you're not caring about what is it that is's doing even if you're passing parameters like we are here in this case so that's a very simple way that's a very straightforward interaction that we have with functions in rust and there's certainly more complicated ones。

 but it's useful to know that these are called unit functions and these are functions that do work but don't necessarily return any value。



![](img/d183e9753b21cb9549f586ea3fd8c812_2.png)