# 哈佛 CS50-AI 6：L1- 知识系统知识 2 (推断，知识工程) 🧠

在本节课中，我们将要学习知识系统中的核心概念：**推断**与**知识工程**。我们将探讨如何让计算机基于已知事实进行逻辑推理，并学习如何将现实世界的问题转化为计算机能够理解和处理的形式。

![](img/d5852528561221d5a7428c4dd6167742_1.png)

## 🔍 推理算法与蕴含

上一节我们介绍了知识表示，本节中我们来看看如何基于知识进行推理。推理算法是一种我们可以用来试图判断是否能够得出某种结论的过程。

这些推理算法最终要回答一个关于**蕴含**的核心问题：给定对世界的一些查询（我们称之为 `alpha`），我们想知道，基于知识库 `K` 中的信息，我们是否可以得出结论 `alpha` 是真的。

用公式表示，即判断 **`KB ⊨ α`** 是否成立。

## 🤖 模型检查算法

那么，我们该如何写一个算法，能够查看知识库并确定查询是否为真呢？其中一种最简单的算法被称为**模型检查**。

![](img/d5852528561221d5a7428c4dd6167742_3.png)

模型是将我们语言中所有命题符号分配给真值（真或假）的某种赋值。你可以将模型视为一个可能的世界。

![](img/d5852528561221d5a7428c4dd6167742_5.png)

模型检查算法的工作原理如下：如果我们想确定知识库 `KB` 是否蕴含查询 `alpha`，我们将枚举所有可能的模型（即考虑所有变量的所有真假赋值组合）。如果在**每一个** `KB` 为真的模型中，`alpha` 也为真，那么我们就知道 `KB ⊨ α`。

以下是模型检查算法的核心逻辑：

```python
def model_check(knowledge, query):
    # 枚举所有可能的模型
    # 检查是否在所有 knowledge 为真的模型中，query 也为真
    # 如果是，则返回 True (蕴含成立)
    # 否则返回 False
```

![](img/d5852528561221d5a7428c4dd6167742_7.png)

## 📝 模型检查实例分析

这有点抽象，让我们看一个例子。假设我们有三个命题符号：
*   `P`：今天是星期二。
*   `Q`：正在下雨。
*   `R`：哈利将去跑步。

![](img/d5852528561221d5a7428c4dd6167742_9.png)

我们的知识库 `KB` 包含：
1.  `P ∧ ¬Q → R` （如果是星期二且没下雨，哈利就去跑步）
2.  `P` （今天是星期二）
3.  `¬Q` （没下雨）

我们的查询 `α` 是：`R` （哈利将去跑步）。

第一步是列举所有可能的模型。对于 `P`, `Q`, `R` 三个变量，共有 2³ = 8 种可能的真假组合。

我们检查每一个模型：
*   在 `KB` 为真的模型中（即同时满足 `P` 为真、`Q` 为假、且 `P ∧ ¬Q → R` 为真），我们发现 `R` 也必须为真。
*   在 `KB` 为假的模型中，我们无需关心 `R` 的真假。

由于在**所有** `KB` 为真的模型中，`R` 都为真，因此我们可以得出结论：`KB ⊨ R`。基于已知信息，哈利一定会去跑步。

## 💻 在Python中实现逻辑与推理

![](img/d5852528561221d5a7428c4dd6167742_11.png)

现在我们来看看如何在编程语言（如Python）中实现命题逻辑和模型检查。我们将使用一个预先编写好的逻辑库。

![](img/d5852528561221d5a7428c4dd6167742_13.png)

![](img/d5852528561221d5a7428c4dd6167742_15.png)

首先，我们需要表示逻辑符号和连接词：

```python
from logic import *

# 创建命题符号
rain = Symbol(“rain”) # 正在下雨
hagrid = Symbol(“hagrid”) # 哈利拜访了海格
dumbledore = Symbol(“dumbledore”) # 哈利拜访了邓布利多
```

我们可以使用逻辑连接词组合这些符号，构建复杂的句子：

![](img/d5852528561221d5a7428c4dd6167742_17.png)

![](img/d5852528561221d5a7428c4dd6167742_19.png)

```python
# 表示“下雨且哈利拜访了海格”
sentence = And(rain, hagrid)
```

![](img/d5852528561221d5a7428c4dd6167742_21.png)

![](img/d5852528561221d5a7428c4dd6167742_23.png)

现在，让我们编码一个具体的知识库并进行查询。假设我们知道：
1.  如果没下雨，哈利就拜访海格：`¬rain → hagrid`
2.  哈利拜访了海格或邓布利多：`hagrid ∨ dumbledore`
3.  哈利没有同时拜访海格和邓布利多：`¬(hagrid ∧ dumbledore)`
4.  哈利拜访了邓布利多：`dumbledore`

我们想知道：现在下雨吗？(`rain`)

```python
knowledge = And(
    Implication(Not(rain), hagrid), # 如果没下雨，则拜访海格
    Or(hagrid, dumbledore), # 拜访海格或邓布利多
    Not(And(hagrid, dumbledore)), # 没有同时拜访两人
    dumbledore # 拜访了邓布利多
)

# 查询：是否在下雨？
query = rain

# 使用模型检查算法进行推理
result = model_check(knowledge, query)
print(result) # 输出：True。可以推断出正在下雨。
```

运行此代码，计算机会基于模型检查得出结论：`knowledge ⊨ rain`。这意味着根据给定的知识，可以确定外面正在下雨。

![](img/d5852528561221d5a7428c4dd6167742_25.png)

![](img/d5852528561221d5a7428c4dd6167742_27.png)

## 🛠️ 知识工程：将问题转化为逻辑

![](img/d5852528561221d5a7428c4dd6167742_29.png)

![](img/d5852528561221d5a7428c4dd6167742_31.png)

这个过程——试图将一个问题转化为确定使用哪些命题符号以及如何用逻辑语句编码知识——被称为**知识工程**。人工智能工程师的任务就是把一个现实世界的问题，提炼成计算机能够理解和推理的知识表示。

如果我们能将任何一般性的问题转化为命题逻辑符号和公式，就可以利用模型检查等推理算法让计算机来解决它。

![](img/d5852528561221d5a7428c4dd6167742_33.png)

以下是几个知识工程的实例：

### 实例一：棋盘游戏《妙探寻凶》

![](img/d5852528561221d5a7428c4dd6167742_37.png)

在《妙探寻凶》游戏中，我们需要推断凶手、凶器和房间。我们可以为每种可能性创建命题符号。

![](img/d5852528561221d5a7428c4dd6167742_39.png)

![](img/d5852528561221d5a7428c4dd6167742_41.png)

以下是需要编码的核心知识：

![](img/d5852528561221d5a7428c4dd6167742_43.png)

![](img/d5852528561221d5a7428c4dd6167742_45.png)

*   **初始知识**：凶手是三人之一，凶器是三选一，房间是三选一。
    *   `mustard ∨ plum ∨ scarlet`
    *   `ballroom ∨ kitchen ∨ library`
    *   `knife ∨ revolver ∨ wrench`
*   **拥有卡片**：如果我拥有“芥末上校”卡片，我就知道他不是凶手。
    *   `¬mustard`
*   **对手的猜测**：如果对手猜测“斯卡雷特小姐在图书馆用扳手”，且有人展示了一张相关卡片，则我知道三件事至少有一件是假的。
    *   `¬scarlet ∨ ¬library ∨ ¬wrench`

![](img/d5852528561221d5a7428c4dd6167742_47.png)

![](img/d5852528561221d5a7428c4dd6167742_49.png)

通过逐步向知识库添加此类信息，并运行模型检查进行查询，计算机就能像玩家一样推理出最终的答案。

![](img/d5852528561221d5a7428c4dd6167742_51.png)

![](img/d5852528561221d5a7428c4dd6167742_53.png)

### 实例二：逻辑谜题（学院分配问题）

问题：四个人（吉德罗、米勒娃、波莫娜、霍拉斯）被分配到四个学院（格兰芬多、赫奇帕奇、拉文克劳、斯莱特林），每人一个学院，且学院各不相同。已知：
1.  吉德罗属于格兰芬多或拉文克劳。
2.  波莫娜不属于斯莱特林。
3.  米勒娃属于格兰芬多。

我们需要找出每个人的学院。

![](img/d5852528561221d5a7428c4dd6167742_55.png)

**知识工程步骤**：
1.  **创建符号**：为每个人和学院的组合创建符号，如 `GilderoyGryffindor`, `MinervaRavenclaw` 等。
2.  **编码约束**：
    *   每人属于且仅属于一个学院：对每个人，其所属学院的符号只有一个为真。
    *   每个学院有且只有一人：对每个学院，属于该学院的人的符号只有一个为真。
3.  **添加已知事实**：
    *   `GilderoyGryffindor ∨ GilderoyRavenclaw`
    *   `¬PomonaSlytherin`
    *   `MinervaGryffindor`

将所有这些知识输入模型检查算法，计算机就能推导出每个人的正确学院。

### 实例三：猜颜色游戏（简化版）

在一个简化版的猜颜色游戏中，有红、蓝、绿、黄四种颜色，以某种顺序排列。玩家猜测后，会得到“有几个颜色在正确位置”的反馈。

![](img/d5852528561221d5a7428c4dd6167742_57.png)

![](img/d5852528561221d5a7428c4dd6167742_59.png)

我们可以为“颜色C在位置P”创建命题符号（如 `RedPos0`）。知识包括：
*   每个位置有一种颜色。
*   每种颜色在一个位置。
*   根据每次猜测的反馈，添加逻辑约束（例如，如果猜测 `[红,蓝,绿,黄]` 得到“2个正确”，则需编码恰好有两个对应位置颜色正确的复杂逻辑）。

通过模型检查，计算机可以穷举所有可能的排列，并找出唯一满足所有反馈条件的顺序。

![](img/d5852528561221d5a7428c4dd6167742_61.png)

## ⚠️ 模型检查的局限性

![](img/d5852528561221d5a7428c4dd6167742_63.png)

你可能已经注意到，在上述猜颜色游戏的例子中，推理过程花费了相当长的时间。这是因为模型检查算法需要枚举所有可能的模型。

![](img/d5852528561221d5a7428c4dd6167742_65.png)

![](img/d5852528561221d5a7428c4dd6167742_67.png)

如果我有 `n` 个命题变量，就需要检查 **2ⁿ** 个可能的世界。对于变量较少的问题（如上述例子），这是可行的。但随着变量数量的增加，计算量会呈指数级增长，模型检查就变得不再适用。

![](img/d5852528561221d5a7428c4dd6167742_69.png)

因此，我们需要寻找更高效的推理方法，这将是后续课程的内容。

![](img/d5852528561221d5a7428c4dd6167742_71.png)

![](img/d5852528561221d5a7428c4dd6167742_73.png)

## 📚 本节课总结

![](img/d5852528561221d5a7428c4dd6167742_75.png)

在本节课中，我们一起学习了：
1.  **推理与蕴含**：如何判断知识库是否蕴含某个查询（`KB ⊨ α`）。
2.  **模型检查算法**：通过枚举所有可能世界，检查 `KB` 为真时 `α` 是否一定为真，从而实现推理。
3.  **Python实现**：如何使用面向对象编程表示命题逻辑，并实现模型检查函数。
4.  **知识工程**：将现实问题（如《妙探寻凶》、逻辑谜题、猜颜色游戏）转化为命题符号和逻辑公式的关键过程。
5.  **局限性认识**：了解了模型检查算法计算复杂度高（O(2ⁿ)）的局限性，为学习更高效的算法打下基础。

![](img/d5852528561221d5a7428c4dd6167742_77.png)

通过将问题形式化为逻辑，并利用算法进行自动推理，我们赋予了计算机解决多种需要逻辑思维问题的能力。