# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p74 21.2.1 Instruction-level Parallelism -BV19m41127Kj_p74-

The modern world has an insatiable appetite for computation。

 so system architects are always thinking about ways to make programs run faster。

The running time of a program is the product of three terms。

The number of instructions in the program multiplied by the average number of processor cycles required to execute each instruction。

 CPI。Multiplied by the time required for each processor cycle， T clock。To decrease the running time。

 we need to decrease one or more of these terms。The number of instructions per program is determined by the ISA and by the compiler that produced the sequence of assembly language instructions to be executed。

Both are fair game， but for this discussion， let's work on reducing the other two terms。

As we've seen， pipelining reduces tea clock by dividing instruction execution into a sequence of steps。

 each of which can complete its task in a shorter tea clock。



![](img/e3d565c1012d662e64576a6bdb09ac54_1.png)

![](img/e3d565c1012d662e64576a6bdb09ac54_2.png)

What about reducing CPI？In our five stage pipeline implementation of the beta。

 we designed the hardware to complete the execution of one instruction every clock cycle。

 so CPI ideal is one。But sometimes the hardware has to introduce NoAA bubbles into the pipeline to delay execution of a pipeline stage if the required operation couldn't yet be completed。

This happens on taking branch instructions when attempting to immediately use a value loaded from memory by the load instruction。

And when waiting for a cash miss to be satisfied from main memory。



![](img/e3d565c1012d662e64576a6bdb09ac54_4.png)

CPI Sta accounts for the cycles lost to theOOs introduced into the pipeline。

Its value depends on the frequency of taken branches and immediate use of load results。

Typically at some fraction of a cycle， for example。

 if a six instruction loop with a load takes eight cycles to complete。

 CPI's stall for the loop would be 26， in other words， two extra cycles for every six instructions。



![](img/e3d565c1012d662e64576a6bdb09ac54_6.png)

Our classic five stage pipeline is an effective compromise that allows for a substantial reduction of T clock while keeping CPI stall to a reasonably modest value。



![](img/e3d565c1012d662e64576a6bdb09ac54_8.png)

There is room for improvement， since each stage is working on one instruction at a time。

 CPI idealde is one。

![](img/e3d565c1012d662e64576a6bdb09ac54_10.png)

Slow operations， for example， completing a multiply in the ALU stage or accessing a large cache in the IF for MMS stages force TC to be large to accommodate all the work that has to be done in one cycle。



![](img/e3d565c1012d662e64576a6bdb09ac54_12.png)

![](img/e3d565c1012d662e64576a6bdb09ac54_13.png)

The order of the instructions in the pipeline is fixed。 If。

 say a load instruction is delayed in the mem stage because of a cache miss。

 all the instructions in earlier stages are also delayed。

 even though their execution may not depend on the value produced by the load。



![](img/e3d565c1012d662e64576a6bdb09ac54_15.png)

The order of instructions in the pipeline always reflects the order in which they were fetched by the IF stage。

 let's look into what it would take to relax these constraints and hopefully improve program runtime。



![](img/e3d565c1012d662e64576a6bdb09ac54_17.png)

Increasing the number of pipeline stages should allow us to decrease the clock cycle time。



![](img/e3d565c1012d662e64576a6bdb09ac54_19.png)

We add stages to break up performance bottlenecks， for example， adding additional pipeline stages。

 MeM1 and MeM2， to allow a longer time for memory operations to complete。



![](img/e3d565c1012d662e64576a6bdb09ac54_21.png)

This comes at a cost to CPI stall since each additional MEM stage means that moreOOP bubbles have to be introduced when there's a low data hazard。

Deeper pipelines mean that the processor will be executing more instructions in parallel。



![](img/e3d565c1012d662e64576a6bdb09ac54_23.png)

Let's interrupt enumerating our performance shopping list to think about limits to pipeline depth。

Each additional pipeline stage includes some additional overhead costs to the time budget。



![](img/e3d565c1012d662e64576a6bdb09ac54_25.png)

We have to account for the propagation set up and hold times for the pipeline registers。

 and we usually have to allow a bit of extra time to account for clock skew， in other words。

 the difference in arrival time of the clock edge at each register。And finally。

 since we can't always divide the work exactly evenly between the pipeline stages。

 there will be some wasted time in stages that have less work。

We'll capture all of these effects as an additional per stage time overhead of O。

If the original clock period was t， then with n pipeline stages。

 the clock period will be t divided by n plus O。

![](img/e3d565c1012d662e64576a6bdb09ac54_27.png)

At the limit， as n becomes large， the speed up approaches T divided by O。



![](img/e3d565c1012d662e64576a6bdb09ac54_29.png)

In other words， the overhead starts to dominate as the time spent on work in each stage becomes smaller and smaller。



![](img/e3d565c1012d662e64576a6bdb09ac54_31.png)

At some point， adding additional pipeline stages has almost no impact on the clock period。



![](img/e3d565c1012d662e64576a6bdb09ac54_33.png)

As a data point， the Intel Co2 X86 chips， nickname Nahalam， have a 14 stage execution pipeline。



![](img/e3d565c1012d662e64576a6bdb09ac54_35.png)

Okay， back to our performance shopping list。There may be times we can arrange to execute two or more instructions in parallel。

 assuming that their executions are independent from each other。

This would increase CPI ideal at the cost of increasing the complexity of each pipeline stage to deal with concurrent execution of multiple instructions。



![](img/e3d565c1012d662e64576a6bdb09ac54_37.png)

If there is an instruction installed in the pipeline by a data hazard。

 there may be following instructions whose execution could still proceed。

Allowing instructions to pass each other in the pipeline is called Out of order execution。

We'd have to be careful to ensure that changing the execution order didn't affect the values produced by the program。

More pipeline stages and wider pipeline stages increase the amount of work that has to be discarded on control hazards。

 potentially increasing CPI stall。So it's important to minimize the number of control hazards by predicting the results of a branch。

 in other words， taken or are not taken so that we increase the chances that the instructions in the pipeline are the ones will want to execute。



![](img/e3d565c1012d662e64576a6bdb09ac54_39.png)

Our ability to exploit wider pipelines and out of order execution depends on finding instructions that can be executed in parallel or in different orders。



![](img/e3d565c1012d662e64576a6bdb09ac54_41.png)

Collectively， these properties are called instruction level parallelism。

Here's an example that will let us explore the amount of instruction level parallelism that might be available。

On the left is an unoptimized loop that computes the product of the first and integers。On the right。

 we've rewritten the code， placing instructions that could be executed concurrently on the same line。

First notice the red line following the BF instruction。

Instructions below the line should only be executed if the BF is not taken。

That doesn't mean we couldn't start executing them before the results of the branch or no。

 but if we executed them before the branch， we would have to be prepared to throw away their results if the branch was taken。



![](img/e3d565c1012d662e64576a6bdb09ac54_43.png)

The possible execution order is constrained by the read after right dependencies shown by the red arrows。

 We recognise these as the potential data hazards that occur when an operaran value for one instruction depends on the result of an earlier instruction。



![](img/e3d565c1012d662e64576a6bdb09ac54_45.png)

In our five stage pipeline， we were able to resolve many of these hazards by bypassing values from the ALU。

 MAm and right back stages back to the RF stage where operaan values are determined。



![](img/e3d565c1012d662e64576a6bdb09ac54_47.png)

Of course， bypassing will only work when the instruction has been executed。

 so its result is available for bypassing。 So in this case。

 the arrows are showing us the constraints on execution order that guarantee bypassing will be possible。

There are other constraints on execution order。The green arrow identifies a right after right constraint between two instructions with the same destination register。



![](img/e3d565c1012d662e64576a6bdb09ac54_49.png)

In order to ensure the correct value is in R2 at the end of the loop。

 the load R comma R2 instruction has to store its result into the register file after the result of the Compare less than instruction is stored into the register file。



![](img/e3d565c1012d662e64576a6bdb09ac54_51.png)

![](img/e3d565c1012d662e64576a6bdb09ac54_52.png)

Similarly， the blue arrow shows a right after read constraint that ensures that the correct values are used when accessing your register。



![](img/e3d565c1012d662e64576a6bdb09ac54_54.png)

In this case， load R R2 must store into R2 after the RA operaand for the BF has been read from R2。

As it turns out， right after write and right after read constraints can be eliminated if we give each instruction result a unique register name。

This can actually be done relatively easily by the hardware by using a generous supply of temporary registers。

 but we won't go into the details of renaming here。



![](img/e3d565c1012d662e64576a6bdb09ac54_56.png)

![](img/e3d565c1012d662e64576a6bdb09ac54_57.png)

The use of temporary registers also makes it easy to discard results of instructions executed before we know the outcomes of branches。



![](img/e3d565c1012d662e64576a6bdb09ac54_59.png)

In this example， we discovered that the potential concurrency was actually pretty good for the instructions following the B F。



![](img/e3d565c1012d662e64576a6bdb09ac54_61.png)

To take advantage of this potential concurrency， we'll need to modify the pipeline to execute some number in of instructions in parallel。



![](img/e3d565c1012d662e64576a6bdb09ac54_63.png)

If we can sustain that rate of execution， CPI ideal would then be one over n since we'd complete the execution of N instructions in each clock cycle as they exited the final pipeline stage。



![](img/e3d565c1012d662e64576a6bdb09ac54_65.png)

![](img/e3d565c1012d662e64576a6bdb09ac54_66.png)

So what value should we choose for n？Instructions that are executed by different ALU hardware are easy to execute in parallel。

 for example， adds and shifts， or integer and floating point operations。Of course。

 if we provided multiple adders， we could execute multiple interte arithmetic instructions concurrently。

Having separate hardware for address arithmetic called load store units would support concurrent execution of load store instructions and inter arithmetic instructions。

This set of lecture slides from Duke gives a nice overview of techniques used in each pipeline stage to support concurrent execution。

Basically， by increasing the number of functional units in the ALU and the number of memory ports on the register file in main memoryory。

 we would have what it takes to support concurrent execution of multiple instructions。

So watch the right trade off between increased circuit costs and increased concurrency。



![](img/e3d565c1012d662e64576a6bdb09ac54_68.png)

As a data point， the Intel Nahalum core can complete up to four micro operations per cycle。

 where each micro operation corresponds to one of our simple risk instructionss。



![](img/e3d565c1012d662e64576a6bdb09ac54_70.png)

Here's a simplified diagram of a modern out of order superscalear processor。

Instruction fetch and decode handles say foreign instructions at a time。

 The ability to sustain this execution rate depends heavily on the ability to predict the outcome of branch instructions。

 En that the wide pipeline will be mostly filled with instructions we actually want to execute。

Good branch prediction requires the use of the history from previous branches。

 and there's been a lot of cleverness devoted to getting good predictions from the least amount of hardware。

If you're interested in the details， search for Brach Predor on Wikipedia。

The Reg renaming happens during instruction Decode。

 after which the instructions are ready to be dispatched to the functional units。

If an instruction needs the result of an earlier instruction as an operaantt。

 the dispatcher has identified which functional unit will be producing the result。

The instruction waits in a queue until the indicated functional unit produces the result。

 and when all the opera end values are known， the instruction is finally taken from the queue and executed。

Since the instructions are executed by different functional units as soon as their opera are available。

 the order of execution may not be the same as in the original program。After execution。

 the functional units broadcast their results so that waiting instructions know when to proceed。

The results are also collected in a large reorder buffer so that they can be retired。 In other words。

 write their results in the register file in the correct order。P。

Theres a lot of circuitry involved in keeping the functional units fed with instructions。

 knowing when instructions have all their operas and organizing the execution results into the correct order。

So how much speed up should we expect from all this machinery。

The effect of CPI is very programd specific， depending as it does on cash hit rates。

 successful branch prediction， available instruction level parallelism， and so on。

Given the architecture described here， the best speed up we could hope for is a factor of four。

Googling around seems that the reality is an average beatup of two， maybe slightly less。

 over what would be achieved by an in order single issue processor。



![](img/e3d565c1012d662e64576a6bdb09ac54_72.png)

What can we expect for future performance improvements in out of order superscalealar pipelines？

Increases in pipeline depth can cause CPI stall and timing overheads to rise。

At the current pipeline depths， the increase in CPIs dollar is larger than the gains from decreased Tlock and so further increases in depth are unlikely。



![](img/e3d565c1012d662e64576a6bdb09ac54_74.png)

A similar trade off exists between using more out of order execution to increase ILP and the increase in CPI stall caused by the impact of mispredicted branches and the inability to run memories any faster。

Power consumption increases more quickly than the performance gains from lower tea clock and additional out of order execution logic。

 The additional complexity required to enable further improvements in branch protection and concurrent execution seems very daunting。

😊。

![](img/e3d565c1012d662e64576a6bdb09ac54_76.png)

All of these factors suggest that it is unlikely we'll see substantial future improvements in the performance of out of order superscalealar pipeline processors。



![](img/e3d565c1012d662e64576a6bdb09ac54_78.png)

So system architects have turned their attention to exploiting data level parallelism and thread level parallelism。

 these are our next two topics。

![](img/e3d565c1012d662e64576a6bdb09ac54_80.png)