# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p52 17.2.5 Supevisor Calls -BV19m41127Kj_p52-

Usesr mode programs need to communicate with the OS to request service or get access to useful OS data like the time of day。

But if they're running in a different MMU context in the OS。

 they don't have direct access to OS code and data。And that might be a bad idea in any case。

 the O is usually responsible for implementing security and access policies。

 and other users of the system would be upset if any random user program could circumvent those protections。

What's needed is the ability for user mode programs to call OS code at specific entry points。

 using registers or the user mode virtual memory to send or receive information。

We' use these supervisor calls to access a well documented and secure OS application programming interface。

An example of such an interface is Posss。A standard interface implemented by many unix like operating systems。

As it turns out， we have a way of transferring control from a user mode program to a specific OSs handler。

 just execute an illegal instruction。We'll adopt the convention of using illegal instructions with an op code field of one to serve as supervisor calls。

The lower bits of these SVC instructions will contain an index indicating which SVC service we're trying to access。



![](img/6870eef3026d6abdb66efcb795191c07_1.png)

Let's see how this would work。Here's our user mode kernel Mo diagram again。

Note that the user mode programs contain supervisor calls with different indices。

 which when executed are intended to serve as requests for different OS services。

When an SVC instruction is executed。The hardware detects the op code field of one as in legal instruction and triggers an exception that runs the OS Iop handler。

 as we saw in the previous segment。The handler saves the processed state in the temporary storage area。

 then dispatches to the appropriate handler based on the Opcode field。

This handler can access the user's registers in the temporary storage area。

 or using the appropriate OS subppers can access the contents of any user mode virtual address。

If information is to be returned to the user， the return values can be stored in the temporary storage area。

 overr say theSa contents of the users are a zero register。Then when the handler completes。

 the potentially updated Sa register values are reloaded into the CPU registers and execution of the user mode program resumes at the instruction following the supervisor call。



![](img/6870eef3026d6abdb66efcb795191c07_3.png)

In the previous segment， we saw how the illegal instruction handler uses a dispatch table to choose the appropriate subhandr depending on the op code field of the illegal instruction。

In this slide， we see the subhandr for SVC instructions。

 in other words those with an op code field of one。

This code uses the low order bits of the instruction to access another dispatch table to select the appropriate code for each of the eight possible supervisor calls。

Our tiny OS only has a meager selection of simple services。

A real O would have supervisor calls for accessing files， dealing with network connections。

 managing virtual memory， spawning new processes， and so on。

 Here's the code for resuming execution of the user mode process when the supervisor call handler is done。



![](img/6870eef3026d6abdb66efcb795191c07_5.png)

Simply restore the save values for the registers and jump to resume execution at the instruction following the SVC instruction。



![](img/6870eef3026d6abdb66efcb795191c07_7.png)

There are times when for some reason the SVC requests cannot be completed and the request should be retried in the future。

For example， the RECH supervisorvis call returns to the next character typed by the user。

But if no character has yet been typed， the Os cannot complete the request at this time。In this case。

 the SVC handler should branch to eye weight， which arranges for the SVC instruction to be reexeed next time this process runs。

And then call Schchedr to run the next process。This gives all the other processes a chance to run before the SVC is tried again。

 hopefully this time successfully。You can see that this code also serves as the implementation for two different SVCs。

A process can give up the remainder of its current execution time slices by calling the yield Supervisor call。

This simply causes the OS to call Schr suspending execution of the current process until its next turn in the roundund Robbin scheduling process。

And to stop execution， the process can call the halt SVC。Looking at the implementation。

 we see that halt is a bit of a misnomer。 What really happens is that a system arranges to re execute the halt SVC each time the process is scheduled。

Which then causes the Os to schedule the next process for execution。

The process appears to halt since the instruction following the Ht SVC is never executed。



![](img/6870eef3026d6abdb66efcb795191c07_9.png)

Adding new SVC handlers is straightforward。First， we need to define new SVC macros for use in user mode programs In this example。

 we're defining SVCs for getting and setting the time of day。Since these are the 8th and 9th S Vcs。

 we need to make a small adjustment to the SVC dispatch code and then add the appropriate entries to the end of the dispatch table。

 The code for the new handlers is equally straightforward。



![](img/6870eef3026d6abdb66efcb795191c07_11.png)

The handler can access the value of the program R0 by looking at the correct entry in the user and State temporary holding area。

It just takes a few instructions to implement the desired operations。

The SVC mechanism provides control to access to OS services and data。As we'll see in a few lectures。

 it will be useful that SVC handlers can't be interrupted since they are running in supervisor mode where interrupts are disabled。

So， for example， if we need to increment a value in main memory using a load add C store sequence。

 but we want to ensure no other process execution intervenes between the load and the store。

 we can encapsulate the required functionality as an SVC， which is guaranteed to be uninterrupttable。

We've made an excellent start at exploring the implementation of a simple time shared operating system。

 We'll continue the exploration in the next lecture when we see how the OS deals with external input output devices。

😊。

![](img/6870eef3026d6abdb66efcb795191c07_13.png)