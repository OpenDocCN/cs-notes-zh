# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p50 17.2.3 Timesharing -BV19m41127Kj_p50-

A key technology for time sharing is the periodic interrupt from the external timer device。

Let's remind ourselves how the interrupt hardware in the beta works。

External devices request and interrupt by asserting the beta interrupt request input。

If the beta is running in user mode。In other words， the supervisor bit stored in the PC is zero。

 asserting IRQ will trigger the following actions on the clock cycle the interrupt is recognized。

The goal is to save the current PC+4 value in the XP register and force the program counter to a particular kernel mode instruction。

 which starts the execution of the interruptt handler code。

The normal process of generating control signals based on the current instruction is superseded by forcing particular values for some of the control signals。

PC cell is set to4 which selects a specified kernel mode address as the next value of the program counter。

The address chosen depends on the type of external interrupt。In the case of the timer interrupt。

 the address is HeX 800008。Note that PC31， the supervisor bit is being set to one。

 and the CPU will be in kernel mode as it startss executing the code of the interrupt handler。

The WA cell， WD cell and Wth control signals are set so that PC+4 is written into the XP register。

 in other words R30 in the register file。And finally。

 MWR is set to zero to ensure that if we're interrupting a store instruction that its execution is aborted correctly。

So in the next cycle， execution starts with the first instruction of the kernel mode interrupt handler。

 which can find the PC+4 of the interrupted instruction in the XP register of the CPU。As we can see。

 the interrupt hardware is pretty minimal， it saves the PC+4 of the interruptted user mode program and the XP register and sets the program counter to some predetermined value that depends on which external interrupt happened。

The remainder of the work to handle the interrupt request is performed in software。

The state of the interrupted process， for example， the values in CPU registers R0 through R30。

 is stored in main memory in an OS data structure called user M State。The appropriate handler code。

 usually a procedure written in C， is in voked to do the heavy lifting。When that procedure returns。

 the process date is reloaded from user M State。The OS subtracts 4 from the value in the XP。

 making it point to the interrupted instruction， and then resumes user mode execution with a jump XP。

Note that in our simple beta implementation， the first instructions for the various interrupt handlers occupy consecutive locations in May memory。

Since interrupt handlers are longer than one instruction。

 this first instruction is invariably a branch to the actual interrupt code。

Here we see that the reset interrupt asserted when the CPU first starts running sets the PC to0。

The legal instruction interrupt sets the PC to4， the timer interrupt sets the PC to 8 and so on。

In all cases， bit 31 of the new PC value is set to one so that handlers execute in supervisor or kernel mode。

 giving them access to the kernel context。A common alternative is to provide a table of new PC values at a known location and have the interrupt hardware access that table to fetch the PC for the appropriate handler routine。

This provides the same functionality as our simple beta implementation。

Since the process data is saved and restored during an interrupt。

 interrupts are transparent to the running user mode program。In essence。

 we borrow a few CPU cycles to deal with the interruptt， then it's back to normal program execution。



![](img/53d7e74c6c3d454035382081a168140e_1.png)

Here's how the timer interrupt handler would work。 Our initial goal is to use the timer interrupt to update a data value in the Os that records the current time of day。

 Let's assume the timer interrupt is triggered every sixth of a second。

A user mode program executes normally， not needing to make any special provision to deal with timer interrupts。

Periodically， the timer interrupts the Use mode program to run the clock interrupt handler code in the OS。

 then resumes execution of the user mode program。The program continues execution just as if the interruptrupt had not occurred。

If the program needs access to the time of day， it makes the appropriate surface request to the OS。

The clock handler code in the OS starts and ends with a small amount of assembly language code to save and restore the state。

In the middle， the assembly code makes a C procedure call to actually handle the interrupt。



![](img/53d7e74c6c3d454035382081a168140e_3.png)

Here's what the handler code might look like In C， we find the declarations for the time of day data value and the structure called user M state that temporal holds the save process state。

There's also the C procedure for incrementing the time of day value。

A timer interrupt executes the BR instruction at location 8。

Which branches to the actual interrupt candler code at clock or to score H。

The code first saves the values of all the CPU registers into the user M state data structure。

Note that we don't save the value of R 31， since is value is always 0。

After setting up the kernel Mo stack， the assembly languagengu stub calls the C procedure above to do the hard work。

When the procedure returns， the CPU registers are reloaded from the saved process date and the XP register value decremented by4 so that it will point to the interrupted instruction。

Then a jump XP resumes user mode execution。O， that was simple enough。

 But what does this all have to do with time sharing。

 Was't our goal to arrange to periodically switch which process was running。A。

 we have code that runs on every timer interrupt， so let's modify it so that every so often we arrange to call the OS's Schr routine。

In this example， we'd set the constant quantum to two if we wanted to call a Schr every second timer interrupt。

The scheduleedr sub is where the time sharing magic happens。



![](img/53d7e74c6c3d454035382081a168140e_5.png)

Here we see the user M state data structure from the previous slide where the user mode process data is stored during interrupts。



![](img/53d7e74c6c3d454035382081a168140e_7.png)

And here's an array of process control block， data structures， one for each process in the system。

The process control block holds the complete state of our process when some other process is currently executing。

 Its the long term storage for processor state。As you can see。

 it includes a copy of M state with the Pro's register values。

 the MMU state and various state associated with the processes input output activities represented here by a number indicating which virtual user interface console is attached to the process。

There are N processes altogether。The variable curve gives the index into the proc table for the currently running process。



![](img/53d7e74c6c3d454035382081a168140e_9.png)

And here's the surprisingly simple code for implementing time sharinging。

Whenever the Sch routine is called， it starts by moving the temporary save state into the process control block for the current process。

It then increments curve to move to the next process。

 making sure it wraps back around to zero when we've just finished running the last of the end processes。

It then reloads the temporary state from the process control block of the new process and sets up the MMU appropriately。

At this point， Sch returns and the clock interrupt handler reloads the CPU registers from the updated temporary safe state and resumes execution。

Vila， we're now running a new process。

![](img/53d7e74c6c3d454035382081a168140e_11.png)

Let's use this diagram to once again walk through how time sharing works。At the top of the diagram。

 you'll see the code for the user mode processes and below the OS code along with its data structures。

The timer interrupts the currently running user mode program and starts execution of the OS's C handler code。

The first thing the handler does is save all the registers into the user M state data structure。

If the Schr routine is called， it moves the temporarily saved state into the process control block。

 which provides the long term storage for a processing state。

Next Schr copies the save state for the next process into the temporary holding area。

 then the clock handler reloads the updated state into the CPUU registers and resumes execution this time running code in the new process。

While we're looking at the OS， note that sinceens its code runs with the supervisor mode bit set to one。

Interrupts are disabled while in the OS。This prevents the awkward problem of getting a second interrupt while still in the middle of handling a first interrupt。

 a situation that might accidentally overwrite the state and userM state。

But that means one has to be very careful when writing OS code。

Any sort of infinite loop can never be interrupted。

You may have experienced this when your machine appears to freeze。

 accepting no inputs and just sitting there like a lump。At this point。

 your only choice is to powercycle the hardware， the ultimate interrupt， and start afresh。

Interrupts are allowed during execution of user mode programs。

 so if they run a mu and need to be interrupted， that's the always's possible since the OS is still responding to say keyboard interrupts。

Every OS has a magic combination of keystrokes that is guaranteed to suspend execution of the current process。

 sometimes arranging to make a copy of the process date for later debugging。Very handy。



![](img/53d7e74c6c3d454035382081a168140e_13.png)