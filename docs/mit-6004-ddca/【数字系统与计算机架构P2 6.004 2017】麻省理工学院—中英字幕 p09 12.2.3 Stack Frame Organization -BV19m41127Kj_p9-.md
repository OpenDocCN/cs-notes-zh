# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p09 12.2.3 Stack Frame Organization -BV19m41127Kj_p9-

We use the stack to hold a procedures activation record that includes the values of the arguments to the procedure call。

We' allocate words on the stack to hold the values of the procedure's local variables。

 assume when we don't keep them in registers。And willll use the stack to save the return address past an LP。

 so the procedure can make nested procedure calls without overriding its return address。

The responsibility for allocating and deallocating the activation record will be shared between the calling procedure。

 the caller and the called procedure。 the callee。 The caller is responsible for evaluating the argument expressions and saving their values in the activation record being built on the stack。

We'll adopt the convention that the arguments are pushed in reverse order。 In other words。

 the first argument will be the last to be pushed on the stack。

We'll explain why we made this choice in a couple of slides。

The code compiled for a procedure involves a sequence of expression evaluations。

 each followed by a push to save the calculated value on the stack。So when the call starts execution。

 the top of the stack contains the value of the first argument， the next word down。

 the value of the second argument， and so on。After the argument values， if any。

 have been pushed on the stack。There's a branch to transfer control to the Pro's entry point。

 saving the address of the instruction following the branch in the leakage pointer， R28。

 a register that will dedicate to that purpose。When the callee returns and execution resumes in the callurer。

 a Dator is used to remove all the argument values from the stack， preserving stack discipline。

So that's the code the compiler generates for the procedure。

 The rest of the work happens in the called procedure。



![](img/6238d4596282184ad2e7dd37242d5bfb_1.png)

The code at the start of the called procedure completes the allocation of the activation record。

Since when we're done， the activation record will occupy a bunch of consecutive words on the stack。

 will sometimes refer to the activation record as a stack frame to remind us of where it lives。

The first action is to save the return address found in the LP Reg。

 This frees up LP to be used by any nested procedure calls in the body of the colee。

In order to make it easy to access value stored in the activation record。

 we'll dedicate another register called the base pointer， BPP or R27。

Which will point to the stack frame we're building。So as we enter the procedure。

 the code saves the pointer to the call stack frame and then。

Uses the current value of the stack pointer to make BPp point to the current stack frame。

 We'll see how to use BPp in just a moment。Now， the code will allocate words in the stack frame to hold the values for the callse local variables ifphony。

Finally， the calling needs to save the values of any registers it will use when executing the rest of its code。

These save values can be used to restore the register values just before returning to the caller。

This is called the Callee Sas Convention， where the callee guarantees that all register values will be preserved across the procedurec call。

With this convention， the code and the caller can assume any values it placed in registers before a nester procedure call will still be there when the nested call returns。

Note that dedicating a register as the base pointer isn't strictly necessary。

 All accesses to the values on the stack can be made relative to the stack pointer。

 but the offsets from S P will change as values are pushed and popped from the stack， for example。

 during procedure calls。It will be easier to understand the generated code if we use BP for all stack frame references。



![](img/6238d4596282184ad2e7dd37242d5bfb_3.png)

Let's return to the question about the order of argument values in the stack frame。

 We adopted the convention of pushing the values in reverse order。 In other words。

 where the value of the first argument is the last one to be pushed。

So why push argument values in reverse order？

![](img/6238d4596282184ad2e7dd37242d5bfb_5.png)

With the arguments pushed in reverse order， the first argument labeled ag 0 will be at a fixed offset from the base pointer。

 regardless of the number of argument values pushed on the stack。

The compiler can use a simple formula to determine the correct BPP offset value for any particular argument。

So the first argument is at offset minus- 12， the second at minus16， and so on。Why is this important？

Some languages such as C support procedure calls with a variable number of arguments。

Usually the procedure can determine from， say， the first argument。

 how many additional arguments to expect。The canonical example is the C print app function。

 where the first argument is a format string that specifies how a sequence of value should be printed。

So a call to printf includes the format string argument。

 plus a varying number of additional arguments。With our calling convention。

 the format string will always be in the same location relative to B P。

 so the printf code can find it without knowing the number of additional arguments in the current call。

The local variables are also at fixed offset from BP。The first local variables at offset 0。

 the second it offset 4 and so on。So we see that having a base pointer makes it easy to access the values of the arguments and local variables using fixed offsets that can be determined at compile time。

The stack above the local variables is available for other uses， for example。

 building the activation record for a nested procedure call。



![](img/6238d4596282184ad2e7dd37242d5bfb_7.png)