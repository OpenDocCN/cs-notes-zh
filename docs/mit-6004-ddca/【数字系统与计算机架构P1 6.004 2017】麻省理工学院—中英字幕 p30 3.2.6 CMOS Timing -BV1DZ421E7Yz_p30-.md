# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p30 3.2.6 CMOS Timing -BV1DZ421E7Yz_p30-

Okay， now that we understand how to build combinational logic8s using CMOSs。

 let's turn our attention to the timing specifications for the gates。

Here is a simple circuit consisting of two CMmoss inverters connected in series。

 which we'll use to understand how to characterize the timing of the inverter on the left。

It will be helpful to build an electrical model of what happens when we change VN。

 the voltage on the inputput to the left inverter。If V in makes a transition from a digital0 to a digital one。

 the PFt switch in the pull up turns off， and the NFt switch in the pull down turns on connecting the output node at the left inverter to ground。

The electrical model for this node includes the distributed resistance and capacitance of the physical wire。

 connecting the output of the left inverter to the input of the right inverter。

And there is also capacitance associated with the gate terminals of the mossFts in the right inverter。

When the output node is connected to the ground， the charge one of this capacitance will flow towards the ground connection through the resistance of the wire and the resistance of the conducting channel of the NFt pull down switch。

Eventually， the voltage on the wire will reach the potential of the ground connection， zero volts。

The process is much the same for falling transitions on VN。

 which cause the output node to charge up to VDD。Now let's look at the voltage waveform as a function of time。

The top plot shows both a rising and later a falling transition for VN。

We see that the output waveform has the characteristic exponential shape for the voltage of a capacitor being discharged or charged through a resistor。

The exponential is characterized by its associated RC time constant， where in this case。

 the R is the net resistance of the wire and MosEt channel。

 and the C is the net capacitance of the wire in MossFt gate terminals。

Since neither the input nor output transition is instantaneous。

 we have some choices to make about how to measure the inverter's propagation delay。Happily。

 we have just the guidance we need from our signaling thresholds。



![](img/ae1e6427c681dae347e6eeefe80627ec_1.png)

The propagation delay of a combinational logic gate is defined to be an upper bound on the delay from valid inputs to valid outputs。

Valid input voltages are defined by the V IL and V I H signaling thresholds。

 and valid output voltages are defined by the V OL and V O H signaling thresholds。

 We've shown these thresholds on the waveform plots。

To measure the delay associated with the rising transition on VN。

 first identify the time when the input becomes a valid digital one。 In other words。

 the time at which VN crosses the VIH threshold。Next。

 identify the time when the output becomes a valid digital zero， in other words。

 the time at which V out crosses the VOL threshold。

The interval between these two time points is the delay for this particular set of input and output transitions。

We can go through the same process to measure the delay associated with a falling input transition。

First， identify the time at which VN crosses the VIL threshold。

Then find the time at which V out crosses the VOH threshold。

The resulting interval is the delay we wanted to measure。Since the propagation delay。

 TPD is an upper bound on the delay associated with any input transition。

 we'll choose a value for TPD that's greater than or equal to the measurements we just made。

When a manufacturer selects the TPD specification for a gate。

It must take into account manufacturing variations。

 the effects of different environmental conditions such as temperature and power supply voltage and so on。

It should choose a TPD that will be an upper bound on any delay measurements their customers might make on actual devices。

From the designer's point of view， we can rely on this upper bound for each component of a larger digital system and use it to calculate the system's TPD without having to repeat all the manufacturer's measurements。

If our goal is to minimize the propagation delay of our system。

 then we'll want to keep the capacitances and resistances as small as possible。

There's an interesting tension here。To make the effective resistance of a moss fit switch smaller。

 we would increase its width。But that would add additional capacitance to the switchitch's gate terminal。

 slowing down transitions on the input node that connects to the gate。

It's a fun optimization problem to figure out transistorizing that minimizes the overall propagation delay。



![](img/ae1e6427c681dae347e6eeefe80627ec_3.png)

Although not strictly required by the static discipline。

 it will be useful define find another timing specification called the contamination delay。

It measures how long a gate's previous output remains valid after the gate's inputs start to change and become invalid。

Technically， the contamination delay will be a lower bound on the delay from an invalid input to an invalid output。

We'll make the delay measurements much as we did for the propagation delay。

On a rising input transition， the delay starts when the input is no longer a valid digital zero。

In other words， when VN crosses the VIL threshold。And the delay ends when the output becomes invalid。

 in other words， when V out crosses the VOH threshold。

We can make a similar delay measurement for the falling input transition。

Since the contamination delay， TCD is a lower bound on the delay associated with any input transition。

We'll choose a value for TCD that's less than or equal to the measurements we just made。

Do we really need the contamination delay specification？Usually not。And if it's not specified。

 designers should assume that the TCD for a combinational device is zero。In other words。

 a conservative assumption is that the outputs go invalid as soon as the inputs go invalid。

By the way， manufacturers often use the term minimum propagation delay to refer to a device's contamination delay。

That terminology is a bit confusing， but now you know what it is they're trying to tell you。



![](img/ae1e6427c681dae347e6eeefe80627ec_5.png)

So here's a quick summary of the timing specifications for combinational logic。

 these specifications tell us how the timing of changes in the output waveform labeled B in this example are related to the timing of changes in the input waveform。

 labeled A。A combinational device may retain its previous output value for some interval of time after an input transition。

The contamination delay of the device is a guarantee on the minimum size of that interval。

In other words， TCD is a lower bound and how long the old output value stays valid。

As stated in Note2， a conservative assumption is that the contamination delay of a device is zero。

Meaning the device's output may change immediately after an input transition。

So TCD gives us information on when B will start to change。Similarly。

 it would be good to know when B as guarantee you'd be done changing after an input transition。

In other words， how long do we have to wait for a change in the inputs to be reflected in an updated value on the outputs？

This is what TPD tells us， since it is an upper bound on the time it takes for B to become valid and stable after an input transition。

As Note 1 points out， in general， there are no guarantees on the behavior of the output in the interval after TCD and before TPD。

 as measured from the input transition。It would be legal for the B output to change several times in that interval or even have a non digital voltage for any part of the interval。

As we'll see in the last video of this chapter， we'll be able to offer more insights into Be's behavior in this interval for a subclass of combinational circuits。

But in general， a designer should make no assumptions about B's value in the interval between TCD and TPD。



![](img/ae1e6427c681dae347e6eeefe80627ec_7.png)

How do we calculate the propagation and contamination delays of a larger combinational circuit from the timing specifications of his components？

Our example is a circuit of four Nand gates where each Nand has a TPD of four nanoseconds and a TCD of one nanosecond。

To find the propagation delay for the larger circuit。

 we need to find the maximum delay from an input transition on nodes A， B。

 or C to a valid and stable value on the output Y。To do this。

 consider each possible path from one of the inputs to y and compute the path delay by summing the TPDs of the components along the path。

Choose the largest such path as a TPD of the overall circuit。In our example。

 the largest delay is a path that includes three Nn gates with a cumulative propagation delay of 12 nanoseconds。

In other words， the output Y is guaranteed to be stable and valid within 12 nanoseconds of a transition on A。

 B， or C。To find the contamination delay for the larger circuit。

 we again investigate all paths from inputs to outputs。

 but this time we're looking for the shortest path from an invalid input to an invalid output。

So we sum the TCDs of the components along each path and choose the smallest such path delay as the TCD of the overall circuit。

In our example， the smallest delay is a path that includes two nan gates with a cumulative contamination delay of two nanoseconds。

In other words， the output y will retain its previous value for at least two nanoseconds after one of the inputs goes invalid。



![](img/ae1e6427c681dae347e6eeefe80627ec_9.png)