# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p16 13.2.4 Jumps and Branches -BV19m41127Kj_p16-

We're on the home stretch now for all the instructions up until now。

 the next instruction has come from the location following the current instruction。

 hence the PC plus4 logic。Branches and jumps change that by altering the value in the PC。

The jump instruction simply takes the value in the RA register and makes it the next PC value。

The PC cell Mo in the upper left hand corner lets the control logic select the source of the next PC value。

When PC cell is zero， the incremented PC value is chosen。When PC cell is2。

 the value of the RA register is chosen。We'll see how the other inputs to the PC Cms are used in just a moment。

The jump in branch instructions also cost the address of the following instruction， in other words。

 the PC+ 4 value。To be written to the R C register。When WD cell is0。

 the zero input of the WD cell Mo is used to select the PC+ 4 value as the right back data。

Here's how the data flow works。The output of the PC plus4 adder is routed to the register file。

 and Wf is set to one to enable that value to be written at the end of the cycle。Meanwhile。

 the value of the RA register coming out of the register file is connected to the two input of the PCL Max。

 so setting PCL to 2 will select the value in the RA register as the next value for the PC。

The rest of the control signals are don't cares， except， of course， for the memory right Na。

 which can never be don't care， lest we cause an accidental right to some memory location。



![](img/000b74d9d4488b111b4069efa2f778b2_1.png)

The branch instruction requires an additional adder to compute the target address by adding the scaled offset from the instructions's literal field to the current PC plus4 value。

Remember that we scale the offset by a factor of four to convert it from the word offset stored in the literal to the byte offset required for the PC。

The output of the offset add becomes the one input to the PC Theelms where if the branch is taken。

 it will become the next value of the PC。Note that multiplying by4 is easily accomplished by shifting the literal2 bits to the left。

 which inserts 20 bits at the low order end of the value。And like before。

 the S extension just requires replicating bit ID 15， in this case， 14 times。So。

 implementing this complicated looking expression requires care in wiring up the input to the offset add。

 but no additional logic。We do need some logic to determine if we should branch or not。

The 32 bit Norgate connected to the first read data port of the Reg file tests the value of the RA register。

The Norris output Z will be 1 if all the bits of the RA register value are 0 and0 otherwise。

The Z value can be used by the control logic to determine the correct value for PC cell。

If Z indicates the branch is taken， PC cell will be1。

 and the output of the offset adder becomes the next value of the PC。If the branch is not taken。

 PC cell will be0， and execution will continue with the next instruction at PC+ 4。



![](img/000b74d9d4488b111b4069efa2f778b2_3.png)

As in the jump instruction， the PC+4 value is routed to the register file to be written into the RC register at the end of the cycle。

Meanwhile， the value of Z is computed from the value of the RA register。

 while the branch offset adder computes the address of the branch target。

The output of the offset adder is routed to the PC cell Max。

 where the3 bit value of the PC cell control signal computed by the control logic based on Z。

Determines whether the next PC value is the branch target or the PC+4 value。

The remaining control signals are unused and set to their default。 Don't care values。



![](img/000b74d9d4488b111b4069efa2f778b2_5.png)

We have one last instruction to introduce the LDR or load relative instruction。

 LDR behaves like a normal load instruction， except that the memory address is taken from the branch offset adder。

Why would it be useful to load a value from a location near the LDR instruction？Normally。

 such addresses would refer to the neighboring instructions。

 so why would we want to load the binary encoding of an instruction into a register to be used as data。

The use case for LDR is accessing large constants that have to be stored in main memory because they are too large to fit into the 16 B literal field of an instruction。

In the example shown here， the compiled code needs to load the constant， 1，2，3，4，56。

So it uses an LDR instruction that refers to a nearby location。

 C1 that has been initialized with the required value。

Since this read only constant as part of the program。

 it makes sense to store it with the instructions for the program。

 usually just after the code for a procedure。Note that we have to be careful to place the storage location so that it won't be executed as an instruction。



![](img/000b74d9d4488b111b4069efa2f778b2_7.png)

To route the output of the offset adder to the main memory address port。

 we'll add the A cell mark so we can select either the RA register value when A cell equals0 or the output of the offset adder when A cell equals 1 as the first AU opera。

For LDR， AS cell will be1 and will then ask the AU to compute the Boolean operation A。In other words。

 the Boolean function whose output is just the value of the first operaantt。

This value then appears on the A U output， which is connected to the main memory address port and the remainder of the execution proceeds。

 just like it did for load。This seems a bit complicated。Mr。 Blue has a good question。

 why not just put the AL Mus on the wire leading to the main memory address port and bypass the AOU altogether？

The answer has to do with the amount of time needed to compute the memory address。

If we move the AL mus here。The data flow for load and store addresses would then pass through two mues。

 the B cell mus and the A cell mus， slowing down the arrival of the address by a small amount。

This may not seem like a big deal， but the additional time would have to be added to the clock period。

 though slowing down every instruction by a little bit。When executing billions of instructions。

 a little extra time on each instruction really impacts the overall performance of the processor。

By placing the A cell mus where we did， its propagation delay overlaps that of the B cell mus。

 so the increased functionality it provides comes with no cost in performance。

Here's the data flow for the LDR instruction。 The output of the offset adder is routed through the ALmX to the AOU。

 the ALU performs the Boolean computation A， and the result becomes the address for main memory。

The returning data is routed through the WD cell mus so that it can be written into the RC register at the end of the cycle。

The remaining control values are given their usual default values。



![](img/000b74d9d4488b111b4069efa2f778b2_9.png)