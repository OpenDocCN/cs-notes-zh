# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p76 9.2.2 Programmable Datapaths -BV1DZ421E7Yz_p76-

Okay， we figured out a way to design hardware to perform a particular computation。

 Draw the state transition diagram for an FSM that describes the sequence of operations needed to complete the computation。

 Then construct the appropriate data path using registers to store values in combination logic to implement the needed operations。

Finally， build an FSM to generate the control signals required by the data path。

Is the data path plus control logic itself an FSM？Well。

 it has registers and some combinational logic， so yes， it is an FSM。Can we draw the tooth table？

In theory， yes， in practice， there are 66 Bs of registers and hence 66 Bs of state。

 So our truth table would need two to the 66 throws。H。

 not very likely that we'd be able to draw the tooth table。

The difficulty comes from thinking of the registers and the data path as part of the state of our superFSM；

 that's why we think about the data path as being separate from the control FSM。

So how do we generalize this approach so we can use one computer circuit to solve many different problems？

Most problems would probably require more storage for operaans and results。

And a larger list of allowable operations would be handy。This is actually a bit tricky。

 What's the minimum set of operations we can get away with。As we'll see later。

 surprisingly simple hardware is sufficient to perform any realizable computation。

At the other extreme， many complex operations， for example。

 fast Fer transform are best implemented a sequence of simpler operations。 For example。

 add and multiply， rather than as a single massive combinational circuit。

These sorts of design tradeoffs are what makes computer architecture fun。

We then combine our larger storage with logic for our chosen set of operations into a general purpose data path that could be reused to solve many different problems。

Let's see how that would work。Here's a data path with four data registers to hold results。

 The A cell and B cell multis allow any of the data registers to be selected as either operaan for a repertoire of arithmetic and Boolean operations。

The result is selected by the Opsell Mo and can be written back into any of the data registers by setting the right naval control signal to one and using the 2bit W cell signal to select which data register will be loaded at the next rising clock edge。

Note that the data registers have a load enable control input。 When this signal is one。

 the register will load a new value from its D input。 Otherwise。

 it ignores the D input and simply reloads its previous value。And， of course。

 we'll add a control FSM to generate the appropriate sequence of control signals for the data path。

The Z input from the data path allows the system to perform data dependent operations。

 where the sequence of operations can be influenced by the actual values in the data registers。

Here's the state transition diagram for the control FSM we'd use if we wanted to use this data path to compute factorial。

 assuming the initial contents of the data registers are as shown。

We need a few more states than in our initial implementation since this data pass can only perform one operation at each step。

So we need three steps for each iteration， one for the multiply， one for the decrement。

And one for the test to see if we're done。As seen here。

 it's often the case that general purpose computer hardware will need more cycles and perhaps involve more hardware than an optimized single purpose circuit。

You can solve many different problems with this system， exponentiation， division， square root。

 and so on， so long as you don't need more than four data registers to hold input data intermediate results with the final answer。

By designing a control FSM， we are in effect programming or digital system。

 specifying the sequence of operations it will perform。

This is exactly how early digital computers worked。



![](img/c1fd53643ee87515201fd0da97865e4a_1.png)

Here's a picture of the EnEAC computer built in 1943 at the University of Pennsylvania。

The Wikipedia article on Endic tells us that EnAC could be programmed to perform complex sequences of operations。

 including loops， branches and subrines。The task of taking a problem and mapping it into the machine was complex and usually took weeks。

After the program was figured out on paper， the process of getting the program into anyAC by manipulating its switches and cables could take days。

This was followed by a period of verification and debugging。

 aided by the ability to execute the program step by step。

It's clear that we need a less cumbersome way to program our computer。



![](img/c1fd53643ee87515201fd0da97865e4a_3.png)