# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p14 13.2.2 ALU Instructions -BV19m41127Kj_p14-

Our first task is to work on the data path logic needed to execute AOU instructions with two register operas。

 Each instruction requires the same processing steps。

 Thatch where the 32 bit encoded instruction is read from main memory from the location specified by the program counter。



![](img/bd97655f199945dee5a5d9206f6a9719_1.png)

Dcode， where the Op code field instruction bits 31 to 26。

 is used to determine the values for the data path control signals。

Read where the contents of the register specified by the RA and RB fields。

 instruction bits 20 to 16 and 15 to 11 are read from the register file。

Execute where the requested operation is performed on the two opera end values。

 We' also need to compute the next value for the PC。

And write back where the result of the operation is written to the register file in the register specified by the R C field instruction bits 25 to 21。

The system's clock signal is connected to the register file and the PC register。

At the rising edge of the clock， the new values computed during the execute phase are written to these registers。

The rising clock edge thus marks the end of execution for the current instruction and the beginning of execution for the next instruction。

 The period of the clock， In other words， the time between rising clock edges needs to be long enough to accommodate the accmulative propagation delay of the logic that implements the five steps described here。

Since one instruction is executed each clock cycle。

 the frequency of the clock tells us the rate at which instructions are executed。

If the clock period is 10 nanoseconds， the clock frequency would be 100 megahertz。

 and our beta would be executing instructions at 100 mips。



![](img/bd97655f199945dee5a5d9206f6a9719_3.png)

Here's a sketch showing the hardware needed for the fetch and decode steps。

The current value of the PC register is routed to main memory as the address of the instruction to be fetched。

For AU instructions， the address of the next instruction is simply the address of the current instruction plus 4。

There's an adder dedicated to performing the PC+ 4 computation。

 and that value is routed back to be used as the next value of the PC。

We've also included a mugux used to select the initial value for the PC when the reset signal is1。



![](img/bd97655f199945dee5a5d9206f6a9719_5.png)

After the memory propagation delay， the instruction bits， ID 31 to0。

 are available and the processing steps can begin。Some of the instruction fields can be used directly as is。

 to determine the value for other control signals we need some logic that computes their values from the bits of the Opcode field。



![](img/bd97655f199945dee5a5d9206f6a9719_7.png)

Now， let's fill in the data path logic needed to execute ALU instructions with two Reg opera。

The instruction bits for the 5 bit RA， RB， and RC fields can be connected directly to the appropriate address inputs of the register file。

The R and RB field supplied the addresses for the two read ports。

 and the RC field supplies the address for the right port。

The outputs of the red dataator port are routed to the inputs of the LU to serve as the two opera。

The ALUFN control signals tell the ALU what operation to perform。

These control signals are determined by the control logic from the6 bit op code field for specificity。

 let's assume that the control logic is implemented using a read only memory where the op code bits are used as the Rom's address。

 and the Roms output are the control signals。 Since there are 6 op code bits will need two to the6 or 64 locations on the rom。

We'll program the contents of the roM to supply the correct control signal values for each of the 64 possible op codes。

The output of the A U is routed back to the right data port of the register file to be written into the R C register at the end of the cycle。

We'll need another control signal， Wf， write Na register file that should have the value 1 when we want to write into the RC register。

Let me introduce you to Wf， the 6 W04 mascot， who， of course。

 is named after her favorite control signal， which she's constantly mentioning。

 Let's follow the flow of data as we execute the A instruction。

After the instruction has been fetched， supplying the RA and RB instruction fields。

 the RA and RB register values appear on the read data port of the register file。

The control logic has decoded the Op code bits and supplied the appropriate AOU function code。

You can find a listing of the possible function codes in the upper right hand corner of the beta diagram handout。

The result of the AOU's computation is sent back to the register file to be written into the RC register。

Of course， we'll need to set Wf to one to enable the right。

 Here we see one of the major advantages of a reduced instruction set computer architecture。

The data pathologic required for execution is very straightforward。

 The other form of A U instructions uses a constant as the second A U operaent。

 The 32 B operaand is formed by sign extending the 16 B2s complement constant stored in the literal field Bs 15 to 0 of the instruction。



![](img/bd97655f199945dee5a5d9206f6a9719_9.png)

In order to select the sine extended constant as the second operaand， we added aMux to the data path。

When its B cell control signal is0， the output of the register file is selected as the opera。

When B cell is 1， the cex stated constant is selected as the operaant。

The rest of the data path is the same as before。Note that no logic gates are needed to perform sign extension。

 It's all done with wiring。To sign extend a two complement number。

 we just need to replicate the high order or sign bit as many times as necessary。

You might find it useful to review the discussion of two's complement in lecture 1 of part1 of the course。

So to form a 32 bitop ran from a 16 bit constant， we just replicate its higher a bit， ID15。

 16 times as we make the connection to the B Selms。



![](img/bd97655f199945dee5a5d9206f6a9719_11.png)

During execution of AU with constant instructions， the flow of data is much as it was before。

 The one difference is that the control logic sets the B cell control signal to one。

 selecting the sign extended constant as the second AU opera。As before。

 the control logic generates the appropriate LU function code。

 and the output of the LU is routed to the register file to be written back to the RC register。

Amazingly， this data path is sufficient to execute most of the instructions in the beta ISA。

We just have the memory and branch instructions left to implement。That's our next task。



![](img/bd97655f199945dee5a5d9206f6a9719_13.png)