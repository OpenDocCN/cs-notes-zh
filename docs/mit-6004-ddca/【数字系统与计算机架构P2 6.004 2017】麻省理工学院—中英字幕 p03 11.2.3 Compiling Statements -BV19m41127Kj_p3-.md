# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p03 11.2.3 Compiling Statements -BV19m41127Kj_p3-

Now， let's turn our attention to compile statement。

The first two statement types are pretty easy to handle。

Unconditional statements are usually assignment expressions or procedure calls will simply ask compile expression to generate the appropriate code。



![](img/e5a84a49bec93bf0d57edfe12f32c149_1.png)

Compound statements are equally easy。Or recursively call compileile statement to generate code for each statement in turn。

The code for Statement 2 will immediately follow the code generated for State 1。

Execution will proceed sequentially through the code for each statement。



![](img/e5a84a49bec93bf0d57edfe12f32c149_3.png)

Here we see the simplest form of the conditional statement where we need to generate code to evaluate the test expression。

 And then if the value in the register is false， skip over the code that executes the statement in the then clause。

The simple assembly language template uses recursive calls to compile expression and compile a statement to generate code for the various parts of the if statement。

The full blown， if statement includes an L's clause。

 which should be executed if the value of the test expression is false。

The template uses some branches and labels to ensure the course of execution is as intended。

You can see that the compilation process is really just the application of many small templates that break the code generation task down step by step into smaller and smaller tasks。

 generating the necessary code to glue all the pieces together in the appropriate fashion。



![](img/e5a84a49bec93bf0d57edfe12f32c149_5.png)

And here's the template for the while statement， which looks a lot like the template for the if statement with a branch at the end that causes a generated code to be re executed until the value of the test expression is false。

With a bit of thought， we can improve on this template slightly。

We've reorganized the codes so that only a single branch instruction。

 BT is executed at each iteration instead of the two branches。

 BF and BR per iteration in the original template。Not a big deal。

 but little optimizations to code inside a loop can add up to big savings in a long running program。

Just a quick comment about another common iteration statement， before for loop。

The fore loop is a short hand wave of expressing iterations where the loop index I。

 in the example shown， is run through a sequence of values。

 and the body of the fore loop is executed once for each value of the loop index。

The for loop can be transformed into the while statement shown here。

 which can then be compiled using the templates shown above。



![](img/e5a84a49bec93bf0d57edfe12f32c149_7.png)

In this example， we've applied our templates to generate code for the iterative implementation of the factorial function that we've seen before。

Look through the generated code and you'll be able to match the code fragments with the templates from the last couple of slides。

Is not the most efficient code， but not bad G the simplicity of the recursive descent approach for compiling high level programs。



![](img/e5a84a49bec93bf0d57edfe12f32c149_9.png)

It's a simple matter to modify the recursive descent process to accommodate variable values that are stored in dedicated registers rather than in main memory。

Optimizing compilers are quite good at identifying opportunities to keep values in registers and hence avoid the load and store operations needed to access values in main memory。

Using this simple optimization， the number of instructions in the loop has gone from 10 down to 4。

Now， the generating code is looking pretty good。But rather than keep tweaking the recursive disscent approach。

 let's stop here。In the next segment， we'll see how modern compilers take a more general approach to generating code。

Still， though， the first time I learned about recursive descent。

 I ran home to write a simple implementation and marveled it。

 having authored my own compiler in an afternoon。😊。



![](img/e5a84a49bec93bf0d57edfe12f32c149_11.png)