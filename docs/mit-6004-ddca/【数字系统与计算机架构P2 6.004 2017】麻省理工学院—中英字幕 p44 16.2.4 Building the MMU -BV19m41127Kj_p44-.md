# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p44 16.2.4 Building the MMU -BV19m41127Kj_p44-

There are three architectural parameters that characterize a virtual memory system。

 and hence the architecture of the MMU。P is the number of address bits used for the page offset in both virtual and physical addresses。

V is the number of address bits used for the virtual page number。

 and M is the number of address bits used for the physical page number。

All the other parameters listed on the right are derived from these three parameters。

As mentioned earlier， the typical page size is between 4 kilo and 16 k。

 The sweet spot in the trade off between the downside of using physical memory to hold unwantedwater locations and the upside of reading as much as possible from secondary storage so as to amortise the high cost of accessing the initial word over as many words as possible。

😊，The size of the virtual address is determined by the instruction set architecture。

We're now making the transition from 32 B architectures。

 which support a 4 gigabyte virtual address space to 64 bit architectures。

 which support a 16 exabyte virtual address space。Exa is the SI prefix for 10 to the 18th。



![](img/301ea80fd92187258c212359819cb498_1.png)

A 64 bit address can access a lot of memory。The limitations of a small virtual address have been the main cause for the extension of many instruction set architectures。

Of course， each generation of engineers thinks that the transition they make will be the final one。

I can remember when we all thought that 32 bits was an unimaginably large address。

Back then we were buying memory by the megabyte， and only in our fantasies did we think one could have a system with several thousand megabytes。

Today， CPU architects are feeling pretty smug about 64 bits。

 We'll see how they feel in a couple of decades。The size of physical addresses is currently between 30 bits for embedded processors with modest memory needs。

And 40 plus bits for servers that handle large data sets。

Since CPU implementations are expected to change every couple of years。

 the choice of physical memory size can be adjusted to match current technologies。

Since programmers use virtual addresses， they are insulated from this implementation choice。

The MMU ensures that existing software will continue to function correctly with different sizes of physical memory。

The programmer may notice differences in performance， but not in basic functionality。



![](img/301ea80fd92187258c212359819cb498_3.png)

For example， suppose our system supported a 32 bit virtual address， a 30 bit physical address。

 and a 4 kilobyte page size， so p equals 12 V equals 32 minus 12 or 20。

 and M equals 30 minus- 12 or 18。There are two to the M physicalical pages。

 which is2 to the 18th in our example。There are two to the V virtual pages。

 which is2 to the 20th in our example。And since there is one entry in the page map for each virtual page。

 there are two to the 20th， approximately 1 million page mapap entries。

Each page map entry contains a physical page number。

 an R bit and a D bit for a total of M plus 2 bits， which is 20 bits in our example。

So there are approximately 20 million bits in the page map。

If we were thinkingy of using a large special purpose static RA to hold the page map。

 this would get pretty expensive。

![](img/301ea80fd92187258c212359819cb498_5.png)

But why use a special purpose memory for the page map， Why not use a portion of main memory。

 which we have a lot of and have already bought and paid for。

We could use a register called the Pagemap pointer to hold the address of the Page mapap array and main memory。

 In other words， the page map would occupy some number of dedicated physical pages。

Using the desired virtual page number as an index， the hardware could perform the usual arrayray access calculation。

To fetch the needed page map entry from main memory。



![](img/301ea80fd92187258c212359819cb498_7.png)

The downside of this proposed implementation is that it now takes two accesses to physical memory to perform one virtual access。

The first to retrieve the page table entry needed for the virtual to physical address translation and the second to actually access the requested location。



![](img/301ea80fd92187258c212359819cb498_9.png)

Once again， caches to the rescue， Most systems incorporate a special purpose cache called a translation Lucaide buffer TLB that maps virtual page numbers to physical page numbers。

The TOB is usually small and quite fast。It's usually fully associative to ensure the best possible heat ratio by avoiding collisions。

If the PPN is found using the TLB， the access to main memory for the page table entry can be avoided and we're back to a single physical access for each virtual access。

The hit ratio of a TOB is quite high， usually better than 99%。

This isn't too surprising since locality and the notion of a working set suggests that only a small number of pages are in active use over short periods of time。

As we'll see in a few slides， there are interesting variations to this simple TOB。

 page map and main memory architecture， but the basic strategy will remain the same。



![](img/301ea80fd92187258c212359819cb498_11.png)

Putting it all together， the virtual address generated by the CPU is first processed by the T O B to see if the appropriate translation from VPN to PPN has been cached。

 If so， the main memory access can proceed directly。If the desired mapping is not in the TLB。

 the appropriate entry in the page map is accessed in main memory。If the page is re。

 the PPN field of the page map entry is used to complete the address translation。And of course。

 the translation is cached in the TOB so that subsequent accesses to this page can avoid the access to the page map。

If the desired page is not resident， the MMU triggers a page fault exception and the page fault handler code will deal with the problem。



![](img/301ea80fd92187258c212359819cb498_13.png)

Here's a final example showing all the pieces in action。 In this example， P equals 10， V equals 22。

 and M equals 14。How many pages can reside in physical memory at one time？

There are two to the M physical pages， so two to the 14th。

How many entries are there on the page table？There is one entry for each virtual page and there are two to the V virtual pages。

 so there are two to the 22nd entries in the page table。How many bits per entry in the page table？

Assume each entry holds the PPPN， the resonant bit and the dirty bit。 Since the PPPN is M bits。

 there are M plus 2 bits in each entry， so 16 bits。How many pages does the page table occupy？

There are two to the V page table entries， each occupying M plus2 divided by 8 bytes。

 so the total size of the page table in this example is 2 to the 23rd bytes。

Each page holds2 to the P or2 to the  tenth0th bytes。

 so the page table occupies2 to the 23rd divided by2 to the 10th， equals 2 to the 13th pages。

What fraction of virtual memory can be red at any a given time。

There are two to the V virtual pages of which2 to the M can be resident。

 so the fraction of resident pages is 2 to the M divided by 2 to the V equals 2 to the 14th over 2 the 22nd。

 which is1 over2 to the eighth。

![](img/301ea80fd92187258c212359819cb498_15.png)

What is the physical address for Vit address HeX 1804？

Which MMU components are involved in the translation？First。

 we have to decompose a virtual address into VPN an offset。The offset is a low order 10 bits。

 so is Hex 4 in this example。The VPN is the remaining address bits， so the VPN is 6。

Looking first in the TOB， we see that the VPN to PPPN mapping for VPN6 is cached。

 so we can construct the physical address by concatenating the PPPN2 with the 10 bit offset4 to get a physical address of HeX 804。

You're right。 It's a pain to do all the bit manipulations when P is not a multiple of four。

How about virtualrual Ad Hex 1080？For this address， the VPN is 4 and the offset is 80。

The translation for VPN4 is not cached in the TlB， so we have to check the page map。

 which tells us that the page is resident in physical page 5。Concatenating the PPN an offset。

 we get HeX 1480 is the physical address。Finally， how about Vir address HexFC？

Here the VPN is0 and the offset is FC。The mapping for VPN0 is not found in the TOB。

 and checking the page map reveals that VPN0 is not resident in main memory。

 so a page fault exception is triggered。There are a few things to note about the example TOB in Page mapap contents。

Note that a TOB entry can be invalid， its Rbit is 0。This can happen when a virtual page is replaced。

 So when we change the R to 0 in the page map， we have to do the same in the T， O B。

And should we be concerned that PPN5 appears twice in the page table？

Note that the entry for VPN3 doesn't matter since its orbitbit is0。

Typically when marking a page not resident， we don't bother to clear out the other fields in the entry since they won't be used when R equals0。

So there's only one valid mapping to PPN5。

![](img/301ea80fd92187258c212359819cb498_17.png)