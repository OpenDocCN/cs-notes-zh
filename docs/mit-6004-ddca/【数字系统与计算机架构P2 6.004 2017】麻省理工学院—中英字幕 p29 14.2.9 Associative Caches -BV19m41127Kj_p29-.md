# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p29 14.2.9 Associative Caches -BV19m41127Kj_p29-

A fully associative cache has a tag comparator for each cash line。

 So the tag field of every cash line in a fully assoative cache is compared with the tag field of the incoming address。

Since all cache lines are searched， a particular memory location can be held in any cache line。

 which eliminates the problems of address conflicts causing conflict misses。

The cash line shown here can hold four different forward blocks， regardless of their address。

The example from the end of the previous segment required a cache that could hold two。

 three word blocks， one for the instructions in the loop and one for the data words。

This fully associate of cash would use two of his cache lines to perform that task and achieve 100% hit re show。

 regardless of the addresses of the instruction and data blocks。

Fully associative caches are very flexible and have high hit ratios for most applications。

Their only downside is cost。The inclusion of a tag comparator for each cash line to implement the parallel search for a tag match add substantially to the amount of circuitry required when there are many cash lines。

Even the use of hybrid storage comparison circuitry called a content addressable memory。

Doesn't make a big debt in the overall cost of a fully associative cash。

Direct mapped caches search to only a single cache line。

Fully Associative caches search all cache lines。Is there a happy middle ground where some small number of cache lines are searched in parallel。

Yes， if you look closely at the diagram of the fully Associative cash shown here。

 you'll see it looks like four one line direct map caches operating in parallel。

What would happen if we designed a cache with four multi line direct map caches operating in parallel？



![](img/1e5a0e5b826bbcc331ff734c9d51ba85_1.png)

The result would be what we call a fourway set As of cash。

An N way set Associative cache is really just n direct map caches。

 Let's call them subcas operating in parallel。Each of the N subcas compares the tag field of the incoming address with the tag field of the cache line selected by the index bits of the incoming address。

The endc line searched on a particular request form a search set。

And the desired location might be held in any member of the set。

The fourway set Assso of cash shown here has eight cache lines in each subcache。

So each set contains four cache lines， one from each subcache， and there are a total of eight sets。

 one for each line of the subcas。An Nway set Asiate of cash can accommodate up to end Blocks whose addresses mapped to the same cash index。

So access to up to end blocks with conflicting addresses can still be accommodated in this cache without misses。

This is a big improvement over a direct map cache where an address conflict will cause the current resident of a cash line to be evicted in favor of the new request。

And an N way said Assoative Ca can have a very large number of cash lines。

 but still only have to pay the cost of N tag comparators。

This is a big improvement over a fully associative cache where a large number of cash lines would require a large number of comparators。

So Nway said Associative caches are a good compromise between a conflict prone direct map cache and the flexible。

 but very expensive， fully associative cash。

![](img/1e5a0e5b826bbcc331ff734c9d51ba85_3.png)

Here's a slightly more detailed diagram， in this case of a three way eight set cache。

Note that there's no constraint that the number of ways be a power of two。

 since we aren't using any address bits to select a particular way。

This means the cache designer can fine tune the cash capacity to fill her space budget。

Just to review the terminology， the N cache lines that will be searched for a particular cash index are called a set。

And each of the N subcas is called away。The hit logic in each way operates in parallel with the logic in other ways。

Is it possible for a particular address to be matched by more than one way？

That possibility isn't ruled out by the hardware， but thestead As of cashsh is managed so that doesn't happen。

Assuming we write the data fetch from D RamM during a cachem into a single subcache。

 we'll talk about how to choose that way in a minute。

There's no possibility that more than one subcache will ever match an incoming address。



![](img/1e5a0e5b826bbcc331ff734c9d51ba85_5.png)

How many ways do we need， We'd like enough ways to avoid the cash lying conflicts we experienced with the direct map cache。

Looking at the graph we saw earlier of memory accesses versus time。 we see that in any time interval。

 there are only so many potential address conflicts that we need to worry about。

The mapping from addresses to cash lines is designed to avoid conflicts between neighboring locations。

So we only need to worry about conflicts between the different regions， code， stack， and data。

In the example shown here， there are three such regions。

 and before if you need two data regions to support copying from one data region to another。

If the time interval is particularly large， we might need double that number to avoid conflicts between accesses early in the time interval and accesses late in the time interval。

The point is that a small number of ways should be sufficient to avoid most cash line conflicts in the cache。



![](img/1e5a0e5b826bbcc331ff734c9d51ba85_7.png)

As with block size， it's possible to have too much of a good thing。

There's an optimum number of ways that minimizes the average memory access time。Beyond that point。

 the additional circuitry needed to combine the hit signals from a large number of ways will start to have a significant propag delay of its own。

 adding directly to the cache hit time and the average memory access time。More to the point。

 the charter on the left shows that there's little additional impact on the mis ratio beyond 4 to8 ways。

For most programs， an eight way set associative cash with a large number of sets will perform on a par with the much more expensive fully associative cash of equivalent capacity。



![](img/1e5a0e5b826bbcc331ff734c9d51ba85_9.png)

There's one final issue to resolve with set associative and fully associative caches。

When theres a cache miss， which cache line should be chosen to hold the data that will be fetched from main memory。

That's not an issue with direct map caches since each data block can only be held in one particular cache line determined by its address。

But in N way set associative caches， there are n possible cache lines to choose from one in each of the ways。

And in a fully associative of cash， any of the cash lines could be chosen。So how to choose？

Our goal is to choose to replace the contents of the cache line。

 which will minimize the impact on the hit ratio in the future。



![](img/1e5a0e5b826bbcc331ff734c9d51ba85_11.png)

The optimal choice is to replace the block that is accessed furthest in the future or perhaps is never accessed again。

 but that requires knowing the future。Here's an idea。

 let's predict future accesses by looking at recent accesses and applying the principle of locality。

If a block has not been recently accessed， it's less likely to be accessed in the near future。



![](img/1e5a0e5b826bbcc331ff734c9d51ba85_13.png)

That suggests the least recently used replacement strategy， usually referred to as LRU。

Weplace the block that was accessed furthest in the past。LRU works well in practice。

 but requires us to keep a list ordered by last use for each set of cash lines。

 which would need to be updated on each cash access。



![](img/1e5a0e5b826bbcc331ff734c9d51ba85_15.png)

When we needed to choose which member of a set to replace， we'd choose the lash Ca line on this list。

For an eightway set Assoative cache there are eight factorial possible orderings。

 so we need log base2 of 8 factorial or 16 state bits to encode the current ordering。

The logic to update these state bits on each access isn't cheap， Basically。

 you need a lookup table to map the current 16 bit value to the next 16 bit value。

So most cache has implemented an approximation to LRU where the update function is much simpler to compute。

There are other possible replacement policies first in first out。

 where the oldest cache line is replaced， regardless of when it was last accessed and random。

 where some sort of pseudoran number generator is used to select the replacement。

All replacement strategies， except for random， can be defeated。

If you know a caches replacement strategy， you can design a program that will have an abysmal hit rate by accessing address as you know the cache just replaced。

I'm not sure I care about how well a program designed to get bad performance runs on my system。

But the point is that most replacement strategies will occasionally cause a particular program to execute much more slowly than expected。

When all said and done， an LRU replacement strategy or a close approximation is a reasonable choice。



![](img/1e5a0e5b826bbcc331ff734c9d51ba85_17.png)