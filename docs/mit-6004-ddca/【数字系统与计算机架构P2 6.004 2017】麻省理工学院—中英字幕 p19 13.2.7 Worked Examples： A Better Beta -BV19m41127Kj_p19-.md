# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p19 13.2.7 Worked Examples： A Better Beta -BV19m41127Kj_p19-

In this problem， we are going to consider several instructions that we want to add to our beta。

For each of these instructions， we will need to decide what the minimum requirement is to add that instruction。

The simplest addition would be a macro that references a single already existing beta instruction。

If our new instruction cannot be implemented by simply defining a macro。

 then we want to consider whether adding a new op code and producing the appropriate control Rom signals for it will enable the new operation to be executed on our existing beta data path。

Finally， if neither the macro or control RoOM solutions work。

 then we need to specify that the instruction cannot be implemented without making actual hardware changes to the beta。

The first instruction that we want to consider adding to our beta is a swap R instruction which swaps the contents of registers RX and RY in a single clock cycle。

The constraint that this must be done in a single clock cycle points us to the fact that the beta hardware does not have the ability to write to two different registers in the same clock cycle。

Thus， in order to add this instruction to the beta， new hardware would't need to be added。



![](img/84e56076d4ff37ccdcb7d4935389ed3e_1.png)

The next instruction that we want to consider adding to our beta is a Neg instruction。

 This instruction should take the two's complement negation of register R。

 X and store it into register R。 Y。 The first question we want to ask ourselves is whether or not we might be able to implement this using a macro。

Since all we are trying to do is produce the negative of a given value。

 we can write a macro for this instruction which subtracts R x from R31 and stores that result into R Y。

Note that the smRA will not work for the corner case。

 which is the largest representable negative number。

 because the negation of that number cannot be represented using 32 B tos complement representation。

For all other cases， however， this macro works as expected。



![](img/84e56076d4ff37ccdcb7d4935389ed3e_3.png)

The next instruction that we want to consider adding to our beta is a PC relative store instruction。

The way this instruction works is that it writes the contents of register R X to a location and memory whose address is computed by adding PC plus 4 plus 4 times the sign extended constant C。

The only existing store operation in the beta is a store that writes to the address that is computed by adding the contents of register。

 RY and the sign extended literal C。Since this is not equivalent to the store relative instructions' behavior。

 that means that we cannot not implement this instruction as a macro。So next。

 we consider whether or not we can implement this instruction using our existing beta data paths。

The beta diagram highlights in red the data flow through the existing beta data paths that would perform the desired PC relative store instruction。

 The way this instruction works is that the extra adder under the instruction memory。

 is used to compute the value of the effective address。

 which is PC plus 4 plus  four times a extended constant。

 The a cell or a select signal is then set to one to pass that value to the A O U as the a opera。😊。

The A L U F N is then set to a to continue passing that value through the A L U in order for it to be used as the address for the data memory。

 This address is labeled M A or memory address in the beta diagram。

The value that is written to memory is the value of register R X in store operations。

 the first operaan corresponds to register R C。 So we set R A2 cell equal to1 in order to select R C。

 which is R X in this case， as the register whose contents should be written to memory。

The value of this register is made available via the RD2 Reg file port。

 which then feeds the MWD or memoryory right data signal for the memory。

There are a couple other memory related signals that we need to set appropriately。 They are M W R。

 which stands for memory， write， read and controls the right enable of the data memory in order to be able to write to the memory。

 the right enable must be set to one。M O E is the memory output enabled。

 We set this to 0 to specify that no output should be enabled from the memory。

 Note that you may think that M O E should be a don't care。

 since we' are never making use of the M D or memory read data signal in our data path。 However。

 by setting it to 0， we allow ourselves to potentially use the same data bus for the read and write data of the memory。

 This is not explicitly shown in our beta diagram， but is the reason that M Oe is specified as0 for us。

😊，The other control signal that we must set to zero is Wf， which stands for right En Register file。

Setting the signal to 0 ensures that no value will be written back into our register file。

 This allows us to then set W D cell and W A cell to don't cares。The last control signal is B cell。

 which is also a don't care because the B opera and is ignored by the ALU for this instruction。

Finally， the PC cell equals 0 in order to increment the PC by 4 so that the next instruction will be fetched。



![](img/84e56076d4ff37ccdcb7d4935389ed3e_5.png)

So our completed control Ro for the STR operation is shown here。



![](img/84e56076d4ff37ccdcb7d4935389ed3e_7.png)

The last instruction we want to add to our beta is the bit clear R X， RY， Rz instruction。

 This instruction performs a bit wise and of the contents of register RY with a complement of the contents of register RX。

There is no existing beta instruction that performs this functionality。

 so using a macro is not an option。Next， we want to consider whether or not we could implement this instruction using our existing data paths with changes to our control room。

To answer this question， you need to realize that the operation that you are trying to perform here is a Boolean operation。



![](img/84e56076d4ff37ccdcb7d4935389ed3e_9.png)

In moduleule1， when implementing the ALU La， we learn that the way that the Bo module works is that if you set the ALUFN to 10 ABCD。

 then the ALU would produce the output defined by this truth table for every pair of bits。

 B sub I and A subi。So for example， to implement the and function， we simply set a equals to1。

 B equals 0， C equals 0， and D equals 0， as shown in this truth table。

 which is a truth table for an and function。The truth table for the bit clear operation is shown here。

 One additional column， not of R， X of I has been added to show the intermediate step of negating R。

 X of I。 Then if you take the and of the R Y of I column and the not R， X of I columns。

 you get the result R Z of I。This means that the ALU offendend for the bit clear operation is 10。

 followed by 0100。

![](img/84e56076d4ff37ccdcb7d4935389ed3e_11.png)

The rest of the control signals can be determined by looking at this highlighted beta diagram。

 which shows in red the paths that must be followed in order to properly implement the bit clear operation。

 In memory specifies the registers， R A and R B and R case， R X and R。

 Y that are to be used by this operation。Setting RA2 cell to0 tells the register file to read RB or RY as the second op brand。

Then， setting a cell and B cell to0 passes the values of Rx and RY to the ALU。

The ALUFN is used to specify the particular Boolean operation that we are performing。

Then WD cell equals 1 in order to feed the results of the ALU back to the register file。

The R C register is R Z， and it is the register that the result should be written to to make that happen。

 we set W A cell equal to 0 and Wf equal to 1。To avoid anything being written to the data memory。

 M W R is set to 0。 M O E can be a don't care because we are not using the memory for reading or writing and setting W D cell to one ignores anything that is on the M R D or memory V data line。

Finally， the PC cell equals 0 in order to increment the PC by 4 so that the next instruction will be fetched。



![](img/84e56076d4ff37ccdcb7d4935389ed3e_13.png)

So our completed control rum for the bit clear operation is shown here。



![](img/84e56076d4ff37ccdcb7d4935389ed3e_15.png)