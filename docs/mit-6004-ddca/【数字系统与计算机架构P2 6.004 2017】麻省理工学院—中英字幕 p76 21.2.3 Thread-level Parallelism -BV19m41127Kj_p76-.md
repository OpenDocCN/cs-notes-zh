# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p76 21.2.3 Thread-level Parallelism -BV19m41127Kj_p76-

In discussing out of order superscalear pipeline CPUs。

 we commented that the costs grow very quickly relative to the performance gains leading to the cost performance curve shown here。



![](img/806d47e19cc8f25bb30b93731c08fc9f_1.png)

If we move down the curve， we can arrive at more efficient architectures that give， say。

 half the performance at a quarter of the cost。When our applications involve independent computations that can be performed in parallel。

 it may be that we would be able to use two cores to provide the same performance as the original expensive core。

 but at a fraction of the cost。If the available parallelism allows us to use additional cores。

 we'll see a linear relationship between increased performance versus increased cost。The key。

 of course， is that the desired computations can be divided into multiple tasks that can run independently with little or no need for communication or coordination between the tasks。

What is the optimal trade off between core cost and the number of cores？

If our computation is arbitrarily divisible without incurring additional overhead。

 then we would continue to move down the curve until we found the cost performance point that gave us the desired performance at the least cost。

In reality， dividing the computation across many course does involve some overhead。 For example。

 distributing the data and code， then collecting and aggregating the results。

 So the optimal trade off is harder to find。 Still， the idea of using a larger number of smaller。

 more efficient cores seems attractive。 Many applications have some computations that can be performed in parallel。

 but also have computations that won't benefit from parallelism。



![](img/806d47e19cc8f25bb30b93731c08fc9f_3.png)

To understand the speed up we might expect from a floatinging parallelism is useful to perform the calculation proposed by computer scientist Jean Omdll in 1967。

 now known as Omdll's Law。Suppose we're considering an enhancement that speeds up some fraction F of the task at hand by a factor of s。

As shown in the figure， the gray portion of the task now takes f over S of the time that it used to require。

Some simple arithmetics lets us calculate the overall speed up that we get from using the enhancement。



![](img/806d47e19cc8f25bb30b93731c08fc9f_5.png)

One conclusion we can draw is that will benefit the most from enhancements that affect a large portion of the required computations。

In other words， we want to make F as large as possible。



![](img/806d47e19cc8f25bb30b93731c08fc9f_7.png)

What's the best speedup we can hope for if we have many cores that can be used to speed up the parallel part of the task？

Here' is the speedup formula based on F and S， where in this case。

 F is the parallel fraction of the task。If we assume that the parallel fraction of the task can be sped up arbitrarily by using more and more cores。

 we see that the best possible speed up is 1 over 1 minus f。For example。

 you write a program that can do 90% of its work in parallel。

 but the other 10% must be done sequentially。The best overall speed up that can be achieved is a factor of 10。

 no matter how many coreries you have at your disposal。Turning the question around。

 suppose you have a0 core machine which you hope to be able to use to achieve a speed up of 500 on your target application。

You would need to be able to criminalize 99。8% of the computation in order to reach your goal。

Clearly， multico machines are most useful when the target task has lots of natural parallelism。



![](img/806d47e19cc8f25bb30b93731c08fc9f_9.png)

Using multiple independent chos to execute a parallel task is called thread level parallelism。

Where each core executes a separate computation thread。The threads are independent programs。

 so the execution model is potentially more flexible than the lockstep execution provided by vector machines。

When there are a small number of threads， you often see the course sharing a common main memory。

 allowing the threads to communicate and synchronize by sharing a common address space。

We'll discuss this further in the next section。This is the approach used in current multicore processors。

 which have between 2 and 12 cores。Shared memory becomes a real bottleneck when there are tens or hundreds of course。

 since collectively they quickly overwhelm the available memory bandwidth。In these architectures。

 threads communicate using a communication network to pass messages back and forth。

We discussed possible network topologies in an earlier lecture。

A cost effective on chipI approach is to use a nearest neighbor mesh network。

 which supports many parallel point to point communications。

 while still allowing multihop communication between any two course。

Message passing is also used in computing clusters where many ordinary CPUs collaborate on large tasks。

There's a standardized message passing interface， MPI。

 and specialized very high throughput low latency message passing communication networks。

 for example， in Fna band， that make it easy to build high performance computing clusters。

In the next couple of sections， we'll look more closely at some of the issues involved in building shared memory multi corere processors。



![](img/806d47e19cc8f25bb30b93731c08fc9f_11.png)