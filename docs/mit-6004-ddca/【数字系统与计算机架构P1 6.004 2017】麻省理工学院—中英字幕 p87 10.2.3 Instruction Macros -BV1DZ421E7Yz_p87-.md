# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p87 10.2.3 Instruction Macros -BV1DZ421E7Yz_p87-

Let's take a closer look at how macros work in UASM。Here we see the definition of the macro cons。

 which has a single parameter N。 The body of the macro is a sequence of four expressions。

 When there' is an implication of the cons macro in this example， with the argument 37。

 the body of the macro is expanded， replacing all occurrences of N with the argument 37。

The resulting text is then processed as if it had appeared in place of the macromification。

In this example， the four expressions are evaluated to give a sequence of four values that will be placed in the next four bytes of the output array。



![](img/5364d6adee45d3bc75bb95e8703a4d99_1.png)

Macro expansions may contain other macro implicationss， which themselves will be expanded。

 continuing until all this left or expressions to be evaluated。

Here we see the macro definition for word， which assems its argument into two consecutive bytes。

And for the macro long， which assembles its argument into four consecutive bytes。

 using the word macro to process the low 16 B of the value， then the high 16 B of the value。

These two UASM statements cause the constant hex dead beef to be converted to 4 Bs。

 which are then deposited in the output array starting at index Hex 100。

Note that the beta expects the least significant by of a multibyte value to be stored at the lowest byte address。

So the least significant byte Hex EF is placed at address Hex 100。

 and the most significant byte Hex DE is placed at the address Hex 103。

This is the Little Indian convention for multibyte values。 The least significant by comes first。

Intel's X 86 architecture is also little Indian。There is a symmetrical big Indianeian convention where the most significant bite comes first。

Both conventions are in active use， and in fact some I essayAs can be configured to use either convention。

There is no right answer for which convention to use。

 but the fact that there are two conventions means that we have to be alert for the need to convert the representation of multi bi values when moving values between one ISA and another。

For example， when we send a data file to another user。



![](img/5364d6adee45d3bc75bb95e8703a4d99_3.png)

As you can imagine， there are strong advocates for both schemes who are happy to defend their point of view at great length。

Given the heat of the discussion， its appropriate that the names for the conventions were drawn from Jonathan Swift's Gulliver's travels in which a civil war is fought over whether to open a soft boiled egg at its big end or its little end。



![](img/5364d6adee45d3bc75bb95e8703a4d99_5.png)

Let's look at the macros used to assemble beta instructions。

The Beta Op Helper macro supports the three Reg instruction format。

 taking his arguments the values to be placed in the Op code， R， RB， and RC fields。

The Allign for directive is a bit of administrative bookkeeping to ensure that instructions will have a by address that's a multiple of four。

In other words， that they span exactly1，32 B word in memory。

Thus followed by an invocation of the long macro to generate the four bys of binary data representing the value of the expression shown here。

The expression is where the actual assembly of the fuels takes place。

Each field is limited to the requisite number of bits using the modo operator。

 then shifted left to the correct position in the 32 B word。



![](img/5364d6adee45d3bc75bb95e8703a4d99_7.png)

And here are the helper macros for the instructions that use 16 bit constant as the second opera an。



![](img/5364d6adee45d3bc75bb95e8703a4d99_9.png)

Let's follow the assembly of an ADC instruction to see how this works。

The ad C macro expands into an imvocation of the beta Op C helper macro passing along the correct value for the Add C op code。

 along with the three operarans。The beta Op C macro does the following arithmetic。The op argument。

 in this case， the value Hex 30， is shifted left to occupy the high order 6 bits of the instruction。



![](img/5364d6adee45d3bc75bb95e8703a4d99_11.png)

Then the R argument， in this case，15 is placed in its proper location。

The 16 bit constant- 32768 is positioned in the low 16 bits of the instruction， and finally。

 the RC argument in this case 0 is positioned in the RC field of the instruction。



![](img/5364d6adee45d3bc75bb95e8703a4d99_13.png)

You can see why we call this processing， assembling and instruction。

 The binary representation of an instruction is assembled from the binary values for each of the instruction fields。

Is not a complicated process， but it requires a lot of shifting and masking tasks that were're happy to let a computer handle。



![](img/5364d6adee45d3bc75bb95e8703a4d99_15.png)

Here's the entire sequence of macro expansions that assemble this ADC instruction into an appropriate 32 bit binary value in main memory。

You can see that the knowledge of beta instruction formats in op code values is built into the bodies of the macro definitions。

 The UAM processing is actually quite general， with a different set of macro definitions。

 it could process assembly language programs for almost any ISA。



![](img/5364d6adee45d3bc75bb95e8703a4d99_17.png)

All the macro definitions for the beta I SA are provided in the beta UAM file。

 which is included in each of the assembly language lab assignments。

Note that we include some convenience macros to define shorthand representations that provide common default values for certain opera。

 For example， except for procedure calls， we don't care about the PC plus 4 values saved in the destination register。

 by branch instructions。 So almost always， we would specify R 31 as the R C register effectively discarding the PC plus 4 values saved by branches。

So we define two argument branch macros that automatically provide R31 as the destination register。

Save some typing。 And more importantly， it makes it easier to understand the assembly language program。



![](img/5364d6adee45d3bc75bb95e8703a4d99_19.png)

Here are a whole set of convenience macros intended to make programs more readable。 For example。

 unconditional branches can be written using the B R macro rather than the more cumbersome B， E， Q。

 R 31。

![](img/5364d6adee45d3bc75bb95e8703a4d99_21.png)

And it's more readable to use branch false and branch true macros when testing the results of compare instructions。



![](img/5364d6adee45d3bc75bb95e8703a4d99_23.png)

And note the push and pop macros at the bottom of the page。

 These expand into multi instruction sequences in this case。

 to add and remove values from a stack data structure pointed to by the S P register。



![](img/5364d6adee45d3bc75bb95e8703a4d99_25.png)

We call these macros pseudo instructions， since they let us provide the programmer with what appears to be a larger instruction set。

 although underneath the covers， we've just used the same small instruction set repertoire developed in lecture 9。



![](img/5364d6adee45d3bc75bb95e8703a4d99_27.png)

![](img/5364d6adee45d3bc75bb95e8703a4d99_28.png)

In this example， we've rewritten the original code we had for the factorial computation using pseudo instructions。

 for example， CMove is a pseudo instruction from moving small constants into a register。

It's easier for us to read and understand the intent of a constant move operation than an add a value to 0 operation provided by the ads C expansion of C move。

Anything we can do to remove the cognitive clutter will be very beneficial in the long run。



![](img/5364d6adee45d3bc75bb95e8703a4d99_30.png)