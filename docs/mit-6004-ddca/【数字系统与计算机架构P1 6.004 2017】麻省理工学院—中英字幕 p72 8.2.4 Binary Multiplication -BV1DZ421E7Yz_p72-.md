# 【数字系统与计算机架构P1 6.004 2017】麻省理工学院—中英字幕 p72 8.2.4 Binary Multiplication -BV1DZ421E7Yz_p72-

One of the biggest and slowest circuits in an arithmetic and logic unit it is the multiplier。

We'll start by developing a straightforward implementation and then in the next section。

 look into trade offs to make it either smaller or faster。

Here's the multiplication operation for two unsigned forbid opera broken down into its component operations。

This is exactly how we learn to do it in primary school。 We take each digit of the multiplier。

 the B operaand， and use our memorized multiplication tables to multiply it with each digit of the multiple can。

 the A operaand。Dealing with any carries into the next column as we process the multiple canned right to left。

The output from this step is called a partial product。

 and then it will repeat the step for the remaining bits of the multiplier。

Each partial product is shifted one digit to the left。

 reflecting the increasing weight of the multiplier digits。In our case。

 the digits are just single bits， in other words， they're zero or 1。

 and the multiplication table is pretty simple。In fact。

 the one bit by one bit binary multiplication circuit is just a two input end gate。And look， mom。

 no carries。The partial products are n bits wide since there are no carries。

If the multiplier has M bits， there will be M partial products。

 and when we add the partial products together， we'll get an N plus M bit result if we account for the possible carryout from the high order bit。



![](img/a8f24cc102045a614c10c5b384c4269d_1.png)

The easy part of the multiplication is forming the partial products。

 it just require some mandate gates。The more expensive operation is adding together the M andbit partial products。



![](img/a8f24cc102045a614c10c5b384c4269d_3.png)

Here's the schematic for the combinational logic needed to implement the4 by4 multiplication。

Which would be easy to extend for larger multipliers。 We'd need more rows or larger multiple cans。

 We'd need more columns。The M times n to input and gauge compute the bits of the M partial products。

The adder modules add the current rows partial product with the sum of the partial products from the earlier rows。

Actually， there are two types of adder modules， the full adder is used when the modules need three inputs。

The simpler half adder is used when only two inputs are needed。

The longest pass through this circuit takes a moment to figure out。

Information is always moving either down a row or left to the adjacent column。

 since there are M rows and in any particular row and columns。

 there are most n plus M modules along any path from input to output。So the latency is order n。

 since M and n differ by just some constant factor。Since this is a combinational circuit。

 the throughput is just one over the latency。And the total amount of hardware is order n squared。

In the next section， we'll investigate how to reduce the hardware costs or separately how to increase the throughput。

But before we do that， let's take a moment to see how the circuit would change if the opera rans were two complement integers instead of unsigned integers。



![](img/a8f24cc102045a614c10c5b384c4269d_5.png)

With a two complement multiplier and multican， the higher bit of each has negative weight。

So when adding together the partial products， we'll need to sign extend each of the M bit partial products to the full n plus M bit width of the addition。

 this will ensure that a negative partial product is properly treated when doing the addition。

And of course， since the higher a bit of the multiplier has a negative weight。

 we'd subtract instead of add the last partial product。Now， for the clever bit。

 we'll add once to various of the columns and then subtract them later with the goal of eliminating all the extra additions caused by the sine extension。

 We' also rewrite the subtraction of the last partial product as far as complementing the partial product and then adding one。

This is all a bit mysterious， but。Here in step 3， we see the effect of all the step2 machinations。

Let's look at the higher bit of the first partial product， x3， y0。

If that partial product is non negative， x3Y0 is a 0。

So all the sign extension bits are zero and can be removed。

The effect of adding1 in that position is to simply complement x3 y0。On the other hand。

 if that partial product is negative， x3 y0 is 1， and all the sine extension bits are 1。Now。

 when we add a1 in that position， we complement the x3Y0 bit back to0， but we also get a carry out。

When that's added to the first sign extension bit， which is itself a one。

 we get0 with another carryout and so on with all the signine extension bits eventually getting flipped to zero as the carry ripples to the end。

Again， the net effect of adding a1 in that position is to simply complement x3， y0。

We do the same for all the other signine extended partial products。

 leaving us with the results shown here in the final step。

 we do a bit of arithmetic on the remaining constants to end up with this table of work to be done。

Somewhat to a surprise， this isn't much different than the original table for the unsigned multiplication。

There are a few partial product bits that need to be complemented。 and two。

1 Bs that need to be added to particular columns。 The resulting circuitry is shown here。

 We've changed some of the antis to Nants to perform the necessary compliments。

 and we've changed the logic necessary to deal with the  two1 Bs that needed to be added in。



![](img/a8f24cc102045a614c10c5b384c4269d_7.png)

The colored elements show the changes made from the original unsigned multiplier circuitry。Basically。

 the circuit for multiplying two complement operas has the same latency。

 throughput and hardware costs as the original circuitry。



![](img/a8f24cc102045a614c10c5b384c4269d_9.png)