# 【数字系统与计算机架构P2 6.004 2017】麻省理工学院—中英字幕 p22 14.2.2 SRAM -BV19m41127Kj_p22-

SRAs are organized as an array of memory locations。

 where a memory access is either reading or writing all the bits in a single location。

Here we see the component layout for an8 location SRAM array where each location holds six bits of data。

You can see that the individual bid cells are organized as eight rows。

 one row per location by six columns， one column per bit in each memory word。

The circuitry around the periphery is used to decode addresses and support Read and write operations。

To access the SRAM， we needed to provide enough address bits to uniquely specify the location。

In this case， we need three address lines to select one of the eight memory locations。

The address decoder logic sets one of the eight word lines， the horizontal wires in the array。

 high to enable a particular row location for the upcoming access。

The remaining word lines are set low disabling the cells they control。

The active word line enables each of the SRAM bit cells on the selected row。

 connecting each cell to a pair of bit lines， vertical wires in the array。During read operations。

 the bit lines carry the analog signals from the enabled bit cells to the sense amplifiers。

 which convert the analog signals to digital data。During right operations。

 incoming data is driven onto the bit lines to be stored into the enabled bit cells。

Larger SRAs will have a more complex organization in order to minimize the length and hence the capacitance of the bit lines。



![](img/0a83761e46073439d2ec25d56d08adc7_1.png)

The heart of the Sram or the bit cells。 The typical cell has two Cmoss inverters wired in a positive feedback loop to create a by stable storage element。

😊，The diagram on the right shows the two stable configurations。In the top configuration。

 the cell is stor a 1 bit。In the bottom configuration is storing a zero bit。

The cell provides stable storage in the sense that as long as there's power。

 the noise immunity of the inverters will ensure that the logic values will be maintained。

 even if there is electrical noise on either inverter input。

Both sides of the feedback loop are connected via access Fes to the two vertical bit lines。

When the word line connected to the gates of the access fes is high， the fes are on， in other words。

 they will make an electrical connection between the cell's internal circuitry and the bit lines。

When the word line is low， the access fetchs are off。

 and the bystable feedback loop is isolated from the bit lines and will happily maintain the storedward value as long as there's power。



![](img/0a83761e46073439d2ec25d56d08adc7_3.png)

During a read operation， the drivers first recharge all the bit lines to VDD， In the words。

 a logical one value， and then disconnect， leaving the bit lines floating at one。

Then the address decoder sets one of the word lines high。

 connecting a row of its cells to their bit lines。Each cell in the selected row then pulls one of its two bit lines to ground。

 And this example is the right bit line that's pulled low。

Transitions on the bit lines are slow since the bit line has a large total capacitance。

 and the mosfets in the two inverters are small to keep the cell as small as possible。

The large capacitance comes partly from the bit line's length and partly from the diffusion capacitance of the access fences in the other cells in the same column。

Rather than wait for the bit line to reach a valid logic level。

 S amplifiers are used to quickly detect the small voltage difference developing between the two bit lines and generate the appropriate digital output。

Since detecting small changes in a voltage is very sensitive to electrical noise。

 the SRAM uses a pair of bit lines for each bit and a differential sense amplifier to provide greater noise immunity。

As you can see， designing a low latency SRAM involves a lot of expertise with the analog behavior of Mosfets and some cleverness to ensure electrical noise will not interfere with the correct operation of the circuitry。



![](img/0a83761e46073439d2ec25d56d08adc7_5.png)

Right operations start by driving the bit lines to the appropriate values。In the example shown here。

 we want to write a 0 bit into the cell。 So the left bit line is set to gr。

 and the right bit line is set to VD D。As before the address decoder then sets one of the word lines high。

 selecting all the cells in a particular row for the right operation。

The drivers have much larger mossfets than those in the cells inverters。

So the internal signals in the enabled cells are forced to the values on the bit lines。

 and the bisable circuits flip into the new staple configuration。

We're basically shing together the outputs of the driver and the internal inverter。

 So this is another analog operation。 This would be a no no in a strictly digital circuit。😊。

Since Nphts usually carry much higher source strain currents than PFts of the same width。

 And given the threshold drop of the NFt access transistor。

 almost all the work of the right is performed by the large npht pull down transistor connected to the bit line with the zero value。

 which easily overpowers the small P fatt pull up of the inverters in the cell。Again。

 SRAM designers need a lot of expertise to correctly balance the sizes of mosfets to ensure fast and reliable right operations。



![](img/0a83761e46073439d2ec25d56d08adc7_7.png)

Its not hard to augment the SraM to support multiple readrite ports。

 A handy addition for register file circuits。 Well do this by adding additional sets of word lines。

 bit lines and drivers and senseapps。 This will give us multiple paths to independently access the by stable storage elements in the various rows of the memory array。

With an endport SRA for each bit will need n word lines。2N bit lines and 2N access Fs。

The additional word lines of increase the effect height of the cell and the additional bit lines increase the effect of width of the cell。

 and so the error required by all these wires quickly dominates the size of the SRAM。

Since both the height and width of a cell increased when adding ports。

 the overall area grows as the square of the number of readwrite ports。

So one has to take care not to gratuitously add pores， lest the cost of the SRAM get out of hand。



![](img/0a83761e46073439d2ec25d56d08adc7_9.png)

In summary， the circuitry for the S Ram is organized as an array of bit cells with one row for each memory location and one column for each bit in a location。

 Each bit is stored by two inverters connected to form a bisple storage element。😊。

Reads and writes are essentially analog operations performed via the bit lines and excess fit。

The SRAM uses six moss spects for each bit cell。Can we do better。

Was the minimum number of mosspats needed to store a single bit of information。



![](img/0a83761e46073439d2ec25d56d08adc7_11.png)