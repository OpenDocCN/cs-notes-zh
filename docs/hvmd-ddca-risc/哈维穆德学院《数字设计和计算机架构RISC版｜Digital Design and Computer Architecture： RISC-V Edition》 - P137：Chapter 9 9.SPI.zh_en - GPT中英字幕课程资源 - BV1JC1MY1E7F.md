# 哈维穆德学院《数字设计和计算机架构RISC版｜Digital Design and Computer Architecture： RISC-V Edition》 - P137：Chapter 9 9.SPI.zh_en - GPT中英字幕课程资源 - BV1JC1MY1E7F

Hello， in this video， we'll be talking about the serial peripheral interface SI。



![](img/d2ca9a588d26664dd4b9b031723e6f71_1.png)

So SPI is a simple way to connect devices with just a few wires。In an SPI system。

 you have a master device that's communicating with one or more slave devices。

The master sends out a clock signal。And a data out signal。

 and the slave responds with a data in signal。And the master can optionally send slave select signal to indicate which one of several slaves are chosen。

So the signals are called SCK or SCLK， that's the serial clock， it's generated by the master。

 and there's one pulse for each bit being transmitted。嗯。

Mosy master out Sla Inn is the serial data coming from the master to the slave。

 And I really like this name because it's completely unambiguous。

 which is which something like transmit data， Is that with respect to the master。

 is it with respect to the slave。Then misso master en slavelave out is the data coming back from the slave to the master。

The name of the。Slave selects varies with manufacturers。

 sometimes it's called slave select or chip andable or chip select。

 often there's a bar over top or a B afterward or an end before it to mean not or bar。

Because they're usually active low to select which slave。

So one of the beauties of SPI is the slave hardware can just be a shift register。

 the master will generate the clock。And it will send this master out slave in。

 So before the first at clock edge， it will put Bi 7 on Mosey。On the rising edge of the clock。

 the slave will capture that bit。And the slave can also respond with its own data being sent back。

Then on the next clock edge， the slave will capture bit six on the next clock edge， bit five。

 bit four。3 bit2 bit1 bit zero， so after eight rising edges of the clock。

 the eight bit shift register in the slave has received eight bits of data one at a time。

And optionally set back data， perhaps from the same。Shift register。

And then you see the Shi enable or sev select。Is low for the entire time that the data is being transmitted to enable the。

Slave device。Here's an example of a slave master talking to three different slaves。

 so the serial clock comes from the master and it goes to all three slaves。Same way。

 master out S in goes to all three slaves。The master enslave out comes from。

All three slaves back to the master， but only one can be driving it at the time。

 while the others need to be tritating。So the master generates the slave select signals，1，2。

 and 3 to indicate which slave should。And capture data from Mosie and respond with Mesa。

Here's another case of a particularly simple。systemystem where there is just one master and one slave。

 and so the slave select is not need it at all。It is important to remember to tie the grounds together in the system because voltages are relative to ground if you don't have a common ground。

 the master and slave aren't able to communicate reliably。So as we said。

 slave select is typically active of low and it might have one of a variety of names the slave is enabled when the slave select is low。

Disabling the slave device may save power consumption， for instance， if it's a data converter。

 turning off the slave select might turn off power to some resistors inside the data converter。

 save energy。So there are a variety of options of how to hook this up。

 if there's one slave device and you don't care about power consumption。

 you could just tie the slave select for that device low， so it's always enabled。

If there are multiple slave devices。You might use GPIO pins。

To turn on the desired device before the SPI transaction takes place and then turn it back off afterward。

Or when there's one or more devices， some SPI controllers have。

Some options to automatically pulse the desired slave select signal during transaction。

 so you program up the master to say which slave you want to talk to and it generates the pulses on the slave select pin for that slave。

So in general to set up SPI communication， first you need to decide which SPI port you're working on。

So our chip F310 has two。SPI ports， SI 1 and SI 2。 And let's use SI1 just for the sake of concreteness。

 There's actually an S 0 as well， but that one is used for communication with the program memory flash chip。

 So it's not available to us for peripherals。Then you need to make the。

Pins for the SPI port into the appropriate IO mode。

 so they function as a special purpose rather than as a general purpose IO。So。

Pick the pins for S clock， meo and Mosy， and put them into the IO0 and put out function  zero。

And if we're using the chip select， we also need to set that mode。Then we need to set the bo rate。

 that's how fast a clock works， and typically on a breadboard。

 you want to set a bo rate of megahertz or less because at high speed signals tend to get distorted on a breadboard and you might not get reliable communication。

SPI is programmable， typicallyp we set our clock polarity and clock phase to zero clock polarity of zero means the clock is initially low。

And then rises。Each time we want to set a bit。AC polarity of one and the clock is initially high。

And then drops。Clock phase。It tells us whether we want to sample the bit。

From Mosie on the first clock transition or the second clock transition。

 and if you care about this stuff， there's diagrams in the data sheet。But for most devices。

 the polarity phase of zero is correct。And for certain devices。

 there are other control registers you might want to set up。Typically， the defaults are adequate。

All right， so now if we want to transmit data， first we take a look at the full flag in the transmit data register to see if there's space to send new data。

We have to wait for that full flag to be zero before we can send something new on the SPI。

Then we write a byte into the Tx data data field。And the SI will start churning away。

 It will make eight clocks and send out the8 bits of data one at a time， and meanwhile。

 receive 8 bits of data back from the slave。嗯。To look at the data we get back。

We go and read the AC data register。And we look at the empty flag。

 which is the most significant bit of that register。 If it's one。

 it means received data is still empty， there is no data yet。

 so we keep repeating reading that register until。The empty flag is zero。And at that point。

 we look in the RX data field and it's got the data sent back。But when you do that read。

 it also takes that data out of the queue coming back。

You can only read it once when the flag is zero， you can't keep reading it because you've lost it after the first time you read it。

So this will all become a little clearer as we do an example， let's talk about the SPI registers。

So there are two SPI ports on our chip at these different base addresses。

Key registers are that bo rate register， the clock mode register transmit data and receive。

So the bo rate register on the bottom 12 bits indicate the clock divisor。

And the frequency of the SPI clock。Is the。Frequency of a qua clock divided by two times one plus the divisor。

 So if we set div to 15。Dip plus 1 is 16，16 times 2 is 32。 So F S clock。Equals 16 megaHtz。Over 32。

Equals 0。5 megahertz。Which is a fairly safe speed for transmitting even with Ausy brboard wiring。

Clock mode， if we wanted to set the phase in polarity， we'd do that in the bottom two bits。

Transmit data register。First， we look at bit 31 to see if it's full， once it's not full。

 we write to bit 7 through0 to transmit data。Similarly for the received data。

 we look at bit 31 to see when it's empty， so long as it's empty。

 we keep reading it once it becomes not empty， once the empty bit is zero。

 then there's data waiting for us and we find it in the bottom 8 bits。

So let's write a library to do SPI in our device driver。So first， we need to define these registers。

Here are。From the data sheet。The registers we just talked about， for instance， the SC divider。

Has 12 bit divisor field。And here we can declare a structure called S clockive bits that has a 12 bit field。

Indicating the divis。So this syntax of this structure， this is called bit fields。And。第。Overall。

 register is 32 bits， UN 32。But of those 32 bits， 12 bits。The bottom 12 bits syndicate the divisor。

 the next 20 are unused。Similarly， we could declare the bits for the S clock mode register。

The bottom bit is the phase， the next bit is the polarity， and the upper 30 bits are unused。

For the transmit data register， the bottom eight bits are the data。

The next 23 bits are unused and the top bit is the full fl。And the same thing with the Rx data。

So now we have to find different data types for each of these different registers。

 this is somewhat different than GPIO where each register just had 32 bits to control 32 different GPIOs。

Now we can declare a structure for the overall SPI。Memory map Ti。

So at an offset of zero is the S clock div。So the first thing in the structure will be S clock div。

 and it will be of type S clock div bits。Which is what we defined up here with 12 bits for the divisor。

Next will be SC mode。It's right here， an offset of four。That was a type Scot mode bit。

With two bits specifying the phase and polarity， the other 32 unused。Now。

The next two locations in the memory map are just unused。So to specify that in our structure。

 we can have an array of two UNent 32s，24 byte numbers。And we'll call them reserved。

That means that later the manufacturer might add some more registers to the system。

 but right now there's nothing at that address。Then we have register for the chip select ID。

Register for ship select default， ship Select mode。

 Those are ones we haven't talked about and usually they're okay by default。

There's some delay registers that we usually don't have to deal with a frame format we'll ignore for now。

 but then we get to the good stuff， transmit data。Received data registers。

 which were of type Tx data bits and Rx databasebits。And those now。Are an offset of 48 and 4 C。

So the four most interesting registers to us are the ones underlined here。But as you see。

 SPI has a variety of other registers that could be used for configuration。

So now let's put together our library。So we need to define the base address。

 This is for the QSPI used for the memory interface。 We probably won't use that one。

 SI 1 starts set 1，0，0，2，4000。And SPR2 at 1003-4000。And then we'll set up some pointers。

Both are pointers to。An SI structure at these base addresses。All right， so。Now。

 before we could use the SPI， we need to initialize it have an initialization function that takes the clock divisor。

Their phase and the polarity and sets that all up。So we begin by calling the pin mode function。

 and we'll set pins two， three， and4 for the SPI all to IO function zero to use them for SPI。Then。

We'll configure some of the SPI control registers， remember we can dereference that pointer to SPI1。

 look at the S clock div， go into the div field that 12 bit field with the clock divisor and put in clock divide。

Same with bit0 and bit1 of S clock mode will put in the polarity。

Now here's a function that will send eight bits and receive eight bits back。

So the first thing we need to do is look at the Tx data registered， see if it's full。

 as long as it is， we just wait。Once we leave this while loop。Transmit data register has room in it。

 so we can even write to the Tx data data field。With the value toent。

And the nice thing about these bit fields is she does all the work to change Sistto's bottom 8 bits of the register without touching the other bits。

Now to receive data back， we're going to need to wait until the received data is not empty and then look at the data field。

But the trick is， once there's data， we can only read it once and the act of reading the data also clears it out。

So we'll do a loop。And we will read the SI Rx data field。We'll look at the empty field。

If it's not empty。Then we got something good。So we want to return the bit data field。Otherwise。

 there was nothing in the RX data， so we would go back to the while loop and we keep reading the register until it's no longer empty。

Now let's talk about generalizing the pin mode function。

 Previous our pin mode had handled input and output。

 but now we wanted to be able to handle a GPIO IO function0。

And you might remember file function zero， we need to set the IO function enable bit。

For that GPIO and also set the IO function select to0 for function  zero or to one。For function 1。

So we can define two new modes。In addition to input and output。

 we now have IO function  zero and IO function 1。Pa mode can put us in any。All right。

Now let's write a little program to do some SP communication。

 We needed to include STD int to get our data types and the easy red 5 IO。Then in our main function。

Let's declare a an8 bit integer。For the value we're sampling from SI。

We'll initialize the SPI at 500 kilohertz。Phase and polarity being zero。And then repeatedly。

 let's send a message of。60， which is 0，1，1，0，0，0，0，0。Then we'll send another message of all zeros。

And we'll get back 8 Bs。 We'll store them in the sample register。

 and maybe we could print what we received and wait。 So this would keep sending messages。

And getting back value。

![](img/d2ca9a588d26664dd4b9b031723e6f71_3.png)