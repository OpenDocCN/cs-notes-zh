# 计算机科学的数学基础：P32：L1.11.7- 停机问题 🛑

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_1.png)

在本节课中，我们将要学习对角线论证的一个核心应用：**停机问题**。我们将看到如何利用对角线论证来证明存在不可计算的集合，并具体探讨一个著名的不可判定问题——停机问题。通过本课，你将理解为什么某些看似简单的问题，计算机却无法解决。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_3.png)

---

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_5.png)

## 概述：对角线论证与不可计算性

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_7.png)

上一节我们介绍了对角线论证，它是一种处理无限集合的优雅方法。本节中我们来看看对角线论证在计算理论中的一个基本应用：证明存在**不可计算的集合**。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_9.png)

我们已经知道，存在**不可数**多个无限的二进制字符串。这是因为无限的二进制字符串与自然数的子集（即自然数的幂集）之间存在一个简单的双射关系。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_11.png)

现在，让我们考虑其中那些被称为**可计算字符串**的字符串。所谓可计算字符串，是指存在一个简单的程序，能够计算出该字符串的任意第n位数字。用公式表示，即存在一个程序 `P`，对于字符串 `s`，当输入 `n` 时，`P(n)` 能返回 `s` 的第 `n` 位。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_13.png)

```
定义：字符串 s 是可计算的，当且仅当存在一个程序 P，使得对于任意 n，P(n) = s[n]。
```

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_15.png)

由于程序本身可以用有限的ASCII字符串来描述，而所有有限ASCII字符串的集合是**可数**的（你可以按长度顺序列出它们，就像对二进制字符串所做的那样），因此，只有可数多个可能的程序。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_17.png)

由此我们可以立即得出结论：只有**可数**多个可计算的无限二进制序列。然而，我们已经知道无限的二进制序列集合是**不可数**的。因此，必然存在**不可计算**的无限二进制序列。事实上，绝大多数无限二进制序列都是不可计算的。

这是一个抽象的存在性结果。但一个合理的问题是：它们具体是什么样子的？我们能否找到一个具体的、有意义的计算问题，并证明它是不可计算的？

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_19.png)

---

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_21.png)

## 停机问题：一个具体的不可判定问题

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_23.png)

接下来，我们将探讨一个特定的、明智的具体计算问题：**停机问题**。

停机问题是指：给定一个程序（描述）和一个输入参数，判断该程序在给定输入上运行是否会**成功停止**（即返回值），还是会永远运行下去、返回错误或发生其他“不好”的事情。

我们说一个程序在输入上“停机”，当且仅当它成功返回一个值。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_25.png)

**停机问题是不可判定的**。这意味着，不存在一个通用的程序 `H`，当输入任意程序 `P` 的描述及其输入 `x` 时，`H(P, x)` 总能正确判断 `P(x)` 是否会停机。

为了更详细地分析，让我们将讨论范围限定在**字符串过程**上。我们考虑那些接受字符串作为参数的程序，并且程序本身也用ASCII字符串描述。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_27.png)

我们定义一个字符串属性 `halts`：
一个字符串 `s` 具有 `halts` 属性，当且仅当将 `s` 解释为一个过程描述 `P`，并将 `P` 应用于它自身（即输入 `s`）时，`P(s)` 成功停止。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_29.png)

```
定义： halts(s) 为真 ⇔ 程序P_s（由s描述）在输入s上成功停止。
```

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_31.png)

这体现了“对角线”的思想：我们将描述程序 `P_s` 的字符串 `s`，作为输入喂给 `P_s` 本身。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_33.png)

---

## 证明：不存在停机判定器

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_35.png)

现在，我们通过反证法来证明不存在能判定 `halts` 属性的程序。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_37.png)

1.  **假设存在判定器**：为了引出矛盾，我们假设存在一个程序 `Q`，它是一个完美的停机判定器。
    *   对于任意输入字符串 `s`，`Q(s)` 返回 `"yes"`，当且仅当 `halts(s)` 为真（即 `s` 描述的进程在输入 `s` 上停止）。
    *   `Q(s)` 返回 `"no"`，当且仅当 `halts(s)` 为假（即永远运行、出错等）。

2.  **构造“互补”程序**：我们利用 `Q` 来构造一个新程序 `Q‘`。`Q‘` 的行为与 `Q` 在输出上“互补”：
    *   如果 `Q(s)` 返回 `"yes"`，则 `Q‘(s)` **不返回任何值**（即它不停止，例如进入无限循环）。
    *   如果 `Q(s)` 返回 `"no"`，则 `Q‘(s)` 返回 `"yes"`。

    ```
    定义 Q‘(s):
        if Q(s) == "yes":
            while True: pass  # 进入无限循环，永不停止
        else if Q(s) == "no":
            return "yes"
    ```

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_39.png)

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_41.png)

3.  **考虑 Q‘ 自身的描述**：令 `t` 为描述程序 `Q‘` 的ASCII字符串。现在，我们问：`halts(t)` 是真还是假？即 `Q‘(t)` 会停止吗？

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_43.png)

4.  **推导矛盾**：
    *   根据 `halts` 的定义：`halts(t)` 为真 ⇔ `Q‘(t)` 停止。
    *   根据 `Q‘` 的定义：
        *   如果 `Q(t)` 返回 `"yes"`（即 `halts(t)` 为真），那么 `Q‘(t)` 会进入无限循环，**永不停止**。
        *   如果 `Q(t)` 返回 `"no"`（即 `halts(t)` 为假），那么 `Q‘(t)` 会返回 `"yes"`，**成功停止**。
    *   将两者结合：
        *   `halts(t)` 为真 ⇒ `Q(t)` 返回 `"yes"` ⇒ `Q‘(t)` 永不停止 ⇒ `halts(t)` 为假。**矛盾**。
        *   `halts(t)` 为假 ⇒ `Q(t)` 返回 `"no"` ⇒ `Q‘(t)` 成功停止 ⇒ `halts(t)` 为真。**矛盾**。

    我们得到了一个逻辑矛盾：`halts(t)` 为真当且仅当它为假。这不可能。

5.  **结论**：因此，我们最初的假设——存在一个完美的停机判定器 `Q`——是错误的。**停机问题是不可判定的**。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_45.png)

---

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_47.png)

## 推论：不存在完美的类型检查器

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_49.png)

上述关于停机问题的不可判定性结论，可以推导出许多其他问题的不可判定性。以下是一个贴近实际兴趣的例子：**类型检查**。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_51.png)

一个完美的类型检查器 `C`，输入一个程序（字符串）`s`，应能判断运行 `s` 是否会导致运行时类型错误。
*   `C(s)` 返回 `"yes"`，表示 `s` 会导致类型错误。
*   `C(s)` 返回 `"no"`，表示 `s` 是类型安全的。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_53.png)

我们将证明：如果存在这样的完美类型检查器 `C`，我们就可以用它来解决停机问题。既然停机问题不可解，那么完美的类型检查器也不可能存在。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_55.png)

**证明思路如下**：

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_57.png)

1.  假设存在完美类型检查器 `C`。
2.  我们利用 `C` 来构造一个停机判定器 `H`，其工作流程如下：
    *   输入：一个程序描述字符串 `s`。
    *   `H` 首先构造一个新程序 `s‘`。`s‘` 是一个“解释器”，它会逐步模拟原程序 `s` 在输入 `s` 自身时的行为，但进行以下修改：
        *   如果模拟发现 `s` 即将导致运行时类型错误，`s‘` 会跳过导致错误的指令，继续模拟。
        *   如果模拟发现 `s` 成功停止并返回值，`s‘` 会**故意**执行一个导致类型错误的操作。
    *   然后，`H` 将 `s‘` 交给类型检查器 `C` 进行检查，并输出 `C(s‘)` 的结果。

3.  **分析 `s‘` 的行为与 `s` 是否停机的关系**：
    *   如果 `s` 在自身输入上**停机**（成功返回），根据构造，`s‘` 会故意引发类型错误。因此 `C(s‘)` 会返回 `"yes"`。根据 `H` 的定义，`H(s)` 也返回 `"yes"`。
    *   如果 `s` 在自身输入上**不停机**（永远循环或出错），那么 `s‘` 在模拟时永远不会遇到 `s` 成功返回的时刻，也就不会故意引发类型错误。同时，`s‘` 会跳过所有 `s` 可能引发的真实类型错误。因此 `s‘` 本身可能永远运行，但**不会产生类型错误**。所以 `C(s‘)` 会返回 `"no"`。根据 `H` 的定义，`H(s)` 也返回 `"no"`。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_59.png)

4.  由此可见，我们构造的 `H` 完美地解决了停机问题。但这与“停机问题不可判定”相矛盾。
5.  因此，最初的假设（存在完美类型检查器 `C`）是错误的。**不存在完美的类型检查器**。

这个论证不仅适用于类型检查，它表明：对于程序行为可能展现的**几乎所有非平凡属性**（如“是否会产生某个特定输出”、“是否会在10步内停止”等），都不存在能完美判定该属性的通用检查器。这就是理论计算机科学家对对角线论证如此尊重和感兴趣的原因，它揭示了计算的固有极限。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_61.png)

---

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_63.png)

## 总结

本节课中我们一起学习了：
1.  利用对角线论证，我们知道了存在**不可计算**的无限序列，因为可计算的序列只是可数集，而无限二进制序列是不可数集。
2.  我们深入探讨了**停机问题**，这是一个具体且重要的不可判定问题。通过巧妙的“自指”构造（让程序分析其自身），我们证明了不存在一个通用算法能判定任意程序是否会停机。
3.  作为推论，我们看到了如何将停机问题的不可判定性，归约到其他实际问题（如**完美的类型检查**）上，从而证明这些问题同样不可判定。

![](img/25b8ce3ae5636f2e6690c69eaf02a5d2_65.png)

对角线论证因此成为计算理论中一个强有力的工具，它具体化并证明了一整套逻辑上不可避免的计算限制。