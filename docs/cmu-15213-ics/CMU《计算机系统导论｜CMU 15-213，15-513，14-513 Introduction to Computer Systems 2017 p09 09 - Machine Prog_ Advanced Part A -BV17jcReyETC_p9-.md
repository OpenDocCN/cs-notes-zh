# CMU《计算机系统导论｜CMU 15-213，15-513，14-513 Introduction to Computer Systems 2017 p09 09 - Machine Prog_ Advanced Part A -BV17jcReyETC_p9-

![](img/c2a89826b3eb60fc603e59e96f8689bb_0.png)

Hi there， I'm giving lecture 9 advanced machine level programming in my office。

 so I had a trouble with the A V today the lecture， so I'm rereing this for the online version。

What we'll be talking today about are a few topics related to machine level programming that concern the memory and how the memory is viewed by machine level programs。

What we'll look at then is the overall how memory is laid out and program can make use of different portions of the memory and look particularly into a class of vulnerabilities that many programs have of their buffers being overrun。

And that vulnerability can be exploited by attackers to gain access to systems and get hold of resources that they shouldn't。

 It's a very common scheme used to attack known systems。 And， in fact， in this upcoming lab。

 the attack lab starting to morrow and do a week from to day。

 you'll have a chance to do your own attacking of of a system。

So the figure on the right shows the overall layout out of memory as viewed by the assembly level machine level program in Linux。

 at least in our versions of Linux， these vary from one system to another。

What you'll see is that even though it's a 64 bit address space。

 the user space spans around2 to the 47th bit worth of address。

The hardware itself supports two of the 48th bits， but the upper portion of it。

Is used by the kernel of the operating system to hold data that's required for your process。

 but for which you do not have direct access。You'll see that at the very top end of the memory space then is the shared library code that are things like Malic and free and printf and functions that you call and are typically shared between all the different executing processes on a program。

Just bought that as the stack。And on typical machines the stack is limited to 8 megabytes。

 One thing to note here is this diagram is not at all to scale。

8 megabytes would hardly show up on a scan of2 to the747th bytes。

Eight megabytes is more like2 to the 23rd。So this is really the stack in absolute terms occupies a much。

 much smaller region than is shown here。The reason why they limit how big the stack is is to avoid programs that have unbounded recursion that would just keep making calls and calls and calls that never return and therefore would fill up the whole memory potentially。

 So what happens is if you try to access more than 8 mebytes。 It'll cause a segmentation fault。

 The program will exit with an error。Down at the lower end of the address space。

 you'll see that the first range of addresses is not used at all。

 And I believe that has something to do with making it less vulnerable to certain classes of attacks。

 But I don't remember exactly The yellow area is called the text segment。 And unlike its name。

 it's not text at all in the sense of strings or anything。 It's actually the code。

 the executable code that was generated by the combination of compiler and linker。

And designed to sit in a particular region of memory。

 This region of memory is typically marked as readable and executable。

 but not writeable so that the programs don't inadvertently or maliciously get modified while they executing。

The pink region is the data region and that's for the globalbal variables that you've declared in your program。

And then the part with the arrow is the heap。 The heap is the region that's managed by programs like Malck。

 And so it's the place where large data structures go that are dynamically allocated and ded。呃。

And the boundaries between these actually vary depending on how much code you have。

 how much global variables you have。Of course， how much heap you use and how much stack you use。

So here's some code that we wrote that would generate various data structures and then prints out what addresses would be。

 So you see there's some globally declared variables， some arrays， some scalers， some functions。

 some。Data regions that were allocated by milk， some very small and some very large。

 and these all get placed in different parts of memory。

And this shows for one execution what the actual regions are， the local。

 as shown in purple ends up in the stack somewhere and so that will be an address。

That has a seven and a bunch of Fs and then some other he digits。The pointers are heap allocated。

 They reside somewhere in the heap region。 In this particular case。

 the very large arrays were allocated toward higher addresses in the heap just below the stack。

 in fact。While the smaller ones were were allocated lower in the heap near the data。

 there is no fixed rule for how a program， how the operating system does this。

 It will vary from one machine to another， at least one operating system to another。

And then the stoutquilocd arrays， global data are stored as set in data。

 and then the two functions that were listed are in the text segment。

It's important to realize though that these are all just regions of the same address space so that a pointer can point to any or one of these regions。

 if you try to access a region that's shown in white here that's not been allocated yet。

 you'll typically get a segmentation fault， meaning an error from the operating system that it's an invalid address。

I mentioned before that the stack is limited to 8 megabyte。

 and here's an example to demonstrate that。 It's a recursive call that if you look in detail what the program does。

 it should for recursive x return minus x， but it will go as deep in the recursion as the number x is。

And。Each time it allocates a stack frame of around 128 kiloby， so a pretty big chunk of memory。

 What you find is after about 46 deep recursive call， you'll hit a segmentation fault。

So you recall back in the C programming lab that you weren't allowed to use recursion to scan through a linked twist。

And the reason is exactly this。 If you have a very large data structure and you try to scan through it recursively。

You generate this huge， deep recursive nesting of calls that is too big for the stacked to hold so you really have to use iteration if you're going to do any kind of functions like that。

So let's go back to the very first lecture， you'll recall that there was an example we showed that could。

I'll give you different results， even though it looks if you look at this function fun。

It should just return the value 3。14。 Anything else it does is not actually being read。

But because of memory referencing of that array， A， depending on what I is。

 you can access the normal array A， but you might also corrupt some other part of memory。So。

 in particular。We found that you could call fund 0 and 1， and that would be fine。

 But if you called fun 2， what it did is it modified the lower4 Bs of the。Double precision number。

 And so it。It harmed some of the lower digits in the floating point representation。

If you did it to fun of  three， you'd overwrite the high odorder bites of the floating point number double D。

 And so that would completely corrupt the value。 And we found with fun of 4 that it would cause a segmentation fault。

 I was just doing some experiments， and you'll find。That if you run front of eight。

 it returns just fine， so it's modifying some part of memory that doesn't appear to be critical for this program。

So the overalls。Scheme， you can say is that。The struct is laid out in the typical manner。

 except we're drawing it with the lower dresses on the bottom。So first a of zero and then a of 1。

 and then the eight bytes worth of double precision data。And then。For。

Times groups of four bytes each， which seem to be a critical state。

 meaning that when you corrupt them， you get a segmentation fault。And that， at position 8。

We found that we could modify it without corrupting the program。

 And maybe that's because the program ran for such a short time that it didn't really matter。

 Or maybe it was some unused space。 It's really hard to know。😔，In general， though。

 this is a kind of scary thing that because the memory is all shared across the whole program。

That oh。An unintentional right by one part of the program can corrupt another。

 And this can become a vulnerability when code is written in a way that enables buffers or arrays to be overflowed beyond what's expected。

 There's nothing in the intrinsic I can see that prevents that。

And so we'll look at how this flaw becomes a potential security vulnerability that can be exploited by attackers。

Let's go back and look at this UniX library function called getS。

 which if you try and put in your code nowadays， it will give all kinds of compiler warnings because it's really a dangerous function。

I want to show it explicitly so you understand what the danger is and this shows a sort of sample implementation of what GS does。

 it's not necessarily the real one but it's the right general functionality。

The idea of get us is that it reads a sequence of characters from the standard input。

And as soon as it encounters either an end of file or a new line， it won't stop。 But up till then。

 it just keeps writing the results in the destination buffer。

The problem here is that there's no argument to get aesthetic。Specifies how big that buffer is。

 And so get us will happily just chug along until it hits either an end of file or end of line。

 So if you pass it a very long string。Thous0 characters，10000 characters， a million characters。

 whatever。 it'll just keep writing until either it reaches the end or something goes wrong。

this sort of unchecked。Copying is true of a lot of the standard library functions， stir copy。

 stir cat。The some versions of format strings with scanf functions and so forth will just specify。

 just keep writing a string until you hit an end of line or a null character。

So here' is an example of some code that uses getdes that is very vulnerable。

 It only has a buffer of four， which isn't a very big buffer， and it's designed to echo。

 meaning it reads something from the input and it writes it to the output。 getdes put us。

But it uses this buffer as the way it's holding that value。



![](img/c2a89826b3eb60fc603e59e96f8689bb_2.png)

So you'll find if you go to the。Code that's linked to this lecture on the website。

Let me find it for you。

![](img/c2a89826b3eb60fc603e59e96f8689bb_4.png)

And it's a function called Buff demo NSP NSP stands for no stack pointer。

 and you'll see pretty soon what that means， no stack protection， I mean。

And youll find that you can type a string。Up to 23 characters。And it will print it back。

 the put S is happy and it exits normally， and so that's good。But if you try and type a string。

That's just one character longer。It'll echo it O。 The pus worked fine， But when Echo went to return。

 it caused a segmentation fall。 So let's try and understand what happened there and what does that all imply。



![](img/c2a89826b3eb60fc603e59e96f8689bb_6.png)

![](img/c2a89826b3eb60fc603e59e96f8689bb_7.png)

So to do that， we have to look at the disassembled code of both echo and call echo。

 You see the echo allocates some space on the stack。 Hex 18 is equivalent to decimal 24。

 So of 24 Bs on the stack。It sets up the stack pointer to be the argument that's called to get us。

And then getdes calls put S， and that des the space and it's done。

 The key part here is the allocation of 24 B。 How much space is allocated on the stack for the buffer。

Call echo。 The only thing that's interesting here is that the return point to it is this hex code 4。

0，0，6， F 6。 So when call。Calls Echo。 Call echocho calls Echo。 It'll push this address on the stack。

So this is like what it looks like then before the return to get us that we've allocated。

We've pushed the address on the stack。We've allocated 24 Bs for the buffer。

 and we're only going to use four of them。 since that's so big。

 So there's 20 Bs here that are unused。And why it's there， it's not exactly clear to me。

 but it's there so that this program actually can accept。

A larger strings than the the actual decor would imply。

And what you find with the co echo then is it puts that particular dress on the stack。

And if I type a string that's only 23 characters or less long。

 you'll see that it fills up not just the provided buffer of four， but up in the unused region。

But it won't corrupt the stack， the return value。 In this case。

 it just at the very end inserts the null string。Right here at the very end of the。The the string。

 And that's why 23 is a magic number， not 24。 I can put in 23 characters terminated by the null character。

 and that will fit within this allocated region。On the other hand。

 if I have a string of 24 characters， then that no character will overwrite the lower order of the return address。

 So instead of returning now to。The original calling position。

 what it will do is jump to whatever code happens to be sitting there at address 400600。

And in this case， it kind of went high haywire and caused the segmentation fault。

So a vulnerability is something that can cause a program to misbehave As an attacker。

 the interest is in taking these vulnerabilities and exploiting them to gain access to the system。

 And so there's a whole class of attacks that used to be very common called stackming that exploit that ability to。

Use an unchecked buffer to overwrite the state of the machine， particularly the return pointer。

 and gain access to this inject take control of the system。

So the idea of it is that the normal execution of P calls Q。

 then it will push an address A onto the stack and then set up the stack frame for Q。

And if Q contains some vulnerable code like getas， it can potentially overrun its stack framement and modify the value of a。

So if， if somewhere in my programme， I have a function that I'd like to call。

That's not the normal return if， and let's say that's at some position S。Then if the。

 the exploit can overwrite that address and convert it from a to S。

Then what will happen when the the function returns is it won't return to the original calling point。

 It will essentially call the function。 Fs will jump to the first instruction of。Of function S。

So what I want to do then to make this kind of attack is create a string that has enough padding to get up into the position I like and then insert into my string。

 the address that I want to。To it to return to。Which， in this case， is this function called smash。

And it's an address4 zero， zero six F b。

![](img/c2a89826b3eb60fc603e59e96f8689bb_9.png)

So I can demonstrate that。嗯。

![](img/c2a89826b3eb60fc603e59e96f8689bb_11.png)

In general， it's a little bit tricky because the。

![](img/c2a89826b3eb60fc603e59e96f8689bb_13.png)

String might contain characters that are not standard ASI digits。But I can write a function。

 So I've written a program called hexify that will take strings of the form of these hexbytes and convert it directly into this raw format。

 So I've created a。String called smashed dot string。Smed out text。

Which looks a little weird at the end because it actually contains some characters that can't be printed on a normal terminal。

But now， if I。呃。Send that string。If I piped that to my Buff demo program。

You'll see that it ends up calling this function called smashash， which prints out I've been smashed。

 So somehow by providing a new string， this sort of cleverly deviceised string into this program。

 I was able to make the program jump to a location and execute a function that wasn't in the normal control flow of the program。

So that's sort of a simple minded attack， but you see the idea that now I'm able to take control of a system and do things with it。



![](img/c2a89826b3eb60fc603e59e96f8689bb_15.png)

That it wasn't Royce intended to do。

![](img/c2a89826b3eb60fc603e59e96f8689bb_17.png)

So an attacker can sort of go to the next level of saying。

 why don't want to just call some function that just happens to be sitting there。

 I want to call a function that I've written the code for。

 and so it can do that by a technique known as code injection。

And the idea of it is to build into that string that we provide。

 not just random digits to pat it out， but actually the bytes that code some instructions。

 the executable code that we'd like to execute。And then the idea is thats。Now。

 we'll overwrite the return address with the starting address of where this exploit code is。

 So now in the function。Calls。If I can inject this combination into the stack。Now。

 when the program executes its normal return， what it'll actually do is jump to some position in the stack and start executing the code that's there。

And after it hits the end， it might。Find a return instruction in the exploit code。

 and that will cause the program back to return back to the original。

Somewhere in the original program， but the idea is I've created this ability to。

Have the system execute code that I've supplied as input to this system and do it in a way if it's done cleverly that's completely undetectable by the operating system itself。

So that's kind of scary and it used to be there are a lot of vulnerable code and a lot of places to exploit it。

So there's been several means to try and make that less common， more difficult for an attacker。

1 is by enforcing better discipline on the code writers so that they don't leave these unprotected buffers。

And the other is to use protections that try to make it harder for an attacker。

 and we'll show two of those。Some done by the system。

 the operating system and some done by the compiler。So the first idea you， as a programmer。

 should adopt is never use a function that。Can create unbounded rights to a buffer。

 It should always give some bound。 So instead of get us， there's a function called F get us。

 which you， among its arguments， is a maximum length。

 and the function will just exit when it's hit that length。

 if it hasn't already hit an end of line or end of file character。

Another technique that the system can do is what's called stack randomization or more generally address based layout randomization。

The idea of this is every time the program executes。

 it starts a stack and a little different position from before。And that way。

 what its doing is exploiting the fact that this particular exploit required that， I know。

By what I inject unto the stack， I had to know the starting position of this particular address。

 this particular block of code。And so if that position keeps changing every time。

 then I can't exploit it very well。And so this not very well formatted series of hex digits shows that for a particular program。

 it ran multiple times， and you'll see each time the vocal variable。

 a fixed vocal variables that some different address。

 and that's because the whole stack has been shifted from one run to the other。

 and that's in general， a good general scheme for avoiding attacks is to use random things that are hard to predict and hard for the attacker to exploit。

Another technique that was only added to the Intel processors relatively recently is to mark some parts of the program as read and writeable。

But not executable。 So in particular， there's good reason to read the stack and write the stack。

 but not very good reasons for programmes to be able to execute codees at sitting on the stack。

So if I simply map。Mark those parts of memory as non executable。

 Then any attempt to jump to some code there will cause a fault， a segmentation fault。 basically。

 you're violating the permissions that the operating system enforces。

And then there's a third technique that are called stack canaries that are inserted by the compiler to try to make vulnerable code less vulnerable。

And the term from this back in the 180s in the coal mines of England。They'd go down into the mine。

 the miners with a canary in a cage。 And if the caroneate died。

 it was the sign that there was some poisonous gas in the mine that had to be evacuated immediately。

 So the idea of a canary Sometimes you'll hear that term a canary and a coal mine is the idea of some very sensitive object that can detect when something is wrong and take the necessary measures。

 So the idea of a stack canary is to put some little marker on the stack in a way that can easily detect a reliably detect if something fish is going on as far as overrunning buffers。

And this has become the default in GCC， so to compute that compile that code。The no NSP NSP code。

 I disable that protection。And you'll see with the same。

Code that if I give a string that's more than 7 digits， it will give an error message。



![](img/c2a89826b3eb60fc603e59e96f8689bb_19.png)

Let me just demo that for you。So Buff demo S， meaning stack protection is enabled。So。

 seven characters is fine。8 characters in poof。 It comes back with all kinds of errors about how the stack smashing has been detected。

So something's right there that it's able to。It didn't enforce my limit of 8， but somehow it。

 it detected that my limit of of。

![](img/c2a89826b3eb60fc603e59e96f8689bb_21.png)

![](img/c2a89826b3eb60fc603e59e96f8689bb_22.png)

F， but it did detect that eight， and that was really8 plus one or nine of byte's worth of data were corrupting the stack。

We too much for what the program supposed to support。

So this is the code for echocho where the red lines are ones that are implementing the sta canary。

 and the general scheme is somewhere often a part of memory that you can't normally access is a byte sequence that got generated specifically and randomly for this particular process when it's running。

And that becomes sort of a key that will be used and so the。The programme， the first three lines。

 will take that key and read it out of memory。 And That's what that kind of funny memory reference does。

And put it on the stack。Just above the buffer region。And then later。

 I'll call these functions that might cause some corruption of the stack。 And after that's all done。

 I'll check whether stack corruption has occurred by reading this value back off the stack and making sure it equals what it was when I first wrote it there。

 If not， let's a sign something's wrong。 And you notice。

 this has to be done differently and randomly for each execution。 Otherwise。

 the attacker could potentially figure out what it's going to be and just masquerade。

Could alter the canary and then restore it back again。

So this is the general scheme then that I have a buffer。It's supposed to be4 Bs long。

 I've added another four Bs of Puistister' canary。And anything that overrs that Canary will get detected。

So attackers are clever lot， and if they see new defenses like the ones we've just shown you。

 they'll try to figure out new attacks。So in particular， we saw this idea of address spaceway out。

Randomization makes it hard to predict exactly where any block of code will be or any buffer。And。And。

Non executable stacks means I can't inject code in the stack。

So a technique that was developed is called return oriented programming or ROP or RAP。

And the idea of it is to try and string together pieces， fragments of code that exist in the。

The existing program， either the data segment or the shared library。

And assemble a sequence that's usable to do whatever I want to do。

Typically you're not going to find some function that does exactly what I want。

 so somehow I have to be able to build this function out of a bunch of smaller pieces rather than just have it all sitting there。

So what I'll do is assemble a collection of what are known as gadgets。

 which are sequences of instructions， each finishing with a return instruction。So。On the X86 machine。

 the B code C3 encodes the REET， the return instruction。

And anti bites before that then are potentially part of a gadget。

So a gadget will be a sequence of bytes。 The ends with C 3。And what I can do is see， for example。

 this code。The first four bys， if they were executed， would have the effect of。

Adding together the registers RDI and RDX and storing that result in register RX。

And then there's a return。Or I might be able to find code like this where there's some just bite sequence sitting there from some instruction or another。

Or instruction sequence， but it happens to code。Some instructions I'm interested in having available。

 like this three byte sequence。En codes the instruction that moves the contents of register R A X to R D。

 I。And it finishes with C3， which is what I want。So in general。

 what will happen is the attacker will load up the stack with the addresses of whole sequence of gadgets。

And of them。 And remember， each gadget ends with the return instruction。

So now if I can get this set up and I I can execute a return instruction。

 then what will happen as I'll start popping。Addresses off the stack。 I'll jump to that address。

 That will be some gadget code， which executes and comes the end and hits another return。

That causes a pop， execute code， return， and so forth until I've gotten through。

So I can take any sequence of these gadgets， some of them might only do one small thing。

And I can assemble them together and create a basically arbitrary code。So for example。

 if I wanted to。Have this effect of adding R D I and R D X together。 I could push on to the stack。

 some padding。And then the start address of that code fragment，4，0，0，4， de 4。

You have to be careful by by ordering you see。And then I'll hit that。When I execute a return。

 then it will jump to this part of the code and will have that effect。Now。

 this one ends pretty quickly， but you can imagine it。

At doing some more interesting things by stringing together whole sequences of gadgets。😊，Okay。

 for the people who took this class in class， we had a little quiz。

 and what I'd like to do is propose that you pause and do a little exercise to test your understanding machine code。



![](img/c2a89826b3eb60fc603e59e96f8689bb_24.png)

So here's a slide。That is a version of the。Quz， one of the quiz problems they had。

That proved difficult for the students。 And so it's worth going over。So here's the problem。

 I'll explain it to you。 Then what you should do is pause this video and try to solve it。

 and then I'll go over the solution with you。So the idea is that there's twostructs here， S1 and S2。

S1 contains an array of four characters， and a single integer。

S 2 contains a array of four characters， and then embedded within it is a copy of S1。

 And you remember with， with。Structures， when you embed them。

 you really do have the exact bites of that structure lying inside of the bigger structure。

It's different if there are pointers to structures， you'd have pointers。 but in this case。

 we have the actual structure itself。And then there's some double。So stop now。

And try to just use this figure here and mark out which bites get used by the different fields of these two structures。

Okay， we're back。What did you find？Well， I'm just going to jump right to the solution and I hope that you got this and you'll be able to understand it。

First of all， S1 is a total of8 bytes， it requires four bytes for the four characters。

 the four single bytes and x， and four bytes for the end。Now。

 one important thing to remember with structures is they have both a size and an alignment。

 This is true of any data structure。 The size is the total number of bytes used。 The alignment is。

The alignment requirement for the largest field within its structure。

Or which is the size if it's a simple primitive data type like an or double。So in this case。

 my alignment requirement is4 since I is a4 byte object。 And you'll see， though。

 that I didn't have to put any padding in this structure because I will start on a multiplefore the way it's been declared。

On the other hand， forstruct S2， you'll see I have the four bytes for y。

 and then I can embed S1 directly after that because it has a4bte alignment requirement。

So I'll use the next eight bys for that。Following that， I'll have to insert four bytes of padding。

In order to satisfy the alignment for the double Q， Q， Q， If I didn't， it would start to address 12。

 which is not a multiple of 16。So by adding that extra four bytes， now QQQ spans from byte。

16 to bite 23。 And there's that wasted space inside。So these kind of problems show up on exams a lot。

 They're very easy for us to generate。 and it's very important that。

 you know and understand and are completely。Comfortable with this idea of structures。

 how they're laid out， whether their alignment requirements are， how big they are。

And how they're organized。

![](img/c2a89826b3eb60fc603e59e96f8689bb_26.png)