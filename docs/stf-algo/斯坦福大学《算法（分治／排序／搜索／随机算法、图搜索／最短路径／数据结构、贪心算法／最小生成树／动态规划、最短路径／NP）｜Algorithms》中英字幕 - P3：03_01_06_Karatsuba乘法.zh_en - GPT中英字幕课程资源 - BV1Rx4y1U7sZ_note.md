# 算法：03：Karatsuba乘法 🧮

![](img/08d33626e79da4d209b86882cb2923cb_0.png)

在本节课中，我们将学习一种不同于小学所学的整数乘法算法——Karatsuba乘法。我们将从直观的例子入手，逐步理解其递归思想，并最终掌握这个看似“神秘”但高效的算法。

---

## 概述

我们通常使用小学所学的竖式乘法来计算两个整数的乘积。然而，算法设计的世界远比我们想象的要丰富。本节将介绍一种名为Karatsuba乘法的递归算法，它通过巧妙的数学变换，用更少的递归调用完成乘法运算。

---

## 一个神秘的例子

让我们通过一个具体例子来感受Karatsuba乘法的神奇之处。我们将计算 **1234 × 5678**，但使用一套全新的步骤。

![](img/08d33626e79da4d209b86882cb2923cb_2.png)

首先，将每个数字分成两半，并定义新的变量：
- 对于 **x = 5678**：前半部分 `A = 56`，后半部分 `B = 78`。
- 对于 **y = 1234**：前半部分 `C = 12`，后半部分 `D = 34`。

![](img/08d33626e79da4d209b86882cb2923cb_4.png)

![](img/08d33626e79da4d209b86882cb2923cb_6.png)

以下是计算步骤：

![](img/08d33626e79da4d209b86882cb2923cb_8.png)

**步骤一**：计算 `A × C` 和 `B × D`。
- `A × C = 56 × 12 = 672`
- `B × D = 78 × 34 = 2652`

![](img/08d33626e79da4d209b86882cb2923cb_10.png)

**步骤二**：计算 `(A + B)` 和 `(C + D)` 的和，然后相乘。
- `A + B = 56 + 78 = 134`
- `C + D = 12 + 34 = 46`
- `(A+B) × (C+D) = 134 × 46 = 6164`

![](img/08d33626e79da4d209b86882cb2923cb_12.png)

**步骤三**：从步骤二的结果中减去步骤一的两个结果。
- `6164 - 2652 - 672 = 2840`

![](img/08d33626e79da4d209b86882cb2923cb_13.png)

**步骤四**：将三个结果按特定方式组合。
- 将 `A×C` 的结果 `672` 后补 **4** 个零：`6720000`
- 将步骤三的结果 `2840` 后补 **2** 个零：`284000`
- 将 `B×D` 的结果 `2652` 保持不变：`2652`
- 将三者相加：`6720000 + 284000 + 2652 = 7006652`

验证可知，`7006652` 正是 `1234 × 5678` 的正确结果。这个计算过程与小学算法完全不同，但得出了相同的答案。

---

![](img/08d33626e79da4d209b86882cb2923cb_15.png)

## 递归乘法思想

上一节我们看到了一个神奇的计算序列，本节中我们来看看其背后的递归思想。要理解Karatsuba算法，首先需要了解一个更简单的递归乘法方法。

### 数字的分解

![](img/08d33626e79da4d209b86882cb2923cb_17.png)

给定两个 **n** 位数 **x** 和 **y**，我们可以将它们各自分解为前半部分和后半部分：

**公式表示：**
```
x = A * 10^(n/2) + B
y = C * 10^(n/2) + D
```
其中，**A** 和 **C** 是各自的前半部分（各 **n/2** 位），**B** 和 **D** 是各自的后半部分。

![](img/08d33626e79da4d209b86882cb2923cb_19.png)

在我们的例子中：
- `x = 5678`, `A = 56`, `B = 78`, `n=4`, `10^(n/2) = 100`
- `y = 1234`, `C = 12`, `D = 34`

### 乘积的表达式

![](img/08d33626e79da4d209b86882cb2923cb_21.png)

将 **x** 和 **y** 的分解式相乘，我们可以得到它们的乘积表达式：

![](img/08d33626e79da4d209b86882cb2923cb_23.png)

**公式推导：**
```
x * y = (A*10^(n/2) + B) * (C*10^(n/2) + D)
      = A*C * 10^n + (A*D + B*C) * 10^(n/2) + B*D
```
我们称这个表达式为 **（星号表达式）**。

这个表达式表明，计算 **x * y** 这个大问题，可以转化为计算四个更小的子问题：`A*C`、`A*D`、`B*C` 和 `B*D`。这就是递归的基础。

![](img/08d33626e79da4d209b86882cb2923cb_25.png)

### 简单的递归算法

基于星号表达式，一个直接的递归算法思路如下：

**算法步骤：**
1.  **递归地计算** `A*C`。
2.  **递归地计算** `A*D`。
3.  **递归地计算** `B*C`。
4.  **递归地计算** `B*D`。
5.  当所有递归调用返回结果后，按照星号表达式进行组合：
    - 将 `A*C` 的结果后补 **n** 个零。
    - 将 `(A*D + B*C)` 的结果后补 **n/2** 个零。
    - 将 `B*D` 的结果保持不变。
    - 将上述三项相加，得到最终乘积。

**递归基（Base Case）：** 当输入的两个数字都只有一位时，直接相乘并返回结果。

这个算法虽然可行，但它进行了 **四次** 递归调用。接下来，我们将看到Karatsuba如何将其优化为仅需 **三次** 递归调用。

---

## Karatsuba乘法算法

上一节我们介绍了需要四次递归调用的简单递归乘法。本节中，Karatsuba算法的核心洞察在于，我们并不需要单独计算 `A*D` 和 `B*C`，而只需要它们的和 `(A*D + B*C)`。通过一个巧妙的数学技巧，我们可以用三次递归调用得到这个和。

### 关键的优化

回顾星号表达式，我们真正关心的三个系数是：
1.  `A*C`
2.  `(A*D + B*C)`
3.  `B*D`

Karatsuba算法通过以下步骤，仅用三次递归调用计算出这三个量：

**算法步骤：**
1.  **递归计算** `P1 = A * C`。
2.  **递归计算** `P2 = B * D`。
3.  **递归计算** `P3 = (A + B) * (C + D)`。

现在，关键的技巧来了。让我们展开 `P3`：
```
P3 = (A+B)*(C+D) = A*C + A*D + B*C + B*D
```
如果我们从 `P3` 中减去 `P1` 和 `P2`：
```
P3 - P1 - P2 = (A*C + A*D + B*C + B*D) - (A*C) - (B*D) = A*D + B*C
```
看！我们得到了所需的第二个系数 `(A*D + B*C)`，而无需单独计算 `A*D` 或 `B*C`。

### 完整的计算流程

因此，完整的Karatsuba乘法递归算法如下：

![](img/08d33626e79da4d209b86882cb2923cb_27.png)

**伪代码描述：**
```
function KaratsubaMultiply(x, y):
    if x 和 y 都是个位数:
        return x * y

    将 x 分解为 A 和 B (A是前半部分，B是后半部分)
    将 y 分解为 C 和 D

    P1 = KaratsubaMultiply(A, C)
    P2 = KaratsubaMultiply(B, D)
    P3 = KaratsubaMultiply(A+B, C+D)

    middle_term = P3 - P1 - P2  # 这就是 (A*D + B*C)

    n = x 和 y 的位数
    return P1 * 10^n + middle_term * 10^(n/2) + P2
```

这个算法在递归调用之外，只进行了简单的加法和减法操作，以及最后的位数补齐（乘以10的幂）和求和。

---

## 总结

![](img/08d33626e79da4d209b86882cb2923cb_29.png)

本节课中我们一起学习了Karatsuba整数乘法算法。我们从一个小学生算法截然不同的神秘计算例子开始，揭示了算法设计的多样性。然后，我们逐步推导：

1.  首先，我们理解了如何将大整数乘法问题**分解**为更小的子问题，并建立了一个需要四次递归调用的简单递归算法框架。
2.  接着，我们看到了Karatsuba算法的核心**优化**：通过计算 `(A+B)*(C+D)` 并利用一次减法，巧妙地用**三次递归调用**替代了原来的四次，从而得到了所需的中间项 `(A*D + B*C)`。

Karatsuba算法展示了即使在整数乘法这样基础的问题上，通过深入的数学洞察和巧妙的算法设计，也能获得令人惊喜的优化。虽然我们现在还不知道它是否比小学算法更快，但这为我们打开了一扇门，去探索“分治算法”这一强大的算法设计范式的效率分析。在后续课程中，我们将获得分析此类算法运行时间的工具箱。