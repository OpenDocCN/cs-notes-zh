# 康奈尔大学《OCaml编程｜CS3110：OCaml Programming： Correct + Efficient + Beautiful》中英字幕 - P164：-164-Menhir and Ocamllex Chap9 Video 11.zh_en - GPT中英字幕课程资源 - BV1Tx4y1s7sP

So let's look in more detail at the parser。 Its job is to convert that token stream into an abstract syntax tree to implement a parser in Oaml。

 we've actually been using a parser generator。

![](img/12ac78e286c10d89f7e27513632eab18_1.png)

If you look at the parser we created before， it was this dot MLLY file。

 I'll explain the why in just a second。😡，That's not a Dmmy mail file。

 that's not strictly speaking Ocal code。😡，What's happening is we're running a tool that comes along with OcaMl that takes in this MLY file and actually generates an ML file from it。

 And if I look inside a build here， I can even find that file it's parser。 MLl。😡。

And there's a whole whole bunch of code in here that has been generated by the Ocaml parser generator to do the parsing based on that much smaller file that we created ourselves。

So I hope you can appreciate not having to write all of that code yourself instead just writing this perhaps cryptic。

 but fairly succinct definition of what the parsing to the language looks like。



![](img/12ac78e286c10d89f7e27513632eab18_3.png)

Okay， so down to explain the why。There's an old parser generator from C called Yak YACCC that stands for yet another compiler compiler The idea of compiler compiler there is that you're taking in something that defines the language and compiling it to produce something that can then compile the language Ocaml Yak was created as an Ocal version of Yak。

And then later on， a more modern version of this was created called Men here。

 which is completely different altogether。Okay， so we're using the men here parser generator and building our parser for the simple calculator language。

 but the reason the file is named MLLY is because of Yak， it's a nod to that history。

Inside our parser。 MLY， we declared a bunch of tokens。

Only one of those tokens carried additional data along with it。

 and we got to specify what that should be。So all of these are tokens that the Leer would produce。

Int carried an Ocal int， along with it。And EOF is a special in the file token。

 which means there are no more tokens produced。We had some more declarations about precedence and associivity。

 We played around with left versus right associative definitions for plus left associative means that of course the parentheses group to the left right associative means they group to the right。

 you can also write nonasso， which would cause the parser to believe that x plus y plus Z is ambiguous and force the programmer to write parentheses to clarify what they mean。

The lower down a token was in this list of associivities， the higher precedence it had。

So because times occurred below plus。Then 1 plus 2 times 3 was parsed as 1 plus quantity 2 times 3。

 not the other way around， quantity 1 plus2 times 3。In our implementation we used a Lexer generator。

 we didn't write the Leer by hand， Instead we wrote a file that Ocamel then used a tool to produce a Leer out of so we wrote this dot MLL file。



![](img/12ac78e286c10d89f7e27513632eab18_5.png)

And Ocael actually compiled that into aexer。ml file。 again。

 we can look in the build directory to find that。And here's all the code from the Lexer that we didn't have to implement ourselves。

 including a bunch of really weird looking stuff up here。



![](img/12ac78e286c10d89f7e27513632eab18_7.png)

So it's nice that we don't have to do that ourselves。

This Lexer generator converts the dot MLL file to a dot ML file。

Now there's an old Lexer generator from C called Lex。We are using Ocal X。

 which is an Ocael equivalent of that。So the extra L at the end of dot MLLL there stands for the P In our Lexer do MLL file。

 we had a header， which has had open parser in it that's actually literally Ocal code that will get copied into the generated file The open there was a convenience to make our parser tokens available in the Leer。

In the Leer， we wrote some identifiers for classes of tokens， white space， digits， and ints。

I actually used regular expressions to do this so a regular expression is something you might have already encountered in computer science。

 if not you will see it in 2800 eventually regular expressions are super useful and something that all of you should definitely learn at some point。

😡，Here I'm using them to help define the syntax of tokens。

White space was any space character or any tab character。And by putting them in square brackets。

 that's Omal's regular expression syntax for saying either one of them。

 and then I could follow that with plus to mean one or more of either。For digits。

 I had the character zero and the character nine， I then put those together inside a square brackets with a dash to say any character from zero to9 inclusive in ASCI。

An int was then one or more digits， so the plus meant one or more。Optly。

 there was a minus sign in front of it， the question mark meant optional。

And so an in then was one or more digits optionally preceded by a minus sign。😡。

So that's the regular expression syntax。I then had a production rule。

 that production rule described how to produce tokens out of the character stream。

I named that rule Re。 That was my choice， but the rule and parse there are keywords。😡。

And as part of that then I just put in what I wanted to read from the token stream and what I wanted to return as a result of having read it。

 so everything inside of the curly braces there was a return value。

Read here therefore became a function that could be used to read a token from the token stream。

Pase was a keyword。When I wrote white here， what that meant is if the next characters from the character stream match the regular expression white。

 then recursively call read on the remaining characters in the stream， thus skipping the white space。

 So Lex Buff here is a variable that the Lexer knows about and that is actually the character stream。

So if the next characters from the characters stream match any of these strings。

 then we'll return the corresponding token next， if the characters from the token stream match the int regular expression。

 then we would return an int token and use some functions that are built in here to convert what we matched from that regular expression into an Ocheml int。

So Leine。 Lexim Lexbuff was the string that matched the int。And finally。

 we could return the end of file token when the character stream became empty。



![](img/12ac78e286c10d89f7e27513632eab18_9.png)