# 密歇根大学《面向所有人的Web应用程序（PHP、SQL、APP、JavaScript和JQuey｜Web Applications for Everybody》 p113 5_JavaScript数组与控制结构.zh_en -BV1Lr421A75d_p113-

![](img/f647f2b59644847dae9fa6c5c4230fe1_0.png)

![](img/f647f2b59644847dae9fa6c5c4230fe1_1.png)

So like PhP and Python in other languages that we've been familiar with JavaScript has a linear arrays and associative structures。

 but the associative arrays are actually objects and so we'll kind of talk about them in more detail in the next major lecture where we talk about object or and programming and so it's kind of interesting that in JavaScript。

You have to learn about objects oriented earlier than other languages another reason why I'm not a fan of JavaScript as first language。

 but it turns out that JavaScript objects are super powerful and flexible and gorgeous and so we'll get to that。

A linear array is quite straightforward。 You say square bracket square， and then you have commas。

 and that's an array。 Now， the interesting thing is， is if you've read about Json。

 you might have actually seen the JavaScriptscript notation for arrays and objects already because Json is exactly the same notation and it's similar to Python as well。

 So this is a linear array that's 0，1，2，3，4。And then you can have key values， things。

 but these are objects not arrays and we'll get to that。 You can say a sub 0。

 which will get you the zero thing of x and B subname。

This is the part that kind of confuses people because that basically says go into the object B and look up the attribute name。

But that's in quotes。And it just is a little bit strange。

 but you can kind of pretend that objects are associative arrays。

 and you can even make them just like I made an object that has no coat in it。

 but it just has data in it as a set of key value pairs。 So people do kind of。

People coming from languages like PhP that expect associative arrays to have key value capabilities。

 they just kind of make objects and don't worry too much about it and we'll get the objects in greater detail later。

You can also kind of construct arrays， do various ways you can make a sort of fresh empty array and then push things onto to the end of it so you kind of have this array and you keep adding to the end of these things。

 push， push push， and so we end up with first and second。

Or you can say array sub zero equals first and array sub 1 equals second。

 and then you end up with an array that looks like that。

 so that's another way of kind of building an array up from little pieces。

You can also use the constructor style where you just put the list of things that you want in the array。

 this little brackets syntax is sort of a shorthand for the constructor style of making a in this case。

 two element array。

![](img/f647f2b59644847dae9fa6c5c4230fe1_3.png)

So control structures， again since you're coming from PhP。

 we're not going to go over everything with the same as PhP because the operators are the same as PhP。

 if statements are like PhP， why loops are like PhHP。

 for loops are like the PhP counted loops and we'll talk about that and break and continue work a lot like PhP so that's part of the reason that I like teaching PhP first because they're both C like languages and then I can going just say hey remember all the stuff we've done in PhP。

There is a difference between the definite loops and PhP you use for each。

 but in JavaScript we' use four and so in this case we're going to say four ball in balls。

 which is kind of a little python like actually， so this is the iteration variable and this is the collection。

This is an object， but don't worry about it。 Pret we'll just pretend it's a key value array for now。

 So this basically says is that ball， the duration variable is going to go through the successive keys in here。

 And then we can just say ball's subbalb to look up these values。

 the corresponding values as you go through。Doesn't have a Python like two iteration variable thing。

 And so this will loop through and go through and hit them all。



![](img/f647f2b59644847dae9fa6c5c4230fe1_5.png)

Up next， we're going to talk about playing with a document object model in JavaScript。

