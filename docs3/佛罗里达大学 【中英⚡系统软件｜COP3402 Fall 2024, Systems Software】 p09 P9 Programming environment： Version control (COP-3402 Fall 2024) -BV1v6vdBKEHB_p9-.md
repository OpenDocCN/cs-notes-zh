# 佛罗里达大学 【中英⚡系统软件｜COP3402 Fall 2024, Systems Software】 p09 P9 Programming environment： Version control (COP-3402 Fall 2024) -BV1v6vdBKEHB_p9-

All right， welcome back to System softwareft。So today we are finishing up the programming environment。

This section here， we went over the editor， at least you have a video for that。

 so hopefully everybody has taken， so who's done the tutor for at least EMAC or VIM。

 who's done Tutor。Okay， good， good， so hopefully everybody's comfortable with editing in their command line editor。

 at least you know， getting in， writing a file， writing text， saving it。

We've gone over the build system last time。诶。And the homework was to read more about。

Writing make files and doing version and control。And write one thing you learned about software engineering practices。

 so let me ask you， did anybody， what did you got say for your reading again？Surprised at how many。

Abstract。去最近。啊上。You're automating the automation。Yeah。

 there are tools that generate build files right there's like， yeah。

 there are several levels of this。Yeah， yeah， these are really the kind of software ecosystem。

 the build build configuration and deployment automation is， especially today。

 there are tons and tons of automated tools， they are programs， you know we're programmers。

 so we write automated tools to help us do everything that we can。So we replace it like expansions。

 is is variable。セュ？Yeah， yeah， well the command line right， yeah。

 the command line gives you all the features， we went over some of those right。

 we went over some of the useful features of the command line， yeah。Like correct if you are。

Like that。Absolutely， yeah， and we'll go over version control today。Yeah。あアイビ。Yeah how great。😊，ごさ。

Yeah great and we'll actually encounter those a little bit。

 these are actually system managed by the kernel as well and so when we go to run an application you actually tell what the environment should look like when you run the application there are also some big vulnerabilities around environment variables there was a bash I think it was。

remember I think it caused remote code execution because the dashH environment variables could basically include code if I'm remembering right。

 and you could use those， you can also use them as an exploit vector as well。All coolol， So yeah。

 hopefully everybody read at least some amount and read about make files。

 Now there was a question about make files and going over that a little bit。 So let's do that。

 So what are your questions on。Make files from last time I know we I was a little delayed because of the projector issue and I think I got through everything I make files but。

I'm sure there are still questions， so what are your someone asked， well。

 did somebody have a question on make files you， right？許スはい。Okay， okay， I'll do that。

 so I'll do that， but let's see if there are specific questions for us and can motivate。

On phony targets okay yeah I didn't go over this much this is a pretty natty gritty implementation detail like a very specific thing in make files。

 so the reason that make has phony targets is because a target is meant to be a file that's generated so one of your colleagues talked about levels of abstraction where you're generating stuff。

And what the whole make file rule is designed around is file generation or it could be code generation could be documentation。

 but the tool that you have here in a rule is this target is supposed to be a file that's generated by this recipe。

And then if there are any dependency well let let me go to a simpler example here。

 so this is supposed to be a file that's generated by the recipe。

 so this is the recipe the recipe is just a sequence of shell commands。

This is the file that's generated。And then dependencies tell you what other files you need to generate first in order to build this in this case。

 mainine is generated by the developer but。You know， talkingalking about levels of abstraction。

 if you have particularlyly for compilers， you have C code generators as well as well as build system generators。

 and so this could be could be a case where you've got a。诶诶。C file being generated。

 and actually I've got。I think I have an example of this， yeah， I's got an example of this。

So this is an example。Of a make file where。Where is the C file？

This is an example of a rule where a C file is being generated， same here。

This is a recipe that it's a parser generator， we'll talk about this a little more when we get to compilers but this is a tool that will generate C code from another specification from another file。

 this dot Y file。And so when you go to generate the when you go to build from the C files。

 the datat files from the C files。There's a dependency here on this C file。

 and then the make will set up this dependency tree as we talked about to figure out how to generate this file first。

 and itll use this rule to do it。Okay， so hopefully that covered a little bit overview again of the makeC file system。

 so to answer your question about phony targets， phony targets。

Are just targets that don't have a file that you generate。

They're just like names of targets that you want to be able to call from the command line so you can say make clean so it's just telling make that clean will never be generated as a file and so basically you should always run this recipe whenever you call clean。

And the reason for this is subtle， like maybe you have something that generates a file called Clean inadvertent or you touch it or something who knows。

 but your make file will still work as you expect， most likely if you don't have these。

 but that's all pho targets are。Yeah。This is。Arow similar to like。嗯。So Docker is， yeah。

 so Docker is for like virtualization management， I mean not really。

 it's really a different part of the software development lifecycle， that's more like deployment。

 I'd say this is make filess are really oriented around。😊。

Building the automating the build of the software Docker are really more for deployment of the software now。

You could combine them in various ways， you could have a deployment step in your build file。

 there's nothing else on that。sorryrryYeah， yeah。Did somebody submitted a ticket it from？Yes。

 so the HTMLI table is not working， but I have a workaround。So。I fixed this this morning。しけすごかな。

I just No， I' I didn't stop。So it's still， yeah， it's still not showing but okay， all right。

 yeah thanks sorry about that， yeah， but I work around for that。So yeah， so I guess it was fixed。

 maybe something else went wrong。But Im still recording， I'm still recording。So yeah。

 so doctorscker different different days of the software development lifecycl。

 once you get into like processes or probably senior design you'll learn more about these other tools。

 other automations of like software development lifecycl， I wish I had the picture up here。

 but know these days especially with large mature software that's reused all over the place。

 a lot of the work of software engineering is in other parts of the very extensive development lifecycle which includes building testing。

 deploying， maintenance， automated to there's also automated testing approaches。

 automated analysis bugfighting approaches。So yeah。

 they little different from Docker is largely for virtualization。

We're not going to learn about Docker here， we're not going to learn about virtualization。Okay。

 so other questions on make。It's a quick review of what make files are。诶。

Theyre really just lists of these rules for building files， for generating files。

 so the target is the name of a file to generate， and the recipe is the sequence of commands to generate it。

So it's a lot like a bear script is just a sequence of commands you write on the command line。

So this recipe， it's just a sequence of commands that you can just enter into the command line。

 but instead of writing them yourself， you store them in this make file and then the make file will run them for you automatically。

And even better， it'll run them for you only when it needs to generate a new version of this file that you're trying to generate。

So here's the basic example that I gave last time where here's how we generate the main program。

 so just make sure everybody's on the same page， this GCC command will compile and link these three source files and it'll produce an executable called mainine。

So in the UniX world， by convention， executables don't have an extension。

 you could certainly put one because remember the name of the file is not doesn't actually mean anything about the contents of the file。

 it's just a convention， so this is the program name being output by the compiler questions on this command。

Yeah。No name of the file this is the name to use for the executable that's generated by GCC。

 so probably you run Gcc like hello。c and then you get Adot out right everybody probably uses the A dot out Ado out is the default name so if you don't put a dashA show option。

GCCT just defaults to a that out yeah。Well make would be confusing because make is the name of the program that runs make files。

Oh， so yeah， so I think you're asked about how to run this。Let me go back to the。Scratch directory。

think I was here。So okay， so this is the same program， so if I run this on the command line。

This is how you build the main program， so now I've got a new version of Maine so it was created right now。

呃。This should be the name of this， this target name should be the name of the file that you're generating in the recipe make doesn't check this for you。

 but that's the convention， the idea is that this target specifies the name of the file that the recipe will generate。

So the name of this target should be whatever file is being generated here。

So I could make a program called make， but that might be a little confusing because。

I have a program called makeC。You know。This doesn't do what make does now I think your question is how do I when does this name get used and so the argument that you pass。

 the arguments that you pass to make are the names of targets to build。

So if you want to ask Ma to build this target using this rule。

 you give it the name of the target that you want to build。

To make this to give the name of the target， so I think last time I gloss over this a little quickly where I just ran make。

And so， so just like everything in the UniX world， there's a focus on good defaults to kind of make development faster。

So if you don't put any target name for makeC， it'll default to the very first target in your file。

 it won't run all of them， it'll only run the first one。嗯。But to make it clear if we actually。

 the defaults are hidden and you wouldn't know that if you didn't you didn't already use this already。

😡，So you can think of， yeah， think of make as just taking all the targets that you want to build that you want to run。

So if I run Make Maine， it will run this recipe。All right， other questions on this。

 the basics of this basic NA file？And so then we talked about prerequisites。

So this is the prerequisite section， so notice here when I run make over and over and over， it says。

 well， Maine has already generated， so I'm not going to bother trying to build it again。

But if I edit one of these dotC files， make will look at your prerequisites and say， all right。

 is any one of these files newer， so it'll literally look at the timestamp so you can see here the timestamp of main dot C is 1219。

 it's later than main， the file that I generated。So what make'll do is it'll look at all of the timestamps of the prerequisites and compare them with the timestamp of the target。

And if any of the dependent files， the press are newer， then the file you generated then it says。

 aha， I need to regenerate this because some of the files that were used to build it。Are newer。

 so I need to rebuild， so now when I run make mainine again。It will rebuild main。

My version of makeI here is for make file is the advanced version where I do separate compilation。

 but it's the same idea。Using these time steps。To determine when to rebuild。啊发。

Other questions on this？🎼Yeah。I can't make bows。Yeah， so Ka。

 this if you review the lecture on command line usage， I go over this program called CA。

 which is prints。The contents of a text file to standard out。

 which is your console when you're running dash。或者。

Such creates the file CAAT will print the text of a two standard out。Oh呃。

So without seeing your exact。Yesす。So without seeing your exact code， it's hard to diagnose why。

Let's say。So maybe if you didn't have the dependencies。No， yeah。

 I don't know why without seeing your exact code， you can send to me on Ed STM or come to office hours and show me the code and I can try to diagnose why it's rebuilding every time。

Yeah， sorry yeah。For if you're a hello world one， you don't， because there's only one file。

 I just want to show you this。You know this notion of build automation and separate compilation because you'll see this everywhere when you go into software engineering。

 but the hellello World program is literally one program so you don't necessarily have to go through all this righam role to do this so use you could use this template for the hello world because all you have to change is the program name and the source files and all the rest is。

Patterns for doing separate compilation for you。But no。

 you could do what the 21st century Cbook does and just have this super basic。

A single target make file， which hopefully everybody read that section， you'll see that's a very。

 very simple starting make file that the book introduces。Yeah， so least for。For a program like this。

call。Well。I guess this is a question of I don't know， maybe a question of taste。You could do this。

 Yeah， you could do this。 I guess it's just it's like a programmer style thing where you。You know。

 don't have sort of。Magic numbers or magic magic strings in your code， you can think of this， I mean。

 you can think of this as a program this make is a programming language of sorts。

And so just like in common programming style， you put constants in variables and then use them in code so if you think of rules as like the statements in the language。

And variables as variables like they are in a programming language。

 then it's like kind of nice practice to do that。 I guess you could also， oh。

 another reason to do this is that some of these。Some of these rules are actually built into make。

 so I think this rule。Comes with me already， so if you even omitted this。

I think this would just work without this rule。Try it out and you can see。

 but some of these rules are built in so you can actually avoid writing the rule。

 you could also include these or use this same template in every project you do and just change just same principles as abstraction and programming languages。

All I can really say is to do that， but you don't have to do it。For it to work。All right。

 everybody more or less good on make， so the actual make file that you need。

 you just need to make a make file for the He of World project that you can be as expedient as you'd like if you want to do something as expedient as this。

Or as sophisticated as you like， if you want to make a fancy version of the make file。

 but just some make file， just want to you some experience of doing that on the command line with your command line editor。

And seeing your source repository so you can actually clone and build it。Oh。

 did you make it morewhile？Yes， yeah， you'll be making them on your own for the future projects。

As well， yeah。Yeah， so the kind of classic。So the kind of minimal expectation for getting。

Building a program from source is to be able to just do this。 These three steps。

 So this is why I put this in a self check for your。For your homework， for your project rather。

And you'll see this like all over， there's probably， I don't know， like thousands。

 hundreds of thousands of projects that follow this basic pattern。

You clone from whatever source repository it has might not be gi might be some other search control。

 go into the directory and you run some variation of make。

 maybe you'll do some configuration first maybe there's generated make files。

 maybe there's a different build system but the ideal is that you just download the source code and then there's an automated process that will build the code for you。

 so what I want everybody to get the experience creating。

Is source projects that are in version control that somebody else can download and look at all of the version history and they can build with a single command。

 they don't have to read through readmes， they don't have to struggle with your idiosyncratic build system that you generated or your idioconsotic way of building stuff they can just run clone make and done somebody else now can run your program that's kind of the ideal here yeah。

哦， yes。So this is in the homework project， this is in the self check。

And this is literally what we do for grading so one of the benefits here is that for automated grading it's very standardized。

 we can just clone your repository run make and then grade it and we don't have to read through specific readme files without how to build yeah。

we'll go into that。 We'll go into that when we do get， yeah。Is there a question。

This is the well yeah， this is an example from the assignment itself， but in general。

 if you just go on any Git project， GitHub project that's in C or any language that typically use C++ that uses Make or one of the make file generators like CMake。

 the instructions will ideally be very simple， it'll be clone this include any dependencies in your system and they talk about that in the 21st century the book as well and then just run this program that will build it for you。

Hopefully， if all of your versions line up and your system has all of the requirements that are。

You have all the requirements in your system for the project。The program is built。So in other words。

Don't impose on others， some really complicated convoluted process where they have to sit down and read through a long text file to get your program running because they're probably not going to do it。

😔，Theyll be more likely to do it if you have this very simple process for getting your program on there。

All right， other questions on make files？So just do simple the simplest make file you can and make sure that you know next week when you submit this。

 that this process works because this is literally what we're going to do for grading。

 we're going to clone your repository under your NID。We're going to enter the directory。

We're going to run make to build it and we're going to run hello， and if that doesn't work。

And it doesn't work。 It broke。 So this is a grading sequence。 of if any step of that breaks， then。

I've mapped this to points， but you should just make sure that this works and also this is a Senate check for yourself as well because you've probably been in a situation where I'm sure this worked at some point。

 or I had a version of this that worked at some point， you may be fiddled with the code。

 you may have lost that code。And you may send it to， maybe you're working on a group project。

 you send it to your collaborator and they're like， I can't get this to work and you're like。

I know this works on my machine。So I want you to go through the process of making a distributable。

 automated process for building and using your software that has checks for your guardrails to make sure that what you see on your machine and you're doing on your machine is exactly replicable or as much as possible on somebody else's machine。

Absolutely， yeah， yeah yeah， so you submit it on time， submit your initial project on time。

 otherwise you cannot resubmit， review the syllabus， review the first lecture。

 but then you can resubmit if things don't work。But the beauty of this build automation。

Is that I can tell you exactly the sequence of steps we use to grade you。

 and you can replicate those steps for yourself and we're all working on the same machine。

So there's very little chance。That if this works。You will not get the great except for like human error or script error error on our side。

 which we will， of course。But you don't have to be the great part about computing is you don't have to trust anyone else。

 you can do it yourself。Try this yourself， you can prove for yourself whether this works and these tools。

 these automated tools and version control tools provide mechanisms to reduce the cognitive load of doing that You don't have to remember what version of code you are on or whether something worked at some point。

You can have the machine automate that for you， then you can have very high assurance that your code will work as expected when somebody else runs it。

 including our graders。very very咖哦。おてやて。ああ。You just type these into the terminal。

 so this is just like a terminal session， you just type this into the terminal and go to your home once you're in use this。

 go home， you clone your own repository， replacing an ID with your own name。

 go into the directory that was created， so I'll go over this more when I go into Git but you can ask Git to clone to a different directory name doesn't have to go to hello。

And this is how your program should build and run。And this is how you're okay， actually。

 do I require clean here？嗯。Yeah， I require clean， so yeah， just look at the。Look at the notes。

 the lecture notes from last time about using。Uing clean， I think I closed it yeah no here， you know。

 just copy this template or use。Just add clean yourself。Oh yeah yeah。Noll change ND。

 and I change NID。僕にま。Everything else should be the same yeah yeah， so this will create a new。

This git clone will create with download the repository from。Getttle light。

And it'll put it in whatever folder you give as the next argument。

 you can omit this folder and by default， you know， remember good defaults。

 it's always part of the design， the UniX philosophy will use the name of the repository as the name of the folder。

 but you can override that you can change that default and give your own folder name So if you do this twice gets going to complain that hello new already exists so you can just put a different directory or delete that directory or remove it。

All right， so let's go over again， this is one of the most version control is probably one of the most important tools。

You'll use as a software engineer。And as yeah， I use it all the time， not just for。Code。

 but also for papers， know， we write papers and we have。Papers are in machine readable formats。

You can use it for documentation， it's really a general purpose， almost like a file system。

 a snapshoting file system that will record the history of all changes that you manually tell it to record。

O。Merge control。So let's Vi control sort of basically all Vi control is is a way to record changes to code。

And really just any file， most of these systems are set up。To be just files。

 source code is just text files。So virtual control records change to files。So。

 why do version control， why bother recording the changes that you've made the source of yeah？

Very good， yeah， something went wrong， you have a log of what happened。

You don't have to guess or try to remember。😔，You can go into your version history。

 check out a prior version。And see the difference， see what changed。呃。Interestingly。

 you can also record who made a change so you can blame whoever made it。

 and actually Git has a command called gett blameme。

For telling you who's responsible for every line of code。Other reasons。你佢得九样。Yeah。

 so similar to similar to what their colleague was saying， if something goes wrong。

 there's a bug and they're actually automated techniques because you have this whole version history。

There's a command and G called G bisSect， which will run a little binary search through the history to find which commit。

Introduce the bug， you know so you have a know you might have thousands of committs。

 but if you remember binary search， you have the search space every time and you can actually zero in on the exact commit。

 the exact change。That before the change， there was no bug after the change there was a bug if the bugs deterministic。

 so sure， absolutely yes。Yes， and this is one of the key kind of features of Git is branching and well I'll show what that looks like。

 but you can have a divergent history of the source code you can and because Git is distributed。

NoThere's no internal notion of which。Copy of the repository is the real one is the official one there's really not really any technical notion of that。

 there may be a social convention behind that， but you can fork you know if you've ever heard of forking a repository or forking a project somebody could just copy it and make their own history indefinitely or you can temporarily make a separate history so you don't mess up other people developing the project while you go and implement whatever feature or fix you need。

 even if you're maybe breaking the build or you're not really ready to release it to other people。

 and so one of the key the wrong of the reasons branching is so useful is because version control also allows lots of different developers to work together simultaneously。

So if it's already hard enough for yourself or myself。

 really to try to manage just a small project on my own and remember everything that'。Changed。

 imagine multiple people accessing it。know multipleip people trying to edit the same file at the same time。

 that's going to be a huge mess， so Vi control one of the key features of it is it allows multiple collaborators。

Potentially thousands of collaborators to work or even millions of collaborators to work on the same project at the same time。

All right， other thoughts on why this is useful yeah。Yeah。

 that's a very good point and so we'll yeah for absolutely。

 so not only and we'll show this in a little bit， not only does it log to change。

But it logs metadata about the change， so not only who did it。

 but developers write a message describing what and why the change was made。

 what the change was and why the change made and now this is these are tools that help you do this you don't you know if you're using get kind of pathologically you might just say change change change but the way you're supposed to use it for have a good log that'll help you figure out what went wrong in code is to write good log messages and I have a guide from Google engineering who is very。

 very good at engineering， very good at software engineering a guide here well they'll actually tell you good recommendations for how to write these commit messages you're going to be working alone in this class in your projects。

You don't have to write essays on each change， but it's good practice for yourself as well and also there's good advice on what is a change so in this class you can really do change like whole file changes if you want but Git has functionality to allow you to have individual lines of a file so if you're working on multiple things at the same time you can commit just part of the changes that you've made。

These are more advanced features we'll get into this in this class and I encourage you to read the free online Gitbook to learn more about this。

 but yeah， super super powerful tool All right， other thoughts on why use version control。

So this is standard in the industry， I remember maybe 10 years ago or five years ago hearing a student saying。

 yeah， my job they're still just like copying files around， I hope that's not common anymore。

 I mean first certainly for the big tech companies。Version control is de Rior。ai， wait a minute？

I brief。A crowdrow strike， they weren't using version controller or something， is that？

I don't't I haven't looked into that， what happened there。Didn't be like。okay， okay。えられ。日けはす。Okay。

 I'll have to look into， I don't know if software Engine or version control was related。对。All right。

 so that's why I use version control， so it gave a little intuition about how version control systems maybe not internally store stuff。

 but the kind of view you get as a user of a version control system is who's got a bank account they a。

So you're probably very concerned about your bank count balance as I think most people are。

When you probably go in and you probably look like， okay， I just want to know my total。😡。

But this balance says nothing about how you got there。😡，So if you're ever shocked when you say。

 oh my God， why do I have like a negative 100 balance？

That balance doesn't tell you about how that happened， right。

 that I spend too much this week that somebody not pay me。

 so how do you know why your balance is your balance？😡，What do you look at？Yeah。

 you look at the statement of charges， the set of transactions。So that total is the sum。😡。

Of each credit and debit。 So I got some income，$43。 I spent money on gas， which is。

Definitely more than this， I spent my hours on food， I got a reimbursement for something。

And so actually you can completely delete this total right you don't need the total now because all you have to do is add up the sequence of changes to your account andvoila you've got your total so I could completely hide this。

😡，And you can always recover the total。By looking adding up the sequences of changes。And well。

 I probably should have put dates here， but yeah， actually now that I think about it。

 if so these are also ordered by date， and so I can figure out how much I had in my account a week ago by just looking at all the transactions in the history。

Up to a week ago， so I can say well， you know at this point。

 okay my bet is really bad so what's the $30$27， so at this point in time I had $27 in my account。

kind of makes sense， so with this transaction log， where have a date， a timetamp and a change。

You can recover your account balance at any point in history。And so this is the same principle。

 if not the actual implementation that version control systems give you。

 and this is the kind of high level interface they give you， is each time you make a change in code。

 you can think of it as a transaction as a difference。That's made to the code。

You add that to an append only log with a timestamp。

And so the state of your current code is just the sum of all those changes。

 the application of all those changes throughout the history of the project。Questions on that。

 can of make sense？Okay， so this is what version control systems do。

 you tell it here's the set of changes I'd like you to record。And you write a message。

Describing what that changes is， that's fundamentally。

 I think pretty much all version control systems give you the ability to do this。All right。

 so let's take a look at。The classic tooling that actually a lot of this change management is originally based on from the UniX world。

け方。Yes， this one， right？Okay。So I got an example here， Okay， so there's this classic。

UnX tool called diF。Diff compares files line by line， So if I get so okay。

 so remember cat prints out the text of a file， so here is。One file here。

 it's a simple Hello world program。And here's another file， new file。

 what's the difference between these two？So I don't want to look at codes。

 so I'm going to use the di tool to tell me， but you're right， there's no include header。

But instead of doing that yourself， there's a tool called DIF。

 which will go line by line in the two files， and it'll report to you without by default。

 it'll report to you only those lines of code that differ between the two files。

So as your colleague said， the only difference here is there's an include， also an empty space。

 an empty line。And so itll has， by default， it has this little notation where it's a right arrow for the rights file。

The right file here is new file， so any lines that are in the right file have a right arrow。

 any file lines that are in the， the left file will have a left arrow。Yeah。Oh， good question。

So what do you think happens if I do it the other way around？Right， so they're removed。Yes。

 so good so one of your colleagues said left arrow， so its exactly right。

 so this default format for diF。This is the notation that it uses for the output it uses left and right arrows。

Now， it's funny that you said red because there is another。

So this dash is this so called universal diF format， I think I don't。

 I don't know why there's no color， you probably have to ask it to do color。

I don't know how to get it through color。Yeah， wheres called。我在。To get color。

 I can ask it to the color。I don't know it didn work before Okay， there we go。

 so there's just a flag to turn on color。So in this format it will show you so this is more designed for code because it'll actually。

It'll actually show you like the yeah it'll show you the context around the change so this is just a different format you don't have to worry about this I don't really show this in class although the Gi tool will show you Ds in this format。

This uses a different representation that just shows minus signs for removed。Code。

And it shows plus signs in green if you turn on color for added code。

 so I just swapped the two files。So the first file is like the old version。

 the second file is the new version， and so in this format it'll say all right， green。

 this is code that's added plus green plus， this is code that's added。And if I remove lines。

 it'll show red， yeah。我在。Oh， these are actually the lion numbers in the file where the changes have happened。

I think this is the left file。And I think this is the right file。 I that right。

 So one to3 and one to 5， I think， actually。Now that I look at it， I'm not sure。Oh no， no， no， wait。

 I think this is mine， I don't know， actually， I don't know。Yeah， you stop me。

I thought they were something to do with the line numbers。But I'm not sure。嗯。We'll get there。

Let's look at the unified dip format。From file line numbers to file line numbers it is。Con。Oh， yeah。

Let's just say that's what it is。A line was added here but the first file line are removed here from the first file。

Oh， I see， it's because it's removed， wait， no， actually， I don't know。

Because the oldha had three lines starting from。Oh right， okay， okay， I guess that's it yeah。

From file line numbers to file line numbers， and I guess it's line one。Oh yeah， okay， it's the range。

 I guess， right， is it the range one to three and one to five。That sounds right， oh。

 it looks like start and count。Yeah。Yeah， it's startuple count。Yeah， so start。

It's one count is three， start is one， count is five。

 and then minus is the previous file plus is the next file。Okay， but just yeah。

 so and this is other metadata about the， you know。

 the timestamps and the names the new and old file。All right， so thats that's the diF tool。

 the diF tool will。Show you automatically what lines have changed between two files。

Questions on that， questions on the diF tool。So can you imagine how you might start building a version control system using diF？

And like the bank account transaction history， you actually don't even need to record。The file。

 you can just record the sequence of dips and some older version control systems actually work this way。

So we can actually save。The output of diff。I'll save it into a file called change dot patchch and I'll just I think patch is flexible about what the format is。

 so I'm going to use the old format， but although it doesn't really matter。

And so here is a file that I've created called Change Dot patchch。

And there's another tool called patchch。Patch takes in your old file。

 so I'm going to make a copy of my old file。So。And're called file。c。So file。 C。

 this is the contents of my old file， right， no include header。And patch is like the。

Is it the inverse of so diF takes an old file， new file， produces patch。

Patch takes old file and the actual patch file and produces the new file。

 so I gave it patch the old file。And the。File holding the diff。

Maybe I should have called change that diff， but this is the file holding the diff。

 this is the old file。It will rewrite。The file to。Updated using the DF file to produce the new file。

いやりもっ。

![](img/3d436202ff018997b53475af5a316f17_1.png)

You can do that yeah， absolutely， you can do that multiple file。There is a recursive version。Of di。

 So I've got these two file trees here， although I only have。One file in them， but you can。

You can do this recursively over an entire， actually there are two files in there now。

 but you can do this recursively with R over an entire。

Over an entire source tree or a tile file tree。So here it's recording the fact that there's a file that didn't exist yet。

Oh， that's a really good question。 conflictfing changes， we'll get there and we talk about kit。

 So here we only have a single change， so there's nothing to conflict with， right？

But if two people have made changes and the dis， if you're applying both of them。

 messes with each other's changes。How do you how do you deal with that So gett has nice ways。

 if not very fine rain ways to deal with。 this's actually multiple ways to deal with this and we'll go over that we'll go that in a little bit and make sure let me check my time all right。

 so this is different patch。 This is the basis of。Version control because diF records changes and if you record a series of changes。

 then you've recorded the history of the versions of a file。Okay。

 so you can think of a version of history as just a sequence of these dips。

Just like a bank account balance， any questions on this so far？Yes。So if diF takes two files。

And gives you the changes between the files。Patch allows you to take the old file。

 Pat allows you to take the old file and the changes and recreate the new file。Does that make sense？

Let me show this diagrammatically。So this。Take。tTo fire。It's called F1 and F2， takes in two files。

And it produces。A change， it records the change between those two。😔，So what Pat does， patch takes in。

The patch tool will take in。The old file。And it change。And it'll recreate the new file for you。

Does that excess？Yeah， all good， okay， yeah， so that's yeah， diagrammatically， that's what this Pat。

Second。That's a good question， so there is a flag in patch that will unpatch。

It'll do the inverse of the patch。To show this diagrammatically， it's capital R。You could also。

Give the new file。And the same change。And it'll give you the old file。Yes， so you can uncat as well。

 but don't worry about that， I mean， it just reverses the pluses and minuses basically。But yes。

 you can unpatch with the patch tool as well。Okay， so that's dip and patch。

And a version is you can think of it as a sequence of diffs conceptually， at least。All right。

 so enter git git is。诶。A version control system。For recording these sequences of changes。

 these versions， different versions of source code。呃。And the  Lo is repo for repository。

 repository is the database of those versions。And Git has some nice features that make it useful for large scale development。

One， it's distributed， which means you can have multiple copies of the database and you can synchronize these multiple copies。

 so if I clone。If I clonet say the Linux repository from the Colel。org。

 I'll have a full copy of the entire history。Of the Linux Chrome development from when they started using get。

And whatever they batted back。You can also create branches of which you talked about so you can create your own history。

And if。If you like， you can merge that history back into whatever the main。

Development history of that project is。So distributed， this is a graphic from distributed。

 this is from the Pro Git book， there's an online free book from， I think the Git developers。诶。

So this is what it means by distributed it。You can have multiple copies of this database on different computers so there can be two different users。

 you can， if you like， you can have a server， record the official version。

 although you don't have to， you can use it in a decentralized fashion。

And so everyone has a complete copy of all versions。Of this source code。And you can do branching。

 so if each one of these C0 C1， these are changes， these are commits that record the change with a message。

So this is the first commit， this is the second commit， the third commit。You can have multiple。

Forked histories on this source code， so this master may be the main branch， the master branch。

 the kind of official branch by default it's called master。

You can make a new branch that adds some bug fix and some other branch that adds some new feature and you can have these branches live at the same time。

 maybe if they're on other people's machines， maybe they're on your machine。

 maybe use multiple branches in your development。And so branching is one of the key features of Git that you can very。

 very easily and quickly make a branch because all a branch really is is just。A name。

That points to a commit。And because all of commits point to their prior version。

 you can have as many trees， you know， if you've ever looked at blockchain and kind of it's like blockchain where you can have multiple histories that all have a shared common history by following the chain questions on this。

This is really just a link list that has multiple heads to the list。That's really all。

 that's one way to think of the think of branching。

 linked list with multiple heads to the list and in。In the lingo of gett， the head is the first。

Or the last commit to the branch is called Head。And in order to。

Resolve these multiple histories you can merge。Branches。

 so here's a case where you have a merge commit here。

 the merge commit just has pointers to both of the histories。嗯。And now in practice。

 if you have merged conflicts， you have to resolve these yourself before you can create this merge history。

 you won't let you create this merge commit until you have commits that can be merged together。

 so Git has automated merge。Tools， they have this thing called a three way merge where they will try to automatically find a merge that allows all changes。

Basically， as long as they're not being changed the same file or the same line in the same file。

 then you can automatically merge more or less。Okay。

 so these instructions are on the hellello World Project as well。

 but make sure after you log into Eustace that you configure your Git repository。

Let's see how much time I have Okay， yeah， not to right。

I think I've already configured this myself in Eustace。So make sure you put your full name。

 your email， and importantly put the editor that you chose to use EMAC or VIm。

Configure the editor because when you go to do commits。

 it will pop open your editor for you to allow you to write in a commit message， yeah。No， no。

 this is a one time， one time。One time configuration。Which I hope I make clear in the hello world。

 yeah， this is a one time。So I don't make it clear here， but yes， you only have to do it once。

 so it's stored in a file in your。So it stored in a file called GitkinPg。In your home directory。

So you don' only have to do it once， and it just creates this file。外新。Yeah。

 you just run this command again and put in your new editor name， yeah。Though， I can。幸せた。

There's a dot here， I don't know if you saw the dot here。So we tried this。

Then you may not have configured get yet。So once you run these commands。

 you'll have configured Git and that file will appear。So run these commands。

 it's in the hello world instructions in the Git section， run these to configure Git。Yeah， all right。

 so let's go over some of the basics of using Git and you can follow along or follow along at home。

 I'll just go through a very simple example and you can actually create you can create this example yourself by putting your own NID here。

So let me create a new repo。Okay， so this is what you see when you clone an empty repo。

So I changed directory into the folder that stored my repository。So Gi status。

 this is something you probably you should be running all the time。

 this tells you whether you've made any changes to the repository， so here it says you know。

 I have no commits yet。I can。Add a new file so remember echo just。

prints out whatever the argument is to the command line I can make and I use redirection to create a new file so I create a new file called Read Me here that just contains the word example repo。

GittAd will stage this file to be committed。And if I look at Git status， it'll tell us。

 here's some changes that you've told me you want to commit。And then to actually create the commit。

 you run get commitit。And in this case I'm going to show you just putting the name of the file。

 so what this will do is it'll commit all of the changes in that file since you've staged the file for this class that's probably okay if you're in a professional setting you'll want to commit stage individual lines。

 commit individual lines of your program and it's much easier to do it with like an IEE。

 there are tools to integrate this in EMX。Vim called Magt and fugitive， I think it's called。

 I showed this actually in the editor。Lecture using the EmX integrated version of this。

 so this will create the commit and when you run get commit。

Some people may have seen DM for writing commit message， I consider this bad practice。

And so you know， if you look at like Google's engineering guide。

 I would call that bad practice just during Gr Gi commit and the changes that you want to commit。

And your editor will pop up。 So the reason for this is that。

Your changes may not be decribable very easily on a single line of ba， so here I've。Add。

Read me to the repo。And then the convention is you add a space and then some explanatory text explaining more details about the change or why you made the change。

 you know this was an empty repo。So I created an initial commit。呃。Ever read me。

And so this is you know， this is very much a toy example， but if you look at you。

 professionally maintained。Gt repositories you'll see detailed。

Commit messages and so this is why it's so important to know your editor。

So because it'll pop up your editor automatically and if you're confused about how to use your editor or quit your editor。

You're going to get stuck， so if you know how to use your editor， you save the file， quit。

And Git will take that file and create the commit for you。

 so it tells us here you know some details about the commit。

And so if you want to confirm that this has happened。There's a few things you can do。

 you can run Gi status and it says。Nothing to commit， working tree clean。

 so no changes have been made right now。If you want to actually view the sequence of metadata about your Git history。

 you can run Git log。So Glog shows a lot of things every commit has a unique ID associated with it。

 it will tell you the author， the data was created and here is my commit message。

So when you have a long commit history。Like if I show you， for instance。

 there's the Linux kernelline run GitLo。You can see they are very。

 very detailed in their commit so this is a merge commit by。Lnus itself。

 that gives great detail about why this commit was made or why this merge was made。

can actually search for。牛皮。So you can see here that there， well。

 these are lots of merge commits here。What we go down to。A non merge commit。

So this is a commit made by this author Dave here， actually this is a revert commit。

 he unid something， but you can see that these professional developers have very， very detailed。

Commit logs。That they're using to explain why these fixes were made and you'll see。For instance。

 fixes to CVEs， you know security vulnerabilities， very detailed descriptions of what this fix was and why it was made yeah。

Which。Oh sure， yeah， so let's do another commit， so let's edit this readme file here。

So I've edited my Read file， which you should all know how to use your editor or at least the basics of opening。

Adding text， saving and quitting。So I've edited my readme file， so here's my readM file。

 I've addeded it。And if I want to commit all the changes in the file stage and commit all the changes in the file。

 I say get commit and the name of the file， so as I said in practice you should be committing individual lines but for this class it's fine to just do individual whole files。

So as soon as I hit En， it pops up an editor for me。

This editor contains some instructions and some information about the commit， everything in。

 as it says here， everything with a hash， symbol will be comments will be ignored。

 will not go into the commit message。And the convention is to have a subject line here or a summary line here。

You add。More detail about the project。And add space and add a paragraph now。

 you may not always need to add the great kind of detail that say you know these Linux developers are。

Especially for a class project。But it's just a file， it's just a file。

 and this is the name of the file it's called commitit editit Me that get manage for you。

And then you just save it， so I just did control X control S in EmX， control X control C to quit。

 and once I quit。Gitt will take the content of that file and it will attach it to the changes I made。

Iss that clear？こ笑いかてどうぞ。呃。The file， you just put a description of the commit。

 you don't have to name the name of the file， you don't have to rename。

The commit says you just put a description， save it and quit， and Gi will manage it for you。

So when you' said Gi log， we'll now see that there are two commits。

 so the latest commit is at the top。And the next commit。

And this is just the metadata of the commits here。You can actually view。

So this is the unique idea of the commit if we copy one of these commits here。

The Git show command will show you。The diF of this commit。

 it'll show you all the metadata as well as the diF。



![](img/3d436202ff018997b53475af5a316f17_3.png)

To show you what changes were made to all of the files and if there are multiple files here。

'll show you the changes that were made to all files。



![](img/3d436202ff018997b53475af5a316f17_5.png)

By default， if you' run Git show， it'll show you the latest change。

The latest change is also alias to the head label。And so I can also if I look in my log，ops。

If I go back to my log here， if I want to see an older commit， I can copy this older commit。

 run Git show。And you'll see the contents of my first commit。And you can， you know。

 you don't remember all this now， but there are there are like little shortcuts for。

For viewing you know， I can view the second to last commit by adding a tilda can view you。

Don't worry about the details of these。All right questions on this so far。The basic commands。

 get status to see if you have any changes so if I。Edit this again。

And I run Git status it'll tell me I've got some modified files that are not yet ready to be committed and so this is a you know。

 if you think you've submitted your project and you run Git status and you see this。

 we will not see these changes with these changes be， these are only your local changes。那就 out。No。

 the commits will not be updated think of it like blockchain where it's you know they won't be updated now there are git does allow you to change histories。

You can actually go and do that。Still back in0。No because the hash code I think is based on the metadata so it's like aend only you could rewrite the history but then you'd have a different you'd have a different history and you'd have to if you're working with other people you'd have to get them to accept that history so no yeah if you messed up your metadata and you put some name in there that wasn't your name there are even mechanisms today for having authentication of who is making the commitments because you could just lie right you could just say any name you want。

But there are ways you can include like basically a digital signature as well to authenticate。

 but yeah good question， but yeah some people complain that Gi allows you to sort of modify history and that's sort of true because yeah you could。

 but you know it's rewriting a history so you have to get others to accept your new history。

All right， any questions on Gi status？Get add。Get commit。And get log。All right。

 so I know there's a lot of command here， and I just want to show you kind of。

high levelvel design of Git and where all these commands fit in and I don't expect you to kind of remember all of this or know how to use all of this so at the end I'm going to give you a very basic workflow that you can just copy from every time you're going to grade your project but to help you understand what all these commands do and why they're there let me give you some information that's from basically from the progit textbook。

So the design of a Git project， if you have one Git project。

 when you clone a project or you creating a project。

There are three pieces of that project that are in that folder。

The first is the working directory and this is your copy of one version of the project。

 this is what you think of as your source code， this is all like the readMe file， the source code。

 the doC file， this is all of the code in your project。

And the working directory is where you can actually。

 you can check out different versions of the project if you like。

 when you clone a project that defaults the working director defaults to the latest change。

 but this is the snapshot of the version of your project。

 this is what you're going to be editing and working with。

The staging area allows you to collect changes together before you commit them now why would you want to do this。

 why wouldt you just commit， why would you want a stage commits？😡，Oh， well。

I think you'd have to commit to do that， I mean you'd want to make a separate branch to do that yeah。

Well， code review is also interpretly done after it commits。Yeah。

 so why would you want to like so you as about you know commit is when you you know。

 when you commit it， you add it to the log and then if you have other collaborators。

 you'll share that log with them， but why would you want to bother，Like。Not just saying， well。

 just commit this whole file every time like the D Malon， I think you were next up there。Okay， yeah。

 so you may have a lot of changes in your working directory。

 but not all you may not want to commit all those changes and you really shouldn't。

 you may have fixed multiple bugs or you may have been adding a feature and then found a bug that you fixed。

😡，And if you read that the Google guide and I'll link to it on making good changes。

 making good change logs， you should have merge those together。

 and in fact if you work in a place like Google or one of these fan companies。

During the code review process， when in order to have your collaborators accept your changes。

 they're going to review your code， including testing it， including looking at your commit messages。

 including looking at the code that was changed and if you give this massive commit that just commits hundreds of lines that fixes 10 bugs and you say fix a lot of bugs andm like。

 they're just going to reject it。And you're not going to go into their official code repository。

 so staging is an for you know when you're doing professional development。

 staging is an important part to collect together only those lines of code that you want to put in your well crafted。

 well described well justified change。And so Stage Gaary allows you to do this where you can have lots of modified code。

 but you only， know I've done this many times in my own code where I want to break down the changes I made into separate pieces so it's clear what happened。

And so that I'm not just shoving a whole bunch of stuff into one commit。

So that's what staging is for and then the repository itself。

 it's actually not in your main directory， it's in a subdirecty called dot Git。By default。

 so if you look here here's， you know my code is in this directory。

 just literally a snapshot of code and then there is a subdirector here called dot gett。

 which contains。You know， compressed and。compresspressed database of all of the files and all of the snapshots of all the files in your entire history yes。

I don't think there's any size limit now， I mean， the size limit may be your directory size limit。

やレギ？That's a good question can you partially comment yeah， you you can actually。

 I think you can yeah， you can， you can ask so。It's getting in theise a little bit。

 but the Gibook makes it very clear that it's not a delta base， it's not literally storing only dis。

 it stores snapshots of the entire tree， but it uses tricks behind the scenes。

storing only dis and pointing to the previous file so I don't know what Gi does under the hood but you can ask it to only check out like the latest clone the latest branch and I think behind the scenes it'll just download whatever data it needs in order to reconstruct that so it may end up downloading more than just that branch but I think it probably will optimize away from that not entirely show the internal but yeah you can ask it to only get part of the history。

Whether it actually gives you a subset of the database， I'm not sure。Okay。

 so those are the three pieces。呃。And so when we do operations like stage commit， check out。

 so when you clone your original project or you sayG check out to get a different version。

It's taking the data from the repository itself， the Git directory and its。

Moving it or checking it out， I mean it's uncompressing it and copying it to the working directory and this is where you're actually going to be editing files。

 making changes to your code。😡，When you stage fixes， there's a separate place in your tree。

 I think it's under like index or something where it'll keep track of all of the changes that you've made。

And then when you commit， it moves those changes into a new commit。

With a loggged message and stores it into the repository。

So this is how these three pieces work together。So these are the operations that operate on these different areas。

 so add is what stages of file， remove。Actually removes a file from the tells the staging area to remove a file commit will is what does the move files from the staging area to the commit。

 and then you use your editor or whatever to modify files in your working directory。

So you can review these reviews at home or look over my basic workflow that I've given you。

 and know these are the different states。Of files， so they're either not in the repository。

 they're unmodified， they're modified or they're staged。

I'll let you guys look at this yourself if you want to look in detail about what each of these operations do。

I've already given an example of committing。you can also use diF to look at what has changed between your actually this is a useful tool。

 so if you've changed stuff。呃。In your read me， for instance， so if I add a new line to the read me。

Then GitDF will show you what has changed so this is nice to pair with Git status。

 Git status will show you that a file has changed， GitDff will show you what has changed between your working directory and the last version。

 you can also compare multiple versions of the file。Okay so last bit on the remote part。

 so for your project， we have a very， very simple architecture， there are only two repositories。

 so remember it's distributed， we have a copy on Giite。

 the Gitto Light server and you have a copy on your Et account。

The project submission is just using Gis tools for synchronizing。These two different repositories。

 and so we'll just use the exact same tools that Git has。When you clone。

 all that happens is all the commits are copied to the two machines and there are these pointers that say。

 hey， this is my view of the remote machines。Branch。

 and this is the review of my local machines branch， yeah。Yes， yes， all right。

 so let me switch to the project we only have four minutes left。

Let me switch to the project itself and go over the project itself。嗯。

So this is assuming that you've already done the first step creating the Hello World program。

And assuming that you've created the make file already。

This shows you all commands you're going to need to run in order to submit your project。

So do the one time configuration of your Git tool in Eustace。And then inside of your Ho folder。

 which is where your program， your script， and your make file should live。

Git andit will create a new Git repository for you， create an empty one in the dotgit folder。

And use Git add to add each of these files。Into your repository。

And use Git commitit in order to stage them into your post store and then use Git commitit to actually make a commit。

So。Oh get ignore， I got to go get ignore， so get ignore。All right， so let me just let me do the。

Do this， these sequence of steps here， so I've done get it。And let me add。Hello dot C。

What else do I have in here， hello timing hellello。script， I don't have the make file yet。

So the reason we have G ignoredre is that if I go to build this。Let's say， I。Compilehelello。c。

I now have this Ha O file。But but it's not a file I want in my repository。

 so one of the main conventions of version control is to not store generated files or binary files。

 they're large， they can be regenerated。So the problem is I've got these additional files that I don't want in my repository。

 but every time I run gets status， it tells me， hey， you have these files here。

So what Get ignoredre does is it。Keeps maintains a list of paths to files that you want to tell Git to never consider as part of the repository。

 so when I put hello。0 or you know if I do this with Git or with EmX。I put hello。

0 in this getign file， I can also put the program hello in here。And when I run Gi status。

 it'll no longer complain to me that Ho do O has not been tracked by。By my。

Repository so now this gett ignore file is something that you want to track because you want other people who use a repository to also ignore the same files。

So if you're an Emax user and you want to ignore these backup files。

You can add those as well or a little trick is to use a wild card to add them if you're an E user。

So now I've modified GIN， and I no longer considers this file as part of my repository。

 but I' modified get ignoregno， so I'll need to make another commit。あ。To my repository。

So the main key here is that when you run Git status。

 the key here is that when you think you're done and you run Gi status，That should be clean。

 that should be empty， there should be nothing to commit。

And when you go to clone your project so if I。Cone my Ho project into Hello new。Hello here too。

I only want to see。These files in my repository I don't want to see hello。 I don't want to see hello。

 no binary files， that's why this is very specific about。

The grading only contains the five files that are required for the project。

 so I want you to use gett ignore。2。Exclude those generated files。

 those binary files from the repository。And then， you， when you want to do the submission。

 you add this remote repository。So you just follow instructions here to add the remote repository and then when you do get push and you can just cut and paste these。

 changing your NID。This is what will synchronize your local repository。Yeah。

 because I already have this done。Yeah。I already have hello there so it's refusing to let me push it but that's what get ignore us for and this is what your state of your repository should be whenever you think you're finished。

 you should be able to run Gi status， nothing's changed there and run this commands in order to verify that what we see is what you think all right。

 thanks everyone， T for going on a minute over and go on Ed stand to ask questions。

 come to office hours and talk soon。

![](img/3d436202ff018997b53475af5a316f17_7.png)