# 佛罗里达大学 【中英⚡系统软件｜COP3402 Fall 2024, Systems Software】 p22 P22 Compiler implementation： Arithmetic (COP-3402 Fall 2024) -BV1v6vdBKEHB_p22-

So look at assist software， we're going to review the function functions with parameters using the examples from the Code Gen2 project。

There are two。IR files given to you， so one function is called mainine。

The other function is called Para test。Main。Has several local variables。

Including eight that we're going to use as parameters to the PR test function。

We assign each of these parameters to a different number， one through eight。

 and then we call PR test passing these eight parameters and saving the return value in x。

Questions on what this simple IR function program does。Yeah。かわりコーディグ。Assignment， yeah。Yeah。

That's right。All right， so PRm test is what Maine is calling。

And it defines its own set of local variables， even though they have some of them have the same name as main。

 they are local variables to this function， so they're not the same variables。

There are eight parameters specified here as parameters。

 and all this version of the function does is assign x to the C parameter。

And you can test your program by assigning this to different parameters like GNH。

 which are the stack allocated ones。🤧And make sure that you get the right value being passed back to Maine。

Questions on the behavior of this simple C， simple IR function。

So just the simplest program possible to validate whether function our events are being passed properly and whether return values are being returned properly。

All right， so let's look at how the code is how code could be generated or one way to generate code for these functions。

 remember there are lots of equivalent assembly programs。

 probably technically an infinite number of them for。The same input program。

 the same simplifyized input program， but I'll show you one that I think is easy to generate and that if you follow the instructions in codegen。

 the suggestions for implementing the。Compil their functions then should make it easier to generate。

Okay， so in simple IR， when you have the function declaration。Which。

Python function in your compiler gets called。Yeah。Enter function， so the enter function。

Is what generates code？For this。Construct in the language。And I didn't show everything here。

 but the only thing that or yeah， so actually let me ask what？What assembly gets generated？

When we enter the function yeah， the prologue， So this is the prologue here and also the definition so I。

I omitted it just for space， but this is what gets generated by the Inter function method。

 the function declaration and the prologue all right good。So local VARS， so what does local VARS do。

 of or what does the code it generates do？Yeah， that's exactly right。

 you allocate space for the local variables on the stack frame。What's the stack frame？我则。Well。

 it is implemented with the stack， but what is the meaning of the stack frame， I think you were na。

 the guy。Desated the spot on the stack。Yeah， all the relevant data for the function call。

 it includes the locals， parameters， what else doesn't include， locals parameters， what else？

Return address so we can restore the color。And the current base pointer。

Where's the current base pointer stored？Old base yeah， right。

 so we can restore the old base pointer of the collar Okay， good， so this local variables。

This will generate code that will allocate space。On the stack。

In the so called stack frame for all of our local variables， there are accommodate a nine here。

Whatever the arithmetic is， I don't want to do the math， but I guess it works out。呃。

So this is generating code and which。Which Python function in your compiler does this code generation？

Enter call。So what is the syntax of a call？So this is what a call looks like in simple IR。い。Yeah。

Right， that's what under call does so what is this this is not that what is the code that generates。

Generates the assembly code to allocate space on the stack of the locals。Yeah。Yeah， yeah， local。

 it's enterloc vs， enterloc virusars。Is the function in your compiler that does the generation。

 and I've given I gave you the code for this already， maybe probably way I don't remember。All right。

 and the prams。Yeah。All right， let me figure it out。There's eight of these。9ine。

What's nine times eight？72 Oh yeah， it's the I think it's the。An extra subtraction to get past the。

Old base pointer and then an extra eight to be 16 by aligned。

 which is the requirement of the AI what that？Oh， you're right， you're right， yeah， 9，10。

11 times eight， yeah， okay， that's88， that's the reason。Iget what I said。

 it was probably already stackied at that point。So because we had。Yeah， I don't know， but yeah。

 you're right， you're right because there's 11 locals I just kind of， you're right。Wait， one， two。

 three， four， five， six， seven， eight， nine，10，11 eight。Things around use as parameters x。

 and then the two locals 11 times8 is lot or 88。All right， good， so crams。

 does anyone remember how we or at least how I recommended in your compiler we handle input parameters yeah？

Well， so that's when you make the call， but the receiver of the parameters。

 how did I recommend we receive parameters？First you。Yeah， exactly right。

 so how are parameters passed in the system 5 ABI for AMD 64？Yeah。Registers and。Right， yeah。Exactly。

 yeah， so the first first six aren't register is the rest are in stack， this only has two parameters。

At least the implementation I'm recommending for you in the compiler there are lots of ways to write a compiler to do this。

 the easiest I believe is to just immediately copy all of the parameter registers into their the space for their local variables so this is RDI is the first register parameter negative8 RVP is the offset from the stack frame for。

The first local variable， yeah。Yeah， we'll get to that very， very soon， actually。Yeah。

 we'll get to that very soon when we see the compilation of this。I mean， like， how does。

Within the function， how does it know？That's a good question。

 so how does the compiler know which offset to use for each？Variable。Yeah。So we create， yeah。

 we generate code to create space， but remember， that happens at runtime when the program runs。

But the compiler， even without having that space actually allocated a memory。

 the compiler is keeping track of some offset like negative8。RC， negative 16 for Rrgv， et cetera。Oh。

 oh， oh， right。How do you like？Oh， we'll see that when we get to the colgue， we'll see that。

 so hold that question for both of you and then let me know if you don't answer that。

 but we'll see how we pull that information off。ffAll right， so that's how we handle parameters。

 parameters just immediately copies。The parameters from either the registers or the stack。

Into their corresponding。Stack frame spaces for those local variables。

So what I thought you' were asking is how do you know the offsets and that's the simple table。

 simple table stores and offset that's given to you and then told local here。Give that to you。Okay。

 so this is also a code that's given to you， these are all assignments here。

 I'm just going to take this whole block。This is code that I gave to you enter assigned。

And we talked about this and we talked about variables。To do an assignment。We just take。

Whatever the value on the right hand side is， collect that into a register。

 so here we're moving immediate move one into some temporary， some scratch register here like RAX。

And then to do the assignment， we're moving that data into。The stack frame offset for the variable。

 so here a has stack frame offset negative of 32 so we do that that move and remember there are many ways to write this code you could avoid having to move if you want I did it this way to make the compiler easier to write for me。

Questions on assignment enter aside。That code's given to you。

 I think we went over that quite a bit as well。Okay， so let's look at the new stuff。

 let's look at the more。Complicated stuff here。Here's the call。

 and so whenever we do a function call in our A。What are the steps that need to happen in order to make the call work with the col？

Yeah。Okay， so we need to put the first six parameters into。The registers。

 according to the convention。What else yeah？どっこしてもいい。Right， yeah。

 exactly reverse order according to our convention。

 and you push whatever the remaining parameters are onto the stack。In reverse order。

 that's just what the system5 API convention is。Okay。

 so we pass the parameters on via registers and then via stackAC if needed。

 and then what's the next step？Further the call。Well， before we do it， oh， so yeah。

 so the return address， so what instruction in the assembly world handles the return address for you？

Call， yeah， the call the call。Instruction does two things that pushes the return address for you。

And then branches crm test。And then once we've made the call and we're back executing our function。

 what needs to happen after that？对。こうした方で。入た。So that's what the call E has to do call。

Doesn't have to worry about that because as long as they all follow the same convention。

And so what I'm getting at is just this assembly code here， this is all the call lure。

 the function doing the calling has to do。It sets up the parameters。

And the first six are pastor registers， the remainder are pushed onto the second reverse order。

It makes a call。Then it tears down the stack。And then it saves the return address。

So the rest of the stack frame construction is done by the function being called。

And remember we talk about lifetime， as long as the two are following the same dimension。

 this will work correctly， this will set up the stack frame。Correctly。

Questions on those course grade steps， past parameters， make the call。Tear down the stack as needed。

And then save the return value。Yeah。对。This subtraction。This makes space for the call of Maine。

Only the call of Maine。So each function makes space for its own local variables。

Tears it down when it's done， returns to the call the function to the calling。

So somebody asked about the stack allocated parameters。So here you can see the first six parameters。

 so ABC， the EFG， ABC， DEF， these are the first six parameters。They correspond to they're the one。

 two， three， fourth， fifth， sixth， et cetera parameters。So these are their offsets， negative 32。

This is parameter A， B， C， D， E， and F。So the first six have their data copied into the six registers that by our convention。

 our calling convention， are used to pass parameters。Questions on that， questions on that code。Yeah。

好。All right， so the remaining parameters。Which are G andH。So this is G and H， I'm sorry， not H。

Confusesing myself already。 So you can see that we have this like descending sequence， negative 32。

 negative 40， et cetera， negative 72 After that， the next one is negative 80 So G。Is in。

Offset negative 80 H is an offset negative88。And you can see that when we do the pushes here。

 H is pushed first。Just because according to our convention， we push them in reverse order。

So you start from the back of the list and push， push， push， push push。That's why。

Negative and negative80 are in that order does that answer yeah？と勢のない。Yeah， yeah。

 pretty much we're not going to pop them in the collie， we're going to just access them。

 buts it's just the convention so you have to pick one way， you have to pick an order。

And I can think the idea is that yeah， it is in like order of。

It's in order of distance from the base pointer I think is why they do it。

 but you could pick any way you want as long as you all follow the same dimension。That it's fine。

 and it's based on the order that you pass the parameters in。

So to make things hopefully not too confusing， but if you had swapped H and G here。

 you put H first and G here， these two would be swapped because it's not the one that's later in the stack frame。

 it's the one that's later in the list of parameters。Well， only H and G are because the first six。

 these first six。These first six， there's registers for these first six。And so any after that。

 any others after that willll have to go on the stack。And you always start at the last one。

You start with the last parameter and work your way forward until the seven。

Pmeter pushing each one onto the stack。This sequence。Of offsets is just a coincidence， because I。

Use these parameters in the order that the local variables are allocated， but that。

That will not always be the case， depends on what order you set the parameters in。Okay， yeah。

Plarified when you're。やす。Exactly， yeah， yeah， so this ad here。This is like doing two pops。

Of the stack allocated parameters。 so notice that each function。

Each function manages whatever it added onto the stack， know it's like C++ programming。Whatever。

Materialally put into the stack， it's responsible for cleaning up。

So you don't have to do it this way， but this convention is easier to work with because each function can make assumptions about the state of the stack when it gets called。

 and the state of the stack when the function returns。

So each function is responsible for adding and removing the same material from the stack。

But yeah that's a good question， this ad corresponds to this push。

 that's why I had kind of silly looking code before where I added zero to the stack order because I just like always emit it because it's just easier to write the compiler that does that。

Yeah。It's not starting at it， it's just a has offset 32， B has offset 40。

That's because the order in which they're declared in local virus。That's where that comes from。

 so it doesn't have to be like that it's just convenient to allocate these in the order that they appear in local bars because you don't have to do it that way。

 but it's just a convenient way to do it。And so there's no convention on how local variables are stored as far as I understand。

Because it's only the local function that will work with those variables in C。Yeah。

So this is what what you calling us about is the 16 is reversing these two pushes。

 so these two pushes have the effect of subtracting。Eight from the stack pointer。

These are subtracting eight from the stack pointer。So each push， at least yeah。

 this push is pushing eight bytes onto the stack and then pushing eight bytes onto the stack。

After the call， we want to restore the stack to where it was。When we made the call。

And this ad will just reverse these pushes， well it's popping， it's like doing two pops。

This is the same thing as doing pop twice。This pops eight bytes。

And remember the stack point or whenever you push， subtract eight bytes。When you pop at eight bytes。

 so adding  eight bytes to the same thing is popping twice。

When you don't care about the return value， or you don't care about the value。That makes sense？

All right， and last but not least， what is this？Co do。What is the meaning of the code。

 the move RAXia？Right， we store the return value， so by convention the return value is stored in RAX。

So PRm test is expected to put its return value there。

And the semantics of simple IR is to save that return value into x。

 this is like an assignment of whatever the return value of the call is。And x。

X has this offset of negative 24， so we remove whatever the return value is into 24。

So hopefully you can see that these， just like when you write C。

 you define a contract between functions。So when you define types， you do this in C。

 say I would have these number of parameters in these types。

And if the function is supposed to say compute exponent。

 you expect that the return value is going to correctly have an exponent。

 it's going to be a certain type。This is that same kind of contracts between functions。

 but at the binary level at the machine code level。Where by convention， all return values are in RX。

Prameterters are passed via registers in the stack in this particular order。

The stack is some part of the stack frame is set up by the caller， some parts by the col。

 but as long as everybody satisfies these interfaces， then your code will work as expected。Questions。

 questions that。So this this code， this is what call Paras does， and this or sorry。

 this is what enter。Thator call does。Which is something that you'll need to write。

I've given you a bunch of kind of helper code and ideas about how to like separate。

 split the list of parameters。嗯。Yeah， so。Yeah， that's what enter call will do。All right。

 which piece of code does return correspond to， it's a little bit of a trick question。Yeah。The move。

And only the move。At least in our compile in our own language。

This epilogue doesn't correspond to any particular piece of code it corresponds to。

Exiting the function node。In this correspond enter return。All right。

 questions on this co generation for。Yeah。啊。Because those are。吧。Yeah， so all types in this language。

 simple IR are just  64 bit integers。移業。Yeah too yeah couldn'。So this is where you use the stack。

 so this is why the first six are pushed in registers and then their data pushed onto the stack。

So whatever is left， whatever or the seventh parameter or greater， those are pushed onto the stack。

So assuming you have sufficient stack pay space， you can have an arbitrary number of parameters on the stack。

Okay， so let's take a look at the。In this case， what will be the callee at runtime？

But the compiler doesn't have to know this， the compiler doesn't care who's calling it。Okay。

 so just like before。We generate the prologue。With enter function。

The local variables allocate space onto the stack。Pms。

Copy the parameter data from the registers and the stack into the local variable。

 so this is what somebody else also asked about so just like in Maine，The first six parameters。

Are being copied from parameter registers， RDI RSI， et cetera for the first six， so for these first。

6ix。Registers。First sorry， first six parameters。The code that I've recommended you use for enter PRms is to just generate a copy for each of the first six prams from each of the。

Pre designatedated six registers into the corresponding local variable space on the stack frame。

Questions on that。It's the same in Maine， it's the same thing we had in Maine。Okay。

 so here's how the stack parameters are handled， and there are different ways to do this。

 this is the way I'm recommending you do it。To move。Data from the。

Stack parameter into the local variable。So， on the stack。The next parameter。Has an all set of 16。

And well， okay。Let me draw the stack really quickly。

 So when this gets called Para test is expecting that。It's caller， whoever it is。

Willll push onto the stack in reverse order。The last two parameters H and G。

 remember we pushed H first and then we pushed G first when we called the BraRAM test。

And then what's next here， we have the return address。Then the old base pointer。

Which is set up by the prologue。And then we had space for the local variables。That's created here。

 so this is AB，C， D， E。F。H。So in some sense we really have two copies of H on the stack。

 which is inefficient， you could write a better compiler that wouldn't do this。

But compiling it this way makes it easier to write the compiler because you don't have to think about where local variables are versus pre so you can just treat them all as local variables。

So what I recommend that you do for enter PRs to avoid more complicated compiler implementation。

It for the first six， you copy them from the pre designatedignated registers because when we generate the call。

 we use those registers， this should correctly pass。The first six parameters。

We get to the stack allocated onces。This is where our base pointer is pointing because the prologue set it up correctly。

And so if we look one slot up， one slot positive plus8， that's our return address。

 so that's not one of our parameters that was pushed after the parameters。

The next slide after that is plus 16， which is our first parameter。

And then plus 24 whichs our next parameter， et cetera， if there are more parameters。

So I suspect this is why parameters are pushed in reverse order because the earlier parameters are actually a lower offset away。

 a smaller offset away from RVB， I suspect that's why I don't know。But yeah。

 so that's why the seventh parameter， the first parameter after the register parameters。

 is offset 16， and so you can see here。Here's offsetet 16。Being copied into。The local variable slot。

For。That parameter， that local variable offset or the variable slot for parameter G。So here。

These were the register parameters were being stored into the local variables。

 you can tell they' local variables because they have negative offsets。

And we do the same thing for the stack allocated ones。

 but we don't get them for registers instead we pull them off the stack that they call or。

 the function that called set up for us， so this is why we have plus 16 for G。

 put it into the local variable slot for G， and then plus 24 for H。That's here and put that。

Into the local variable slot for H。Yeah。I don't think you can do a store and a load in one instruction。

But prove me wrong， I I don't think the Intel architecture supports it。

I can actually load this up because I keep getting asked this。

This is the reference I use I've linked to。For you。But there's like a ton of。

Variations of the move instructions。And I don't think。

So this M means memory R means register and I don't think there's one where you can do。

Memory to memory because it's like it's two separate operations， it's like， I mean。

 it's two separate operations on like the memory bus。

So you as the compiler writer have to do that in two steps if you want to transfer between memory locations。

So yeah， this needs to be too separate if it's a move immediate， I don't think you have to do that。

 I think you can go do an immediate into。Into memory if you wanted to， but。

I wrote it in a way that I don't do that。Yeah， so this is so to make this even more confusing。

 this is Intel syntax so the destination registers on the left， the destination is on the left。

 so don't worry about that， but yeah these have to be two instructions。Okay。

 questions on collecting the parameters and copying them into local variables。

 That's what this code does。 And this is what the。Enter。Prams。Function in your compiler generates。

Let me。away a little bit。Over really a good place for it。Okay， so here's our assignment。

 we just we looked at assignment already， I believe。So， this is the assignment。

And then we looked at return also as well， so this is the return。です。Move extraction。Okay。

 questions on。How parameters are received。Courses on how functions are called and parameters are past to them。

All right， so the nice part about having these conventions and why they're in place is because you can write code。

Separate， write and build code， separately， even in other languages。

 and you'll be able to call functions that are written in other languages。So let me just quickly。

Show you。A couple examples。Of this。Okay， so here' is a C program， let me increase the size of this。

Here on the left， I have a simple IR program。That takes in two parameters， computes a times B。

 we'll go over today how arithmetic pick works， but computes a times B and then just returns that result。

 You don't really need to write a function for this supported in the language。

 but here's a function that computes the。Multiplies two。Two of its premise together and returns it。

And this is a C function that calls a function called mallt。

Gives the two parameters and then prints the result。Questions on this code。Yeah。完に。スパ。

I'm using the windowing version of it。That's why。🤧K。Questions on the sea code。

Any questions on what's in malt。h？Mult H just has this a declaration， so header files generally。

Or just used for declarations， it has no information about where the function is actually defined remember when we talked about how your program gets compiled and eventually run。

 it's the linker that finds or make sure that all function calls in your program have been defined somewhere。

 otherwise you get a linker error like when you get a linker like no main defined。嗯。

Because you've linked with some other program that is trying to call me。Yeah。

This is some preprocesor stuff， this is called a header guard。

 it prevents a header file from inadvertly being included multiple times， so it says。

If this macro has not been defined yet， then enter this block， define it。So if you， if I， if I call。

Basically， if I did like this。The contents of Mlt H would only be processed once because of that。

It's a common trick in sea。Well what？Thered be no errors。 Yeah， therell be no errors。

This if guard will just make sure you're not declaring this multiple times。

 So all the header files do this， you can see。Standard hint。You can see here that all header files。

 all standard header files do this because you might have diamondme dependencies。

 there may be two libraries that you depend on， but they both depend on standard IO。

 so they both include standard IO so this is just a kind of。Old school hack for avoiding。

Multiple inclusion。Okay， so this calls malt。And this defines a function called malt。So， let me。

To get my。Example up。I I already have a code。Compiled。Okay， so here is the。

Compiled version of the multi。ir function， this is what our compiler will generate。

 and so you can see the prologue。Allocating space for the locals。

This is the code that copies the parameters to the local variables。

This is the code that will do the multiplication， that we'll go over that today。

And sets the return value and then the epilogue， so just like what we talked about。

It uses the same A to。Take parameters in versus these two parameter registers， RDI and RSI。

And it sets the result of the multiplication to RAX。Questions on that。

T what we just went over for handling parameters。Now， if we look at the assembly code for。

Do have a single code here for main。Sure interop， so we have the C program interruptop。

 this is the one that calls malt。I can compile it to。Assembly。Like this。

 and actually I didn't look at what this looks like yet。对不起啊。

So this is what the GCC compiler generates， and so you can see that okay， well let me。

I don't remember what the code looks like， so it's passing seven and three via。

What are supposed to be the。嗯。What are supposed to be。RDI and RSI。So on the left here we have。

Our mult function on the right here we have the main that was generated by GCC。Oh， it think's there。

16 by， that's a mistake on my part。Oh yeah okay so by default these are 16 byte probably should have cast them to be 64 byte integers oh well。

 so it's not going to support 8 byte 64 byte but anyway you can see that it's moving the first parameter7 into the DI register EDI is just a 32 bit version of it it's moving it into the EDI register and it's moving three into the ESI register so that's our second parameter that's what will be taken as a second parameter。

And then it runs call malt。And then finally， it does exactly what our compiler does。

 It takes the return value and stores it into。It's space for the local variable that it's supposed to save to x。

And then it sets up the call to printnt F and does printf。

 but you can see here that it's doing the exact same thing that our compiler does because that's why we can actually write code that you can call C from and you can actually also call C programs from your code。

Isn' that amazing， you'll find it amazing that you can like generate code that the C compiler will call。

 you can write code。I have a question about that。That issue with of loading。YeahYeah。みなし。No。

 I think it's just that when you have constant integers， they have the type int。I think that's why。

 So if I think if I cast them to be。Let's see if that's right， no， I can't even cast them。

To be 64 bit。Yeah yeah， you can。They're still。It might actually， Oh， yeah， you're right。

 It might actually be an optimization。 You're right， because it's。It knows that you don't need。

64 bits for three and seven。Turning off optimization to now。

 now if I put like a some large 64 bit number in here。呃。Let's see。Let meI'm very bad at math here。

So that's one byte， two bytes， three bytes， four bytes。Let's do five bytes， let's see if that will。

Force it to be 64 bit。呃。Yeah， so I forced it to， I don't know why I did it this way。

I basically gave it a number that was more than 32 bits， that was five bytes。And it now is using RDI。

 So I think you're right， it is something like an optimization where it's saying for three。

 I don't need 64 bits。呃。But anyway， the minor thing， don't worry about this。

 The thing to remember is that。呃。By following this convention。

We can call C functions and C functions can call us because they're all just generating assembly code。

 and as long as they have the same convention for function calls passing parameters like in the SI and D register。

 DI register， or the E prefix is just whether it's 32 or 64 bits。嗯。

And we passed the return value in RAX。Right here。Yeah， that we'll be able to。

Link these programs together and make a single running program。Questions on that。

 questions on that interop， yeah。Assuming its。Releance。那个。Sure， yeah， yeah。

 let me show this work actually working， hopefully。

So here I'm compiling the C program with our output assembly program into a program called Interop。

And it should run。 and yeah， give us 21。 So it gives us， it gives us the number we expect。

 So the C program。C program is what defines Maine。And it's calling whatever this function is getting a return value and printing it。

 and it's giving you 21， which is what our assembly program does So the header file is this is just a kind of a convention in C C does not have real library management or module management。

Each C file is compiled individually。And just to kind of remind you。Of our lecture on。

How a C program gets run。If we compile these two what are called compilation units individually。

I'm sorry， so we had two programs Interop。c。And multit dot s， which is what our compiler generated。

 So we have two compilation units。They just define functions， they each just define a function。U。

So our。Our compiled Mlt dot S gets compiled into an object file by or assembled into an object file by our asseler。

And it exports a single function called malt。So that's what this symbol table is saying to us that we've got a single。

Symbol in our program called Malt。And in our。Program that calls Maine， it defines the main function。

And because it calls malt and calls print F， this undefined here， this UND means。

This binary file has no functions defined calledMt printf， it has no symbols for Malltten printf。

Does it make sense？At what stage。And if you just like to do regular。厳し？あ。What's the。

processs doesn't start complaining。At the linker phase， so when we do GccC。

 that's the compilation and assembly if you run if you do it on a GCC file。

 so if I were to say try to compile just like just try to compile mut。t S， what what I get。

 I'd get a complaint that there's no main。Because the linker is also linking with the C runtime。

 which is what calls made。Anyway， this is go back and review that class。

 I know it was a pretty fast class because I think I had to shrink stuff for the。For the hurricane。

But we have these two files here， the linker is what matches these two up。

 so if I were trying to if I were to try to just compile Interop。 C by itself。

 I'd get a complaint about an undefined reference to Mt。

And that's because I didn't provide the linker， the assembly file or the binary file that had the definition of malt。

And in my compiled version of the file， which was Interop， if I look at its symbol table。

 it's going to have a lot of stuff in it。But it will have a definition of malt。

 malt is no longer undefined。And it'll have Maine。It'll have both because it took those two binary files。

 those two object files and it length them， it made sure that the definitions， yeah。

 made sure that any unsatisfied definitions were now found in this linked file。I don't know that。

 if that helps。The header file is not involved in licking at all， all the header file does。

 so if we were to take this out。And try to compile， not link， but just try to compile Interop。C。

We'd get a warning that malt is not defined now in C， if you don't define a function ahead of time。

 it makes the assumption that you're defining a function， I think。

 with no parameters that returns an integer， so it has kind of a default behavior。U。

But it's better practice to actually declare your。Declare your function so that the compiler will know what the data type of that function is。

 and so that's one of the purposes that header files serves is just providing to the compiler the data type of your function。

 how many parameters there are return value et cetera。So that's really all header files are。

 they're not actually connected to the linker directly。It's like a simulation of modules。All right。

 questions on this。So you can also go the other direction， we'll use this。

 you can use this in class to。Use print F or something like print F。In your programs。

So I'll give you an example where I defined some very basic print functions。

So you won't be able to call printf very easily because we don't have strengths as a data to our characters。

 I mean you could probably get it to work， but it' would be a massive pain。

 so you can write a C program that has some very simple definitions like print int and readin and use printf and scanf to implement those so any questions on these functions here。

So printntf just takes an integer and then print it just takes an integer and then calls print F to print it out。

ReadDt just uses scan after read an integer and then returns that integer。Yeah。こいぞ。This PRID 64。

This is the format string for a 64 bit integer。It differs on different machines。

 so it's good practice to use the macro to do it。And so you can actually because if you follow this A correctly。

 you can actually call these functions from。Simple I。So we can actually call， read it。And you know。

 it just generates the code for a function call， and it's the linker that will make sure that code is there from our C program。

 the same thing for print print。And so you can actually write some sophisticated stuff and see and then call it from your。

From your simple IR program， so let me show you。Quickly what that looks like。

So I have a pre compileiled version of this， but so actually going to show you the。

What the assembly looks like for this first。So it's just what you'd expect if you sort of grasp the。

They function calls。So here's the call to readI。There are no parameters to redentt so it doesn't pass any parameters。

 does the call to redentt， this is my silly add zero to the stack pointer。

 which is my lazy way of writing the program， and then it just saves the return value。And that's it。

 that's how it calls read it， and read it in the C， will go off and do whatever you want。

 whatever you've written there in the program。And similarly， for print。This will。呃。

This will take the parameter and put it into RDI， which is what。嗯。The A specs。

 call print in and then save its return value。Make sense， yeah。Yeah。But is there like。The。No。

 there isn't， this is all done by the Lier， so well there may be there may be a pseudo up for it。

 but。I mostly先。Like this。No， so the compilation units are expected to have undefined functions in them。

 actually these aren't even functions， these are really just symbols。

And so the object file is expected to have symbols that are undefined。

 and then it's up to the linker。To do that， so there's no notion of a header file。

That I know of in the assembly world， it's really the linker file is kind of like the header file that you can actually define a linker file that specifies。

 I guess， like the order to you' linking in。But no。

 instead the assembler will just create a file that has。

A list of a symbol table that has all the things that are defined in it。

 all the things that are undefined， and the linker will line those up for you。All questions on this？

So I'll release these examples， I don't if you release these examples。

 but you can also do this on your own and you can actually test your AI against actual C code that's generated by GCC。

All right， let me get intorimetic since I'm quite quite late in the class， but hopefully that's。

A little clear on how functions work， how the function API works。And how you can actually test it。

Let's see。No， I did it wow， that's funny， maybe I did maybe it's arypoic I'm adding。Add them up。

All right。Let me switch gears to how we generate arithmetic arithmetic code。

So this is an example of an arithmetic simplicity operation。

It looks just like an assignment where we have the variable you want to save the result in。

 we have the assignment symbol。But on the right hand side， instead of a single variable or number。

 we have an operaand followed by an operator， followed by an operaand。And remember。

 this is just ASI text。This doesn't correspond intrinsically to an X86 ad instruction or whatever a math ad instruction。

 it's just a symbol。And so our compiler will take this IR code and it'll。Give it meaning。

 itll give it semantics by it to generating assembly code that will produce the same result。

And so in the Intel assembly with the AT&T syntax， this is somewhat what like what we're going to generate for this。

 we're going to generate an ad instruction。That will oh I forgot the comma。

 but remember that in AT and T syntax， the destination is on the right。

 what makes this extra confusing is that the destination register is also the left operaand of the。

Arithmetic， so I'll show more examples of this， it makes things a little confusing with addition it doesn't really matter。

 but subtraction it does because abstraction is not， it's the word commutative。Is that commutative？

So you have to kind of reverse in your head the order of the operas。For the Intel AT&T syntax。

Instructions， so the opera are reversed conveniently。

 these are called A and B and A and B in the two languages。

 although that doesn't have to be the case yeah。えこれで。

AhThat's a good question so in simple IR you can only do a single edition at once and in one wanted operation so I designed that language that way to make it much easier to map between the two but if you had an arbitrarily long arithmetic expression if you wanted to implement that in assembly you'd have to break that down into individual operations。

Yeah。おせかれ。大りす。If of inter。じゃあ、あどでト。Well， it depends on your language。

 So if I had a language like this where I said x equals something。 So for instance。

And this is the way like C works。F like scratch。And this is the way something like what C would look like you'd have1 plus2 times3 times y plus4。

Each of these would this would be decomposed into three operations and the result of it would be saved in X。

So you can think of this as being translated into some temporary operation that you'd have。

Three times y go first and then you have a temporary variable for one plus。T1， I think， right？

And then you'd have another temporary or just put it directly into x4。The result of1 plus t1 plus4。

 does that make sense？So this is like。So I made this more confusing with the order of operations。

 but three times y I think would go first， right？Here are times why I would go first。

 so you do that sorry， this should not be that be why。

So let me just take away variables to make this less confusing。So you do this operation first。

 so you'd have to compute that and then save it in some other place， you have to save it somewhere。

And then once you have that result， then you can compute this operation。And that's here。あで。Yeah。

 actually， we can。We could， you mean like Google an example of what it looks like， I think we can。

Yeah， think I might be able to generate one， I could generate one and C， actually。呃。

So I could write a function that takes in。ACo of parameters。And let's say it returns。

A plus two times B plus C。And let's compile。

![](img/940acac2c340d1c6d925e8dbd2484b64_1.png)

![](img/940acac2c340d1c6d925e8dbd2484b64_2.png)

So let's compile this just to assembly。I call this operations， let's sea。

To make sure optimization is off let's see what we get。呃。I don't see， oh。

 so it's playing clever tricks with the multiplication。Let me。So we see oh。

It's going to be annoying to interpret， but it's doing。So this is B。This always interpret。

 but it is doing it is decomposing those into multiple ads。If that if that makes sense。

Let me do a simpler one， let me just to add them together。Because I can't read assembly very well。

So we just add these three together。A+ B plus C， and this decomposes it into two ad instructions。

So it's。It's saving A and it's putting A and B into registers and then doing the ad。

And the result gets stored in a register， and then it's taking C。Putting in to register and then。

Adding the inner the。The first edition to the second edition， does that kind of make sense？

Kind of let me go through how the actual like assembly code works for assemblyse Chgons work for Ed。

Because ad only takes two arguments， but there's really like three things。 There's the result。

 and then there's two operas。 So let me， let me go over the。

The Intel instruction set to show you how that works and then it'll make this make more sense。

 I think， but you can see here we have the same thing that arch compile generates。

 these offsets from RBP for A B and C。They're four bytes because they' are 32 vigers， but same idea。

🤧U。Okay， so to make this convert this into assembly。

 we have the fact that we've got variables instead of memory addresses so we have to convert those just like we did with parameters。

 we have to deal with integer constants， but we can do that with immediates。

 and we have to do the actual assignment。All right。

 so this is the syntax of operations in the language we have。

A symbol followed by an operaand and in the language that I've given you。

 operaran can either be an identifier for a variable or a constant number。

These are a set of operators that that we have in our language plus minus star slash and percent。

 so I've given these their conventional meetings， additions subtract， multiplication division。

 and modulo， a remainder， and then Op2 is also an identifier or a number。

So that's the language that we have for operations in simple IR。So for this example。

 this is a name for the destination variable that we're going to store the result in。

 this is one of the opera ends is a。Name opera， a variable opera， this is the other opera。

 and this is the operator。Questions on that， questions on the syntax。

And so opera can also be integers， we have these five operations available。Okay。

 so we access variables just like we do。Just like we did for parameters or assignment。

We just take whatever the symbol table says is our offset from the base pointer。

And save it into a temporary register， so a lot of these operations do not work on memory most of the processor operations work on registers。

 so we copyied stuff into scratch registers too to do operations on them。

 so here we're just copying whatever variable corresponds to this offset into RAX。Questions on that。

 I think we saw this many types， hopefully this is。Clly review。So inte constantss。

 we can just do immediate for that。I think we saw that a little bit。

 but that's a move immediate for that so you can just use money sign and then literally copy the integer ASI that was given by the user。

Okay， so this is the subtlety， but how do we save the result well。

 we do the same thing we do to save variables， but the subtle part about arithmetic。

Is that this ad instruction。Is equivalent to this。So RAX， yeah， yeah isn'。Stay there so。

The destination register is yeah， but in the AT&T syntax。

 the destination register is the second opera。This is what's confusing about the AT&T syn。じゃあ、あ護とインで。

どれのおさ。来た？Oh， maybe I made a mistake notes。Okay， oh， division is a little different， yeah。

So it almost doesn't matter。There are like scratch registers you can use RBx is not technically scratch register。

 but I thought it was convenient to use it because A and B are like。

Because it to makes sense as the first opera brand and second opera brand。But in the AT&T Cense， Act。

 you just have to remember that for addition。This second opera is both the。

Left upper end of the edition。Or of whatever the operation is， and also the destination register。

So this is just how the architecture is designed。Originally A was an accumulator register。

 I think that's why I was called hey， I'm not sure。

 but the idea is that you're accumulating this into this register。

 so this register already has a value and then you're taking this value。

 whatever it is and adding it to。The current value of this register。

So it doesn't exactly correspond to our simple IR， right， because our simple IR has three variables。

But the addition instruction only takes two arguments。In the Intel world。Because this argument。

 the destination argument is both the operaand， one of the operaran and also the destination register。

So it's like overriding that value。Does it make sense？

This will make the compiled example that I showed hopefully also make a little more sense so if you just remember this。

That this ad instruction is in our kind of intuitive math world doing this。

 It's taking whatever the value of RX is。Adding it to the value of RBX， the first argument。

And then saving that result in RAX。That's the behavior of this。Machine instruction。Questions on that？

And that's going to be true for addition subtraction multiplication division works。

Division is even more confusing， so we'll go over division how division works。

And these are all probably due to like you know。Some。

Making like the engineering of this easier or something like that。 All right。

 so subtraction works the same way where。The first operaand。

 or I should say the left operaand of this subtraction is the second argument to subtraction。

 and it's also the destination register。So just like with。Just like with。Addition。

They didn't show the。The formula here。But this subion。Means。This。

RAX gets set to whatever RAX was first， minus RBX。Okay。

 so the reason that this is important is that if you swap these two registers or you make a mistake。

Youve got to get。You might get different results so for addition。

 what's the result of this add instruction so I move 9 into Rx 5 into RBX what's the value that add computes14。

 What if I swap the opera， what's the value that computes14 Okay but what about subtraction So I've just changed this to be subtraction if I have9 and five here。

 what's the result of this subtraction。So we have a lot of answers， what about the second one？Yeah。

 so you can see where this gets confusing。So remember the meaning of this subtraction， well。

 this subtraction。Means because the。Second parameter is RBX， that's the destination register。

So this subtraction means this。So RVX is five。Minus9。Negative  four。And the result is stored in RBX。

 it not RX。快审走了 yeah。18 cents。Generally。Think of it as being like if you want to just conceptualize it。

Instruction。I guess we would be。Objects， preposition， subject。RAX from。Maybe， yeah。

Maybe I could think of it like like， well yeah， I don't know if that helps。Yeah， I think so。Yeah。

 I don't know， there are programming languages that I think use the program is used as arrows。

So you could think of it like。This difference in syntax。

 where the destination is on the right of something versus on the left， I don't know。

 I't know if that helps。Yeah， I don't know， I don't know the best way thing， but yeah。

 move this to or add this to yeah， you could think of it if you want to translate it to English that could。

If that's helpful， yeah。Because I made a mistake， sorry。So this subtraction。

 what's the result of this subtraction？F so RAX is nine。

That's going to be the left upper end minus RBx， which is5， that's four。

 and whereas the result going to be stored。RAX， so the second argument is the destination register in the Intel world。

All right questions， questions on this。So you just have to get used to it in the AT&T set。

Multplication works the same way。And it's commutative， so it wouldn't make a difference on the value。

 but remember the right register is the destination register and so you may not get the result you expect if you make a mistake and swap swap these arguments。

All right， so okay， so I think I have a。Full example here， yeah。🤧。Excuse me。

 so here's a full example of。Having two local variables A and B。

 we assign them to9 and five like we did in our example， and then we add them together。

 let's look at what the translation of this to assembly would look like。

So this is what our offset table look like， our simple table will look like because we just allocate each local variable in order。

In our local variables， so we get negative a for x， negative 16 for a， negative 24 for B。

 questions on this。I think we've seen this a lot， I think。So first， there's three steps here。

 one is we。Take the data that we're trying to。Oerate on。

 put them into register so that's the A and B， the values of A and B。

 perform the actual arithmetic and then store the resulting。

Data that we get into back into a variable， the variable that was told to us by the developer。

Questions on these three steps。Okay， so let's see what these three steps look like just for x equals a plus B。

So this is the code that we've seen many， many times， I think by now， to load the。

The values of variables into a register， so this is loading from memory。

 the offset negative 16 from the base pointer into RAX。Value of a。

 and then load the value of B into the Reg RBX。Questions on that。We've seen that。

 I think several times。And here's the addition， so remember。Excuse me， the destination opera。

Is on the right in the or the second opera end on the。Add instruction on the arithmetic operations。

So here's how we compute the results。And it doesn't really matter that we've changed the value of RX because the kind of official value of a is here in memory。

 We haven't touched that'。Taking this temporary register and。

Genrated instructions that will update it to be the result of this edition。

And then we save the result that we have RAX into whatever variable that our assignment S for。

 which in this case was x。Questions on this code？Okay， so integer constants are very。

 very similar except instead of。Loading from a variable will just load from。

An immediate register that shouldn't be negative， load an we'll do an immediate load and then everything else is the same。

 just do the addition and store the result in X。Yeah。

 it's not supposed to be negative apologies to this， it should I be that yeah。Yes。

 it'll be dollar sign negative seven， that's right。可愛いよ。おまでも。No， you wouldn't have to。

 the register itself would hold a negative value。Be like twos complement negative values or something like that。

 there's an encoding in the machine level for negative numbers。

They they are encoded with what you might think of as positive and they're just encoded with binary binary digits。

 so like half the digits are allocated to subtraction machine handles all this for you。

 you don't have to worry worry about that。So yeah， this is the should be here。Okay。

 so before we get into division， let me show that。Example。

So here was the C program where we this is C now not SR， where we have two additions。And A。

 B and C are in negative  four， negative 8， negative 12 offsets， so you can see here that。あ。Well。

 it's a different allocation of registers than in our language， but this is storing。Or loading。

A into EDX into the Dx register。This is loading B into the。A EAX register。

This is just computing EDX equals EDX plus EAX， right because the EDX is the destination register。

This is loading C into。EAX。And then this is computing。EAX equals EAX plus Ex。

So if you're working all that out， that looks like a+ b plus C， right I think？

So this is a+ B and the result of a+。This is the result of A+ B。Saaved into EdDX。And this is。嗯。

The result of A C。C plus。A+ B or C+ Evx。Saaved into EDX， so EDx was a+ B。

 so this is computing C plus a plus B。Save the result in EAX， so it'll be the return five。

 does that make sense of the compile make more sense， so yeah。

 I think you have to know the Intel instruction set for it to make sense。Yeah。

 you can see what if we did like a full compiler that could do math expressions。

 we would have to decompose the expression into all of its individual instructions and work out all the intermediate values to do that。

But in our language， simple IR， there's only one instruction preparation making this far。

 far simpler。次では。If you want to see how to do expressions。

 take my a compile construction class every spring。

 and we'll go over actually like parsing and doing that coach generation。All right question so far。

All right let me go over division because division is very， very specific on the。

Intel instruction set。So there's only unlike add as subtract。

You can see here the division instruction only takes a single register。

And that's because division only works with certain registers。

 I assume they do this just to save money you know， so they don't have， I think the adders。

 there's lots of adders probably in the machine or lot learned。

 something I don't know the exact reason， but you have to use very specific registers in order to do division。

On the Intel instruction set。So specifically， RAX always holds the nuerator， you。

 the first operaand of division。And then you set， you do the division， giving it the denominator。

 giving it the register that you want to do division with。That makes sense。

 so E or RAX is always the first opera of the division yeah。Yeah that's a very。

 very good question so division operates along two registers。

 you can do like I think it's 128 divided by 128 bits divided by a 64 bit number。😊。

And so you in order again， 128， that number， you would take two registers。Now。

 if you don't want to use two registers， you can do CDQ to just fill up RDX with。

Whatever bits it needs， so this is because of the sign bit。

 I think did they teach you about sign bits and two complemented CDA？Yeah。

 so this has to do with that where RDX needs to hold。

It needs to have the sign bit as well for like two's complement representation and so CDQ。

 if you only have the value for your en numerator in for your division in RAX。

 CDQ will ensure that RDX has，The right data for that division。

So you don't have to worry too much about this， just know that you put the。

Left Opera of the division in RAX。You call CDQ and then I divide with the right upperand of the division。

So again， there's nothing really to learn here， this is just the instruction set we're given。

 this is how it works in this instruction set。And the results are always in two very specific registers。

 the quotient that is the result of the division， integer division is always in RX。

 and the remainder is an RDX， so the same operation is used for both division and modular。

Let me see if there's。So I actually have assembly code here， there's still two minutes left。

 I'll just quickly go through this so questions so far on this just is the instruction set design。

Okay。So yeah I already went through this， so here's an example same as before。

 except we do a divided by B。Here's what the semi code looks like， set up the operarans。

Perform the division， so because we stored RAX in here。

 we performed the division with CDQ and then I div the right upper end。And then we store the result。

 which will always be an RAX into。X。So for modlo。All the only difference is which register you take the result from RAX holds the quotient。

 the result of the division， RDX holds the remainder。And that's it so this is all documented here。

 I'll give you the Intel66 documentation， I'll give you tips in the Code Gen3 project， but yeah。

 still a minute to spare so questions on this。All right， so that's arithmetic in the。

In Intel and then we'll yeah， Co N3 will be your project where you implement this All right everyone。

 have a going。

![](img/940acac2c340d1c6d925e8dbd2484b64_4.png)