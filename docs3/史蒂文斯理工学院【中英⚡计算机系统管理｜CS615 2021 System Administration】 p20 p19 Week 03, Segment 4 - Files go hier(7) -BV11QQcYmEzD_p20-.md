# 史蒂文斯理工学院【中英⚡计算机系统管理｜CS615 2021 System Administration】 p20 p19 Week 03, Segment 4 - Files go hier(7) -BV11QQcYmEzD_p20-

Hello and welcome back to CS615 System Administration。This is week 3 segment 4。

 and we're going to wrap up our discussion of petitions and file systems to the extent that we're covering them on this high level。

When we left off in our previous video， we had shown that we can mount the file system we created on our partition and then create files there。

 so perhaps now it would be a good time to look a bit more closely into what types of files the UniX file system supports and how they behave before we then consider and what partitions and file systems are typically mounted on the systems we consider in this class。

Here， let's have a look。Here is an empty directory containing， as usual， the two entries。

 dot and dot dot， referencing the current parent directory respectively。We create a regular file。

Which Ls then shows as such by leaving the first character of the permission string as a dash。Next。

 we create a directory。Which are less than identified by a L D。

Each of these is now an entry in our current directory。As we mentioned before。

 each file is referenced by an ine which contains all the metadata associated with it。

 the permissions， ownership， timests， etc。And it is the file name existing in this director here that creates the reference to the in。

And while we generally do not call them that， every single file name to Iode mapping is called a heart link。

But we can have multiple names for the same file， so if a file already exists。

 we can link another directory entry to it。To do that， we use the LN command。There。Now。

 these two files are indistinguishable other than my name。 more accurately， they are not two files。

 but they are two names for one and the same file。We can verify this by looking at the i note number of the files。

 which unless shows us by passing the dash eye flag。As we see here。

 our directory has one i note number， and the two regular files share the same iNot。That is。

 as far as the system is concerned， they are the same file， and theas also tells us over here。

That there are two names for this file。This number here is the link count。

 the number of names that exist for this file in the file system。

But there's also another type of link， a so called symbolic link。

 which is created when using the S flag to L N。The difference here is now that we can see that this followinging link is indeed a separate file。

It has its own iode number， and the file type is shown as L。A symbolic link。

 then is a special type of file that simply says， hey， don't look at me。

 Look at that guy for any operation。The O S command also shows us the target of the symbolic link in the output。

So when we read the file。Would we get back。

![](img/f72aeda6e9d82100f19c82b18c266dab_1.png)

Is the contents of the file it points to。O。So we've seen files of type directory， regular file。

 which is really only a different way of saying heartlink and symbolic links。

 but there are other types of files。One of them。Is a file。

A  P4 is effectively the manifestation of the concept of a pipe in the file system。

And behaves just like one， too。Anything written to the PIO can be read from it in that order。



![](img/f72aeda6e9d82100f19c82b18c266dab_3.png)

Als， therefore uses the P character to indicate a pfo also known as a named pipe。

The behavior of a fifo can be a bit surprising on first use。But to illustrate。

 let's start a process that reads from the PIfo in the background。We can then cut the sim link。

 which redirects to the regular file into the P form。

At which time the backgrounded cat process reads the data and prints it to standard it out。



![](img/f72aeda6e9d82100f19c82b18c266dab_5.png)

When we hit return again， the shell tells us that the background process has completed。

We also know that apparently not all messages generated by the shell correctly support UTF aid characters。

But it also serves as a good reminder that while file names can contain characters other than just ASI。

 as we are showing here， that bin not always be a good idea。Anyway。

Let's take a look at what other file types we can find。

Let's create a few heart links to existing path names in the current directory。

And then we asked the file command to report on all of them。There we go。Now， we see a fifo。

A character special device such as a T T Y， for example， a block special device such as a hard drive。

A directory， regular files containing U T LV Unicode， a symbolic link， and a socket。

 an inter processcess communications rendezvous point in the file system that provides access to the socketss API for communications between processes on the same host。



![](img/f72aeda6e9d82100f19c82b18c266dab_7.png)

Here， Alice D Eli I showed us more information where again。

 the first letter of the permission string gives us the type of file。

So those are the different types of files you will likely encounter in the normal Uni system。

There are a few other types of files， but some of them are implementation specific to the operating system in question。

Either way， let's now take a look at what file systems we find mounted by default on the different Uni versions we consider。

First， free BSD。Here we see that the root file system。Is mounted from de GPT root F S。

 suggesting the use of a G I D partition table。The mount command shows what file systems are currently mounted and doesn't differ from the output of D F in this case。

Okay， so far， so good， simple enough。Now， let's look at Omniios。 We've done this before。

 but just to recap， when we run Mount， we see a rather different view with several other things showing up as being mounted。

That is， we have our root file system， which we know to be on the Z F S pool。

 as well as several special purpose pseudo file system。Let's look at the Mo tab manual page。

This is one of those weird things where we project information from the running system into the file system。

 making it appears a regular file。So looking at Etsy mount tab。We find no surprises。

 The same file systems mounted as shown before。How does the earth know which things to mount。

Let's look at the V FS tab manual page。Now， this is an actual file。

 a configuration file describing the defaults from mounting file systems and boot time。

Let's take a look at that file。There， this describes which things to mount。

 as well as what type of pseudoophile system to use for the given special mount point。All right， now。

 let's compare。To Linux。And Feora Linux， in particular。Again。

 D F gives us some information here showing the use of some special file systems like Temp Fs。

 as well as the dispat root file system。Now， if we run moundund。Then。



![](img/f72aeda6e9d82100f19c82b18c266dab_9.png)

Well， that's a lot of things showing up there that we didn't see Em R D。 F。 Look at all that。

Fora nowadays uses a system D to bootstrap and manage the running system。

 so we get a whole bunch of extra weirdnesses dashshed in here。We see temper face， deer face。

 control groups， et cetera， et cetera， all with a respective mountund flag shown on the right。

Let's see what man F step says on this system。The E C F step file contains information about which file systems can be mounted。

When we look at the fire。We note that it contains only a single entry for the root file system of type EX T 4 and mapped to the device with a given U U I。

It is quite different from what we have seen。Currently being mounted。

 So where is that information tracked。Ats the Mt。Points to Proc self mount using the Proca pseudo file system to reflect information about the running system into the file system hierarchy。

 as we mentioned before。Proox South mounts looks like so。

Note that it looks like a regular file because of 0 by size。Yet when we get it。

It shows us all the things， just like the Mount command did。So again。

 we notice a discrepancy between what file systems are available when running， for example。

 DF to report on free space and what other pseudo file systems are active。

For many of the pseudofi systems， it simply doesn't make much sense to report file usage。

 since by definition， they only abstract system properties into the file API。Now。

 all this is a bit strange。 We've talked at length about disks and petitions。

 but most of the things we've seen here are not really file systems。 nor are they backed by disks。

This is to illustrate how the Psis API P and concepts have been so successful that we've increasingly used it for other purposes。

Secondly， it's worth noting that the default layout of the AWS instances we spin up do not necessarily represent those of actual production systems for specific purposes。

So let's instead show a different example。What you see here is the DF output on the server on which the Qse website is hosted。

This is an NSD virtual private server， but as you can tell， here we have a more normal layout。

We have a root disk that provides the operating system and is no surprise mounted under slash。

But we also have additional discs for different purposes。

We see one disk dedicated to the home directories of all the users on the system。

And another disc mounted for miscellaneous data files。The O also has a PTY F。

 a Kron F and a Tber Fs mount， but nothing quite as crazy as the Fiora system the instance。



![](img/f72aeda6e9d82100f19c82b18c266dab_11.png)

Okay， so let's visualize how we mo our discs。Even though nowadays。

 we frequently have one monolithic partition containing absolutely everything。

 we still may see different disks， rather than different places。

And this is really the only stipulation we have in mounting partitions。

 A partition can be mounted anywhere with the exception of the rule partition。

 which must be mounted under the slash。Now， way back in the old days。

 when disk space was still expensive and sparse， it was rather common to have one disc that contained the bare minimum files needed to boot the system。

 And that partition would be the rude partition。 While another disk might contain all the additional files that would show up under their user。

 for example， and En those private files living on yet another partition。This， by the way。

 is one of the reasons for why we have a slash user directory instead of just providing all files under slash。

We'll see in a second that there's actually some logic to this。Now， as we also just saw。

 there are other things we can mount， but those super file systems don't get represented here in the graphics。

 since we want to draw a distinction between what data lives where。And for that。

 we probably also want to think about some sort of standardanitization across different operating systems and consider where we want to install software。

 A topic will go into much more detail in the next week's videos。



![](img/f72aeda6e9d82100f19c82b18c266dab_13.png)

And what do we do when we want to look up some information such about which files go where or what the fat hierarchy should look like。

Nope， we don't type which files go where into Google and then follow the first stack overflow link to some random personal pining that the kernel should go under slash all my kernels。

Instead。We pull up the manual page。Man here， provided by the OS。

 provide a description of the Phi system hierarchy。It notes， for example。



![](img/f72aeda6e9d82100f19c82b18c266dab_15.png)

That under bin， we keep the utility system both single and multi user environments。

The system configuration files。Go under Etsy。It tells us where users' data goes。Where libraries go。

And notes that we don't want to rely on user being available since it might reside on a separate disk。

 not yet available at boot time。Tell us about the location for system utilities。

Thereby answering the question as to why we have both a bin and an nestpin。

Describes the user hierarchy itself。Which somewhat mirrors the rude hierarchy。



![](img/f72aeda6e9d82100f19c82b18c266dab_17.png)

And it tells us where various other files go。Such as log files。And P files。



![](img/f72aeda6e9d82100f19c82b18c266dab_19.png)

So you see all this has some logic and reason behind it and having this hierarchy defined and then adhered to by software providers and system administrators makes it easier for you to both use as well as maintain the system。



![](img/f72aeda6e9d82100f19c82b18c266dab_21.png)

By careful separation of these files， you can use different partitions with different mount flexs and security settings。

 for example， or may be better able to budget your disk space or avoid run away processes eating the up disk space or eye notes on one partition from interfering with operations of the system as a whole。



![](img/f72aeda6e9d82100f19c82b18c266dab_23.png)

Okay， this brings us to the end of our discussion of file systems and Mo points。

 Here are a few things for you to think about and to experiment with。

Take a look at the different mounted file systems and the different operating systems。

 as we've shown in this video， make sure you understand the differences and identify what each is used for。

Also， take a look at the mount options。 That is the way in which the fire system is mounted。

 Many fire systems allow for different behaviour based on the mount flags。 You may， for example。

 be able to mount the disc readonni or with certain properties enabled or dis to boost performance。

Make sure you understand what all the different flags mean and why they might be employed。

We also saw that sometimes there's a discrepancy between what E F steps says should be mounted and what is mounted。

 Try to explain under what circumstances such discrepancies arise and whether or not that's a problem。

Next， think a bit about the file system。Remember at the beginning of this topic。

 we ran some experiments trying to fill up disk space and using a by notess。

Rerun those exercises and see if things make more sense now， if you've gained a better understanding。

Then think about specific limitations in the file system。 For example。

 how large can a single file be， What limits its size。We saw that we can use， for example。

 U T of 8 characters in the file names。 What other characters can you use。

 What characters can't you use in a file name。 Why not。How long can a fire name be。Try it out。

 Cate a file name with hundred，200 or 2000 characters。What， if anything is the limit。

 if they are a limit， Why is that。Now， take that thinking beyond the file name。

 If each component in the path name is a file name separated by a slash。

 And if there's a limit on the file name length， what's the limit on the path name。

If a file name is nothing but a heartlink and if you can have multiple heart links for the same file。

 can you have 1002000。What， if any， is the limit。And finally。

 try to create a hard thing across disks or mount points。 You should find that that fails。

 But why is that。If you are able to answer all these questions。

 then you'll be in good shape and think you'll have gained a better understanding of the topics we've covered so far。

But we're not quite done yet with file system hierarchies。 I'm afraid。

 as we've seen when looking at the here manual page。

 there's a reason for why we put things into one place or another。

 But just how do the files get there。Well， we'll cover that directly and indirected by way of software installation concepts in the coming videos until the next time。

 Thanks for watching， Gs。😊。

![](img/f72aeda6e9d82100f19c82b18c266dab_25.png)